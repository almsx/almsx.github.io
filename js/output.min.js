'use strict';
(function() {
  /**
   * @param {!Object} headers
   * @return {?}
   */
  function when(headers) {
    /** @type {number} */
    var i = 0;
    return function() {
      return i < headers.length ? {
        done : false,
        value : headers[i++]
      } : {'use strict';
(function() {
  /**
   * @param {!Object} a
   * @return {?}
   */
  function d(a) {
    /** @type {number} */
    var i = 0;
    return function() {
      return i < a.length ? {
        done : false,
        value : a[i++]
      } : {
        done : true
      };
    };
  }
  /**
   * @param {!Object} target
   * @return {?}
   */
  function require(target) {
    var condition = "undefined" != typeof Symbol && Symbol.iterator && target[Symbol.iterator];
    return condition ? condition.call(target) : {
      next : d(target)
    };
  }
  /**
   * @param {!Object} s
   * @return {?}
   */
  function replace(s) {
    if (!(s instanceof Array)) {
      s = require(s);
      var item;
      /** @type {!Array} */
      var values = [];
      for (; !(item = s.next()).done;) {
        values.push(item.value);
      }
      /** @type {!Array} */
      s = values;
    }
    return s;
  }
  /**
   * @param {!Function} obj
   * @param {!Function} e
   * @return {undefined}
   */
  function create(obj, e) {
    obj.prototype = spawn(e.prototype);
    /** @type {!Function} */
    obj.prototype.constructor = obj;
    if (_) {
      _(obj, e);
    } else {
      var key;
      for (key in e) {
        if ("prototype" != key) {
          if (Object.defineProperties) {
            /** @type {(ObjectPropertyDescriptor<?>|undefined)} */
            var d = Object.getOwnPropertyDescriptor(e, key);
            if (d) {
              Object.defineProperty(obj, key, d);
            }
          } else {
            obj[key] = e[key];
          }
        }
      }
    }
  }
  /**
   * @param {!Array} result
   * @return {undefined}
   */
  function addMethods(result) {
    /** @type {!Array} */
    result = ["object" == typeof window && window, "object" == typeof self && self, "object" == typeof global && global, result];
    /** @type {number} */
    var i = 0;
    for (; i < result.length; ++i) {
      var root = result[i];
      if (root && root.Math == Math) {
        break;
      }
    }
  }
  /**
   * @param {!Object} a
   * @param {!Object} b
   * @return {undefined}
   */
  function F(a, b) {
    /** @type {!Object} */
    this.a = a;
    defineProperty(this, "description", {
      configurable : true,
      writable : true,
      value : b
    });
  }
  /**
   * @param {string} a
   * @param {?} fn
   * @return {undefined}
   */
  function call(a, fn) {
    /** @type {!Array} */
    var array = [];
    /** @type {number} */
    var i = 1;
    for (; i < arguments.length; ++i) {
      array[i - 1] = arguments[i];
    }
    console.warn.apply(console, [a].concat(replace(array)));
  }
  /**
   * @param {!Object} a
   * @param {?} lineNumber
   * @return {undefined}
   */
  function log(a, lineNumber) {
    /** @type {!Array} */
    var array = [];
    /** @type {number} */
    var i = 1;
    for (; i < arguments.length; ++i) {
      array[i - 1] = arguments[i];
    }
    console.error.apply(console, [a].concat(replace(array)));
  }
  /**
   * @param {?} accelerometer_data
   * @return {?}
   */
  function callback(accelerometer_data) {
    if (!accelerometer_data) {
      log("Assertion failed");
    }
    return accelerometer_data;
  }
  /**
   * @param {!Object} a
   * @param {string} c
   * @param {!Object} t
   * @param {?} i
   * @param {number} b
   * @return {undefined}
   */
  function m(a, c, t, i, b) {
    /** @type {!Object} */
    this.a = a;
    /** @type {string} */
    this.g = c;
    /** @type {!Object} */
    this.type = t;
    /** @type {boolean} */
    this.capture = !!i;
    this.b = void 0 === b ? null : b;
    /** @type {number} */
    this.key = ++nextKey;
  }
  /**
   * @return {undefined}
   */
  function lang() {
  }
  /**
   * @return {undefined}
   */
  function textarea() {
  }
  /**
   * @param {number} progressOld
   * @param {number} progressNew
   * @return {?}
   */
  function dispatchEvent(progressOld, progressNew) {
    return progressOld > progressNew ? 1 : progressOld < progressNew ? -1 : 0;
  }
  /**
   * @return {undefined}
   */
  function Unitcell() {
    /** @type {null} */
    this.b = this.a = this.c = null;
  }
  /**
   * @param {!Object} parent
   * @param {number} name
   * @return {?}
   */
  function fn(parent, name) {
    if (0 == name) {
      return parent.b || [];
    }
    if (!parent.a) {
      return call("Priority array values are zero"), [];
    }
    if (!(name in parent.a)) {
      return [];
    }
    parent = get(parent, name);
    return parent ? parent : (call("Priority array values are zero"), []);
  }
  /**
   * @param {!Object} obj
   * @return {?}
   */
  function parse(obj) {
    if (!obj.a) {
      return obj.b ? obj.b.slice() : [];
    }
    /** @type {!Array} */
    var sequence_messages = [];
    var object = callback(obj.c);
    /** @type {number} */
    var index = 0;
    for (; index < object.length; ++index) {
      var array = get(obj, object[index]);
      if (array) {
        sequence_messages.push.apply(sequence_messages, replace(array));
      }
    }
    return sequence_messages;
  }
  /**
   * @param {!Object} obj
   * @param {?} key
   * @return {?}
   */
  function get(obj, key) {
    return 0 == key ? obj.b : callback(obj.a)[key];
  }
  /**
   * @return {undefined}
   */
  function a() {
    /** @type {null} */
    this.a = null;
  }
  /**
   * @param {(number|string)} o
   * @param {?} params
   * @return {undefined}
   */
  function Input(o, params) {
    if (o.a) {
      var h = parse(o.a);
      var i = h.length;
      /** @type {number} */
      var j = 0;
      for (; j < i; ++j) {
        var a = h[j];
        if (-1 != parse(o.a).indexOf(a)) {
          try {
            a.v.apply(a.context, [params]);
          } catch (_data) {
            error(_data);
          }
        }
      }
    }
  }
  /**
   * @param {?} d
   * @return {undefined}
   */
  function error(d) {
    var b = d.stack || d.toString();
    if (0 > String(b).indexOf(d.message)) {
      log(d.message);
    }
    log(b);
  }
  /**
   * @param {?} values
   * @param {!Object} opts
   * @param {?} arg
   * @param {?} data
   * @param {?} f
   * @param {?} c
   * @return {undefined}
   */
  function Matrix(values, opts, arg, data, f, c) {
    m.call(this, values, arg, data, f, c);
    /** @type {!Object} */
    this.proxy = opts;
    /** @type {boolean} */
    this.c = this.u = false;
  }
  /**
   * @param {!Object} m
   * @return {undefined}
   */
  function comparator(m) {
    /** @type {boolean} */
    m.c = true;
    /** @type {null} */
    m.a = null;
    /** @type {null} */
    m.proxy = null;
    /** @type {null} */
    m.g = null;
    /** @type {null} */
    m.b = null;
  }
  /**
   * @param {string} value
   * @return {undefined}
   */
  function t(value) {
    /** @type {string} */
    this.c = value;
    this.a = {};
    /** @type {number} */
    this.b = 0;
  }
  /**
   * @param {!Array} m
   * @param {!Object} el
   * @param {boolean} a
   * @param {!Object} b
   * @return {?}
   */
  function f(m, el, a, b) {
    /** @type {number} */
    var i = 0;
    for (; i < m.length; ++i) {
      var e = m[i];
      if (!e.c && e.a == el && e.capture == !!a && e.b == b) {
        return i;
      }
    }
    return -1;
  }
  /**
   * @return {?}
   */
  function watch() {
    if (!client.addEventListener || !Object.defineProperty) {
      return false;
    }
    /** @type {boolean} */
    var raw = false;
    /** @type {!Object} */
    var options = Object.defineProperty({}, "passive", {
      get : function() {
        /** @type {boolean} */
        raw = true;
      }
    });
    client.addEventListener("test", Function.prototype, options);
    client.removeEventListener("test", Function.prototype, options);
    return raw;
  }
  /**
   * @param {!Object} value
   * @return {?}
   */
  function parseInt(value) {
    value = value[p];
    return value instanceof t ? value : null;
  }
  /**
   * @param {!Object} data
   * @return {undefined}
   */
  function filter(data) {
    if (data && !data.c) {
      var m = data.g;
      var p = data.type;
      var key = data.proxy;
      if (m.removeEventListener) {
        m.removeEventListener(p, key, data.capture);
      }
      if (p = parseInt(m)) {
        key = data.type;
        if (key in p.a) {
          var e = p.a[key];
          var f = e.indexOf(data);
          if (0 <= f) {
            e.splice(f, 1);
            comparator(data);
            if (0 == p.a[key].length) {
              delete p.a[key];
              --p.b;
            }
          }
        }
        if (0 == p.b) {
          /** @type {null} */
          m[p] = null;
        }
      } else {
        comparator(data);
      }
    }
  }
  /**
   * @param {!Object} c
   * @return {?}
   */
  function handler(c) {
    var b = c.a;
    var a = c.b;
    var u = c.u;
    var l = a ? b.bind(a) : b;
    return u ? function(canCreateDiscussions) {
      /** @type {!Array} */
      var message = [];
      /** @type {number} */
      var i = 0;
      for (; i < arguments.length; ++i) {
        message[i] = arguments[i];
      }
      l.apply(null, replace(message));
      filter(c);
    } : l;
  }
  /**
   * @param {string} name
   * @param {!Object} f
   * @return {?}
   */
  function addEventListener(name, f) {
    /** @type {!Window} */
    var b = window;
    /** @type {(null|undefined)} */
    var value = void 0 === value ? null : value;
    var c = parseInt(b);
    if (!c) {
      c = new t(b);
      b[p] = c;
    }
    f = c.add(name, f, false, false, value);
    if (f.proxy) {
      return f;
    }
    value = handler(f);
    f.proxy = value;
    if (b.addEventListener) {
      if (!(void 0 !== result)) {
        result = watch();
      }
      /** @type {(boolean|undefined)} */
      c = result ? void 0 : false;
      if (!(void 0 !== c)) {
        /** @type {boolean} */
        c = false;
      }
      b.addEventListener(name.toString(), value, c);
    } else {
      throw Error("addEventListener is unavailable.");
    }
    return f;
  }
  /**
   * @param {!Window} host
   * @return {undefined}
   */
  function resolve(host) {
    /** @type {!Element} */
    var b = document.createElement("a");
    /** @type {string} */
    b.href = "goodbye.html";
    host.location.replace(b.href);
  }
  /**
   * @return {undefined}
   */
  function click() {
    try {
      if (window == window.top) {
        window.open("", "_self", "");
        window.close();
      } else {
        resolve(window);
      }
    } catch (a) {
      resolve(window.top);
    }
  }
  /**
   * @return {undefined}
   */
  function FloatRange() {
  }
  /**
   * @param {number} num
   * @return {?}
   */
  function c(num) {
    return {
      B : Math.floor(num / 3600),
      F : Math.floor(num % 3600 / 60),
      H : Math.floor(num % 60),
      X : num - Math.floor(num)
    };
  }
  /**
   * @param {number} g
   * @return {?}
   */
  function map(g) {
    return 10 > g ? "0" + g : g;
  }
  /**
   * @param {!Object} f
   * @return {?}
   */
  function init(f) {
    f = c(f);
    return "PT" + f.B + "H" + f.F + "M" + f.H + "S";
  }
  /**
   * @param {!Object} t
   * @return {?}
   */
  function expect(t) {
    t = c(t);
    return map(t.B) + ":" + map(t.F) + ":" + map(t.H);
  }
  /**
   * @param {!Function} attrName
   * @param {?} opt_errorDetails
   * @return {undefined}
   */
  function message(attrName, opt_errorDetails) {
    /** @type {!Function} */
    this.T = attrName;
    this.U = opt_errorDetails;
    /** @type {boolean} */
    this.c = false;
    /** @type {null} */
    this.l = null;
    /** @type {number} */
    this.G = 0;
    /** @type {null} */
    this.a = null;
    /** @type {!Array} */
    this.g = [];
    /** @type {!Array} */
    this.b = [];
    this.D = {
      S : new a,
      R : new a
    };
  }
  /**
   * @param {!Object} data
   * @param {!Object} a
   * @return {undefined}
   */
  function test(data, a) {
    /** @type {!Object} */
    data.l = a;
    /** @type {boolean} */
    data.c = true;
    /** @type {number} */
    data.G = Date.now();
    Input(data.D.S, a.getTitle());
    a.onClosePlayerRequest.add(function() {
      start(data);
      click();
    });
    data.b = a.getCurrentSectionsStateProgress();
    a.onSelectedSectionChange.add(function() {
      if (data.l && data.a) {
        var n = data.l.getSuspendData();
        push(data.a).J(n);
      }
    });
    a.onSectionProgressChange.add(function() {
      var id = a.getCurrentSectionIndex();
      if (0 <= id && !data.b[id] && 1 <= a.getCurrentSectionProgress()) {
        callback(data.a);
        /** @type {boolean} */
        data.b[id] = true;
        update(data);
      }
    });
    update(data);
  }
  /**
   * @param {!Object} item
   * @return {undefined}
   */
  function start(item) {
    if (item.g) {
      var c = require(item.g);
      var result = c.next();
      for (; !result.done; result = c.next()) {
        filter(result.value);
      }
    }
    if (item.c) {
      /** @type {boolean} */
      item.c = false;
      /** @type {number} */
      c = Date.now() - item.G;
      result = callback(item.a);
      push(result).I(c);
    }
    Input(item.D.R);
    callback(item.a).b();
  }
  /**
   * @param {!Object} parent
   * @return {undefined}
   */
  function update(parent) {
    var i = parent.b.filter(function(namesArrayOfFilesFixed) {
      return namesArrayOfFilesFixed;
    }).length;
    var r = callback(parent.a);
    equal(r, i / parent.b.length);
    r.c(null, i >= parent.U.O);
    push(r).h();
  }
  /**
   * @param {!Object} c
   * @param {string} d
   * @return {?}
   */
  function $(c, d) {
    try {
      /** @type {number} */
      var c = 0;
      for (; 255 > c; ++c) {
        if (c[d]) {
          return c[d];
        }
        var w = c.parent;
        if (!w || c == w) {
          break;
        }
        c = w;
      }
    } catch (e) {
    }
    return null;
  }
  /**
   * @param {string} obj
   * @return {?}
   */
  function run(obj) {
    /** @type {!Window} */
    var w = window;
    return $(w, obj) || w.opener && $(w.opener, obj);
  }
  /**
   * @param {!Object} y
   * @param {string} value
   * @return {undefined}
   */
  function Date(y, value) {
    /** @type {!Object} */
    this.a = y;
    /** @type {number} */
    this.b = 0;
    /** @type {string} */
    this.c = value;
  }
  /**
   * @param {boolean} i
   * @return {undefined}
   */
  function value(i) {
    var ms_since_1970 = run("API");
    this.a = ms_since_1970 ? new Date(ms_since_1970, i) : null;
    /** @type {boolean} */
    this.b = false;
  }
  /**
   * @return {undefined}
   */
  function r() {
    /** @type {null} */
    this.a = null;
  }
  /**
   * @param {(number|string)} y
   * @param {!Function} props
   * @return {undefined}
   */
  function equal(y, props) {
    push(y).w(props);
  }
  /**
   * @param {(number|string)} val
   * @return {?}
   */
  function push(val) {
    if (!val.a) {
      throw Error("Connector not created");
    }
    return val.a;
  }
  /**
   * @param {?} elem
   * @param {number} fn
   * @return {undefined}
   */
  function ctx(elem, fn) {
    /** @type {null} */
    this.a = null;
    /** @type {number} */
    this.l = fn;
  }
  /**
   * @param {!Object} a
   * @param {string} c
   * @param {string} g
   * @return {undefined}
   */
  function Color(a, c, g) {
    /** @type {!Object} */
    this.a = a;
    /** @type {string} */
    this.c = c;
    /** @type {number} */
    this.b = 0;
    /** @type {string} */
    this.g = g;
  }
  /**
   * @param {(!Function|RegExp|string)} a
   * @param {(!Function|RegExp|string)} b
   * @return {undefined}
   */
  function BenchmarkImpl(a, b) {
    var code = run("API_1484_11");
    this.a = code ? new Color(code, a, b) : null;
    /** @type {boolean} */
    this.b = false;
  }
  /**
   * @param {!Object} a
   * @param {number} b
   * @return {undefined}
   */
  function A(a, b) {
    /** @type {null} */
    this.a = null;
    this.l = void 0 === b ? "2" : b;
    /** @type {!Object} */
    this.o = a;
  }
  /**
   * @param {!Object} data
   * @return {undefined}
   */
  function Group(data) {
    message.call(this, data.N, data);
    /** @type {!Object} */
    this.K = data;
  }
  /**
   * @param {!Object} options
   * @return {?}
   */
  function add(options) {
    return new Group({
      N : options.apiVersion,
      flags : options.flags,
      W : options.contentId,
      O : options.chaptersCount,
      P : options.edition
    });
  }
  var args;
  /** @type {!Function} */
  var spawn = "function" == typeof Object.create ? Object.create : function(a) {
    /**
     * @return {undefined}
     */
    function GUI() {
    }
    /** @type {!Object} */
    GUI.prototype = a;
    return new GUI;
  };
  var func;
  if ("function" == typeof Object.setPrototypeOf) {
    /** @type {function(!Object, ?): !Object} */
    func = Object.setPrototypeOf;
  } else {
    var out;
    a: {
      var metadata = {
        M : true
      };
      var options = {};
      try {
        options.__proto__ = metadata;
        out = options.M;
        break a;
      } catch (a) {
      }
      /** @type {boolean} */
      out = false;
    }
    /** @type {(function(!Object, !Function): ?|null)} */
    func = out ? function(a, b) {
      /** @type {!Function} */
      a.__proto__ = b;
      if (a.__proto__ !== b) {
        throw new TypeError(a + " is not extensible");
      }
      return a;
    } : null;
  }
  /** @type {(function(!Object, !Function): ?|null)} */
  var _ = func;
  /** @type {!Function} */
  var defineProperty = "function" == typeof Object.defineProperties ? Object.defineProperty : function(object, name, descriptor) {
    if (object != Array.prototype && object != Object.prototype) {
      object[name] = descriptor.value;
    }
  };
  addMethods(this);
  /**
   * @return {?}
   */
  F.prototype.toString = function() {
    return this.a;
  };
  (function() {
    /**
     * @param {string} opts
     * @return {?}
     */
    function create(opts) {
      if (this instanceof create) {
        throw new TypeError("Symbol is not a constructor");
      }
      return new F("jscomp_symbol_" + (opts || "") + "_" + widgetUniqueIDIndex++, opts);
    }
    /** @type {number} */
    var widgetUniqueIDIndex = 0;
    return create;
  })();
  /**
   * @param {!Array} url
   * @param {?} input
   * @param {!Array} session
   * @return {?}
   */
  window.__getCssClass = function(url, input, session) {
    input = void 0 === input ? {} : input;
    /** @type {!Array} */
    url = [url];
    if (session) {
      url.push(session);
    }
    /** @type {!Array<?>} */
    session = url.slice();
    var result = require(Object.keys(input));
    var key = result.next();
    for (; !key.done; key = result.next()) {
      key = key.value;
      var val = input[key];
      var rq = require(url);
      var s = rq.next();
      for (; !s.done; s = rq.next()) {
        if (s = s.value, "boolean" == typeof val) {
          if (val) {
            session.push(s + "_" + key);
          }
        } else {
          if ("string" == typeof val) {
            session.push(val ? s + "_" + key + "_" + val : s + "_" + key);
          } else {
            throw Error("modifier '" + key + "' has invalid type");
          }
        }
      }
    }
    return session.join(" ");
  };
  /** @type {number} */
  var nextKey = 0;
  create(textarea, lang);
  /**
   * @param {number} i
   * @param {!Array} parent
   * @return {undefined}
   */
  Unitcell.prototype.remove = function(i, parent) {
    if ((parent = get(this, parent)) && parent) {
      i = parent.indexOf(i);
      if (0 <= i) {
        parent.splice(i, 1);
      }
    }
  };
  create(a, textarea);
  /**
   * @param {string} data
   * @param {string} c
   * @param {number} i
   * @return {undefined}
   */
  a.prototype.add = function(data, c, i) {
    i = void 0 === i ? 0 : i;
    var o = this.a = this.a || new Unitcell;
    data = {
      v : data,
      context : c
    };
    if (0 == i) {
      o.b = o.b || [];
    } else {
      if (o.c = o.c || [0], o.a = o.a || {}, !(i in o.a)) {
        /** @type {!Array} */
        o.a[i] = [];
        c = o.c;
        var fn = void 0;
        /** @type {(function(number, number): ?|undefined)} */
        fn = void 0 === fn ? dispatchEvent : fn;
        /** @type {number} */
        var index = 0;
        var count = c.length;
        for (; index < count;) {
          /** @type {number} */
          var j = index + count >> 1;
          var state = fn(i, c[j]);
          if (0 < state) {
            /** @type {number} */
            index = j + 1;
          } else {
            /** @type {number} */
            count = j;
            /** @type {boolean} */
            var value = !state;
          }
        }
        /** @type {number} */
        value = value ? index : ~index;
        if (0 > value) {
          c.splice(-(value + 1), 0, i);
        }
      }
    }
    if (o = get(o, i)) {
      o.push(data);
    } else {
      call("Priority array values are zero");
    }
  };
  /**
   * @param {number} f
   * @param {?} context
   * @param {?} x
   * @return {undefined}
   */
  a.prototype.remove = function(f, context, x) {
    x = void 0 === x ? 0 : x;
    if (this.a) {
      var a = fn(this.a, x);
      var startLen = a.length;
      /** @type {number} */
      var j = 0;
      for (; j < startLen; ++j) {
        var node = a[j];
        if (node.v == f && node.context == context) {
          /** @type {number} */
          f = j;
          if (x = get(this.a, x)) {
            x.splice(f, 1);
          }
          break;
        }
      }
    } else {
      call("Signal has no handlers!");
    }
  };
  /**
   * @param {?} fn
   * @param {?} context
   * @param {number} data
   * @return {?}
   */
  a.prototype.has = function(fn, context, data) {
    if (!this.a) {
      return false;
    }
    data = fn(this.a, void 0 === data ? 0 : data);
    var tldCount = data.length;
    /** @type {number} */
    var i = 0;
    for (; i < tldCount; ++i) {
      var d = data[i];
      if (d.v == fn && d.context == context) {
        return true;
      }
    }
    return false;
  };
  var client = Function("return this")();
  Object.getPrototypeOf({});
  /**
   * @param {?} event
   * @return {?}
   */
  client.onerror = function(event) {
    /** @type {!Array} */
    var options = [];
    /** @type {number} */
    var a = 0;
    for (; a < arguments.length; ++a) {
      options[a] = arguments[a];
    }
    a = require(options);
    options = a.next().value;
    a.next();
    a.next();
    a.next();
    if (a = a.next().value) {
      error(a);
    } else {
      log(options);
    }
    return true;
  };
  create(Matrix, m);
  /**
   * @param {(!Function|string)} a
   * @param {string} val
   * @param {string} url
   * @param {boolean} type
   * @param {!Object} e
   * @return {?}
   */
  t.prototype.add = function(a, val, url, type, e) {
    var i = a.toString();
    a = this.a[i];
    if (!a) {
      /** @type {!Array} */
      a = this.a[i] = [];
      this.b++;
    }
    var d = f(a, val, type, e);
    if (-1 < d) {
      val = a[d];
      if (!url) {
        /** @type {boolean} */
        val.u = false;
      }
    } else {
      val = new Matrix(val, null, this.c, i, !!type, e);
      /** @type {string} */
      val.u = url;
      a.push(val);
    }
    return val;
  };
  /**
   * @param {string} i
   * @param {number} a
   * @param {boolean} v
   * @param {!Object} d
   * @return {?}
   */
  t.prototype.remove = function(i, a, v, d) {
    i = i.toString();
    if (!(i in this.a)) {
      return false;
    }
    var data = this.a[i];
    a = f(data, a, v, d);
    return -1 < a ? (comparator(data[a]), data.splice(a, 1), 0 == data.length && (delete this.a[i], this.b--), true) : false;
  };
  /** @type {string} */
  var p = "ispring_lm_" + (1E6 * Math.random() | 0);
  var result;
  /**
   * @param {?} y
   * @return {undefined}
   */
  message.prototype.L = function(y) {
    try {
      var white = new FloatRange;
      a: {
        switch(this.T) {
          case "scorm12":
            var o = new ctx(white, true);
            break a;
          case "scorm2004":
            o = new A(true, this.K.P);
            break a;
        }
        throw Error("unknown api version");
      }
      this.a = o;
    } catch (valueProgess) {
      log(valueProgess);
      this.o(y);
    }
    if (this.a) {
      white = this.a;
      o = this.V.bind(this, y);
      y = this.o.bind(this, y);
      white.a = white.g();
      if (white.a) {
        white.a.C(o, y);
      } else {
        y();
      }
    }
  };
  /**
   * @param {?} invalidArgumentCallback
   * @return {undefined}
   */
  message.prototype.V = function(invalidArgumentCallback) {
    var item = this;
    var next_argument = push(callback(this.a)).A();
    invalidArgumentCallback(next_argument ? next_argument : void 0).then(function(snapshotOut) {
      return test(item, snapshotOut);
    });
    /** @type {!Array} */
    this.g = [addEventListener("unload", function() {
      return start(item);
    }), addEventListener("beforeunload", function() {
      return start(item);
    })];
  };
  /**
   * @param {?} a
   * @return {undefined}
   */
  message.prototype.o = function(a) {
    log("Lms initialization failed");
    a().then(function() {
    });
  };
  args = Date.prototype;
  /**
   * @return {?}
   */
  args.i = function() {
    try {
      return "true" == this.a.LMSInitialize("");
    } catch (a) {
    }
    return false;
  };
  /**
   * @param {string} top
   * @return {?}
   */
  args.m = function(top) {
    this.f("cmi.core.exit", top ? "suspend" : "");
    this.h();
    try {
      return "true" == this.a.LMSFinish("");
    } catch (b) {
    }
    return false;
  };
  /**
   * @return {?}
   */
  args.h = function() {
    if (0 == this.b) {
      this.f("cmi.core.exit", this.c ? "suspend" : "");
    }
    try {
      /** @type {boolean} */
      var uiState = "true" == this.a.LMSCommit("");
      this.b += 1;
      return uiState;
    } catch (b) {
    }
    return false;
  };
  /**
   * @param {string} d
   * @return {?}
   */
  args.j = function(d) {
    try {
      return this.a.LMSGetValue(d);
    } catch (b) {
    }
    return "";
  };
  /**
   * @param {string} n
   * @param {string} value
   * @return {?}
   */
  args.f = function(n, value) {
    if ("number" === typeof value) {
      /** @type {string} */
      value = parseFloat(Number(value).toFixed(2)) + "";
    }
    try {
      return "true" == this.a.LMSSetValue(n, value);
    } catch (c) {
    }
    return false;
  };
  args = value.prototype;
  /**
   * @return {?}
   */
  args.i = function() {
    return this.b ? true : this.b = this.a ? this.a.i() : false;
  };
  /**
   * @param {?} m
   * @param {?} x
   * @return {undefined}
   */
  args.C = function(m, x) {
    if (this.b) {
      m();
    } else {
      if (this.a) {
        if (this.b = this.a.i()) {
          m();
        } else {
          x();
        }
      } else {
        /** @type {boolean} */
        this.b = false;
        x();
      }
    }
  };
  /**
   * @param {boolean} date
   * @return {?}
   */
  args.m = function(date) {
    return this.a ? this.a.m(date) : false;
  };
  /**
   * @return {?}
   */
  args.h = function() {
    return this.a ? this.a.h() : false;
  };
  /**
   * @param {string} d
   * @return {?}
   */
  args.j = function(d) {
    return this.a ? this.a.j(d) : "";
  };
  /**
   * @param {string} p
   * @param {string} message
   * @return {?}
   */
  args.f = function(p, message) {
    return this.a ? this.a.f(p, message) : false;
  };
  /**
   * @return {undefined}
   */
  args.w = function() {
  };
  /**
   * @param {number} n
   * @return {undefined}
   */
  args.I = function(n) {
    this.f("cmi.core.session_time", expect(n / 1E3));
  };
  /**
   * @return {?}
   */
  args.A = function() {
    return this.j("cmi.suspend_data");
  };
  /**
   * @param {string} a
   * @return {undefined}
   */
  args.J = function(a) {
    this.f("cmi.suspend_data", a);
  };
  /**
   * @param {boolean} res
   * @param {string} val
   * @return {undefined}
   */
  args.s = function(res, val) {
    if (null === res) {
      if (null === val) {
        this.f("cmi.core.lesson_status", "unknown");
      } else {
        this.f("cmi.core.lesson_status", val ? "completed" : "incomplete");
      }
    } else {
      this.f("cmi.core.lesson_status", res ? "passed" : "failed");
    }
  };
  /**
   * @return {undefined}
   */
  r.prototype.b = function() {
    push(this).m(true);
    click();
  };
  /**
   * @param {boolean} val
   * @param {string} index
   * @return {undefined}
   */
  r.prototype.c = function(val, index) {
    push(this).s(val, index);
  };
  create(ctx, r);
  /**
   * @return {?}
   */
  ctx.prototype.g = function() {
    return new value(this.l);
  };
  /**
   * @param {!Object} z
   * @param {string} c
   * @return {undefined}
   */
  ctx.prototype.c = function(z, c) {
    push(this).s(null, c);
  };
  /**
   * @return {undefined}
   */
  ctx.prototype.b = function() {
    r.prototype.b.call(this);
    click();
  };
  args = Color.prototype;
  /**
   * @return {?}
   */
  args.i = function() {
    try {
      return "true" == this.a.Initialize("");
    } catch (a) {
    }
    return false;
  };
  /**
   * @param {boolean} value
   * @return {?}
   */
  args.m = function(value) {
    this.f("cmi.exit", value ? "suspend" : "normal");
    this.f("adl.nav.request", "exitAll");
    this.h();
    try {
      return "true" == this.a.Terminate("");
    } catch (b) {
    }
    return false;
  };
  /**
   * @return {?}
   */
  args.h = function() {
    if (0 == this.b) {
      this.f("cmi.exit", this.g ? "suspend" : "normal");
    }
    try {
      /** @type {boolean} */
      var uiState = "true" == this.a.Commit("");
      this.b += 1;
      return uiState;
    } catch (b) {
    }
    return false;
  };
  /**
   * @param {string} d
   * @return {?}
   */
  args.j = function(d) {
    try {
      return this.a.GetValue(d);
    } catch (b) {
    }
    return "";
  };
  /**
   * @param {string} n
   * @param {string} value
   * @return {?}
   */
  args.f = function(n, value) {
    if ("number" == typeof value) {
      /** @type {string} */
      value = parseFloat(value.toFixed(7)) + "";
    }
    try {
      return "true" == this.a.SetValue(n, value);
    } catch (c) {
    }
    return false;
  };
  args = BenchmarkImpl.prototype;
  /**
   * @return {?}
   */
  args.i = function() {
    return this.b ? true : this.b = this.a ? this.a.i() : false;
  };
  /**
   * @param {?} m
   * @param {?} x
   * @return {undefined}
   */
  args.C = function(m, x) {
    if (this.b) {
      m();
    } else {
      if (this.a) {
        if (this.b = this.a.i()) {
          m();
        } else {
          x();
        }
      } else {
        /** @type {boolean} */
        this.b = false;
        x();
      }
    }
  };
  /**
   * @param {boolean} date
   * @return {?}
   */
  args.m = function(date) {
    return this.a ? this.a.m(date) : false;
  };
  /**
   * @return {?}
   */
  args.h = function() {
    return this.a ? this.a.h() : false;
  };
  /**
   * @param {string} d
   * @return {?}
   */
  args.j = function(d) {
    return this.a ? this.a.j(d) : "";
  };
  /**
   * @param {string} p
   * @param {string} message
   * @return {?}
   */
  args.f = function(p, message) {
    return this.a ? this.a.f(p, message) : false;
  };
  /**
   * @param {string} t
   * @return {undefined}
   */
  args.w = function(t) {
    this.f("cmi.progress_measure", t);
  };
  /**
   * @param {number} n
   * @return {undefined}
   */
  args.I = function(n) {
    this.f("cmi.session_time", init(n / 1E3));
  };
  /**
   * @return {?}
   */
  args.A = function() {
    return this.j("cmi.suspend_data");
  };
  /**
   * @param {string} a
   * @return {undefined}
   */
  args.J = function(a) {
    if (!this.a) {
      throw Error();
    }
    if (a.length <= ("2" == this.a.c ? 4E3 : 64E3)) {
      this.f("cmi.suspend_data", a);
    }
  };
  /**
   * @param {boolean} res
   * @param {string} val
   * @return {undefined}
   */
  args.s = function(res, val) {
    this.f("cmi.success_status", null === res ? "unknown" : res ? "passed" : "failed");
    this.f("cmi.completion_status", null === val ? "unknown" : val ? "completed" : "incomplete");
  };
  create(A, r);
  /**
   * @return {?}
   */
  A.prototype.g = function() {
    return new BenchmarkImpl(this.l, this.o);
  };
  create(Group, message);
  /** @type {!Array} */
  var remotes = ["iSpring", "roll", "LMS", "create"];
  var output = client;
  if (!(remotes[0] in output || "undefined" == typeof output.execScript)) {
    output.execScript("var " + remotes[0]);
  }
  var i = void 0;
  for (; remotes.length && (i = remotes.shift());) {
    if (remotes.length || void 0 === add) {
      if (output[i] && output[i] !== Object.prototype[i]) {
        output = output[i];
      } else {
        output = output[i] = {};
      }
    } else {
      /** @type {function(!Object): ?} */
      output[i] = add;
    }
  }
  /** @type {function(?): undefined} */
  message.prototype.initialize = message.prototype.L;
}).call(this);

        done : true
      };
    };
  }
  /**
   * @param {!Object} a
   * @return {?}
   */
  function forEach(a) {
    var b = "undefined" != typeof Symbol && Symbol.iterator && a[Symbol.iterator];
    return b ? b.call(a) : {
      next : when(a)
    };
  }
  /**
   * @param {!Object} obj
   * @return {?}
   */
  function each(obj) {
    if (!(obj instanceof Array)) {
      obj = forEach(obj);
      var info;
      /** @type {!Array} */
      var v = [];
      for (; !(info = obj.next()).done;) {
        v.push(info.value);
      }
      /** @type {!Array} */
      obj = v;
    }
    return obj;
  }
  /**
   * @param {!Function} obj
   * @param {!Function} value
   * @return {undefined}
   */
  function expect(obj, value) {
    obj.prototype = spawn(value.prototype);
    /** @type {!Function} */
    obj.prototype.constructor = obj;
    if (inner) {
      inner(obj, value);
    } else {
      var name;
      for (name in value) {
        if ("prototype" != name) {
          if (Object.defineProperties) {
            /** @type {(ObjectPropertyDescriptor<?>|undefined)} */
            var d = Object.getOwnPropertyDescriptor(value, name);
            if (d) {
              Object.defineProperty(obj, name, d);
            }
          } else {
            obj[name] = value[name];
          }
        }
      }
    }
  }
  /**
   * @param {!Array} result
   * @return {?}
   */
  function addMethods(result) {
    /** @type {!Array} */
    result = ["object" == typeof window && window, "object" == typeof self && self, "object" == typeof global && global, result];
    /** @type {number} */
    var i = 0;
    for (; i < result.length; ++i) {
      var obj = result[i];
      if (obj && obj.Math == Math) {
        return obj;
      }
    }
    return globalThis;
  }
  /**
   * @param {string} key
   * @param {string} object
   * @return {undefined}
   */
  function getCollection(key, object) {
    if (object) {
      var obj = global;
      key = key.split(".");
      /** @type {number} */
      var value = 0;
      for (; value < key.length - 1; value++) {
        var k = key[value];
        if (!(k in obj)) {
          obj[k] = {};
        }
        obj = obj[k];
      }
      key = key[key.length - 1];
      value = obj[key];
      object = object(value);
      if (object != value && null != object) {
        defineProperty(obj, key, {
          configurable : true,
          writable : true,
          value : object
        });
      }
    }
  }
  /**
   * @return {undefined}
   */
  function deregisterStateChangeListener() {
    /**
     * @return {undefined}
     */
    deregisterStateChangeListener = function() {
    };
    if (!global.Symbol) {
      global.Symbol = fakeSymbol;
    }
  }
  /**
   * @param {number} value
   * @param {!Object} tile
   * @return {undefined}
   */
  function Cell(value, tile) {
    /** @type {number} */
    this.b = value;
    defineProperty(this, "description", {
      configurable : true,
      writable : true,
      value : tile
    });
  }
  /**
   * @return {undefined}
   */
  function register() {
    deregisterStateChangeListener();
    var $iterator$ = global.Symbol.iterator;
    if (!$iterator$) {
      $iterator$ = global.Symbol.iterator = global.Symbol("Symbol.iterator");
    }
    if ("function" != typeof Array.prototype[$iterator$]) {
      defineProperty(Array.prototype, $iterator$, {
        configurable : true,
        writable : true,
        value : function() {
          return genIterator(when(this));
        }
      });
    }
    /**
     * @return {undefined}
     */
    register = function() {
    };
  }
  /**
   * @param {string} obj
   * @return {?}
   */
  function genIterator(obj) {
    register();
    obj = {
      next : obj
    };
    /**
     * @return {?}
     */
    obj[global.Symbol.iterator] = function() {
      return this;
    };
    return obj;
  }
  /**
   * @return {undefined}
   */
  function RunHandlerTask() {
    /** @type {boolean} */
    this.G = false;
    /** @type {null} */
    this.f = null;
    this.s = void 0;
    /** @type {number} */
    this.b = 1;
    /** @type {number} */
    this.g = 0;
    /** @type {null} */
    this.m = null;
  }
  /**
   * @param {!Window} object
   * @return {undefined}
   */
  function has(object) {
    if (object.G) {
      throw new TypeError("Generator is already running");
    }
    /** @type {boolean} */
    object.G = true;
  }
  /**
   * @param {!Object} r
   * @param {!Array} x
   * @return {undefined}
   */
  function l(r, x) {
    r.m = {
      ue : x,
      Ne : true
    };
    r.b = r.g;
  }
  /**
   * @param {!Object} b
   * @param {!Object} a
   * @param {number} m
   * @return {?}
   */
  function ok(b, a, m) {
    /** @type {number} */
    b.b = m;
    return {
      value : a
    };
  }
  /**
   * @param {!Function} f
   * @return {undefined}
   */
  function Matrix2D(f) {
    this.b = new RunHandlerTask;
    /** @type {!Function} */
    this.f = f;
  }
  /**
   * @param {!Object} node
   * @param {!Array} value
   * @return {?}
   */
  function at(node, value) {
    has(node.b);
    var data = node.b.f;
    if (data) {
      return walk(node, "return" in data ? data["return"] : function(command_module_id) {
        return {
          value : command_module_id,
          done : true
        };
      }, value, node.b.return);
    }
    node.b.return(value);
    return reduce(node);
  }
  /**
   * @param {!Object} item
   * @param {!Function} callback
   * @param {!Array} id
   * @param {!Function} a
   * @return {?}
   */
  function walk(item, callback, id, a) {
    try {
      var target = callback.call(item.b.f, id);
      if (!(target instanceof Object)) {
        throw new TypeError("Iterator result " + target + " is not an object");
      }
      if (!target.done) {
        return item.b.G = false, target;
      }
      var c = target.value;
    } catch (r) {
      return item.b.f = null, l(item.b, r), reduce(item);
    }
    /** @type {null} */
    item.b.f = null;
    a.call(item.b, c);
    return reduce(item);
  }
  /**
   * @param {!Object} item
   * @return {?}
   */
  function reduce(item) {
    for (; item.b.b;) {
      try {
        var value = item.f(item.b);
        if (value) {
          return item.b.G = false, {
            value : value.value,
            done : false
          };
        }
      } catch (r) {
        item.b.s = void 0;
        l(item.b, r);
      }
    }
    /** @type {boolean} */
    item.b.G = false;
    if (item.b.m) {
      value = item.b.m;
      /** @type {null} */
      item.b.m = null;
      if (value.Ne) {
        throw value.ue;
      }
      return {
        value : value.return,
        done : true
      };
    }
    return {
      value : void 0,
      done : true
    };
  }
  /**
   * @param {!Object} item
   * @return {undefined}
   */
  function AppEventHandlerIterator(item) {
    /**
     * @param {?} value
     * @return {?}
     */
    this.next = function(value) {
      has(item.b);
      if (item.b.f) {
        value = walk(item, item.b.f.next, value, item.b.O);
      } else {
        item.b.O(value);
        value = reduce(item);
      }
      return value;
    };
    /**
     * @param {!Array} value
     * @return {?}
     */
    this.throw = function(value) {
      has(item.b);
      if (item.b.f) {
        value = walk(item, item.b.f["throw"], value, item.b.O);
      } else {
        l(item.b, value);
        value = reduce(item);
      }
      return value;
    };
    /**
     * @param {?} value
     * @return {?}
     */
    this.return = function(value) {
      return at(item, value);
    };
    register();
    /**
     * @return {?}
     */
    this[Symbol.iterator] = function() {
      return this;
    };
  }
  /**
   * @param {!Object} p
   * @return {?}
   */
  function activate(p) {
    /**
     * @param {(Object|string)} d
     * @return {?}
     */
    function next(d) {
      return p.next(d);
    }
    /**
     * @param {!Array} value
     * @return {?}
     */
    function show(value) {
      return p.throw(value);
    }
    return new Promise(function(finishPut, stateToValidate) {
      /**
       * @param {!Object} options
       * @return {undefined}
       */
      function resolve(options) {
        if (options.done) {
          finishPut(options.value);
        } else {
          Promise.resolve(options.value).then(next, show).then(resolve, stateToValidate);
        }
      }
      resolve(p.next());
    });
  }
  /**
   * @param {!Function} opt_msg
   * @return {?}
   */
  function promise(opt_msg) {
    return activate(new AppEventHandlerIterator(new Matrix2D(opt_msg)));
  }
  /**
   * @param {string} elem
   * @param {!Function} callback
   * @return {?}
   */
  function client_api(elem, callback) {
    register();
    if (elem instanceof String) {
      /** @type {string} */
      elem = elem + "";
    }
    /** @type {number} */
    var c = 0;
    var data = {
      next : function() {
        if (c < elem.length) {
          /** @type {number} */
          var j = c++;
          return {
            value : callback(j, elem[j]),
            done : false
          };
        }
        /**
         * @return {?}
         */
        data.next = function() {
          return {
            done : true,
            value : void 0
          };
        };
        return data.next();
      }
    };
    /**
     * @return {?}
     */
    data[Symbol.iterator] = function() {
      return data;
    };
    return data;
  }
  /**
   * @param {string} object
   * @param {!Object} value
   * @param {string} p
   * @return {?}
   */
  function filterObject(object, value, p) {
    if (null == object) {
      throw new TypeError("The 'this' value for String.prototype." + p + " must not be null or undefined");
    }
    if (value instanceof RegExp) {
      throw new TypeError("First argument to String.prototype." + p + " must not be a regular expression");
    }
    return object + "";
  }
  /**
   * @param {string} obj
   * @param {string} name
   * @return {?}
   */
  function isFunction(obj, name) {
    return Object.prototype.hasOwnProperty.call(obj, name);
  }
  /**
   * @param {string} a
   * @param {!Function} fn
   * @param {!Object} value
   * @return {?}
   */
  function indexOf(a, fn, value) {
    if (a instanceof String) {
      /** @type {string} */
      a = String(a);
    }
    var az = a.length;
    /** @type {number} */
    var i = 0;
    for (; i < az; i++) {
      var win = a[i];
      if (fn.call(value, win, i, a)) {
        return {
          ld : i,
          Cd : win
        };
      }
    }
    return {
      ld : -1,
      Cd : void 0
    };
  }
  /**
   * @param {string} args
   * @param {!Array} key
   * @param {string} value
   * @return {?}
   */
  function extend(args, key, value) {
    key = void 0 === key ? {} : key;
    /** @type {!Array} */
    args = [args];
    if (value) {
      args.push(value);
    }
    /** @type {!Array<?>} */
    value = args.slice();
    var api = forEach(Object.keys(key));
    var l = api.next();
    for (; !l.done; l = api.next()) {
      l = l.value;
      var target = key[l];
      var api = forEach(args);
      var t = api.next();
      for (; !t.done; t = api.next()) {
        if (t = t.value, "boolean" == typeof target) {
          if (target) {
            value.push(t + "_" + l);
          }
        } else {
          if ("string" == typeof target) {
            value.push(target ? t + "_" + l + "_" + target : t + "_" + l);
          } else {
            throw Error("modifier '" + l + "' has invalid type");
          }
        }
      }
    }
    return value.join(" ");
  }
  /**
   * @param {string} options
   * @param {?} header
   * @return {undefined}
   */
  function fail(options, header) {
    /** @type {!Array} */
    var args = [];
    /** @type {number} */
    var i = 1;
    for (; i < arguments.length; ++i) {
      args[i - 1] = arguments[i];
    }
    console.warn.apply(console, [options].concat(each(args)));
  }
  /**
   * @param {string} value
   * @param {?} text
   * @return {undefined}
   */
  function debug(value, text) {
    /** @type {!Array} */
    var args = [];
    /** @type {number} */
    var i = 1;
    for (; i < arguments.length; ++i) {
      args[i - 1] = arguments[i];
    }
    console.error.apply(console, [value].concat(each(args)));
  }
  /**
   * @param {boolean} a
   * @param {string} b
   * @return {undefined}
   */
  function substring(a, b) {
    if (!a) {
      debug("Assertion failed" + (b ? ": " + b : ""));
    }
  }
  /**
   * @param {!Object} str
   * @param {string} start
   * @return {?}
   */
  function $(str, start) {
    substring(str, start);
    return str;
  }
  /**
   * @param {?} value
   * @return {?}
   */
  function isString(value) {
    return void 0 !== value;
  }
  /**
   * @param {!Object} value
   * @return {?}
   */
  function isEmpty(value) {
    /** @type {string} */
    var type = typeof value;
    return "object" == type && null != value || "function" == type;
  }
  /**
   * @param {?} num
   * @return {?}
   */
  function Number(num) {
    return "string" == typeof num;
  }
  /**
   * @param {?} value
   * @return {?}
   */
  function identity(value) {
    return null != value;
  }
  /**
   * @param {!Function} value
   * @return {undefined}
   */
  function isNaN(value) {
    /** @type {(string|undefined)} */
    var x = void 0 === x ? "Unhandled value: " + JSON.stringify(value, null, " ") : x;
    debug(x);
  }
  /**
   * @param {number} s
   * @param {string} url
   * @param {!Object} storage
   * @param {?} directory
   * @param {?} name
   * @return {undefined}
   */
  function report(s, url, storage, directory, name) {
    /** @type {number} */
    this.b = s;
    /** @type {string} */
    this.src = url;
    /** @type {!Object} */
    this.type = storage;
    /** @type {boolean} */
    this.capture = !!directory;
    this.zb = void 0 === name ? null : name;
    /** @type {number} */
    this.key = ++nextKey;
  }
  /**
   * @param {?} callback
   * @param {!Function} opts
   * @param {?} app
   * @param {?} path
   * @param {?} e
   * @param {?} image
   * @return {undefined}
   */
  function Event(callback, opts, app, path, e, image) {
    report.call(this, callback, app, path, e, image);
    /** @type {!Function} */
    this.proxy = opts;
    /** @type {boolean} */
    this.f = this.dc = false;
  }
  /**
   * @param {!Object} a
   * @return {undefined}
   */
  function isNumber(a) {
    /** @type {boolean} */
    a.f = true;
    /** @type {null} */
    a.b = null;
    /** @type {null} */
    a.proxy = null;
    /** @type {null} */
    a.src = null;
    /** @type {null} */
    a.zb = null;
  }
  /**
   * @param {!Object} options
   * @param {!Function} validator
   * @return {?}
   */
  function _format(options, validator) {
    var base = {};
    if (options) {
      var name;
      for (name in options) {
        if (validator.call(void 0, options[name], name, options)) {
          base[name] = options[name];
        }
      }
    }
    return base;
  }
  /**
   * @param {!Object} data
   * @param {!Function} callback
   * @return {undefined}
   */
  function createElement(data, callback) {
    var i;
    for (i in data) {
      if (data.hasOwnProperty(i)) {
        callback.call(void 0, data[i], i, data);
      }
    }
  }
  /**
   * @param {!Array} node
   * @param {!Function} $
   * @return {?}
   */
  function exit(node, $) {
    var self = {};
    var api = forEach(Object.keys(node));
    var n = api.next();
    for (; !n.done; n = api.next()) {
      n = n.value;
      self[n] = $(node[n]);
    }
    return self;
  }
  /**
   * @param {?} value
   * @param {!Function} key
   * @return {?}
   */
  function all(value, key) {
    var obj = key(value);
    if (void 0 !== obj) {
      return obj;
    }
    if ("object" !== typeof value || null == value) {
      return value;
    }
    if (Array.isArray(value)) {
      return value.map(function(promises) {
        return all(promises, key);
      });
    }
    obj = {};
    var api = forEach(Object.keys(value));
    var i = api.next();
    for (; !i.done; i = api.next()) {
      i = i.value;
      obj[i] = all(value[i], key);
    }
    return obj;
  }
  /**
   * @param {string} url
   * @return {undefined}
   */
  function Module(url) {
    /** @type {string} */
    this.src = url;
    this.b = {};
    /** @type {number} */
    this.f = 0;
  }
  /**
   * @param {string} m
   * @param {!Function} b
   * @param {number} msg
   * @param {!Object} k
   * @return {?}
   */
  function send(m, b, msg, k) {
    /** @type {number} */
    var d = 0;
    for (; d < m.length; ++d) {
      var e = m[d];
      if (!e.f && e.b == b && e.capture == !!msg && e.zb == k) {
        return d;
      }
    }
    return -1;
  }
  /**
   * @return {?}
   */
  function T() {
    if (!w.addEventListener || !Object.defineProperty) {
      return false;
    }
    /** @type {boolean} */
    var messageId = false;
    /** @type {!Object} */
    var options = Object.defineProperty({}, "passive", {
      get : function() {
        /** @type {boolean} */
        messageId = true;
      }
    });
    w.addEventListener("test", Function.prototype, options);
    w.removeEventListener("test", Function.prototype, options);
    return messageId;
  }
  /**
   * @param {!Object} result
   * @return {?}
   */
  function dPlus(result) {
    result = result[j];
    return result instanceof Module ? result : null;
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function func(data) {
    if (!data || data.f) {
      return false;
    }
    var x = data.src;
    var state = data.type;
    var n = data.proxy;
    if (x.removeEventListener) {
      x.removeEventListener(state, n, data.capture);
    }
    if (state = dPlus(x)) {
      n = data.type;
      if (n in state.b) {
        var e = state.b[n];
        var f = e.indexOf(data);
        if (0 <= f) {
          e.splice(f, 1);
          isNumber(data);
          if (0 == state.b[n].length) {
            delete state.b[n];
            --state.f;
          }
        }
      }
      if (0 == state.f) {
        /** @type {null} */
        x[j] = null;
      }
    } else {
      isNumber(data);
    }
    return true;
  }
  /**
   * @param {!Object} state
   * @return {?}
   */
  function initGetterSetter(state) {
    var b = state.b;
    var t = state.zb;
    var interrupted = state.dc;
    var c = t ? b.bind(t) : b;
    return interrupted ? function(str) {
      /** @type {!Array} */
      var value = [];
      /** @type {number} */
      var i = 0;
      for (; i < arguments.length; ++i) {
        value[i] = arguments[i];
      }
      c.apply(null, each(value));
      func(state);
    } : c;
  }
  /**
   * @param {!Object} a
   * @param {string} type
   * @param {!Object} value
   * @param {!Object} options
   * @param {string} i
   * @return {?}
   */
  function on(a, type, value, options, i) {
    i = void 0 === i ? null : i;
    var name = isEmpty(options) ? $(options).capture : !!options;
    var c = dPlus(a);
    if (!c) {
      c = new Module(a);
      a[j] = c;
    }
    value = c.add(type, value, !(!options || !options.once), name, i);
    if (value.proxy) {
      return value;
    }
    i = initGetterSetter(value);
    /** @type {string} */
    value.proxy = i;
    if (a.addEventListener) {
      if (!isString(title)) {
        title = T();
      }
      options = title ? options : name;
      if (!isString(options)) {
        /** @type {boolean} */
        options = false;
      }
      a.addEventListener(type.toString(), i, options);
    } else {
      throw Error("addEventListener is unavailable.");
    }
    return value;
  }
  /**
   * @return {undefined}
   */
  function nano() {
  }
  /**
   * @return {undefined}
   */
  function method() {
    /** @type {null} */
    this.sb = this.G = this.s = this.O = null;
  }
  /**
   * @param {!Object} options
   * @param {string} callback
   * @return {undefined}
   */
  function parsePath(options, callback) {
    options.sb = options.sb || [];
    options.sb.push(callback);
  }
  /**
   * @param {string} context
   * @param {!Object} value
   * @param {string} name
   * @param {!Function} fn
   * @return {?}
   */
  function end(context, value, name, fn) {
    context.O = context.O || [];
    value = on(value, name, fn, void 0, context);
    context.O.push(value);
    return value;
  }
  /**
   * @param {!Array} args
   * @param {!Object} val
   * @return {undefined}
   */
  function error(args, val) {
    args = $(args.O);
    var num = args.indexOf(val);
    if (0 <= num) {
      args.splice(num, 1);
    }
    func(val);
  }
  /**
   * @param {?} b
   * @param {!Array} x
   * @param {!Function} a
   * @return {?}
   */
  function max(b, x, a) {
    b.s = b.s || {};
    x.add(a, b, void 0);
    /** @type {number} */
    var name = ++timerId;
    b.s[name] = {
      bd : x,
      xb : a,
      priority : void 0
    };
    return name;
  }
  /**
   * @param {?} obj
   * @param {?} type
   * @return {undefined}
   */
  function assertEquals(obj, type) {
    if (obj.s && obj.s[type]) {
      var p = obj.s[type];
      p.bd.remove(p.xb, obj, p.priority);
      delete obj.s[type];
    } else {
      fail("unknown signal key");
    }
  }
  /**
   * @param {?} args
   * @param {number} fn
   * @return {undefined}
   */
  function visit(args, fn) {
    if (fn) {
      if (args.s) {
        var val = _format(args.s, function(CP) {
          return CP.bd.Kd == fn;
        });
        var out = forEach(Object.keys(val));
        val = out.next();
        for (; !val.done; val = out.next()) {
          assertEquals(args, val.value);
        }
      }
      if (args.O) {
        val = args.O.filter(function(l) {
          return l.src == fn;
        });
        out = forEach(val);
        val = out.next();
        for (; !val.done; val = out.next()) {
          error(args, val.value);
        }
      }
    }
  }
  /**
   * @param {!Object} a
   * @param {boolean} b
   * @return {?}
   */
  function memDir(a, b) {
    a.G = a.G || [];
    if (!b || a.G.includes(b)) {
      return b;
    }
    a.G.push(b);
    return b;
  }
  /**
   * @param {string} progressOld
   * @param {string} progressNew
   * @return {?}
   */
  function linkMarkers(progressOld, progressNew) {
    return progressOld > progressNew ? 1 : progressOld < progressNew ? -1 : 0;
  }
  /**
   * @param {!NodeList} a
   * @param {!Object} b
   * @return {?}
   */
  function arraysEqual(a, b) {
    if (a.length !== b.length) {
      return false;
    }
    /** @type {number} */
    var i = 0;
    for (; i < a.length; ++i) {
      if (a[i] !== b[i]) {
        return false;
      }
    }
    return true;
  }
  /**
   * @return {undefined}
   */
  function SelectorSet() {
    /** @type {null} */
    this.f = this.b = this.s = null;
  }
  /**
   * @param {!Object} value
   * @param {number} state
   * @return {?}
   */
  function tokenize(value, state) {
    if (0 == state) {
      return value.f || [];
    }
    if (!value.b) {
      return fail("Priority array values are zero"), [];
    }
    if (!(state in value.b)) {
      return [];
    }
    value = sprintf(value, state);
    return value ? value : (fail("Priority array values are zero"), []);
  }
  /**
   * @param {!Object} row
   * @return {?}
   */
  function keys(row) {
    if (!row.b) {
      return row.f ? row.f.slice() : [];
    }
    /** @type {!Array} */
    var ret = [];
    var months = $(row.s);
    /** @type {number} */
    var i = 0;
    for (; i < months.length; ++i) {
      var style = sprintf(row, months[i]);
      if (style) {
        ret.push.apply(ret, each(style));
      }
    }
    return ret;
  }
  /**
   * @param {!Object} obj
   * @param {?} key
   * @return {?}
   */
  function sprintf(obj, key) {
    return 0 == key ? obj.f : $(obj.b)[key];
  }
  /**
   * @param {number} fn
   * @return {undefined}
   */
  function Model(fn) {
    fn = void 0 === fn ? null : fn;
    method.call(this);
    /** @type {null} */
    this.b = null;
    /** @type {number} */
    this.Kd = fn;
  }
  /**
   * @param {!Object} client
   * @param {!Function} cb
   * @return {undefined}
   */
  function tail(client, cb) {
    /**
     * @param {?} granted
     * @return {undefined}
     */
    function callback(granted) {
      cb.call(void 0, granted);
      client.remove(callback, void 0, type);
    }
    /** @type {(number|undefined)} */
    var type = void 0 === type ? 0 : type;
    client.add(callback, void 0, type);
  }
  /**
   * @param {!Object} msg
   * @return {?}
   */
  function escape(msg) {
    return function(b) {
      return msg.dispatch(b);
    };
  }
  /**
   * @param {?} res
   * @return {undefined}
   */
  function tag(res) {
    var b = res.stack || res.toString();
    if (0 > String(b).indexOf(res.message)) {
      debug(res.message);
    }
    debug(b);
  }
  /**
   * @param {!Object} options
   * @return {undefined}
   */
  function getNode(options) {
    if (options) {
      $(!options.disposed);
      if ("function" == typeof options.cd) {
        options.cd();
      }
      /** @type {boolean} */
      options.disposed = true;
    }
  }
  /**
   * @param {number} y
   * @param {number} value
   * @param {?} max
   * @return {?}
   */
  function clip(y, value, max) {
    return Math.max(value, Math.min(max, y));
  }
  /**
   * @param {!Object} url
   * @return {?}
   */
  function fetch(url) {
    /** @type {(undefined|{})} */
    var data = void 0 === data ? {} : data;
    return new Promise(function(resolve, errorFunc) {
      /**
       * @return {?}
       */
      function response() {
        /** @type {!Array} */
        var keys = [];
        /** @type {!Array} */
        var result = [];
        var merged = {};
        var b;
        xhr.getAllResponseHeaders().replace(/^(.*?):[^\S\n]*([\s\S]*?)$/gm, function(canCreateDiscussions, key, a) {
          keys.push(key = key.toLowerCase());
          result.push([key, a]);
          b = merged[key];
          merged[key] = b ? b + "," + a : a;
          return "";
        });
        return {
          ok : 2 == (xhr.status / 100 | 0),
          status : xhr.status,
          statusText : xhr.statusText,
          url : xhr.responseURL,
          clone : response,
          text : function() {
            return Promise.resolve(xhr.responseText);
          },
          json : function() {
            return Promise.resolve(xhr.responseText).then(function(pStringValue) {
              return JSON.parse(pStringValue);
            });
          },
          blob : function() {
            return Promise.resolve(new Blob([xhr.response]));
          },
          headers : {
            keys : function() {
              return keys;
            },
            entries : function() {
              return result;
            },
            get : function(value) {
              return merged[value.toLowerCase()];
            },
            has : function(type) {
              return type.toLowerCase() in merged;
            }
          }
        };
      }
      /** @type {!XMLHttpRequest} */
      var xhr = new XMLHttpRequest;
      $(data);
      xhr.open(data.method || "get", url, true);
      var i;
      for (i in data.headers) {
        if (data.headers.hasOwnProperty(i)) {
          xhr.setRequestHeader(i, data.headers[i]);
        }
      }
      /** @type {boolean} */
      xhr.withCredentials = "include" == data.credentials;
      /**
       * @return {undefined}
       */
      xhr.onload = function() {
        resolve(response());
      };
      xhr.onerror = errorFunc;
      xhr.send(data.body);
    });
  }
  /**
   * @param {string} url
   * @return {?}
   */
  function login(url) {
    var result;
    return promise(function(c) {
      if (1 == c.b) {
        return ok(c, fetch(url), 2);
      }
      result = c.s;
      return result.ok ? c.return(result.json()) : c.return(null);
    });
  }
  /**
   * @return {?}
   */
  function fetchBuffer() {
    return new Promise(function(callback, resolve) {
      /** @type {!XMLHttpRequest} */
      var xhr = new XMLHttpRequest;
      xhr.open("GET", "data-1.bin", true);
      /** @type {string} */
      xhr.responseType = "arraybuffer";
      /**
       * @return {undefined}
       */
      xhr.onload = function() {
        callback(new Blob([xhr.response]));
      };
      xhr.onerror = resolve;
      xhr.send(null);
    });
  }
  /**
   * @param {string} name
   * @return {?}
   */
  function getFont(name) {
    /** @type {string} */
    var data_duplex_ = getContent(name) + ", ";
    a: {
      switch(name) {
        case "Lato":
        case "Roboto":
        case "Open Sans":
        case "Exo 2":
        case "Montserrat":
          /** @type {string} */
          name = "sans-serif";
          break a;
        case "Merriweather":
        case "Roboto Slab":
        case "PT Serif":
          /** @type {string} */
          name = "serif";
          break a;
        default:
          /** @type {string} */
          name = "";
      }
    }
    return data_duplex_ + name;
  }
  /**
   * @param {string} name
   * @return {?}
   */
  function getContent(name) {
    switch(name) {
      case "Lato":
        return "Lato";
      case "Roboto":
        return "roboto";
      case "Open Sans":
        return "open-sans";
      case "Merriweather":
        return "merriweather";
      case "Roboto Slab":
        return "roboto-slab";
      case "Exo 2":
        return "exo-2";
      case "Montserrat":
        return "montserrat";
      case "PT Serif":
        return "pt-serif";
      default:
        return "";
    }
  }
  /**
   * @return {?}
   */
  function now() {
    return !!navigator.platform && /iPad|iPhone|iPod/.test(navigator.platform);
  }
  /**
   * @return {?}
   */
  function isInputEventSupported() {
    return window.navigator.userAgent.includes("MSIE") || window.navigator.userAgent.includes("Trident");
  }
  /**
   * @param {!Node} obj
   * @param {(Object|string)} a
   * @return {undefined}
   */
  function write(obj, a) {
    if (obj instanceof HTMLElement || obj instanceof SVGElement) {
      if (isInputEventSupported()) {
        Object.keys(a).forEach(function(key) {
          var val = a[key];
          val = null === val ? "" : val.toString();
          variables[key] = val;
        });
        cssVars(Object.assign({}, defaults, {
          variables : variables
        }));
      } else {
        Object.keys(a).forEach(function(i) {
          var c1 = a[i];
          if (null != c1) {
            c1 = c1.toString();
          }
          obj.style.setProperty(i, c1);
        });
      }
    }
  }
  /**
   * @param {(Object|string)} value
   * @return {?}
   */
  function getComputedStyle(value) {
    value = (value = window.getComputedStyle(value)) && value.transform ? value.transform : "";
    if ("none" == value) {
      /** @type {string} */
      value = "";
    }
    return value;
  }
  /**
   * @param {string} shouldRemoveScope
   * @return {?}
   */
  function element(shouldRemoveScope) {
    /** @type {!Array} */
    var newItems = [];
    /** @type {number} */
    var i = 0;
    for (; i < arguments.length; ++i) {
      newItems[i] = arguments[i];
    }
    return newItems.filter(function(result) {
      return result;
    }).join(" ");
  }
  /**
   * @return {undefined}
   */
  function container() {
    method.apply(this, arguments);
  }
  /**
   * @param {!Object} data
   * @param {?} value
   * @return {?}
   */
  function append(data, value) {
    var a = new Model(data);
    memDir(data, a);
    if (value) {
      if (Array.isArray(value)) {
        value = forEach(value);
        var options = value.next();
        for (; !options.done; options = value.next()) {
          max(data, options.value, function(key) {
            return a.dispatch(key);
          });
        }
      } else {
        max(data, value, function(key) {
          return a.dispatch(key);
        });
      }
    }
    return a;
  }
  /**
   * @param {number} p2
   * @return {undefined}
   */
  function expression(p2) {
    container.call(this);
    /** @type {number} */
    this.b = p2;
  }
  /**
   * @param {!Object} data
   * @param {(Object|string)} value
   * @return {?}
   */
  function log(data, value) {
    var name = "string" == typeof data ? data : data.value;
    if (!arrData) {
      return fail("i18n is not initialized"), name;
    }
    data = arrData;
    if (data.b.hasOwnProperty(name)) {
      name = $(data.b[name] || es6methods.includes(name));
      if ("string" !== typeof name) {
        /** @type {string} */
        name = "";
      }
      if (value) {
        data = data.f;
        var api = forEach(Object.keys(value));
        var a = api.next();
        for (; !a.done; a = api.next()) {
          var key = a.value;
          a = value[key];
          key = data(key);
          var g = void 0;
          /** @type {(string|undefined)} */
          g = void 0 === g ? "g" : g;
          name = name.replace(new RegExp(key.replace(/([.*+?^=!:${}()|[\]/\\])/g, "\\$1"), g), a);
        }
      }
      value = name;
    } else {
      fail("unknown message id: " + name);
      value = name;
    }
    return value;
  }
  /**
   * @param {string} value
   * @return {?}
   */
  function round(value) {
    /** @type {number} */
    var whiteRating = parseInt(value.substr(1, 2), 16);
    /** @type {number} */
    var pageInd = parseInt(value.substr(3, 2), 16);
    /** @type {number} */
    value = parseInt(value.substr(5, 2), 16);
    return [whiteRating, pageInd, value];
  }
  /**
   * @param {string} a
   * @return {?}
   */
  function getChildren(a) {
    var b = round(a);
    /** @type {number} */
    a = b[0] / 255;
    /** @type {number} */
    var c = b[1] / 255;
    /** @type {number} */
    b = b[2] / 255;
    /** @type {number} */
    var y = Math.max(a, c, b);
    /** @type {number} */
    var x = Math.min(a, c, b);
    /** @type {number} */
    var annWidth = 0;
    /** @type {number} */
    var g = 0;
    /** @type {number} */
    var lineHeightProportion = .5 * (y + x);
    if (y != x) {
      if (y == a) {
        /** @type {number} */
        annWidth = 60 * (c - b) / (y - x);
      } else {
        if (y == c) {
          /** @type {number} */
          annWidth = 60 * (b - a) / (y - x) + 120;
        } else {
          if (y == b) {
            /** @type {number} */
            annWidth = 60 * (a - c) / (y - x) + 240;
          }
        }
      }
      /** @type {number} */
      g = 0 < lineHeightProportion && .5 >= lineHeightProportion ? (y - x) / (2 * lineHeightProportion) : (y - x) / (2 - 2 * lineHeightProportion);
    }
    return [Math.round(annWidth + 360) % 360, g, lineHeightProportion];
  }
  /**
   * @param {string} p
   * @param {number} start
   * @return {?}
   */
  function setPoint(p, start) {
    var countainer = forEach(round(p));
    p = countainer.next().value;
    var vulnData = countainer.next().value;
    countainer = countainer.next().value;
    return "rgba(" + p + ", " + vulnData + ", " + countainer + ", " + start + ")";
  }
  /**
   * @param {string} target
   * @return {?}
   */
  function equals(target) {
    var countainer = forEach(getChildren(target));
    target = countainer.next().value;
    var vulnData = countainer.next().value;
    countainer = countainer.next().value;
    return "hsl(" + target + ", " + 100 * vulnData + "%, " + 100 * (countainer + -.08) + "%)";
  }
  /**
   * @param {string} val
   * @param {number} callback
   * @return {?}
   */
  function text(val, callback) {
    var countainer = forEach(round(val));
    val = countainer.next().value;
    var vulnData = countainer.next().value;
    countainer = countainer.next().value;
    return "rgba(" + val + ", " + vulnData + ", " + countainer + ", " + callback + ")";
  }
  /**
   * @param {number} item
   * @param {number} start
   * @return {?}
   */
  function traverse(item, start) {
    var cs = forEach(getChildren(item));
    var d = cs.next().value;
    item = cs.next().value;
    cs = cs.next().value;
    d = d.toFixed(2);
    /** @type {string} */
    item = (100 * item + -1).toFixed(2);
    start = (100 * cs + start).toFixed(2);
    return "hsl(" + d + ", " + item + "%, " + start + "%)";
  }
  /**
   * @param {!Function} match
   * @return {?}
   */
  function handler(match) {
    /**
     * @return {undefined}
     */
    function end() {
      if (group) {
        clearTimeout(group.id);
        group = void 0;
      }
    }
    var group;
    return {
      Sg : function() {
        return isString(group);
      },
      cancel : end,
      flush : function() {
        if (group) {
          var start = group.zb;
          end();
          start();
        }
      },
      call : function(name) {
        /**
         * @return {undefined}
         */
        function request() {
          end();
          match.apply(null, each(value));
        }
        /** @type {!Array} */
        var value = [];
        /** @type {number} */
        var i = 0;
        for (; i < arguments.length; ++i) {
          value[i] = arguments[i];
        }
        end();
        group = {
          id : setTimeout(request, 100),
          zb : request
        };
      },
      Eg : function(canCreateDiscussions) {
        /** @type {!Array} */
        var value = [];
        /** @type {number} */
        var i = 0;
        for (; i < arguments.length; ++i) {
          value[i] = arguments[i];
        }
        end();
        match.apply(null, each(value));
      }
    };
  }
  /**
   * @return {?}
   */
  function spin() {
    return 1180 >= window.innerWidth && 739 < window.innerWidth;
  }
  /**
   * @param {string} type
   * @return {undefined}
   */
  function User(type) {
    /** @type {string} */
    this.Type = type;
  }
  /**
   * @param {?} data
   * @param {!Object} type
   * @return {?}
   */
  function callFunction(data, type) {
    /** @type {function(new:Uint8Array, (Array<number>|ArrayBuffer|ArrayBufferView|SharedArrayBuffer|null|number), number=, number=): ?} */
    var result = Uint8Array;
    var num;
    if (num = data.length) {
      /** @type {number} */
      var i = 0;
      for (; 1 < --num % 4 && "=" === data.charAt(num);) {
        ++i;
      }
      /** @type {number} */
      num = Math.ceil(3 * data.length) / 4 - i;
    } else {
      /** @type {number} */
      num = 0;
    }
    /** @type {!Uint8Array} */
    result = new result(num);
    /** @type {(number|undefined)} */
    var y = void 0 === y ? 0 : y;
    /** @type {number} */
    num = 0;
    /** @type {number} */
    i = 0;
    for (; i < data.length;) {
      var c4 = data.charCodeAt(i++);
      if (61 === c4 && 1 < num) {
        break;
      }
      if (void 0 === (c4 = decodeChars[c4])) {
        throw Error("invalid encoding");
      }
      switch(num) {
        case 0:
          var lastChar = c4;
          /** @type {number} */
          num = 1;
          break;
        case 1:
          /** @type {number} */
          result[y++] = lastChar << 2 | (c4 & 48) >> 4;
          lastChar = c4;
          /** @type {number} */
          num = 2;
          break;
        case 2:
          /** @type {number} */
          result[y++] = (lastChar & 15) << 4 | (c4 & 60) >> 2;
          lastChar = c4;
          /** @type {number} */
          num = 3;
          break;
        case 3:
          /** @type {number} */
          result[y++] = (lastChar & 3) << 6 | c4;
          /** @type {number} */
          num = 0;
      }
    }
    if (1 === num) {
      throw Error("invalid encoding");
    }
    /** @type {number} */
    data = (new Uint32Array(result.buffer.slice(0, Uint32Array.BYTES_PER_ELEMENT)))[0];
    /** @type {!Uint8Array} */
    y = new Uint8Array(result.buffer.slice(Uint32Array.BYTES_PER_ELEMENT));
    type = type(data).Type;
    data = type.decode(y);
    return type.toObject(data);
  }
  /**
   * @param {!Object} out
   * @param {!Function} name
   * @return {?}
   */
  function _update(out, name) {
    var query = {
      order : [],
      A : {}
    };
    out = forEach(out);
    var child = out.next();
    for (; !child.done; child = out.next()) {
      child = child.value;
      var index = name(child);
      query.order.push(index);
      query.A[index] = child;
    }
    return query;
  }
  /**
   * @param {!Object} d
   * @return {?}
   */
  function match(d) {
    return 0 >= d.order.length ? null : d.A[d.order[0]];
  }
  /**
   * @param {!Object} results
   * @param {!Function} cb
   * @return {undefined}
   */
  function ready(results, cb) {
    results.order.forEach(function(i) {
      cb(results.A[i]);
    });
  }
  /**
   * @param {!Array} args
   * @param {!Function} p
   * @return {?}
   */
  function trigger(args, p) {
    var i = args.findIndex(function(options) {
      return options.name === p.name;
    });
    var results = args.slice();
    switch(p.name) {
      case "bold":
      case "italic":
      case "underline":
      case "strikethrough":
        if (0 <= i) {
          return args;
        }
        results.push(p);
        break;
      case "fontSize":
      case "fontColor":
      case "textHighlightColor":
      case "textHighlightOpacity":
        if (0 <= i) {
          /** @type {!Function} */
          results[i] = p;
        } else {
          results.push(p);
        }
        break;
      default:
        throw isNaN(p), Error("Unknown text style type: " + p);
    }
    return results;
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function flush(data) {
    return each(data).concat().sort(function(shotsGroup, cookieOne) {
      return linkMarkers(shotsGroup.name, cookieOne.name);
    }).map(function(storedComponents) {
      return JSON.stringify(storedComponents);
    }).join("$");
  }
  /**
   * @param {?} g
   * @param {string} type
   * @return {?}
   */
  function color(g, type) {
    return 0 <= g.findIndex(function(a) {
      return a.name === type;
    });
  }
  /**
   * @param {?} parent
   * @return {?}
   */
  function replace(parent) {
    parent = void 0 === parent ? {} : parent;
    return {
      url : void 0 === parent.url ? null : parent.url,
      style : void 0 === parent.style ? [] : parent.style
    };
  }
  /**
   * @param {!Object} a
   * @param {!Object} b
   * @return {?}
   */
  function rotate(a, b) {
    var cloud;
    if (cloud = a.url === b.url) {
      b = b.style;
      /** @type {boolean} */
      cloud = flush(a.style) === flush(b);
    }
    return cloud;
  }
  /**
   * @return {?}
   */
  function uuid() {
    var key;
    for (; void 0 === key || pro6pp_cache.hasOwnProperty(key) || !isNaN(+key);) {
      /** @type {string} */
      key = Math.floor(Math.random() * caveWidth).toString(32);
    }
    /** @type {boolean} */
    pro6pp_cache[key] = true;
    return key;
  }
  /**
   * @param {!Object} list
   * @param {!Function} cb
   * @return {undefined}
   */
  function parallel(list, cb) {
    ready(list, cb);
  }
  /**
   * @param {!Object} p
   * @return {?}
   */
  function toString(p) {
    return p && "p" === p.type ? p : null;
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function get(data) {
    data = void 0 === data ? {} : data;
    var b = data.B || [];
    if ("string" === typeof b) {
      b = abs(b);
    }
    return "ol" == data.N ? {
      type : "p",
      key : data.key || uuid(),
      N : data.N,
      textAlign : data.textAlign || "left",
      B : b,
      bb : data.bb || false
    } : {
      type : "p",
      key : data.key || uuid(),
      N : data.N || "text",
      textAlign : data.textAlign || "left",
      B : b
    };
  }
  /**
   * @param {!Object} args
   * @return {?}
   */
  function abs(args) {
    var action = replace();
    return each(args).concat().map(function(textToFormat) {
      return {
        text : textToFormat,
        sa : action
      };
    });
  }
  /**
   * @param {!Object} value
   * @return {?}
   */
  function handleTaskNonConflict(value) {
    value = toString(value);
    return !!value && 0 == value.B.length;
  }
  /**
   * @param {!Object} b
   * @return {?}
   */
  function $A(b) {
    var push;
    if (!push) {
      /**
       * @param {?} len
       * @return {?}
       */
      push = function(len) {
        return len;
      };
    }
    b = b.B;
    if (0 == b.length) {
      return [];
    }
    /** @type {!Array} */
    var results = [{
      text : b[0].text,
      ga : push(b[0].sa),
      Wb : false
    }];
    /** @type {number} */
    var i = 1;
    for (; i < b.length; ++i) {
      var e = b[i];
      var labelAngle = push(e.sa);
      /** @type {number} */
      var address = results.length - 1;
      if (rotate(labelAngle, results[address].ga)) {
        results[address].text += e.text;
      } else {
        results.push({
          text : e.text,
          ga : labelAngle,
          Wb : false
        });
      }
    }
    /** @type {boolean} */
    results[results.length - 1].Wb = true;
    return results;
  }
  /**
   * @param {!Object} args
   * @return {?}
   */
  function index(args) {
    /** @type {!Array} */
    var result = [];
    args = forEach(args);
    var c = args.next();
    for (; !c.done; c = args.next()) {
      result.push({
        text : c.value,
        sa : replace()
      });
    }
    return result;
  }
  /**
   * @param {?} parent
   * @return {?}
   */
  function createButton(parent) {
    parent = void 0 === parent ? {} : parent;
    return {
      key : void 0 === parent.key ? uuid() : parent.key,
      L : void 0 === parent.L ? false : parent.L,
      content : addChild([get({
        B : void 0 === parent.text ? [] : parent.text
      })])
    };
  }
  /**
   * @param {number} node
   * @return {?}
   */
  function insertBefore(node) {
    node = void 0 === node ? {} : node;
    return {
      ka : void 0 === node.ka ? [createButton({
        L : true,
        text : index(log("Roll.Exercise.Question.Content.PossibleAnswer") + " 1")
      }), createButton({
        text : index(log("Roll.Exercise.Question.Content.PossibleAnswer") + " 2")
      }), createButton({
        text : index(log("Roll.Exercise.Question.Content.PossibleAnswer") + " 3")
      })] : node.ka
    };
  }
  /**
   * @param {?} result
   * @return {?}
   */
  function reply(result) {
    result = void 0 === result ? {} : result;
    return {
      key : void 0 === result.key ? uuid() : result.key,
      text : void 0 === result.text ? "" : result.text
    };
  }
  /**
   * @param {!Array} i
   * @return {?}
   */
  function store(i) {
    var c = i = void 0 === i ? {} : i;
    i = void 0 === c.Da ? false : c.Da;
    c = void 0 === c.ia ? [reply({
      text : log("Roll.Exercise.TypeIn.Answer") + " 1"
    })] : c.ia;
    return {
      Da : i,
      ia : c
    };
  }
  /**
   * @param {string} value
   * @return {?}
   */
  function getTypes(value) {
    value = void 0 === value ? {} : value;
    var data = void 0 === value.L ? [] : value.L;
    var d3_time_parseMonth = void 0 === value.Ga ? [] : value.Ga;
    return {
      type : void 0 === value.type ? "correctIncorrect" : value.type,
      o : addChild([get({
        B : void 0 === value.o ? [] : value.o
      })]),
      L : addChild([get({
        B : data
      })]),
      Ga : addChild([get({
        B : d3_time_parseMonth
      })])
    };
  }
  /**
   * @param {!Object} s
   * @param {(!Function|boolean)} type
   * @return {?}
   */
  function sub(s, type) {
    s = toString(match(("answered" == s.type ? s.o : type ? s.L : s.Ga).T));
    return $(s);
  }
  /**
   * @param {!Object} result
   * @return {?}
   */
  function generate(result) {
    var previewKey = void 0 === result.key ? uuid() : result.key;
    var handleType = result.type;
    var processRelativeTime = void 0 === result.M ? null : result.M;
    var buffer = void 0 === result.description ? "" : result.description;
    if (void 0 === result.content) {
      a: {
        var value = result.type;
        switch(value) {
          case "mc":
          case "mr":
            value = insertBefore();
            break a;
          case "ti":
            value = store();
            break a;
          default:
            throw isNaN(value), Error("Unknown question type: " + value);
        }
      }
    } else {
      value = result.content;
    }
    result = void 0 === result.V ? getTypes() : result.V;
    return {
      key : previewKey,
      type : handleType,
      M : processRelativeTime,
      description : addChild([get({
        B : buffer
      })]),
      content : value,
      V : result
    };
  }
  /**
   * @param {number} data
   * @return {?}
   */
  function arrToConnection(data) {
    data = void 0 === data ? {} : data;
    return {
      ab : void 0 === data.ab ? false : data.ab,
      Ma : void 0 === data.Ma ? false : data.Ma,
      H : void 0 === data.H ? true : data.H
    };
  }
  /**
   * @param {?} options
   * @return {?}
   */
  function showTooltip(options) {
    options = void 0 === options ? {} : options;
    return {
      type : "",
      key : options.key || uuid(),
      w : options.w || "inner"
    };
  }
  /**
   * @param {?} data
   * @return {?}
   */
  function getDefaults(data) {
    data = void 0 === data ? {} : data;
    var b = data.B || [];
    if ("string" === typeof b) {
      b = abs(b);
    }
    return Object.assign({}, showTooltip(data), {
      width : data.width || 0,
      height : data.height || 0,
      B : b
    });
  }
  /**
   * @param {!Object} keys
   * @return {?}
   */
  function serialize(keys) {
    var query = {
      order : [],
      A : {}
    };
    keys = forEach(keys);
    var child = keys.next();
    for (; !child.done; child = keys.next()) {
      child = child.value;
      query.order.push(child.key);
      query.A[child.key] = child;
    }
    return query;
  }
  /**
   * @param {!Array} a
   * @return {?}
   */
  function addChild(a) {
    a = void 0 === a ? [get()] : a;
    return {
      T : serialize(a)
    };
  }
  /**
   * @param {string} val
   * @return {?}
   */
  function isUndefined(val) {
    return "mc" === val.type || "mr" === val.type ? val : null;
  }
  /**
   * @param {!Array} result
   * @return {undefined}
   */
  function parsePirateBayProxyList(result) {
    /** @type {number} */
    var i = 0;
    for (; i < result.length; ++i) {
      /** @type {number} */
      var id = Math.floor(Math.random() * i);
      var filePath = result[i];
      result[i] = result[id];
      result[id] = filePath;
    }
  }
  /**
   * @param {!Array} status
   * @param {!Object} doc
   * @return {?}
   */
  function findTemplatesInDocument(status, doc) {
    status = status.map(function(ja) {
      return {
        ja : ja,
        selected : false
      };
    });
    if (doc) {
      parsePirateBayProxyList(status);
    }
    return status;
  }
  /**
   * @param {(Object|string)} a
   * @param {!Object} x
   * @return {?}
   */
  function translate(a, x) {
    var c = isUndefined(a);
    if (c) {
      x = {
        da : findTemplatesInDocument(c.content.ka, x)
      };
    } else {
      if (x = "ti" === a.type ? a : null) {
        x = {
          Bd : x.content,
          ua : ""
        };
      } else {
        throw Error("Unknown question type: " + a.type);
      }
    }
    return {
      ba : a,
      o : false,
      j : x
    };
  }
  /**
   * @param {?} data
   * @param {!Object} callback
   * @return {?}
   */
  function reverse(data, callback) {
    return data.map(function(x) {
      return translate(x, callback);
    });
  }
  /**
   * @param {!Object} p
   * @return {?}
   */
  function getInfo(p) {
    var preMasterSecretEncrypted = reverse(p.Z, p.R.Ma);
    return {
      key : p.key,
      type : "E",
      Sb : p,
      la : preMasterSecretEncrypted,
      ca : 0 < preMasterSecretEncrypted.length ? 0 : -1
    };
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function is(data) {
    var value = data.ca;
    return 0 <= value ? data.la[value] : null;
  }
  /**
   * @param {?} value
   * @return {?}
   */
  function value(value) {
    value = void 0 === value ? [] : value;
    var y = serialize(value);
    /** @type {!Array} */
    var promises = [];
    value = forEach(value);
    var item = value.next();
    for (; !item.done; item = value.next()) {
      if (item = (item = item.value) && "E" === item.type ? item : null) {
        promises.push(getInfo(item));
      }
    }
    return {
      T : y,
      progress : 0,
      oa : promises
    };
  }
  /**
   * @param {!Array} prop
   * @return {?}
   */
  function status(prop) {
    prop = void 0 === prop ? {} : prop;
    return {
      title : prop.title || "",
      kf : prop.kf || ""
    };
  }
  /**
   * @param {!Object} msg
   * @param {!Object} expected
   * @return {?}
   */
  function deepEqual(msg, expected) {
    return (Math.floor(msg.progress) + expected.reduce(function(canCreateDiscussions, upload) {
      return canCreateDiscussions + Math.floor(upload.progress);
    }, 0)) / (expected.length + 1);
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function stringify(data) {
    return data.chapters[data.fa + 1] || null;
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function equal(data) {
    var index = data.fa;
    return -1 == index ? data.intro : data.chapters[index];
  }
  /**
   * @param {?} context
   * @return {?}
   */
  function go(context) {
    /** @type {!Array} */
    var notes = [];
    var api = forEach(context.T.order);
    var nextOther = api.next();
    for (; !nextOther.done; nextOther = api.next()) {
      notes.push(context.T.A[nextOther.value]);
    }
    return value(notes);
  }
  /**
   * @param {!Object} options
   * @return {?}
   */
  function attach(options) {
    var data = {
      aa : options.u.aa,
      title : options.u.title,
      jb : options.u.qa
    };
    if (options.u.qa) {
      data.jb = {
        src : options.u.qa.src,
        width : options.u.qa.width,
        height : options.u.qa.height,
        color : options.u.qa.color,
        opacity : options.u.qa.opacity
      };
    }
    /** @type {({aa: ?, jb: ?, title: ?}|{})} */
    data = void 0 === data ? {} : data;
    data = {
      title : data.title || "",
      aa : data.aa || "",
      jb : data.jb || null
    };
    options = go(options.j);
    return {
      key : "intro",
      u : data,
      j : options,
      index : -1,
      progress : 0
    };
  }
  /**
   * @param {!Object} node
   * @return {?}
   */
  function routes(node) {
    var intro = attach(node.intro);
    var tutorial = node.chapters.order.map(function(p, params) {
      p = node.chapters.A[p];
      params = {
        key : p.key,
        index : params,
        u : status({
          title : p.u.title
        }),
        j : go(p.j)
      };
      return {
        key : params.key || "",
        u : params.u || status(),
        j : params.j || value(),
        index : params.index || 0,
        progress : params.progress || 0
      };
    });
    return {
      intro : intro,
      chapters : tutorial,
      fa : -1,
      Ta : false
    };
  }
  /**
   * @param {!Function} obj
   * @return {?}
   */
  function trim(obj) {
    return isUndefined(obj.ba) ? obj : null;
  }
  /**
   * @param {!Function} data
   * @return {?}
   */
  function parseInt(data) {
    var modelElementPath = data.ba;
    return "ti" === modelElementPath.type && modelElementPath ? data : null;
  }
  /**
   * @param {number} id
   * @return {?}
   */
  function t(id) {
    return next(id, "string");
  }
  /**
   * @param {number} num
   * @return {?}
   */
  function skip(num) {
    return next(num, "int32");
  }
  /**
   * @param {number} n
   * @return {?}
   */
  function foo(n) {
    return next(n, "bool");
  }
  /**
   * @param {undefined} num
   * @return {?}
   */
  function step(num) {
    return next(num, "float");
  }
  /**
   * @param {(Object|string)} key
   * @return {?}
   */
  function callback(key) {
    /** @type {string} */
    var b = b || "Type" + ++fd;
    b = new Color(b);
    var api = forEach(Object.keys(key));
    var x = api.next();
    for (; !x.done; x = api.next()) {
      x = x.value;
      var c = key[x];
      var value = c.La;
      if ("string" == typeof value) {
        b = b.add(new Date(x, c.id, value, c.Cc));
      } else {
        if (value instanceof Color) {
          b = b.add(new Date(x, c.id, value.name, c.Cc));
        } else {
          value = callback(value);
          b = b.add(new Date(x, c.id, value.name, c.Cc));
        }
      }
    }
    edgePointB.add(b);
    return b;
  }
  /**
   * @param {number} num
   * @param {string} name
   * @param {number} value
   * @return {?}
   */
  function next(num, name, value) {
    return {
      id : num,
      La : name,
      Cc : void 0 === value ? "required" : value
    };
  }
  /**
   * @param {number} id
   * @param {string} data
   * @return {?}
   */
  function commit(id, data) {
    if ("function" == typeof data) {
      data = data(id).La;
    }
    return next(id, data, "repeated");
  }
  /**
   * @param {number} i
   * @param {string} callback
   * @return {?}
   */
  function pick(i, callback) {
    if ("function" == typeof callback) {
      callback = callback(i).La;
    }
    return next(i, callback, "optional");
  }
  /**
   * @param {(Object|string)} result
   * @return {?}
   */
  function clone(result) {
    var obj = callback({});
    var c = callback({}).add(new Date("type", 0, "string", "required"));
    var api = forEach(Object.keys(result));
    var s = api.next();
    for (; !s.done; s = api.next()) {
      s = s.value;
      result[s] = result[s].add(new Date("type", 0, "string", "required"));
    }
    /**
     * @param {!Object} value
     * @return {?}
     */
    obj.fromObject = function(value) {
      return result[value.type].fromObject(value);
    };
    /**
     * @param {!Object} data
     * @return {?}
     */
    obj.toObject = function(data) {
      return result[data.type].toObject(data);
    };
    /**
     * @param {!Object} type
     * @param {?} data
     * @return {?}
     */
    obj.encode = function(type, data) {
      return result[type.type].encode(type, data);
    };
    /**
     * @param {!Object} v
     * @param {?} total
     * @return {?}
     */
    obj.decode = function(v, total) {
      /** @type {!Array} */
      var args = [];
      /** @type {number} */
      var i = 1;
      for (; i < arguments.length; ++i) {
        args[i - 1] = arguments[i];
      }
      if (v instanceof protobuf.Reader) {
        i = v.pos;
        var module = c.decode.apply(c, [v].concat(each(args)));
        module = result[module.type];
        v.pos = i;
        return module.decode.apply(module, [v].concat(each(args)));
      }
      throw Error("top level union is not supported");
    };
    /**
     * @param {!Object} data
     * @return {?}
     */
    obj.verify = function(data) {
      var key = result[data.type];
      return key ? key.verify(data) : "unknown type " + data.type;
    };
    return obj;
  }
  /**
   * @param {!Object} self
   * @param {!Object} data
   * @return {undefined}
   */
  function check(self, data) {
    var locale = data.la;
    if (locale) {
      var d = self.la;
      /** @type {number} */
      var i = 0;
      for (; i < d.length; ++i) {
        var value = d[i];
        var result = locale[i];
        value.o = result.o;
        result = result.j;
        var data = trim(value);
        if (data) {
          if (value = data.j, data = result, data.da) {
            var i = value.da;
            /** @type {!Map} */
            result = new Map;
            i = forEach(i);
            var item = i.next();
            for (; !item.done; item = i.next()) {
              item = item.value;
              result.set(item.ja.key, item);
            }
            /** @type {!Array} */
            i = [];
            data = forEach(data.da);
            item = data.next();
            for (; !item.done; item = data.next()) {
              item = item.value;
              var data = result.get(item.key);
              data.selected = item.selected;
              i.push(data);
            }
            /** @type {!Array} */
            value.da = i;
          }
        } else {
          if (data = parseInt(value)) {
            data.j.ua = result.ua;
          } else {
            throw Error("Unknown question type: " + value.ba.type);
          }
        }
      }
      data = data.ca;
      if ("number" == typeof data) {
        /** @type {!Object} */
        self.ca = data;
      }
    }
  }
  /**
   * @param {!Object} data
   * @param {number} self
   * @return {undefined}
   */
  function handleRequest(data, self) {
    var bar = self.intro;
    var dataset = self.chapters;
    self = self.rd;
    if (bar) {
      var i = bar.progress;
      bar = bar.oa;
      if ("number" == typeof i) {
        /** @type {number} */
        data.intro.progress = i;
      }
      if (bar) {
        then(data.intro.j.oa, bar);
      }
    }
    if (dataset) {
      data.chapters.forEach(function(self, i) {
        var bar = $(dataset)[i];
        i = bar.progress;
        bar = bar.oa;
        if (i) {
          /** @type {!Object} */
          self.progress = i;
        }
        if (bar) {
          then(self.j.oa, bar);
        }
      });
    }
    if ("number" == typeof self && 0 <= self) {
      /** @type {number} */
      data.fa = self;
    }
  }
  /**
   * @param {!Object} f
   * @param {!Object} action
   * @return {undefined}
   */
  function then(f, action) {
    var i = {};
    action = forEach(action);
    var s = action.next();
    for (; !s.done; i = {
      Jb : i.Jb
    }, s = action.next()) {
      if (i.Jb = s.value, s = f.find(function(f) {
        return function(b) {
          return f.Jb.key == b.key;
        };
      }(i))) {
        var body = "E" != s.type ? null : s;
        if (body) {
          check(body, i.Jb);
        } else {
          throw Error("Unexpected block state type: " + s.type);
        }
      }
    }
  }
  /**
   * @param {!Array} payload
   * @param {?} next
   * @return {?}
   */
  function onMessage(payload, next) {
    payload = routes(payload.content);
    if (next) {
      try {
        var x = callFunction(next, function() {
          return user;
        });
        handleRequest(payload, x);
      } catch (options) {
        fail(options);
      }
    }
    return payload;
  }
  /**
   * @param {?} p2
   * @return {?}
   */
  function getValue(p2) {
    return p2.map(function(r) {
      var n = r.ba;
      var nodeKey = n.key;
      n = n.type;
      var o = r.o;
      var data = trim(r);
      if (data) {
        r = onError(data.j);
      } else {
        if (data = parseInt(r)) {
          r = {
            ua : data.j.ua
          };
        } else {
          throw Error("Unknown question type: " + r.ba.type);
        }
      }
      return {
        key : nodeKey,
        type : n,
        o : o,
        j : r
      };
    });
  }
  /**
   * @param {?} trace
   * @return {?}
   */
  function onError(trace) {
    return {
      da : trace.da.map(function(nextProps) {
        return {
          key : nextProps.ja.key,
          selected : nextProps.selected
        };
      })
    };
  }
  /**
   * @param {?} val
   * @return {?}
   */
  function sortBy(val) {
    return val.map(function(rawData) {
      var data = "E" != rawData.type ? null : rawData;
      if (data) {
        return {
          key : data.key,
          type : data.type,
          ca : data.ca,
          la : getValue(data.la)
        };
      }
      throw Error("Unexpected block state type: " + rawData.type);
    });
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function Animation(data) {
    return {
      intro : {
        progress : data.intro.progress,
        oa : sortBy(data.intro.j.oa)
      },
      chapters : data.chapters.map(function(self) {
        return {
          progress : self.progress,
          oa : sortBy(self.j.oa)
        };
      }),
      rd : data.fa
    };
  }
  /**
   * @param {?} elem
   * @return {undefined}
   */
  function Client(elem) {
    this.cb = elem.Cb.cb;
    /** @type {boolean} */
    this.qd = false;
    this.Ia = elem.Cb.Ia;
    this.Ha = elem.Cb.Ha;
    this.Ra = elem.Cb.Ra;
    this.fonts = {
      Wa : getContent("Lato"),
      dh : getContent(elem.kb.qc),
      content : getContent(elem.kb.ec)
    };
  }
  /**
   * @return {?}
   */
  function getExtendedRouter() {
    /**
     * @return {undefined}
     */
    function addToList() {
      removeFromList(ret);
      /** @type {null} */
      ret = null;
    }
    /** @type {function(): undefined} */
    var selected = execute;
    var ret;
    return {
      call : function() {
        if (ret) {
          addToList();
        }
        ret = apply(selected);
      },
      cancel : addToList,
      Tg : function() {
        return null != ret;
      }
    };
  }
  /**
   * @param {!Object} self
   * @return {?}
   */
  function constructor(self) {
    /**
     * @return {undefined}
     */
    function render() {
      if (!m) {
        var elapsed = clip(Date.now() - k, 0, duration);
        var w = parseInt(elapsed / duration);
        d.animate(w);
        if (elapsed < duration) {
          apply(render);
        } else {
          if (_oldStartWalking) {
            _oldStartWalking();
          }
          if (g) {
            g.remove(c);
          }
          instance.dispatch(true);
          apply(function() {
            d.complete();
          });
        }
      }
    }
    /**
     * @return {undefined}
     */
    function c() {
      /** @type {boolean} */
      m = true;
      $(g).remove(c);
      instance.dispatch(false);
    }
    var d = self.fb;
    var parseInt = self.Hb;
    var duration = self.duration;
    var g = self.Xa;
    var ChromosomeInfo = self.Yd;
    var _oldStartWalking = self.Uc;
    /** @type {number} */
    var k = Date.now();
    /** @type {boolean} */
    var m = false;
    var instance = new Model;
    if (ChromosomeInfo) {
      ChromosomeInfo();
    }
    apply(render);
    if (g) {
      g.add(c);
    }
    return instance;
  }
  /**
   * @param {number} a
   * @return {?}
   */
  function object(a) {
    return --a * a * a + 1;
  }
  /**
   * @param {number} a
   * @return {?}
   */
  function fill(a) {
    return .5 > a ? 4 * a * a * a : (a - 1) * (2 * a - 2) * (2 * a - 2) + 1;
  }
  /**
   * @param {number} min
   * @return {?}
   */
  function mapResponse(min) {
    return function(max) {
      return max < min ? 0 : object((max - min) / (1 - min));
    };
  }
  /**
   * @param {!Function} callback
   * @return {?}
   */
  function wrapMethod(callback) {
    return function(identifierPositions) {
      return 1 - callback(identifierPositions);
    };
  }
  /**
   * @param {!Object} b
   * @param {!Node} c
   * @param {string} position
   * @param {!ClientRect} a
   * @return {undefined}
   */
  function reset(b, c, position, a) {
    b = b.getBoundingClientRect();
    /** @type {number} */
    var height = 0;
    if ("top" == position && a.top > b.top) {
      /** @type {number} */
      height = b.top - a.top;
    } else {
      if ("bottom" == position && a.bottom < b.bottom) {
        /** @type {number} */
        height = b.bottom - a.bottom;
      }
    }
    if (height) {
      var y = c.scrollTop;
      constructor({
        duration : 500,
        Hb : object,
        fb : {
          animate : function(t) {
            c.scrollTop = y + height * t;
          },
          complete : function() {
          },
          wb : []
        }
      });
    }
  }
  /**
   * @param {!Element} obj
   * @return {?}
   */
  function g(obj) {
    var elem = window.getComputedStyle(obj);
    /** @type {number} */
    var middle = elem && elem.opacity ? parseFloat(elem.opacity) : 1;
    return {
      animate : function(d) {
        /** @type {string} */
        obj.style.opacity = "" + (middle - middle * d);
      },
      complete : function() {
        /** @type {string} */
        obj.style.opacity = "";
      },
      wb : ["opacity"]
    };
  }
  /**
   * @param {!Array} resolve
   * @return {?}
   */
  function exec(resolve) {
    return {
      animate : function(id) {
        resolve.forEach(function(c) {
          return c.animate(id);
        });
      },
      complete : function() {
        resolve.forEach(function(completionNotifier) {
          return completionNotifier.complete();
        });
      },
      wb : []
    };
  }
  /**
   * @param {!Function} name
   * @param {!Object} module
   * @param {string} data
   * @return {?}
   */
  function resolve(name, module, data) {
    /**
     * @return {?}
     */
    function _resolvedMatcher() {
      return [name, module, data];
    }
    /** @type {string} */
    _resolvedMatcher.Hd = "renderBinding";
    return _resolvedMatcher;
  }
  /**
   * @param {string} v
   * @param {!Function} name
   * @param {!Object} url
   * @return {?}
   */
  function assign(v, name, url) {
    if ("string" != typeof v) {
      if (!wm.has(v)) {
        wm.set(v, uuid());
      }
      v = wm.get(v);
    }
    return {
      id : v,
      Zd : resolve(name, url, void 0)
    };
  }
  /**
   * @return {?}
   */
  function enter() {
    if (!mObs) {
      /** @type {!MutationObserver} */
      mObs = new MutationObserver(function(args) {
        args = forEach(args);
        var current = args.next();
        for (; !current.done; current = args.next()) {
          current = current.value;
          var a = forEach(current.removedNodes);
          var element = a.next();
          for (; !element.done; element = a.next()) {
            var subject = element.value;
            var b = forEach(storage.keys());
            element = b.next();
            for (; !element.done; element = b.next()) {
              element = element.value;
              var self = $(storage.get(element));
              if (self.remove && subject.contains && subject.contains(element)) {
                self.remove();
              }
            }
          }
          current = forEach(current.addedNodes);
          a = current.next();
          for (; !a.done; a = current.next()) {
            a = a.value;
            subject = forEach(storage.keys());
            element = subject.next();
            for (; !element.done; element = subject.next()) {
              element = element.value;
              b = $(storage.get(element));
              if (b.add && a.contains && a.contains(element)) {
                b.add();
              }
            }
          }
        }
      });
    }
    return mObs;
  }
  /**
   * @param {string} f
   * @param {string} a
   * @return {?}
   */
  function connect(f, a) {
    /** @type {boolean} */
    var c = 0 == storage.size;
    substring(!storage.has(f));
    storage.set(f, a);
    if (c) {
      enter().observe(document, {
        subtree : true,
        childList : true
      });
    }
    return function() {
      storage.delete(f);
      if (0 == storage.size) {
        enter().disconnect();
      }
    };
  }
  /**
   * @param {!Function} modSrc
   * @return {?}
   */
  function getRequirements(modSrc) {
    return Object.freeze({
      Ke : modSrc
    });
  }
  /**
   * @param {string} val
   * @return {?}
   */
  function split(val) {
    var b = categories[categories.length - 1].fc;
    return b.has(val) ? b.get(val).value : val.Ke;
  }
  /**
   * @param {!Array} args
   * @return {undefined}
   */
  function iterate(args) {
    var api = forEach(args);
    var s = api.next();
    for (; !s.done; s = api.next()) {
      s = s.value;
      if (s.clear) {
        s.clear();
      }
      try {
        s.clear = s.Yc();
        if (s.clear && "function" != typeof s.clear) {
          debug("useEffect's callback should return a function");
          s.clear = void 0;
        }
      } catch (conv_reverse_sort) {
        console.log(conv_reverse_sort);
      }
    }
    /** @type {number} */
    args.length = 0;
  }
  /**
   * @return {undefined}
   */
  function condition() {
    iterate(array);
  }
  /**
   * @param {!Object} i
   * @return {undefined}
   */
  function appendX(i) {
    if (i.clear) {
      i.clear();
      /** @type {null} */
      i.clear = null;
    }
    if (i.sync) {
      array.push(i);
    } else {
      rest.push(i);
    }
  }
  /**
   * @return {undefined}
   */
  function concat() {
    if (!operatorIsActive) {
      operatorIsActive = getExtendedRouter().call;
    }
    operatorIsActive();
  }
  /**
   * @return {undefined}
   */
  function execute() {
    if (!pattern) {
      if (pattern = true, iterate(rest), pattern = false, ie) {
        debug("render was already started");
        concat();
      } else {
        /** @type {boolean} */
        ie = true;
        items.clear();
        var out = moduleFactories;
        /** @type {!Map} */
        moduleFactories = new Map;
        out = forEach(out.keys());
        var fn = out.next();
        for (; !fn.done; fn = out.next()) {
          fn = fn.value;
          try {
            fn();
          } catch (conv_reverse_sort) {
            console.log(conv_reverse_sort);
          }
        }
        condition();
        /** @type {boolean} */
        ie = false;
      }
    }
  }
  /**
   * @param {string} id
   * @return {undefined}
   */
  function patch(id) {
    moduleFactories.set(id, true);
    concat();
  }
  /**
   * @return {?}
   */
  function pop() {
    if (!categories.length) {
      throw Error("Hooks can be used only in render functions");
    }
    var value = $(categories[categories.length - 1]).pd;
    if (!availableMachines.has(value)) {
      availableMachines.set(value, {
        Bb : 0,
        ra : [],
        pc : 0,
        Ea : [],
        eb : value,
        connected : false
      });
    }
    return $(availableMachines.get(value));
  }
  /**
   * @param {string} callback
   * @return {?}
   */
  function join(callback) {
    var $ = pop();
    var m = $.Bb;
    if ($.ra.length == m) {
      callback = "function" == typeof callback ? callback() : callback;
      $.ra.push([callback, function(fn) {
        if ("function" === typeof fn) {
          fn = fn($.ra[m][0]);
        }
        $.ra[m][0] = fn;
        patch($.eb);
      }]);
    }
    ++$.Bb;
    return $.ra[m];
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function parse(data) {
    var params = pop();
    var i = params.Bb;
    if (params.ra.length == i) {
      params.ra.push({
        current : data
      });
    }
    data = params.ra[i];
    ++params.Bb;
    return data;
  }
  /**
   * @param {!Object} i
   * @param {string} b
   * @param {string} total
   * @return {undefined}
   */
  function done(i, b, total) {
    var state = pop();
    var index = state.pc;
    if (state.Ea.length == index) {
      state.Ea.push({
        Yc : i,
        sync : total,
        clear : null,
        ad : b
      });
    } else {
      /** @type {!Object} */
      state.Ea[index].Yc = i;
    }
    i = state.Ea[index];
    if (!(!state.connected || b && arraysEqual($(i.ad), b))) {
      appendX(i);
      if (!i.sync) {
        concat();
      }
    }
    /** @type {string} */
    i.ad = b;
    ++state.pc;
  }
  /**
   * @param {!Function} records
   * @param {!Array} version
   * @return {undefined}
   */
  function select(records, version) {
    done(records, version, false);
  }
  /**
   * @param {!Function} m
   * @param {!Array} b
   * @return {undefined}
   */
  function call(m, b) {
    done(m, b, true);
  }
  /**
   * @param {!Object} a
   * @return {?}
   */
  function flatten(a) {
    var s = parse();
    select(function() {
      /** @type {!Object} */
      s.current = a;
    }, [a]);
    return s.current;
  }
  /**
   * @param {!Function} f
   * @param {!Array} b
   * @return {?}
   */
  function union(f, b) {
    var result = parse();
    var a = flatten(b);
    if (!(a && arraysEqual(a, b))) {
      result.current = f();
    }
    return result.current;
  }
  /**
   * @param {!Function} obj
   * @param {!Array} v
   * @return {?}
   */
  function attr(obj, v) {
    return union(function() {
      return obj;
    }, v);
  }
  /**
   * @return {undefined}
   */
  function oldSetupComputes() {
  }
  /**
   * @param {string} type
   * @param {!Object} name
   * @param {?} data
   * @param {!Object} op
   * @param {?} i
   * @return {?}
   */
  function stop(type, name, data, op, i) {
    data = void 0 === data ? new Map : data;
    i = void 0 === i ? "html" : i;
    var field = name ? name : "__root";
    if (!recFrames.has(type)) {
      recFrames.set(type, {});
    }
    var ret = recFrames.get(type);
    if (!ret[field]) {
      var temp = require(void 0, i);
      /** @type {number} */
      var variation = 0;
      /** @type {!Map} */
      var root = new Map(data);
      /** @type {number} */
      var name = 0;
      /** @type {!Array} */
      var tokens = [];
      /** @type {!Array} */
      var h = [];
      /** @type {null} */
      var callback = null;
      /** @type {!Map} */
      var map = new Map;
      /** @type {!Map} */
      var obj = new Map;
      /**
       * @param {string} expected
       * @param {!Function} callback
       * @return {?}
       */
      var test = function(expected, callback) {
        /** @type {!Array} */
        var data = [];
        /** @type {number} */
        var d = 1;
        for (; d < arguments.length; ++d) {
          data[d - 1] = arguments[d];
        }
        if ("function" === typeof expected && "renderBinding" == expected.Hd || null == expected) {
          return test.Na(function() {
            return expected;
          }, data[0]);
        }
        if ("function" === typeof expected) {
          return d = data[0], data = data[1], test.Sc("_c" + ++variation, expected, d, data);
        }
        if (callback && !Object.is(expected, callback)) {
          fail("Template literal was changed");
        }
        /** @type {string} */
        callback = expected;
        data = temp.apply(null, [expected].concat(each(data)));
        /**
         * @return {undefined}
         */
        data.uf = function() {
          temp = require(void 0, i);
        };
        test.Gd();
        if (op) {
          op.current = data;
        }
        return data;
      };
      test.fc = data;
      /**
       * @param {string} obj
       * @param {undefined} key
       * @return {?}
       */
      test.Na = function(obj, key) {
        if (!tokens[name]) {
          /** @type {!Map} */
          tokens[name] = new Map;
        }
        var t = tokens[name];
        var f = h[name];
        obj = obj();
        if (!obj) {
          return ++name, null;
        }
        var password = forEach(obj());
        obj = password.next().value;
        var cmd = password.next().value;
        password = password.next().value;
        if (!t.get(obj)) {
          var property = t.has(obj) ? each(t.keys()).concat().indexOf(obj) : t.size;
          t.set(obj, add(obj, test.Qc("_u" + name + "_" + property, i), root, key, password));
        }
        ++name;
        if (f) {
          f.delete(obj);
        }
        return $(t.get(obj))(cmd);
      };
      /**
       * @param {!Array} layersArray
       * @param {!Function} dissoc
       * @return {?}
       */
      test.Ac = function(layersArray, dissoc) {
        /** @type {!Array} */
        var result = [];
        /** @type {!Set} */
        var triggerLookup = new Set;
        layersArray.forEach(function(name, item) {
          if (item = dissoc(name, item)) {
            name = item.id;
            item = item.Zd;
            /** @type {boolean} */
            var key = name;
            for (; triggerLookup.has(name);) {
              /** @type {string} */
              name = name + "_copy";
            }
            triggerLookup.add(name);
            if (key != name) {
              debug("duplicated child id: " + key);
            }
            var context = forEach(item());
            item = context.next().value;
            key = context.next().value;
            context = context.next().value;
            if (!name) {
              debug("unexpected error: empty id in render all");
              /** @type {string} */
              name = "_c" + ++variation;
            }
            result.push(test.Sc(name, item, key, context));
          }
        });
        return result;
      };
      /**
       * @param {string} text
       * @param {?} f
       * @return {?}
       */
      test.Qc = function(text, f) {
        return stop(type, (name || "") + text, root, void 0, f);
      };
      /**
       * @param {string} b
       * @param {string} result
       * @param {number} msg
       * @param {!Array} value
       * @return {?}
       */
      test.Sc = function(b, result, msg, value) {
        b = test.Qc(b, i);
        if (map.has(b)) {
          obj.delete(b);
        } else {
          result = add(result, b, root, value);
          map.set(b, result);
        }
        return $(map.get(b))(msg);
      };
      /**
       * @return {undefined}
       */
      test.Gd = function() {
        if (obj.size) {
          var i = forEach(obj.keys());
          var n = i.next();
          for (; !n.done; n = i.next()) {
            var d = n.value;
            var out = map.get(d);
            if (out) {
              out.Qa();
              map.delete(d);
            } else {
              fail("clean child error");
            }
          }
        }
        /** @type {number} */
        i = 0;
        for (; i < h.length; ++i) {
          d = h[i];
          out = forEach(d.keys());
          n = out.next();
          for (; !n.done; n = out.next()) {
            n = n.value;
            var diffVector = d.get(n);
            if (diffVector) {
              diffVector.Qa();
              tokens[i].set(n, null);
            } else {
              if (null !== diffVector) {
                fail("clean child error");
              }
            }
          }
        }
        /** @type {!Map} */
        obj = new Map(map);
        /** @type {!Array<?>} */
        h = tokens.map(function(data) {
          return new Map(data);
        });
        /** @type {number} */
        name = variation = 0;
      };
      /**
       * @param {!Function} name
       * @param {!Function} fn
       * @param {?} value
       * @return {?}
       */
      test.K = function(name, fn, value) {
        fn = fn();
        return fn ? test(name, fn, value) : (++variation, null);
      };
      /**
       * @param {string} key
       * @param {!Object} type
       * @return {?}
       */
      test.Dc = function(key, type) {
        var node = root.get(key);
        if (!(node && node.value === type)) {
          items.set(key, true);
          if (node) {
            /** @type {!Object} */
            node.value = type;
          } else {
            root.set(key, {
              value : type
            });
          }
        }
        return test;
      };
      /**
       * @return {undefined}
       */
      test.Qa = function() {
        /**
         * @return {undefined}
         */
        test.Qa = function() {
          console.log("clean already complete");
        };
        /** @type {null} */
        ret[field] = null;
        var out = forEach(map.values());
        var value = out.next();
        for (; !value.done; value = out.next()) {
          value.value.Qa();
        }
        out = forEach(tokens);
        value = out.next();
        for (; !value.done; value = out.next()) {
          var api = forEach(value.value.values());
          value = api.next();
          for (; !value.done; value = api.next()) {
            if (value = value.value) {
              value.Qa();
            }
          }
        }
        map.clear();
        obj.clear();
      };
      /** @type {function(string, !Function): ?} */
      ret[field] = test;
    }
    return ret[field];
  }
  /**
   * @param {string} c
   * @param {?} f
   * @param {!Object} opts
   * @return {?}
   */
  function test(c, f, opts) {
    var definition = forEach(f());
    f = definition.next().value;
    var b = definition.next().value;
    definition = definition.next().value;
    c = f(c, b);
    if (c instanceof Element) {
      contains(c, definition);
    }
    if (opts) {
      /** @type {string} */
      opts.current = c;
    }
    return c;
  }
  /**
   * @param {!Object} e
   * @param {string} value
   * @return {undefined}
   */
  function contains(e, value) {
    if (value) {
      if (value = String(value), value.includes(" ")) {
        debug("multiple classNames not allowed: " + value);
      } else {
        if (e instanceof Element) {
          if (e.classList) {
            e.classList.add(value);
          } else {
            if (e.classList) {
              var css = e.classList.contains(value);
            } else {
              if (e.classList) {
                css = e.classList;
              } else {
                css = e.className;
                css = Number(css) && css.match(/\S+/g) || [];
              }
              css = css.includes(value);
            }
            if (!css) {
              css = "string" == typeof e.className ? e.className : e.className.baseVal;
              /** @type {string} */
              var data = "";
              if (css) {
                /** @type {string} */
                data = css + " ";
              }
              /** @type {string} */
              value = data + value;
              if ("string" == typeof e.className) {
                /** @type {string} */
                e.className = value;
              } else {
                /** @type {string} */
                e.className.baseVal = value;
              }
            }
          }
        }
      }
    }
  }
  /**
   * @param {string} fn
   * @param {!Object} config
   * @param {?} data
   * @param {number} id
   * @param {number} name
   * @return {?}
   */
  function add(fn, config, data, id, name) {
    /**
     * @return {?}
     */
    function next() {
      if (!m) {
        var value = $(data);
        categories.push({
          pd : next,
          fc : value
        });
        if (availableMachines.has(next)) {
          value = $(availableMachines.get(next));
          /** @type {number} */
          value.Bb = 0;
          /** @type {number} */
          value.pc = 0;
        }
        value = fn(config, obj);
        var self = $(categories.pop());
        if (self = availableMachines.get(self.pd)) {
          substring(value instanceof Element);
          if (self.connected) {
            substring(self.gd == self.ra.length, "Incorrect hooks count\nexpected: " + self.gd + "\nactual: " + self.ra.length);
            substring(self.fd == self.Ea.length, "Incorrect effects count\nexpected: " + self.fd + "\nactual: " + self.Ea.length);
          } else {
            /** @type {boolean} */
            self.connected = true;
            var api = forEach(self.Ea);
            var i = api.next();
            for (; !i.done; i = api.next()) {
              i = i.value;
              appendX(i);
              if (!i.sync) {
                concat();
              }
            }
            self.gd = self.ra.length;
            self.fd = self.Ea.length;
          }
          /** @type {boolean} */
          self = true;
        } else {
          /** @type {boolean} */
          self = false;
        }
        if (val && self) {
          if (val != value) {
            window.console.error("[" + fn.name + "] dom element was changed");
          }
        } else {
          val = value;
        }
        if (val instanceof Element) {
          contains(val, node.className);
          contains(val, name);
        }
        if (node.La) {
          /** @type {null} */
          node.La.current = val;
        }
        return value;
      }
    }
    /**
     * @param {!Object} target
     * @return {?}
     */
    function handle(target) {
      a: {
        var result = $(data);
        result = forEach(result.keys());
        var request = result.next();
        for (; !request.done; request = result.next()) {
          if (items.has(request.value)) {
            /** @type {boolean} */
            result = true;
            break a;
          }
        }
        /** @type {boolean} */
        result = false;
      }
      if ((result = !result) && !(result = obj === target) && (result = "object" === typeof obj && "object" === typeof target)) {
        a: {
          var key;
          for (key in obj) {
            if (!(key in target) || obj[key] !== target[key]) {
              /** @type {boolean} */
              result = false;
              break a;
            }
          }
          var prop;
          for (prop in target) {
            if (!(prop in obj)) {
              /** @type {boolean} */
              result = false;
              break a;
            }
          }
          /** @type {boolean} */
          result = true;
        }
      }
      if (result && val) {
        return val;
      }
      /** @type {!Object} */
      obj = target;
      return $(next());
    }
    data = void 0 === data ? new Map : data;
    id = void 0 === id ? "" : id;
    name = void 0 === name ? "" : name;
    var obj;
    /** @type {null} */
    var val = null;
    var node = "string" == typeof id || id.___css ? {
      className : String(id)
    } : id;
    /** @type {boolean} */
    var m = false;
    /**
     * @return {undefined}
     */
    handle.Qa = function() {
      /** @type {boolean} */
      m = true;
      var page = availableMachines.get(next);
      if (page) {
        page = forEach(page.Ea);
        var p = page.next();
        for (; !p.done; p = page.next()) {
          p = p.value;
          if (p.clear) {
            p.clear();
          }
        }
        availableMachines.delete(next);
      }
      config.Qa();
    };
    return handle;
  }
  /**
   * @param {string} id
   * @param {string} type
   * @param {!Array} value
   * @param {?} options
   * @return {?}
   */
  function build(id, type, value, options) {
    value = void 0 === value ? stop(type) : value;
    options = void 0 === options ? new Map : options;
    /** @type {boolean} */
    var oldCondition = path.has(value);
    if (!oldCondition) {
      path.set(value, add(id, value, options));
    }
    var add = path.get(value);
    var obj = add(type);
    if (!oldCondition) {
      var unload = connect(obj, {
        remove : function() {
          if (!document.body.contains(obj)) {
            add.Qa();
            unload();
            path.delete(value);
          }
        }
      });
    }
    if (!obj.hasAttribute("data-root")) {
      obj.setAttribute("data-root", "true");
    }
    apply(condition);
    return obj;
  }
  /**
   * @param {!Function} name
   * @return {?}
   */
  function isArray(name) {
    return function(defs, state) {
      return build(name, defs, state, state && state.fc);
    };
  }
  /**
   * @param {!Array} win
   * @param {!Array} elem
   * @return {?}
   */
  function handle(win, elem) {
    /** @type {function(?, !Object): ?} */
    var p = process;
    elem = void 0 === elem ? win : elem;
    var fun = isArray(p);
    var script = stop(elem);
    /** @type {boolean} */
    var f = true;
    return function(obj) {
      if (f) {
        var result = fun(obj, script);
        win(result);
        /** @type {boolean} */
        f = false;
      } else {
        patch(function() {
          fun(obj, script);
        });
      }
    };
  }
  /**
   * @param {string} value
   * @return {?}
   */
  function wrap(value) {
    if (value) {
      if ("string" == typeof value) {
        var elem = {};
        value = (elem.id = value, elem);
      }
      return Object.entries(value).filter(function(args) {
        args = forEach(args);
        args.next();
        return null != args.next().value;
      }).map(function(b) {
        var a = forEach(b);
        b = a.next().value;
        a = a.next().value;
        return b + "=" + a;
      }).join(";");
    }
  }
  /**
   * @param {(Object|number)} parent
   * @param {!Object} finalizers
   * @param {?} self
   * @param {string} newState
   * @return {?}
   */
  function Cadence(parent, finalizers, self, newState) {
    self = void 0 === self ? {} : self;
    return extend(parent + finalizers, self, newState);
  }
  /**
   * @param {number} a
   * @param {number} options
   * @param {number} d
   * @param {number} c
   * @param {number} b
   * @param {number} r
   * @return {?}
   */
  function E(a, options, d, c, b, r) {
    a = q(q(options, a), q(c, r));
    return q(a << b | a >>> 32 - b, d);
  }
  /**
   * @param {number} i
   * @param {number} b
   * @param {number} c
   * @param {number} d
   * @param {undefined} t
   * @param {number} n
   * @param {number} a
   * @return {?}
   */
  function o(i, b, c, d, t, n, a) {
    return E(b & c | ~b & d, i, b, t, n, a);
  }
  /**
   * @param {number} n
   * @param {number} c
   * @param {number} b
   * @param {number} a
   * @param {undefined} t
   * @param {number} x
   * @param {number} s
   * @return {?}
   */
  function md5_gg(n, c, b, a, t, x, s) {
    return E(c & a | b & ~a, n, c, t, x, s);
  }
  /**
   * @param {number} b
   * @param {number} c
   * @param {number} d
   * @param {number} e
   * @param {undefined} t
   * @param {number} s
   * @param {number} a
   * @return {?}
   */
  function md5_hh(b, c, d, e, t, s, a) {
    return E(c ^ d ^ e, b, c, t, s, a);
  }
  /**
   * @param {number} b
   * @param {number} c
   * @param {number} d
   * @param {number} a
   * @param {undefined} t
   * @param {number} x
   * @param {number} s
   * @return {?}
   */
  function md5_ii(b, c, d, a, t, x, s) {
    return E(d ^ (c | ~a), b, c, t, x, s);
  }
  /**
   * @param {number} x
   * @param {number} y
   * @return {?}
   */
  function q(x, y) {
    /** @type {number} */
    var c = (65535 & x) + (65535 & y);
    return (x >> 16) + (y >> 16) + (c >> 16) << 16 | 65535 & c;
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function c(data) {
    var b = data[0];
    /** @type {!Array} */
    data = Array(b.length >> 2);
    /** @type {number} */
    var c = 0;
    for (; c < data.length; c++) {
      /** @type {number} */
      data[c] = 0;
    }
    /** @type {number} */
    c = 0;
    for (; c < 8 * b.length; c = c + 8) {
      data[c >> 5] |= (255 & b.charCodeAt(c / 8)) << c % 32;
    }
    /** @type {number} */
    b = 8 * b.length;
    data[b >> 5] |= 128 << b % 32;
    /** @type {number} */
    data[14 + (b + 64 >>> 9 << 4)] = b;
    /** @type {number} */
    b = 1732584193;
    /** @type {number} */
    c = -271733879;
    /** @type {number} */
    var d = -1732584194;
    /** @type {number} */
    var a = 271733878;
    /** @type {number} */
    var i = 0;
    for (; i < data.length; i = i + 16) {
      var oldb = b;
      var oldc = c;
      var r = d;
      var el = a;
      c = md5_ii(c = md5_ii(c = md5_ii(c = md5_ii(c = md5_hh(c = md5_hh(c = md5_hh(c = md5_hh(c = md5_gg(c = md5_gg(c = md5_gg(c = md5_gg(c = o(c = o(c = o(c = o(c, d = o(d, a = o(a, b = o(b, c, d, a, data[i], 7, -680876936), c, d, data[i + 1], 12, -389564586), b, c, data[i + 2], 17, 606105819), a, b, data[i + 3], 22, -1044525330), d = o(d, a = o(a, b = o(b, c, d, a, data[i + 4], 7, -176418897), c, d, data[i + 5], 12, 1200080426), b, c, data[i + 6], 17, -1473231341), a, b, data[i + 7], 22, -45705983), 
      d = o(d, a = o(a, b = o(b, c, d, a, data[i + 8], 7, 1770035416), c, d, data[i + 9], 12, -1958414417), b, c, data[i + 10], 17, -42063), a, b, data[i + 11], 22, -1990404162), d = o(d, a = o(a, b = o(b, c, d, a, data[i + 12], 7, 1804603682), c, d, data[i + 13], 12, -40341101), b, c, data[i + 14], 17, -1502002290), a, b, data[i + 15], 22, 1236535329), d = md5_gg(d, a = md5_gg(a, b = md5_gg(b, c, d, a, data[i + 1], 5, -165796510), c, d, data[i + 6], 9, -1069501632), b, c, data[i + 11], 14, 643717713), 
      a, b, data[i], 20, -373897302), d = md5_gg(d, a = md5_gg(a, b = md5_gg(b, c, d, a, data[i + 5], 5, -701558691), c, d, data[i + 10], 9, 38016083), b, c, data[i + 15], 14, -660478335), a, b, data[i + 4], 20, -405537848), d = md5_gg(d, a = md5_gg(a, b = md5_gg(b, c, d, a, data[i + 9], 5, 568446438), c, d, data[i + 14], 9, -1019803690), b, c, data[i + 3], 14, -187363961), a, b, data[i + 8], 20, 1163531501), d = md5_gg(d, a = md5_gg(a, b = md5_gg(b, c, d, a, data[i + 13], 5, -1444681467), c, d, 
      data[i + 2], 9, -51403784), b, c, data[i + 7], 14, 1735328473), a, b, data[i + 12], 20, -1926607734), d = md5_hh(d, a = md5_hh(a, b = md5_hh(b, c, d, a, data[i + 5], 4, -378558), c, d, data[i + 8], 11, -2022574463), b, c, data[i + 11], 16, 1839030562), a, b, data[i + 14], 23, -35309556), d = md5_hh(d, a = md5_hh(a, b = md5_hh(b, c, d, a, data[i + 1], 4, -1530992060), c, d, data[i + 4], 11, 1272893353), b, c, data[i + 7], 16, -155497632), a, b, data[i + 10], 23, -1094730640), d = md5_hh(d, a = 
      md5_hh(a, b = md5_hh(b, c, d, a, data[i + 13], 4, 681279174), c, d, data[i], 11, -358537222), b, c, data[i + 3], 16, -722521979), a, b, data[i + 6], 23, 76029189), d = md5_hh(d, a = md5_hh(a, b = md5_hh(b, c, d, a, data[i + 9], 4, -640364487), c, d, data[i + 12], 11, -421815835), b, c, data[i + 15], 16, 530742520), a, b, data[i + 2], 23, -995338651), d = md5_ii(d, a = md5_ii(a, b = md5_ii(b, c, d, a, data[i], 6, -198630844), c, d, data[i + 7], 10, 1126891415), b, c, data[i + 14], 15, -1416354905), 
      a, b, data[i + 5], 21, -57434055), d = md5_ii(d, a = md5_ii(a, b = md5_ii(b, c, d, a, data[i + 12], 6, 1700485571), c, d, data[i + 3], 10, -1894986606), b, c, data[i + 10], 15, -1051523), a, b, data[i + 1], 21, -2054922799), d = md5_ii(d, a = md5_ii(a, b = md5_ii(b, c, d, a, data[i + 8], 6, 1873313359), c, d, data[i + 15], 10, -30611744), b, c, data[i + 6], 15, -1560198380), a, b, data[i + 13], 21, 1309151649), d = md5_ii(d, a = md5_ii(a, b = md5_ii(b, c, d, a, data[i + 4], 6, -145523070), 
      c, d, data[i + 11], 10, -1120210379), b, c, data[i + 2], 15, 718787259), a, b, data[i + 9], 21, -343485551);
      b = q(b, oldb);
      c = q(c, oldc);
      d = q(d, r);
      a = q(a, el);
    }
    /** @type {!Array} */
    data = [b, c, d, a];
    /** @type {string} */
    b = "";
    /** @type {number} */
    c = 0;
    for (; c < 32 * data.length; c = c + 8) {
      /** @type {string} */
      b = b + String.fromCharCode(data[c >> 5] >>> c % 32 & 255);
    }
    /** @type {string} */
    data = b;
    /** @type {string} */
    c = "";
    /** @type {number} */
    d = 0;
    for (; d < data.length; d++) {
      /** @type {number} */
      b = data.charCodeAt(d);
      /** @type {string} */
      c = c + ("0123456789ABCDEF".charAt(b >>> 4 & 15) + "0123456789ABCDEF".charAt(15 & b));
    }
    /** @type {string} */
    optionUsed3 = c;
    /** @type {string} */
    data = "x" + optionUsed3.toLowerCase().slice(0, 5) + "__";
    outChance.push("");
    return {
      v : Cadence.bind(null, data)
    };
  }
  /**
   * @param {!Object} el
   * @return {?}
   */
  function click(el) {
    var api = forEach(join(function() {
      return {
        width : 0,
        height : 0
      };
    }));
    var c = api.next().value;
    var validateJSON = api.next().value;
    select(function() {
      var mutationElement = $(el.current);
      var mObs = new ResizeObserver(function(input) {
        input = forEach(input).next().value;
        validateJSON(input.contentRect);
      });
      mObs.observe(mutationElement);
      return function() {
        mObs.disconnect();
      };
    }, [el]);
    return [c.width, c.height];
  }
  /**
   * @param {!Object} target
   * @param {!Array} fn
   * @return {?}
   */
  function listen(target, fn) {
    fn = void 0 === fn ? returnFalse : fn;
    var api = forEach(join(false));
    var b = api.next().value;
    var action = api.next().value;
    api = forEach(join(false));
    var v = api.next().value;
    var j = api.next().value;
    api = forEach(click(target));
    var h = api.next().value;
    var m3 = api.next().value;
    select(function() {
      /** @type {boolean} */
      var a = target.current.scrollWidth > target.current.clientWidth;
      if (a != b) {
        action(a);
      }
      /** @type {boolean} */
      a = target.current.scrollHeight > target.current.clientHeight;
      if (a != v) {
        j(a);
      }
      oldSetupComputes.apply(null, [h, m3].concat(each(fn)));
    }, [target, b, v, h, m3, fn]);
    return [b, v];
  }
  /**
   * @param {!Object} s
   * @return {?}
   */
  function create(s) {
    var b = void 0 === s.dd ? {
      width : 0,
      height : 0
    } : s.dd;
    var c = s.$e;
    var type = s.I;
    var align = s.align;
    var i = void 0 === s.offsetX ? 0 : s.offsetX;
    s = void 0 === s.offsetY ? 0 : s.offsetY;
    var result = {
      x : 0,
      y : 0
    };
    switch(type) {
      case "top":
        /** @type {number} */
        result.y = -c.height - s;
        break;
      case "bottom":
        result.y = b.height + s;
        break;
      case "left":
        /** @type {number} */
        result.x = -c.width - i;
        break;
      case "right":
        result.x = b.width + i;
        break;
      default:
        isNaN(type);
    }
    switch(align) {
      case "start":
        switch(type) {
          case "top":
          case "bottom":
            result.x = i;
            break;
          case "right":
          case "left":
            /** @type {!Object} */
            result.y = s;
            break;
          default:
            isNaN(type);
        }break;
      case "center":
        switch(type) {
          case "top":
          case "bottom":
            /** @type {number} */
            result.x = (b.width - c.width) / 2;
            break;
          case "right":
          case "left":
            /** @type {number} */
            result.y = (b.height - c.height) / 2;
            break;
          default:
            isNaN(type);
        }break;
      case "end":
        switch(type) {
          case "top":
          case "bottom":
            /** @type {number} */
            result.x = b.width - c.width - i;
            break;
          case "right":
          case "left":
            /** @type {number} */
            result.y = b.height - c.height - s;
            break;
          default:
            isNaN(type);
        }break;
      default:
        isNaN(align);
    }
    return result;
  }
  /**
   * @param {!Function} value
   * @param {!ClientRect} c
   * @param {!ClientRect} b
   * @return {?}
   */
  function clamp(value, c, b) {
    /** @type {number} */
    var maxPrimaryDepth = 0;
    switch(value) {
      case "top":
        /** @type {number} */
        maxPrimaryDepth = b.top - c.top;
        break;
      case "right":
        /** @type {number} */
        maxPrimaryDepth = c.right - b.right;
        break;
      case "bottom":
        /** @type {number} */
        maxPrimaryDepth = c.bottom - b.bottom;
        break;
      case "left":
        /** @type {number} */
        maxPrimaryDepth = b.left - c.left;
        break;
      default:
        isNaN(value);
    }
    return Math.max(0, maxPrimaryDepth);
  }
  /**
   * @param {!Function} value
   * @param {!Function} position
   * @param {!ClientRect} c
   * @param {!ClientRect} b
   * @return {?}
   */
  function mouseMove(value, position, c, b) {
    /** @type {number} */
    var maxPrimaryDepth = 0;
    switch(position) {
      case "start":
        switch(value) {
          case "top":
          case "bottom":
            /** @type {number} */
            maxPrimaryDepth = c.right - b.right;
            break;
          case "right":
          case "left":
            /** @type {number} */
            maxPrimaryDepth = c.bottom - b.bottom;
            break;
          default:
            isNaN(value);
        }break;
      case "center":
        switch(value) {
          case "top":
          case "bottom":
            /** @type {number} */
            maxPrimaryDepth = Math.max(c.right - b.right, b.left - c.left);
            break;
          case "right":
          case "left":
            /** @type {number} */
            maxPrimaryDepth = Math.max(c.bottom - b.bottom, b.top - c.top);
            break;
          default:
            isNaN(value);
        }break;
      case "end":
        switch(value) {
          case "top":
          case "bottom":
            /** @type {number} */
            maxPrimaryDepth = b.left - c.left;
            break;
          case "right":
          case "left":
            /** @type {number} */
            maxPrimaryDepth = b.top - c.top;
            break;
          default:
            isNaN(value);
        }break;
      default:
        isNaN(position);
    }
    return Math.max(0, maxPrimaryDepth);
  }
  /**
   * @param {!Function} value
   * @return {?}
   */
  function getPlacement(value) {
    switch(value) {
      case "top":
        return "bottom";
      case "right":
        return "left";
      case "bottom":
        return "top";
      case "left":
        return "right";
      default:
        throw isNaN(value), Error();
    }
  }
  /**
   * @param {!Function} direction
   * @param {!ClientRect} e
   * @param {!ClientRect} pos
   * @return {?}
   */
  function setPosition(direction, e, pos) {
    switch(direction) {
      case "top":
      case "bottom":
        if (e.right >= pos.right) {
          return "end";
        }
        if (e.left <= pos.left) {
          return "start";
        }
        break;
      case "right":
      case "left":
        if (e.bottom > pos.bottom) {
          return "end";
        }
        if (e.top < pos.top) {
          return "start";
        }
        break;
      default:
        isNaN(direction);
    }
    return "center";
  }
  /**
   * @param {!Object} d
   * @param {string} type
   * @param {string} c
   * @return {undefined}
   */
  function bind(d, type, c) {
    var s = parse(c);
    /** @type {string} */
    s.current = c;
    /** @type {boolean} */
    var isCenter = null === c;
    select(function() {
      if (!isCenter) {
        var e = on(d.current, type, function(spec) {
          return $(s.current)(spec);
        });
        return function() {
          return func(e);
        };
      }
    }, [d, type, isCenter]);
  }
  /**
   * @param {?} createElement
   * @param {!Object} self
   * @return {?}
   */
  function postLink(createElement, self) {
    var size = void 0 === self.size ? "small" : self.size;
    var theme = void 0 === self.color ? "dark" : self.color;
    var stop = parse();
    bind(stop, "mousedown", function(event) {
      return event.stopPropagation();
    });
    var Cross = "text" == self.type ? resolve(href, self.text) : self.content;
    return createElement(fixture, stop, extend("x17c2d__tooltip", {
      type : self.type,
      size : size,
      color : theme
    }), createElement(Cross));
  }
  /**
   * @param {?} action
   * @param {?} params
   * @return {?}
   */
  function href(action, params) {
    return action(topic, "x17c2d__text", params);
  }
  /**
   * @param {!Object} data
   * @param {!Function} update
   * @param {?} callback
   * @param {number} options
   * @return {undefined}
   */
  function view(data, update, callback, options) {
    if (void 0 !== data.current) {
      clearTimeout(data.current);
      data.current = void 0;
    }
    if (0 >= options) {
      update(callback);
    } else {
      /** @type {number} */
      data.current = setTimeout(function() {
        data.current = void 0;
        update(callback);
      }, options);
    }
  }
  /**
   * @return {?}
   */
  function lookup() {
    var api = forEach(join(function() {
      return false;
    }));
    var vulnData = api.next().value;
    var unhilightDataAndHideTooltip = api.next().value;
    var data = parse(void 0);
    api = attr(function(callback, qs) {
      return view(data, function(g) {
        return unhilightDataAndHideTooltip(g);
      }, callback, qs);
    }, []);
    return [vulnData, api];
  }
  /**
   * @param {!Object} item
   * @return {?}
   */
  function onResize(item) {
    var name = item.od;
    var value = item.defaultValue;
    var index = item.units;
    var element = item.element;
    var f = item.kd;
    var type = (item = (item = window.getComputedStyle(element)) && item.getPropertyValue(name)) ? parseFloat(item) : value;
    var shortName = element.style.getPropertyValue(name);
    return {
      animate : function(key) {
        element.style.setProperty(name, "" + f(key, type) + (index || ""));
      },
      complete : function() {
        element.style.setProperty(name, shortName);
      },
      wb : [name]
    };
  }
  /**
   * @param {!Object} content
   * @param {(HTMLDocument|boolean)} f
   * @param {(HTMLDocument|boolean)} cb
   * @return {?}
   */
  function collapseWhitespaceSmart(content, f, cb) {
    var transform = content.style.transform;
    var css = getComputedStyle(content);
    return {
      animate : function(data) {
        /** @type {!Array} */
        var directories = [css];
        var out = cb && cb(data);
        if (null != out) {
          var actual = forEach(out);
          out = actual.next().value;
          actual = actual.next().value;
          directories.push("translate(" + out + "px, " + actual + "px)");
        }
        data = f && f(data);
        if (null != data) {
          if ("number" == typeof data) {
            directories.push("scale(" + data + ")");
          } else {
            out = forEach(data);
            data = out.next().value;
            out = out.next().value;
            if (null != data) {
              directories.push("scaleX(" + data + ")");
            }
            if (null != out) {
              directories.push("scaleY(" + out + ")");
            }
          }
        }
        /** @type {string} */
        content.style.transform = directories.join(" ");
      },
      complete : function() {
        content.style.transform = transform;
      },
      wb : ["transform"]
    };
  }
  /**
   * @param {!Object} self
   * @return {?}
   */
  function play(self) {
    var a = self.fb;
    var c = self.duration;
    var st = self.Xa;
    var data = self.before;
    var after = self.after;
    return new Promise(function(readyFun) {
      /**
       * @return {undefined}
       */
      function next() {
        var b = clip(Date.now() - k, 0, c);
        a.animate(b / c);
        if (d || b >= c) {
          $throw$();
          a.complete();
          readyFun();
          if (after) {
            after();
          }
        } else {
          requestAnimationFrame(next);
        }
      }
      /** @type {number} */
      var k = Date.now();
      /** @type {boolean} */
      var d = false;
      var $throw$ = st ? st.add(function() {
        /** @type {boolean} */
        d = true;
      }) : function() {
      };
      if (data) {
        data();
      }
      next();
    });
  }
  /**
   * @param {!Object} node
   * @return {?}
   */
  function animate(node) {
    var kd = node.opacity;
    var scrollHeight = node.maxHeight;
    var prevTag = node.scale;
    var nextTag = node.translate;
    var d = node.duration;
    var data = node.before;
    var c = node.after;
    return {
      play : function(text, context) {
        return play({
          fb : exec([kd ? onResize({
            od : "opacity",
            defaultValue : 1,
            element : text,
            kd : kd
          }) : null, scrollHeight ? onResize({
            od : "max-height",
            defaultValue : 0,
            units : "px",
            element : text,
            kd : scrollHeight
          }) : null, prevTag || nextTag ? collapseWhitespaceSmart(text, prevTag, nextTag) : null].filter(identity)),
          duration : d,
          Xa : context,
          before : data && function() {
            return $(data)(text);
          },
          after : c && function() {
            return $(c)(text);
          }
        });
      }
    };
  }
  /**
   * @param {number} index
   * @param {number} callback
   * @param {!Function} target
   * @return {?}
   */
  function hideAll(index, callback, target) {
    /** @type {number} */
    var stride = callback - index;
    return function(i) {
      if (target) {
        i = target(i);
      }
      return 1 <= i ? callback : index + stride * i;
    };
  }
  /**
   * @param {!Object} p
   * @param {?} v
   * @param {!Function} f
   * @return {?}
   */
  function getter(p, v, f) {
    /** @type {number} */
    var s = v[0] - p[0];
    /** @type {number} */
    var orientation = v[1] - p[1];
    return function(x) {
      if (f) {
        x = f(x);
      }
      return 1 <= x ? v : [p[0] + s * x, p[1] + orientation * x];
    };
  }
  /**
   * @param {!Object} val
   * @return {?}
   */
  function floor(val) {
    var date = parse();
    /** @type {!Object} */
    date.current = val;
    return date;
  }
  /**
   * @param {!Element} text
   * @param {!Function} cb
   * @return {?}
   */
  function renderer(text, cb) {
    /**
     * @return {undefined}
     */
    function c() {
      if (!e) {
        var params = text.getBoundingClientRect();
        if (Math.round(params.top) != Math.round(a.top) || Math.round(params.left) != Math.round(a.left) || params.width != a.width || params.height != a.height) {
          a = params;
          cb();
        }
        requestAnimationFrame(c);
      }
    }
    var a = text.getBoundingClientRect();
    /** @type {boolean} */
    var e = false;
    requestAnimationFrame(c);
    return function() {
      /** @type {boolean} */
      e = true;
    };
  }
  /**
   * @param {!Object} params
   * @return {?}
   */
  function search(params) {
    var callback = params.ea;
    var list = params.nb;
    var limit = params.ne;
    var q = params.show;
    var f = params.Nb;
    var update = params.Tb;
    var response = parse(null);
    var options = parse(null);
    var result = attr(function() {
      if (options.current) {
        if (options.current.parentElement == list) {
          list.removeChild(options.current);
        }
        /** @type {null} */
        options.current = null;
      }
    }, [list]);
    var a = attr(function() {
      result();
      if (response.current) {
        list.removeChild(response.current);
      }
    }, [result, list]);
    select(function() {
      return a;
    }, [a]);
    params = q ? limit() : null;
    var value = callback(params);
    call(function() {
      if (response.current != value) {
        if (value) {
          result();
          /** @type {string} */
          value.style.position = "absolute";
          substring(null == response.current);
          list.appendChild(value);
          if (f) {
            f(value);
          }
        } else {
          if (response.current) {
            options.current = response.current;
            if (update) {
              update(response.current).then(result);
            } else {
              result();
            }
          }
        }
        response.current = value;
      }
    });
    return response;
  }
  /**
   * @param {!Object} args
   * @return {?}
   */
  function normalize(args) {
    var b = args.Ae;
    var I = args.I;
    var value = args.align;
    var l = args.ff;
    var key = args.Sd;
    args = args.offset;
    var a = b(args);
    var result = clamp(I, a, l);
    if (result) {
      I = getPlacement(I);
      var a = create(Object.assign({}, key, {
        I : I
      }));
      var i = b(a);
      if (clamp(I, i, l) < result) {
        key.I = I;
        args = a;
      }
    }
    if (result = mouseMove(key.I, value, a, l)) {
      if ("center" == value) {
        value = setPosition(key.I, a, l);
      } else {
        a: {
          switch(value) {
            case "start":
              /** @type {string} */
              value = "end";
              break a;
            case "center":
              throw Error();
            case "end":
              /** @type {string} */
              value = "start";
              break a;
            default:
              throw isNaN(value), Error();
          }
        }
      }
      a = create(Object.assign({}, key, {
        align : value
      }));
      b = b(a);
      if (mouseMove(key.I, value, b, l) < result) {
        key.align = value;
        args = a;
      }
    }
    return args;
  }
  /**
   * @param {!Object} e
   * @param {!Object} options
   * @return {?}
   */
  function center(e, options) {
    e = e ? e.getBoundingClientRect() : {
      top : 0,
      right : document.body.scrollHeight - document.body.offsetHeight ? document.body.offsetWidth : window.innerWidth,
      bottom : document.body.offsetHeight,
      left : 0
    };
    return {
      top : e.top + options.y,
      right : e.right + options.x,
      bottom : e.bottom + options.y,
      left : e.left + options.x
    };
  }
  /**
   * @param {!Object} args
   * @return {?}
   */
  function move(args) {
    /**
     * @param {number} pos
     * @return {?}
     */
    function move(pos) {
      var x = pos.x + s.left + data.x;
      pos = pos.y + s.top + data.y;
      return {
        left : x,
        top : pos,
        right : x + options.width,
        bottom : pos + options.height
      };
    }
    var el = args.element;
    var img = args.Ze;
    var column = args.Se;
    var I = args.I;
    var a = args.align;
    var x = args.ze;
    var two_window = args.Vd;
    /** @type {({x: number, y: number})} */
    var data = column ? {
      x : window.pageXOffset,
      y : window.pageYOffset
    } : {
      x : 0,
      y : 0
    };
    args = center(args.eh, data);
    var s = el.getBoundingClientRect();
    el = {
      width : s.width,
      height : s.height
    };
    var options = {
      width : img.offsetWidth,
      height : img.offsetHeight
    };
    img = x && x() || {
      x : 0,
      y : 0
    };
    img = {
      dd : el,
      $e : options,
      I : I,
      align : a,
      offsetX : img.x,
      offsetY : img.y
    };
    x = create(img);
    if (two_window) {
      x = normalize({
        Ae : move,
        I : I,
        align : a,
        ff : args,
        Sd : img,
        offset : x
      });
    }
    if (column) {
      x.x += s.left + data.x;
      x.y += s.top + data.y;
    }
    return x;
  }
  /**
   * @param {!Object} a
   * @return {?}
   */
  function show(a) {
    var video = a.rf;
    var c = a.Za;
    var start = a.position;
    var isDialogShow = a.hf;
    a = a.rc;
    /** @type {string} */
    video.style.left = "0";
    /** @type {string} */
    video.style.top = "0";
    /** @type {string} */
    video.style.transform = "translateX(" + start.x + "px) translateY(" + start.y + "px)";
    if (a = !a) {
      a = video.getBoundingClientRect();
      /** @type {boolean} */
      a = !(0 <= a.top && 0 <= a.left && a.bottom <= (window.innerHeight || document.documentElement.clientHeight) && a.right <= (window.innerWidth || document.documentElement.clientWidth));
    }
    if (a) {
      return video.style.display = "none", isDialogShow(false, 0), Promise.resolve();
    }
    /** @type {number} */
    c = start.y > c.current.getBoundingClientRect().top ? -5 : 5;
    return animate({
      opacity : hideAll(0, 1),
      translate : getter([0, c], [0, 0]),
      duration : 150
    }).play(video);
  }
  /**
   * @param {!Object} e
   * @return {undefined}
   */
  function draw(e) {
    var type = e.ea;
    var item = e.Za;
    var pos = e.je;
    var c = void 0 === e.show ? true : e.show;
    var isSuccess = void 0 === e.$b ? false : e.$b;
    var splitterAttr = void 0 === e.I ? "top" : e.I;
    var align = void 0 === e.align ? "center" : e.align;
    var k = void 0 === e.offset ? {
      x : 0,
      y : 8
    } : e.offset;
    var body = void 0 === e.Rd ? 500 : e.Rd;
    var table = void 0 === e.De ? isSuccess ? 500 : 0 : e.De;
    var q = void 0 === e.vc ? false : e.vc;
    var f = void 0 === e.rc ? false : e.rc;
    var meta = parse(null);
    var subject = parse(null);
    e = forEach(lookup());
    var ic = e.next().value;
    var callback = e.next().value;
    select(function() {
      if (!c) {
        callback(false, table);
      }
    }, [table, callback, c]);
    bind(item, "mouseover", function() {
      return callback(true, body);
    });
    bind(item, "mouseout", function() {
      return callback(false, table);
    });
    bind(item, "click", function(event) {
      return q && event.preventDefault();
    });
    var results = search({
      ea : type,
      nb : ops.tooltip || document.body,
      ne : function() {
        return pos;
      },
      show : c && ic,
      Nb : function(value) {
        /**
         * @return {?}
         */
        function update() {
          return move({
            element : item.current,
            Ze : value,
            I : splitterAttr,
            align : align,
            Se : true,
            ze : function() {
              return k;
            },
            Vd : true
          });
        }
        meta.current = renderer(item.current, function() {
          return callback(false, 0);
        });
        return show({
          Za : item,
          rf : value,
          position : update(),
          hf : callback,
          rc : !!f
        }).then(function() {
          subject.current = new ResizeObserver(function() {
            var obj = update();
            /** @type {string} */
            value.style.transform = "translateX(" + obj.x + "px) translateY(" + obj.y + "px)";
          });
          subject.current.observe(value);
        });
      },
      Tb : function() {
        if (meta.current) {
          meta.current();
          /** @type {null} */
          meta.current = null;
        }
        if (subject.current) {
          subject.current.disconnect();
          /** @type {null} */
          subject.current = null;
        }
        return Promise.resolve();
      }
    });
    call(function() {
      var doc = results.current;
      if (isSuccess && doc) {
        /**
         * @return {?}
         */
        var ready = function() {
          return callback(true, 0);
        };
        /**
         * @return {?}
         */
        var f = function() {
          return callback(false, table);
        };
        doc.addEventListener("mouseover", ready);
        doc.addEventListener("mouseout", f);
        return function() {
          doc.removeEventListener("mouseover", ready);
          doc.removeEventListener("mouseout", f);
        };
      }
    });
  }
  /**
   * @param {!Object} options
   * @return {undefined}
   */
  function template(options) {
    draw(Object.assign({}, options, {
      je : resolve(postLink, {
        type : "text",
        text : options.text,
        size : options.size,
        color : options.color
      })
    }));
  }
  /**
   * @param {?} callback
   * @param {string} result
   * @return {?}
   */
  function post(callback, result) {
    var e = result.text;
    var oldSite = result.oc;
    result = result.Ob;
    var target = parse();
    var folderId = forEach(listen(target, ["string" == typeof e ? e : e.html])).next().value;
    template({
      ea : callback,
      Za : target,
      text : e,
      show : !oldSite && folderId
    });
    return callback(left, "x31b75__text", target, wrap(result), e);
  }
  /**
   * @param {string} type
   * @param {!Object} v
   * @return {?}
   */
  function isDefined(type, v) {
    if (!v.hasOwnProperty(type)) {
      throw Error("getValueByCheckedKey(" + type + ")");
    }
    return v[type];
  }
  /**
   * @param {!Object} b
   * @param {!Object} data
   * @return {undefined}
   */
  function focus(b, data) {
    var c = data.focused;
    var val = data.Vg;
    if (isString(c) && !isString(val)) {
      throw Error("useFocusProps: `focused` requires `onFocusedChange`");
    }
    select(function() {
      if (isString(c)) {
        var element = b.current;
        /** @type {boolean} */
        var d = element === document.activeElement;
        if (c && !d) {
          element.focus();
        } else {
          if (!c && d) {
            element.blur();
          }
        }
      }
    }, [c, b]);
    bind(b, "focus", function() {
      if (val) {
        val(true);
      }
    });
    bind(b, "blur", function() {
      if (val) {
        val(false);
      }
    });
  }
  /**
   * @param {?} callback
   * @param {!Object} options
   * @return {?}
   */
  function label(callback, options) {
    return callback(fileData, extend("x12792__preloader", {
      size : void 0 === options.size ? "medium" : options.size,
      color : void 0 === options.color ? "accent" : options.color
    }));
  }
  /**
   * @param {?} action
   * @param {!Object} options
   * @return {?}
   */
  function _init(action, options) {
    var body = parse();
    var value = void 0 === options.size ? "medium" : options.size;
    var TEXT_TYPE = void 0 === options.state ? "default" : options.state;
    var f = void 0 === options.minWidth ? true : options.minWidth;
    var move = options.Ob;
    var e = options.Wa;
    var style = getKey(options);
    var awesomeIcon = style.Ub;
    var l = style.text;
    var featureIcon = style.Vb;
    a: {
      switch(options.style) {
        case "link":
        case "linkInversed":
        case "linkSecondary":
          style = options.fh || "default";
          break a;
        case "primary":
        case "secondary":
        case "secondaryInversed":
        case "destructive":
        case "ghost":
        case "ghostInversed":
        case "complementary":
          /** @type {string} */
          style = "default";
          break a;
        default:
          throw isNaN(options), Error();
      }
    }
    /** @type {boolean} */
    var lastWasTextNode = "loading" == TEXT_TYPE;
    /** @type {boolean} */
    var defaultPrevented = lastWasTextNode || "disabled" == TEXT_TYPE;
    var config = isDefined(options.style, {
      primary : defaultOptions,
      secondary : builder,
      destructive : menu,
      ghost : errorLayer,
      ghostInversed : components,
      link : linkObj,
      linkInversed : model,
      linkSecondary : root,
      complementary : types,
      secondaryInversed : args
    });
    value = element(extend("xcc0a8__button", {
      size : value,
      spacing : "icon" == options.ya && "default" == style ? "icon" : style,
      "min-width" : "icon" != options.ya && f,
      loading : lastWasTextNode
    }), config.button.za({
      pressed : "pressed" == TEXT_TYPE
    }));
    template({
      ea : action,
      Za : body,
      show : !!e,
      text : "string" === typeof e ? e : "object" === typeof e ? e.text : "",
      size : "object" === typeof e ? e.size : void 0,
      color : "object" === typeof e ? e.color : void 0,
      I : "object" === typeof e ? e.I : void 0,
      align : "object" === typeof e ? e.align : void 0,
      offset : "object" === typeof e ? e.offset : void 0
    });
    focus(body, options);
    return action(type, value, defaultPrevented, function(event) {
      if (!(defaultPrevented || event.defaultPrevented)) {
        event.preventDefault();
        if (options.F) {
          options.F();
        }
      }
    }, body, wrap(move), action.K(attack, function() {
      return !!awesomeIcon && {
        className : element("xcc0a8__icon", config.icon),
        Wa : e,
        icon : awesomeIcon
      };
    }), action.K(header, function() {
      return !!l && {
        className : element("xcc0a8__text", config.text),
        text : l,
        oc : !!e
      };
    }), action.K(attack, function() {
      return !!featureIcon && {
        className : element("xcc0a8__icon", config.icon),
        icon : featureIcon
      };
    }), action.K(Bar, function() {
      return lastWasTextNode && {
        className : element("xcc0a8__preloader", config.wa),
        color : isDefined(options.style, {
          primary : "onColor",
          secondary : "subdued",
          destructive : "onColor",
          ghost : "subdued",
          ghostInversed : "onColor",
          link : "subdued",
          linkInversed : "onColor",
          linkSecondary : "subdued",
          complementary : "subdued",
          secondaryInversed : "onColor"
        })
      };
    }));
  }
  /**
   * @param {?} $
   * @param {!Object} html
   * @return {?}
   */
  function attack($, html) {
    return $(commaParts, html.className, $(html.icon));
  }
  /**
   * @param {?} $
   * @param {!Object} elem
   * @return {?}
   */
  function header($, elem) {
    return $(imgTag, elem.className, $(post, {
      text : elem.text,
      oc : elem.oc
    }));
  }
  /**
   * @param {?} h
   * @param {!Object} opts
   * @return {?}
   */
  function Bar(h, opts) {
    return h(folderPath, opts.className, h(label, {
      size : "small",
      color : opts.color
    }));
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function getKey(data) {
    switch(data.ya) {
      case "icon":
        return {
          Ub : data.icon,
          text : null,
          Vb : null
        };
      case "text":
        return {
          Ub : null,
          text : data.text,
          Vb : null
        };
      case "iconAndText":
        return {
          Ub : data.icon,
          text : data.text,
          Vb : null
        };
      case "textAndIcon":
        return {
          Ub : null,
          text : data.text,
          Vb : data.icon
        };
      case "iconAndTextAndIcon":
        return data;
      default:
        throw isNaN(data), Error();
    }
  }
  /**
   * @param {?} fn
   * @return {?}
   */
  function width(fn) {
    return fn(tokens);
  }
  /**
   * @param {number} a
   * @param {number} b
   * @return {undefined}
   */
  function Promise(a, b) {
    /** @type {number} */
    this.b = a;
    this.f = void 0 === b ? null : b;
  }
  /**
   * @param {!Object} src
   * @param {string} x
   * @return {?}
   */
  function encode(src, x) {
    return src.className() + "_" + x;
  }
  /**
   * @param {!Object} val
   * @param {string} key
   * @param {string} value
   * @return {?}
   */
  function createItem(val, key, value) {
    return encode(val, key) + "_" + value;
  }
  /**
   * @param {!Array} opts
   * @param {!Array} node
   * @return {?}
   */
  function removeClass(opts, node) {
    node = void 0 === node ? {} : node;
    opts = opts.map(function(f) {
      return f instanceof Promise ? f : new Promise(f);
    });
    var result = opts.map(function(_doodle) {
      return _doodle.className();
    });
    var api = forEach(Object.keys(node));
    var value = api.next();
    for (; !value.done; value = api.next()) {
      value = value.value;
      var items = node[value];
      var api = forEach(opts);
      var item = api.next();
      for (; !item.done; item = api.next()) {
        item = item.value;
        if (null == items) {
          result.push(encode(item, value));
        } else {
          result.push(createItem(item, value, items));
        }
      }
    }
    return result.join(" ");
  }
  /**
   * @return {?}
   */
  function FALSE() {
    return null;
  }
  /**
   * @return {undefined}
   */
  function _done() {
    if (_cachedContacts.size) {
      var api = forEach(_cachedContacts.keys());
      var obj = api.next();
      for (; !obj.done; obj = api.next()) {
        stats(obj.value);
      }
      _cachedContacts.clear();
    }
    apply(_done);
  }
  /**
   * @param {!Function} a
   * @return {undefined}
   */
  function push(a) {
    _cachedContacts.set(a, true);
  }
  /**
   * @param {!Object} n
   * @return {undefined}
   */
  function context(n) {
    var value = n.Ya;
    var body = n.X;
    body = void 0 === n.block ? body : n.block;
    n = n.relatedData;
    container.call(this);
    /** @type {!Array} */
    this.Kb = [];
    if (value || body) {
      compute(this, $(value || body));
    }
    /** @type {null} */
    this.P = this.Lb = null;
    this.Lc = n || this;
    this.tb = {};
    /** @type {null} */
    this.ac = this.ub = this.ma = this.m = null;
    /** @type {!Map} */
    this.Fd = new Map;
  }
  /**
   * @param {!Object} s
   * @return {?}
   */
  function unshift(s) {
    if (!s.ub) {
      s.ub = append(s);
    }
    return s.ub;
  }
  /**
   * @param {!Object} x
   * @param {string} name
   * @param {string} value
   * @return {undefined}
   */
  function f(x, name, value) {
    if (value) {
      if ("string" === typeof value) {
        /** @type {string} */
        x.tb[name] = value;
      } else {
        if (value) {
          /** @type {null} */
          x.tb[name] = null;
        }
      }
    } else {
      delete x.tb[name];
    }
    push(x);
  }
  /**
   * @param {!Object} value
   * @param {string} name
   * @return {?}
   */
  function set(value, name) {
    value = $(value.Kb[0]);
    $(!value.f);
    return new Promise(value.b, name);
  }
  /**
   * @param {!Function} array
   * @param {string} key
   * @return {undefined}
   */
  function random(array, key) {
    compute(array, key, void 0);
    push(array);
  }
  /**
   * @param {!Object} self
   * @return {undefined}
   */
  function stats(self) {
    _cachedContacts.delete(self);
    var currentRow = self.W();
    if (currentRow != self.ma) {
      var ma = self.ma;
      self.ma = currentRow;
      if (ma) {
        if (self.ac) {
          self.ac();
        } else {
          debug("[" + self.constructor.name + "] component element was changed");
        }
      }
    }
    if (self.ub && document.body.contains(self.ma)) {
      self.ub.dispatch();
    }
  }
  /**
   * @param {!Object} a
   * @param {string} name
   * @return {?}
   */
  function addEventListener(a, name) {
    if (!a.m) {
      a.m = {};
    }
    if (!a.m[name]) {
      a.m[name] = append(a);
      push(a);
    }
    return a.m[name];
  }
  /**
   * @param {!Object} obj
   * @param {string} type
   * @return {?}
   */
  function removeEvent(obj, type) {
    obj = obj.m && obj.m[type] || null;
    return !!obj && 0 < (obj.b ? keys(obj.b).length : 0);
  }
  /**
   * @param {!Function} value
   * @param {string} fn
   * @param {!Object} options
   * @return {undefined}
   */
  function compute(value, fn, options) {
    if (!(fn instanceof Promise)) {
      fn = new Promise(fn, options);
    }
    value.Kb.push(fn);
  }
  /**
   * @param {?} options
   * @return {?}
   */
  function showPlaceholderIfEmpty(options) {
    return options.Kb ? removeClass(options.Kb, options.tb) : "";
  }
  /**
   * @param {!Object} e
   * @param {!Object} key
   * @return {?}
   */
  function keyDown(e, key) {
    if (!e.P) {
      e.P = {};
    }
    if (!e.P[key]) {
      e.P[key] = {};
    }
    return stop(e.Lc, key, e.Fd, e.P[key]);
  }
  /**
   * @param {string} value
   * @param {string} name
   * @return {?}
   */
  function merge(value, name) {
    if (!idToBufferFileMap.has(value)) {
      idToBufferFileMap.set(value, {});
    }
    var params = idToBufferFileMap.get(value);
    if (!params[name]) {
      /**
       * @param {string} type
       * @param {?} canCreateDiscussions
       * @return {?}
       */
      params[name] = function(type, canCreateDiscussions) {
        /** @type {!Array} */
        var args = [];
        /** @type {number} */
        var i = 1;
        for (; i < arguments.length; ++i) {
          args[i - 1] = arguments[i];
        }
        return "string" === typeof type ? merge(value, name + type) : value.Rc.apply(value, [name, type].concat(each(args)));
      };
      /**
       * @param {?} key
       * @param {string} value
       * @return {?}
       */
      params[name].optional = function(key, value) {
        return value ? params[name](key) : FALSE;
      };
    }
    return params[name];
  }
  /**
   * @param {!Object} data
   * @param {string} key
   * @return {?}
   */
  function map(data, key) {
    if (data.P && data.P[key]) {
      return $(data.P[key].current);
    }
    data.D();
    return (data = data.Lb) && data[key] || null;
  }
  /**
   * @return {undefined}
   */
  function RegExp() {
    this.b = {};
  }
  /**
   * @param {!Object} s
   * @param {string} n
   * @return {?}
   */
  function pad(s, n) {
    if (isString(s.b[n])) {
      return n + "_" + ++s.b[n];
    }
    /** @type {number} */
    s.b[n] = 0;
    return n;
  }
  /**
   * @param {!Object} opts
   * @return {undefined}
   */
  function init(opts) {
    var s = opts.Ja;
    var val = void 0 === opts.wd ? "" : opts.wd;
    var fn = opts.Ya;
    var param = opts.Pb;
    var sep = void 0 === opts.md ? "" : opts.md;
    var newValue = void 0 === opts.placeholder ? "" : opts.placeholder;
    var value = void 0 === opts.prompt ? "" : opts.prompt;
    opts = opts.Db;
    this.b = s;
    this.m = val;
    /** @type {!Object} */
    this.Db = opts;
    /** @type {boolean} */
    this.s = !!value;
    val = fn || new Promise("paragraph-block");
    fn = val.className();
    /** @type {string} */
    fn = fn + (" " + createItem(val, "type", this.type()));
    /** @type {string} */
    fn = fn + (" " + createItem(val, "align", this.textAlign()));
    if (param) {
      /** @type {string} */
      fn = fn + (" " + createItem(val, "after", param));
    }
    if (0 == this.b.B.length) {
      /** @type {string} */
      fn = fn + (" " + encode(val, this.s ? "prompt" : "empty"));
    }
    /** @type {string} */
    this.f = fn;
    /** @type {boolean} */
    this.mb = ["subtitle"].includes(this.type());
    /** @type {boolean} */
    this.G = ["header", "subheader"].includes(this.type());
    /** @type {boolean} */
    this.O = ["blockquote", "pullquote"].includes(this.type());
    this.g = sep + s.key;
    this.placeholder = value || newValue;
  }
  /**
   * @param {?} key
   * @return {?}
   */
  function observe(key) {
    if (!arrayKey[key]) {
      arrayKey[key] = {};
    }
    return arrayKey[key];
  }
  /**
   * @param {!Object} options
   * @param {string} value
   * @param {string} condition
   * @return {?}
   */
  function loop(options, value, condition) {
    if (!options) {
      return null;
    }
    /** @type {string} */
    var ret = "";
    /** @type {!Array} */
    var textDecorations = [];
    /** @type {string} */
    var val = "";
    /** @type {number} */
    var opacity = 1;
    options.style.forEach(function(p) {
      switch(p.name) {
        case "bold":
          ret = ret + ("font-weight: " + (condition ? "normal" : "bold") + ";");
          break;
        case "italic":
          ret = ret + ("font-style: " + (value ? "normal" : "italic") + ";");
          break;
        case "underline":
          textDecorations.push("underline");
          break;
        case "strikethrough":
          textDecorations.push("line-through");
          break;
        case "fontSize":
          ret = ret + ("font-size: " + p.size + "px;");
          p = p.size;
          /** @type {boolean} */
          var m = 739 >= window.innerWidth;
          /** @type {string} */
          ret = ret + ("line-height: " + (15 >= p ? "1.5" : 18 >= p ? m ? "2" : "1.9" : 21 >= p && m ? "1.7" : 23 >= p && !m ? "1.75" : "normal") + ";");
          break;
        case "fontColor":
          ret = ret + ("color: " + p.color + ";");
          break;
        case "textHighlightColor":
          val = p.color;
          break;
        case "textHighlightOpacity":
          opacity = p.opacity;
          break;
        default:
          isNaN(p);
      }
    });
    if (textDecorations.length) {
      ret = ret + ("text-decoration: " + textDecorations.join(" ") + ";");
    }
    if (val && "noColor" !== val) {
      var countainer = forEach(round(val));
      options = countainer.next().value;
      var vulnData = countainer.next().value;
      countainer = countainer.next().value;
      ret = ret + ("background-color: rgba(" + options + ", " + vulnData + ", " + countainer + ", " + opacity + ");");
    }
    return ret;
  }
  /**
   * @param {?} config
   * @return {?}
   */
  function track(config) {
    var inline = config.ga;
    return config.mb ? replace({
      url : inline.url
    }) : inline;
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function prompt(data) {
    var options = data.B;
    data = data.mb;
    if (0 == options.length) {
      return [];
    }
    /** @type {!Array} */
    var result = [{
      text : options[0].text,
      ga : track({
        ga : options[0].sa,
        mb : data
      })
    }];
    /** @type {number} */
    var i = 1;
    for (; i < options.length; ++i) {
      var option = options[i];
      var labelAngle = track({
        ga : option.sa,
        mb : data
      });
      /** @type {number} */
      var j = result.length - 1;
      if (rotate(labelAngle, result[j].ga)) {
        result[j].text += option.text;
      } else {
        result.push({
          text : option.text,
          ga : labelAngle
        });
      }
    }
    return result;
  }
  /**
   * @param {!Object} options
   * @param {!Function} url
   * @param {number} offset
   * @param {boolean} val
   * @return {?}
   */
  function render(options, url, offset, val) {
    offset = void 0 === offset ? false : offset;
    val = void 0 === val ? false : val;
    var obj = options.Ja();
    var filename = prompt({
      B : obj.B,
      mb : options.mb
    });
    /** @type {!Array} */
    var out = [];
    var s = new RegExp;
    /** @type {boolean} */
    var k = false;
    var module = observe(obj.key);
    filename.forEach(function(node, canCreateDiscussions) {
      var res = loop(node.ga, options.O, options.G);
      var m = pad(s, obj.key + "_" + node.text + "_" + res);
      var label = node.text;
      if (canCreateDiscussions == filename.length - 1 && label.endsWith("\n")) {
        /** @type {string} */
        label = label + i;
      }
      res = (node = node.ga.url) ? require(module, ":" + m)(source, node, url, res, label) : require(module, ":" + m)(doc, res, label);
      if (node) {
        /** @type {boolean} */
        k = true;
      }
      out.push(res);
    });
    if (out.length) {
      if (k) {
        delete arrayKey[obj.key];
      }
    } else {
      out.push(require()(settings, i));
    }
    /** @type {string} */
    var field = "enter-blockquote-field-" + options.Db;
    /** @type {string} */
    var plotLineOrBandProto = "enter-list-item-field-" + options.Db;
    /** @type {string} */
    var T = "enter-paragraph-field-" + options.Db;
    var $ = require(module, ":" + options.g);
    /** @type {string} */
    var visibility = val ? " lock-block" : "";
    /** @type {string} */
    var expr = offset ? " read-only-block" : "";
    /** @type {string} */
    visibility = options.className() + visibility + expr;
    /** @type {boolean} */
    offset = !val && !offset;
    switch(obj.N) {
      case "header":
        return $(project_name, visibility, obj.key, offset, out);
      case "subheader":
        return $(elem, visibility, obj.key, offset, out);
      case "blockquote":
      case "pullquote":
        return $(newPost, visibility, obj.key, wrap(field), offset, out);
      case "ol":
      case "ul":
        return $(_this7, visibility, obj.key, wrap(plotLineOrBandProto), offset, out);
    }
    return $(featureTable, visibility, obj.key, val ? null : options.placeholder, wrap(T), offset, out);
  }
  /**
   * @param {!Object} optNoBorder
   * @param {!Function} optLineDash
   * @return {undefined}
   */
  function Box(optNoBorder, optLineDash) {
    /** @type {!Object} */
    this.type = optNoBorder;
    /** @type {!Array} */
    this.b = [];
    /** @type {!Function} */
    this.f = optLineDash;
  }
  /**
   * @return {undefined}
   */
  function calc() {
    context.call(this, {
      X : "rich-text-editor"
    });
    this.f = {};
    /** @type {boolean} */
    this.l = false;
    /** @type {null} */
    this.h = this.b = null;
    this.S = append(this);
  }
  /**
   * @param {!Object} x
   * @param {!Function} a
   * @return {undefined}
   */
  function u(x, a) {
    /** @type {!Function} */
    x.f.E = a;
  }
  /**
   * @param {!Object} b
   * @return {?}
   */
  function formatter(b) {
    return b.b.filter(function() {
      return true;
    }).reduce(function(name, options) {
      return name + "-" + options.key;
    }, "");
  }
  /**
   * @param {!Object} data
   * @param {!Object} value
   * @param {number} options
   * @return {?}
   */
  function run(data, value, options) {
    var t = toString(value);
    if (t) {
      return data.l ? (value = data.g.T, value = (value = toString($(0 >= value.order.length ? null : value.A[value.order[0]]))) && "subheader" === value.N ? value.key : "") : value = "", options = new init({
        wd : value,
        Ja : t,
        Pb : data.b,
        placeholder : "",
        prompt : "",
        Db : options
      }), data.b = options.type(), data.h && (f(data.h, "before", options.type()), data.h = null), render(options, escape(data.S), false, data.C());
    }
    options = (0, data.f[value.type])(value, data.b, escape(data.S), false, data.C());
    if (t = !value || "e" !== value.type && "i" !== value.type && "c" !== value.type ? null : value) {
      if ("float-left" === t.w) {
        /** @type {number} */
        data.h = options;
      } else {
        /** @type {string} */
        data.b = "c" === t.type ? "collage-" + t.w : "media-" + t.w;
      }
    }
    if (value && "E" === value.type && value) {
      /** @type {string} */
      data.b = "exercise";
    }
    if (value && "a" === value.type && value) {
      /** @type {string} */
      data.b = "attachment";
    }
    if (value && "c" === value.type && value) {
      /** @type {string} */
      data.b = "collage";
    }
    return options.D();
  }
  /**
   * @param {!Object} c
   * @return {?}
   */
  function batch(c) {
    /** @type {!Array} */
    var res = [];
    /** @type {null} */
    var box = null;
    parallel(c.g.T, function(separator) {
      var item = toString(separator);
      if (item && attributes.includes(item.N)) {
        item = $(toString(separator));
        if (box && box.type !== item.N) {
          res.push(box);
          /** @type {null} */
          box = null;
        }
        /** @type {boolean} */
        var value = "ol" === item.N && !!item.bb;
        if (!box || value) {
          if (box) {
            res.push(box);
          }
          box = new Box(item.N, value);
        }
        box.b.push(separator);
      } else {
        if (box) {
          res.push(box);
          /** @type {null} */
          box = null;
        }
        res.push(separator);
      }
    });
    if (box) {
      res.push(box);
    }
    return res;
  }
  /**
   * @param {!Object} html
   * @param {!Object} item
   * @param {number} options
   * @return {?}
   */
  function content(html, item, options) {
    /** @type {string} */
    var name = "enter-paragraph-field-" + options;
    if (item instanceof Box) {
      options = item.b.map(function(pref, canvas) {
        return run(html, pref, canvas);
      });
      var callback = require({}, formatter(item));
      return "ul" === item.type ? callback(src, wrap(name), options) : callback(pageContent, item.f ? {
        "counter-reset" : "li 0"
      } : null, options);
    }
    return run(html, item, options);
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function stream(data) {
    /** @type {string} */
    data.b = "none";
    return batch(data).map(function(res, paging) {
      return content(data, res, paging);
    });
  }
  /**
   * @param {string} p
   * @return {undefined}
   */
  function A(p) {
    calc.call(this);
    /** @type {string} */
    this.g = p;
  }
  /**
   * @param {string} obj
   * @return {?}
   */
  function getNodeValue(obj) {
    if (!(!obj.length || obj.startsWith("data:"))) {
      obj.startsWith("blob:");
    }
    return obj;
  }
  /**
   * @param {number} width
   * @return {?}
   */
  function showModal(width) {
    /** @type {number} */
    var volumeSlider = Math.max(0, Math.round(width / 1073741824 * 10) / 10);
    if (1 < volumeSlider) {
      return log("Common.FileSize.ToGb", {
        volume : volumeSlider
      });
    }
    /** @type {number} */
    volumeSlider = Math.max(0, Math.round(width / 1048576 * 10) / 10);
    return 1 < volumeSlider ? log("Common.FileSize.ToMb", {
      volume : volumeSlider
    }) : log("Common.FileSize.ToKb", {
      volume : Math.max(0, Math.round(width / 1024))
    });
  }
  /**
   * @param {string} type
   * @param {!Object} data
   * @return {?}
   */
  function combine(type, data) {
    var x = data.pa;
    var d = data.Oa;
    var tileSize = data.size;
    var current = data.color;
    var message = data.$b;
    var show = data.show;
    var callbackId = data.I;
    var a = data.align;
    data = data.vc;
    var object = parse();
    template({
      ea : type,
      Za : object,
      text : d,
      size : tileSize,
      color : current,
      $b : message,
      show : show,
      I : callbackId,
      align : a,
      vc : data
    });
    return test(type, x, object);
  }
  /**
   * @param {?} $filter
   * @param {?} $analytics
   * @return {?}
   */
  function MessagesThreadController($filter, $analytics) {
    return $filter(date, "x6e61f__container", $filter(label, $analytics));
  }
  /**
   * @param {?} cb
   * @return {?}
   */
  function hash(cb) {
    return cb(mutationsMap);
  }
  /**
   * @param {?} cb
   * @return {?}
   */
  function eq$(cb) {
    return cb(n);
  }
  /**
   * @param {?} callback
   * @return {?}
   */
  function strings(callback) {
    return callback(hex);
  }
  /**
   * @param {?} expect
   * @return {?}
   */
  function loggedIn(expect) {
    return expect(even_more_comments);
  }
  /**
   * @param {?} number
   * @return {?}
   */
  function number(number) {
    return number(s);
  }
  /**
   * @param {?} callback
   * @return {?}
   */
  function size(callback) {
    return callback(returnObj);
  }
  /**
   * @param {?} callback
   * @return {?}
   */
  function place(callback) {
    return callback(token);
  }
  /**
   * @param {?} map
   * @param {string} component
   * @return {?}
   */
  function query(map, component) {
    var state = component.disabled;
    var onCreateArgs = component.nd;
    component = component.xc;
    return map(ch, "x99dbb__attachment-buttons", map.K(combine, function() {
      return onCreateArgs && {
        pa : resolve(_init, {
          state : state ? "disabled" : "default",
          style : "linkSecondary",
          ya : "icon",
          icon : resolve(place),
          size : "small",
          F : onCreateArgs
        }),
        Oa : log("Roll.Editor.AttachmentBlock.UpdateButtonTooltip")
      };
    }), map(combine, {
      pa : resolve(_init, {
        state : state ? "disabled" : "default",
        style : "linkSecondary",
        ya : "icon",
        icon : resolve(size),
        size : "small",
        F : component
      }),
      Oa : log("Roll.Editor.AttachmentBlock.DownloadButtonTooltip")
    }));
  }
  /**
   * @param {?} $
   * @param {?} DataTable
   * @return {?}
   */
  function factory($, DataTable) {
    var ext = DataTable.ve;
    return $(output, "x99dbb__attachment-icon", $.Na(function() {
      switch(ext) {
        case "doc":
        case "docx":
          return resolve(hash, {});
        case "ppt":
        case "pptx":
          return resolve(strings, {});
        case "xls":
        case "xlsx":
          return resolve(number, {});
        case "pdf":
          return resolve(eq$, {});
        default:
          return resolve(loggedIn, {});
      }
    }));
  }
  /**
   * @param {?} h
   * @return {?}
   */
  function addButton(h) {
    return h(SiteRow, "x99dbb__placeholder");
  }
  /**
   * @param {?} h
   * @param {!Object} b
   * @return {?}
   */
  function display(h, b) {
    var b14 = b.fileName;
    var pre = b.fileSize;
    var lb = b.lb;
    var resolveToObject = b.uc;
    var endpoint = b.Qe;
    var c = b.disabled;
    var b_state = b.nd;
    var num = b.xc;
    b = parse();
    bind(b, "click", function() {
      if (endpoint) {
        num();
      }
    });
    return h(expected, extend("x99dbb__attachment-block", {
      clickable : endpoint,
      lock : !!c
    }), b, h.Na(function() {
      return resolveToObject ? resolve(MessagesThreadController, {
        size : "medium"
      }, "x99dbb__attachment-icon") : resolve(factory, {
        ve : lb
      });
    }), extend("x99dbb__attachment-subblock", {
      uploading : resolveToObject
    }), "x99dbb__attachment-info", "x99dbb__attachment-name", b14, "x99dbb__attachment-size", showModal(pre), h(query, {
      disabled : !!c,
      nd : b_state,
      xc : endpoint ? function() {
      } : num
    }), h.K(addButton, function() {
      return !endpoint && !resolveToObject;
    }));
  }
  /**
   * @param {string} b
   * @return {undefined}
   */
  function Class(b) {
    b = b.block;
    context.call(this, {
      X : "attachment-block"
    });
    /** @type {string} */
    this.b = b;
  }
  /**
   * @param {string} b
   * @param {string} c
   * @return {?}
   */
  function open(b, c) {
    var file;
    if (maps[b.key]) {
      var a = file = maps[b.key];
      if (a.b !== b) {
        /** @type {string} */
        a.b = b;
        push(a);
      }
    } else {
      file = new Class({
        block : b
      });
      maps[b.key] = file;
    }
    f(file, "after", c);
    return file;
  }
  /**
   * @param {!Object} o
   * @return {?}
   */
  function fn(o) {
    var width = o.width;
    var height = o.height;
    var options = o.U;
    return {
      src : o.src,
      U : options ? options : Object.assign({}, style),
      width : options ? width - options.left - options.right : width,
      height : options ? height - options.top - options.bottom : height
    };
  }
  /**
   * @param {!Object} p
   * @return {?}
   */
  function position_to_css(p) {
    var x = p.width;
    var y = p.height;
    p = fn(p);
    /** @type {number} */
    var screenX = x - p.width;
    /** @type {number} */
    var screenY = y - p.height;
    return {
      width : x / p.width * 100,
      height : y / p.height * 100,
      positionX : 0 === screenX ? 0 : p.U.left / x * (x / screenX) * 100,
      positionY : 0 === screenY ? 0 : p.U.top / y * (y / screenY) * 100
    };
  }
  /**
   * @param {!Object} s
   * @return {undefined}
   */
  function val(s) {
    context.call(this, {
      Ya : s.Ya,
      X : s.X,
      block : s.block,
      relatedData : s.relatedData
    });
    /** @type {boolean} */
    this.b = false;
    /** @type {null} */
    this.Aa = this.Pa = this.g = this.f = null;
  }
  /**
   * @param {!Object} options
   * @return {?}
   */
  function makeupBalloon(options) {
    var box = {
      display : options.b ? "none" : ""
    };
    if (null != options.f) {
      box.left = options.f;
    }
    if (null != options.g) {
      box.top = options.g;
    }
    if (null != options.Pa) {
      box.width = options.Pa;
    }
    if (null != options.Aa) {
      box.height = options.Aa;
    }
    return box;
  }
  /**
   * @param {!Object} c
   * @return {undefined}
   */
  function transform(c) {
    var b = c.block;
    c = c.ib;
    context.call(this, {
      X : "media-block"
    });
    this.b = b;
    /** @type {!Object} */
    this.Ba = c;
    this.hb = (new Promise("media-block", "wrapper")).className();
    this.Aa = (new Promise("media-block", "content")).className();
    this.Pa = (new Promise("media-block", "placeholder")).className();
    /** @type {number} */
    this.g = 0;
    this.f(b);
  }
  /**
   * @param {!Object} node
   * @param {!Object} el
   * @param {number} v
   * @return {?}
   */
  function setup(node, el, v) {
    var api = forEach(join(1));
    var exp = api.next().value;
    var expect = api.next().value;
    var b = union(function() {
      return fn(node);
    }, [node]);
    select(function() {
      if (el.current) {
        var t = el.current;
        /** @type {number} */
        var s = b.width / (v ? v.width : t.clientWidth);
        /** @type {number} */
        t = b.height / (v ? v.height : t.clientHeight);
        if (1 < s || 1 < t) {
          expect(Math.max(s, t));
        } else {
          expect(1);
        }
      }
    }, [el, b, v]);
    return exp;
  }
  /**
   * @param {?} fn
   * @return {?}
   */
  function timer(fn) {
    return fn(jpath, "x9c461__image-selection");
  }
  /**
   * @param {?} callback
   * @param {!Object} params
   * @return {?}
   */
  function onload(callback, params) {
    var src = params.image;
    var size = params.Gc;
    var a = params.width;
    var c = params.height;
    var data = params.F;
    params = params.Dd;
    var source = position_to_css(src);
    /** @type {string} */
    a = a && c ? "width: " + a + "px; height: " + c + "px;" : "";
    return callback(parserErr, extend("x9c461__image", {
      withZoom : !!params
    }), "\n\t    " + a + "\n\t\tbackground: url(" + getNodeValue(src.src) + ") no-repeat;\n\t\tbackground-size: " + source.width + "% " + source.height + "%;\n\t\tbackground-position: " + source.positionX + "% " + source.positionY + "%;", data, callback.K(timer, function() {
      return size;
    }));
  }
  /**
   * @param {?} cb
   * @param {!Object} model
   * @return {?}
   */
  function decode(cb, model) {
    var name = model.image;
    var options = model.tf;
    var treatErrorsAsValues = model.Gc;
    var collisionMode = model.Dd;
    var r = model.ke;
    var data = model.Pf;
    var variable_encoded = void 0 === model.className ? "" : model.className;
    model = model.F;
    var o = parse();
    var b = union(function() {
      return fn(name);
    }, [name]);
    r = setup(name, r ? r : o, data);
    return cb(triggerPublishSeed, variable_encoded + " " + extend("x9c461__image-container", {
      fill : !options,
      origin : !!options
    }), o, cb(onload, {
      image : name,
      Dd : collisionMode,
      Gc : treatErrorsAsValues,
      width : options ? b.width / r : void 0,
      height : options ? b.height / r : void 0,
      F : model
    }));
  }
  /**
   * @param {string} i
   * @param {!Object} options
   * @return {?}
   */
  function length(i, options) {
    /** @type {!Object} */
    var icon = Object.assign({}, options);
    var key = options.image;
    /** @type {!Object} */
    options = (delete icon.image, icon);
    icon = union(function() {
      return Object.assign({}, key, {
        U : key.U ? key.U : Object.assign({}, style)
      });
    }, [key]);
    return test(i, resolve(decode, Object.assign({}, options, {
      image : icon
    })));
  }
  /**
   * @param {!Object} c
   * @param {string} s
   * @return {undefined}
   */
  function addClass(c, s) {
    var m = floor(s);
    call(function() {
      var mutationElement = c instanceof Element ? c : c && c.current;
      if (mutationElement) {
        var mObs = new ResizeObserver(function(config) {
          config = forEach(config).next().value;
          if (m.current) {
            m.current(config.contentRect);
          }
        });
        mObs.observe(mutationElement);
        return function() {
          mObs.disconnect();
        };
      }
    }, [c, m]);
  }
  /**
   * @return {?}
   */
  function makeElement() {
    var api = forEach(join(true));
    api.next();
    var fn = api.next().value;
    return attr(function() {
      return fn(function(canCreateDiscussions) {
        return !canCreateDiscussions;
      });
    }, []);
  }
  /**
   * @return {?}
   */
  function createRouter() {
    /** @type {!Array} */
    var dict = [];
    return {
      add : function(obj) {
        dict.push(obj);
        return function() {
          /** @type {number} */
          var c = dict.indexOf(obj);
          if (0 <= c) {
            dict.splice(c, 1);
          }
        };
      },
      dispatch : function(value) {
        var api = forEach(dict);
        var iterator = api.next();
        for (; !iterator.done; iterator = api.next()) {
          iterator = iterator.value;
          iterator(value);
        }
      }
    };
  }
  /**
   * @param {!Object} opts
   * @return {undefined}
   */
  function complete(opts) {
    var type = opts.Xa;
    var $ = opts.Zc;
    var element = opts.element;
    var e = opts.Vc;
    var on = opts.eb;
    type.dispatch();
    if ($) {
      $(element).play(element, type).then(function() {
        /** @type {null} */
        e.current = null;
        on();
      });
    }
  }
  /**
   * @param {!Object} n
   * @return {?}
   */
  function save(n) {
    var el = n.element;
    var text = n.Wc;
    var t = n.Nb;
    n = n.Tb;
    var parent = flatten(el);
    var eb = makeElement();
    var data = parse(createRouter());
    var o = parse(null);
    var options = parse(null);
    var b = floor(t);
    var c = floor(n);
    if (el !== parent) {
      if (t && el) {
        o.current = el;
      }
      if (text && el) {
        text(el);
      }
      if (n && !el) {
        options.current = parent;
      }
    }
    select(function() {
      if (el) {
        var pkg = data.current;
        var d = c.current;
        complete({
          element : el,
          eb : eb,
          Xa : pkg,
          Zc : b.current,
          Vc : o
        });
        return function() {
          return complete({
            element : $(el),
            eb : eb,
            Xa : pkg,
            Zc : d,
            Vc : options
          });
        };
      }
    }, [el, b, c, eb]);
    return options.current || o.current;
  }
  /**
   * @param {?} r
   * @return {undefined}
   */
  function notify(r) {
    var i = r.nb;
    var c = r.element;
    var p = r.We;
    r = r.If;
    var data = parse(null);
    var element = ops[i] || document.body;
    var type = attr(function() {
      var d = data.current;
      if (d) {
        $(d.parentElement == element, 'useExternalLayer(): layer "' + i + "\" doesn't contain element");
        element.removeChild(d);
        /** @type {null} */
        data.current = null;
      }
    }, [data, i, element]);
    call(function() {
      return type;
    }, [type]);
    if (c != data.current) {
      type();
      if (c) {
        if (r) {
          r(c);
        }
        /** @type {string} */
        c.style.position = "absolute";
        element.appendChild(c);
        if (p) {
          p(c);
        }
      }
      data.current = c;
    }
  }
  /**
   * @param {number} prop
   * @return {?}
   */
  function defaultValue(prop) {
    return 40 >= prop ? 40 : 220 * Math.pow(prop - 40 + 1, .146);
  }
  /**
   * @param {!Function} position
   * @param {!Object} scale
   * @return {?}
   */
  function axis_def(position, scale) {
    switch(position) {
      case "center":
        return {
          duration : 220,
          Ab : [0, 40],
          vb : [0, 0],
          opacity : [0, 1]
        };
      case "right":
        return {
          duration : defaultValue(scale.getBoundingClientRect().width),
          Ab : [scale.getBoundingClientRect().width, 0],
          vb : [0, 0]
        };
      case "bottom":
        return {
          duration : defaultValue(scale.getBoundingClientRect().height),
          Ab : [0, scale.getBoundingClientRect().height],
          vb : [0, 0]
        };
      case "left":
        return {
          duration : defaultValue(scale.getBoundingClientRect().width),
          Ab : [-scale.getBoundingClientRect().width, 0],
          vb : [0, 0]
        };
      default:
        throw isNaN(position), Error();
    }
  }
  /**
   * @param {!Object} args
   * @return {?}
   */
  function toggle(args) {
    var d = args.Ad;
    var i = args.zd;
    var visible = args.opacity;
    var after = args.after;
    return animate({
      duration : args.duration,
      opacity : visible ? hideAll(visible[0], visible[1], fill) : void 0,
      translate : getter(d, i, fill),
      after : function(elem) {
        /** @type {string} */
        elem.style.pointerEvents = "";
        /** @type {string} */
        elem.style.transform = "";
        if (visible) {
          elem.style.opacity = visible[1].toString();
        }
        if (after) {
          after();
        }
      },
      before : function(elem) {
        /** @type {string} */
        elem.style.pointerEvents = "none";
        elem.style.opacity = visible ? visible[0].toString() : "";
      }
    });
  }
  /**
   * @param {?} callback
   * @param {!Object} args
   * @return {?}
   */
  function jsonp(callback, args) {
    var data = args.F;
    return callback(child, extend("xd16ae__layer", {
      type : args.type
    }), data);
  }
  /**
   * @param {?} data
   * @return {undefined}
   */
  function up(data) {
    var server = data.nb;
    var report = data.af;
    var element = data.ea.K(jsonp, function() {
      return report;
    });
    data = save({
      element : element,
      Nb : function() {
        return sequence;
      },
      Tb : function() {
        return animdTransforms;
      },
      Wc : function(value) {
        /** @type {string} */
        value.style.opacity = "0";
      }
    });
    notify({
      nb : server,
      element : element || data
    });
    call(function() {
      if (element) {
        var demoBtn = ops[server] || document.body;
        var pointerEvents = demoBtn.style.pointerEvents;
        /** @type {string} */
        demoBtn.style.pointerEvents = "auto";
        return function() {
          demoBtn.style.pointerEvents = pointerEvents;
        };
      }
    }, [element, server]);
  }
  /**
   * @param {!Object} type
   * @param {!HTMLElement} element
   * @return {?}
   */
  function _create(type, element) {
    switch(type) {
      case "center":
        return {
          left : Math.round(.5 * (window.innerWidth - element.clientWidth)),
          top : Math.round(.5 * (window.innerHeight - element.clientHeight))
        };
      case "right":
        return {
          top : 0,
          bottom : 0,
          right : 0
        };
      case "bottom":
        return {
          bottom : 0,
          left : 0,
          right : 0
        };
      case "left":
        return {
          top : 0,
          bottom : 0,
          left : 0
        };
      default:
        throw isNaN(type), Error();
    }
  }
  /**
   * @param {!Object} el
   * @param {!HTMLElement} value
   * @return {undefined}
   */
  function position(el, value) {
    /**
     * @param {string} val
     * @return {?}
     */
    function px(val) {
      return isString(val) ? val + "px" : "";
    }
    el = _create(el, value);
    var left = el.left;
    var y = el.bottom;
    var x = el.right;
    value.style.top = px(el.top);
    value.style.right = px(x);
    value.style.bottom = px(y);
    value.style.left = px(left);
  }
  /**
   * @param {!Object} e
   * @return {?}
   */
  function change(e) {
    var key = e.type;
    var toElement = e.element;
    var id = e.qb;
    e = parse(null);
    var eb = makeElement();
    var r = union(function() {
      if (!dateFormatterCache.has(key)) {
        dateFormatterCache.set(key, []);
      }
      return $(dateFormatterCache.get(key));
    }, [key]);
    if (toElement && null === e.current) {
      /** @type {number} */
      e.current = r.push({
        Rg : e,
        eb : eb,
        qb : id
      }) - 1;
      if (e.current && 1 <= e.current) {
        r[e.current - 1].eb();
      }
    }
    if (!(toElement || e.current !== r.length - 1)) {
      if (e.current && 1 <= e.current) {
        r[e.current - 1].eb();
      }
      r.pop();
      /** @type {null} */
      e.current = null;
    }
    id = attr(function() {
      if (r.length) {
        var state = r[r.length - 1];
        if (state.qb) {
          state.qb();
        }
      }
    }, [r]);
    return {
      Oe : 0 === e.current,
      activeElement : e.current === r.length - 1 ? toElement : null,
      qb : id
    };
  }
  /**
   * @param {?} el
   * @return {undefined}
   */
  function hide(el) {
    select(function() {
      if (el) {
        var adResizeContainer = window.document.body;
        var overflow = adResizeContainer.style.overflow;
        /** @type {string} */
        adResizeContainer.style.overflow = "hidden";
        return function() {
          adResizeContainer.style.overflow = overflow;
        };
      }
    }, [el]);
  }
  /**
   * @param {!Object} obj
   * @return {undefined}
   */
  function processData(obj) {
    var $ = obj.ea;
    var i = obj.I;
    var element = obj.oe;
    var field = obj.close;
    var attributes = obj.Jf;
    obj = void 0 === obj.Ue ? "dark" : obj.Ue;
    var parent = $(element());
    var data = change({
      type : "center" == i ? "popup" : "sidePanel",
      element : parent,
      qb : field
    });
    field = data.Oe;
    element = data.activeElement;
    data = data.qb;
    hide(field);
    up({
      ea : $,
      nb : "popup",
      af : field ? {
        type : obj,
        F : data
      } : null
    });
    $ = save({
      element : element,
      Nb : function(value) {
        value = axis_def(i, value);
        return toggle({
          duration : value.duration,
          Ad : value.Ab,
          zd : value.vb,
          opacity : value.opacity
        });
      },
      Tb : function(info) {
        info = axis_def(i, info);
        return toggle({
          duration : info.duration,
          Ad : info.vb,
          zd : info.Ab,
          opacity : [1, 0],
          after : attributes
        });
      },
      Wc : function(value) {
        /** @type {string} */
        value.style.opacity = "0";
      }
    });
    notify({
      nb : "popup",
      element : element || $,
      We : function(x) {
        return position(i, x);
      }
    });
    addClass(document.body, function() {
      return parent && position(i, parent);
    });
    addClass(parent, function() {
      return parent && position(i, parent);
    });
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function getOptions(data) {
    var url = data.ea;
    var callbackId = data.I;
    var width = data.pa;
    data = data.ge;
    var api = forEach(join(null));
    var args = api.next().value;
    var test = api.next().value;
    processData({
      ea : url,
      I : callbackId,
      oe : function() {
        return args && resolve(width, args);
      },
      close : data ? function() {
        return test(null);
      } : void 0
    });
    return union(function() {
      return {
        open : test,
        close : function() {
          return test(null);
        }
      };
    }, []);
  }
  /**
   * @return {?}
   */
  function getText() {
    return "ontouchstart" in document.documentElement;
  }
  /**
   * @param {!Object} fn
   * @param {!Function} loadNotifs
   * @param {!Function} saveNotifs
   * @return {undefined}
   */
  function bindEvents(fn, loadNotifs, saveNotifs) {
    /** @type {(number|undefined)} */
    var value = void 0 === value ? 300 : value;
    var css = parse(0);
    var v = parse(0);
    bind(fn, "click", function(event) {
      event.preventDefault();
      css.current += 1;
      if (1 === css.current) {
        /** @type {number} */
        v.current = setTimeout(function() {
          if (saveNotifs) {
            saveNotifs();
          }
          /** @type {number} */
          css.current = 0;
          clearTimeout(v.current);
        }, value);
      }
      if (2 === css.current) {
        loadNotifs();
        /** @type {number} */
        css.current = 0;
        clearTimeout(v.current);
      }
    });
  }
  /**
   * @param {?} cb
   * @return {?}
   */
  function iter(cb) {
    return cb(urConfigJson);
  }
  /**
   * @param {?} level
   * @return {?}
   */
  function level(level) {
    return level(expr);
  }
  /**
   * @param {?} callback
   * @return {?}
   */
  function info(callback) {
    return callback(ast);
  }
  /**
   * @param {?} require
   * @return {?}
   */
  function app(require) {
    return require(json);
  }
  /**
   * @param {undefined} x
   * @param {number} y
   * @param {?} h
   * @param {number} d
   * @return {?}
   */
  function openWindow(x, y, h, d) {
    /** @type {number} */
    var f = Math.abs(h.width - window.innerWidth) / 2 / d;
    /** @type {number} */
    h = Math.abs(h.height - window.innerHeight) / 2 / d;
    /** @type {number} */
    d = -h;
    x = clip(x, -f, f);
    y = clip(y, d, h);
    return {
      hd : x,
      jd : y
    };
  }
  /**
   * @param {!Object} e
   * @param {!Object} o
   * @param {!Object} params
   * @param {?} target
   * @param {number} d
   * @return {?}
   */
  function navigate(e, o, params, target, d) {
    e = openWindow(params.x + (e.x - o.x) / d, params.y + (e.y - o.y) / d, target, d);
    return {
      x : e.hd,
      y : e.jd
    };
  }
  /**
   * @param {!Object} item
   * @param {number} scale
   * @return {?}
   */
  function moment(item, scale) {
    return {
      $a : item.current.offsetWidth * scale > window.innerWidth,
      gb : item.current.offsetHeight * scale > window.innerHeight
    };
  }
  /**
   * @param {!Object} str
   * @param {number} options
   * @return {?}
   */
  function config(str, options) {
    var data = parse({
      $a : false,
      gb : false
    });
    select(function() {
      data.current = moment(str, options);
    }, [str, data, options]);
    return data;
  }
  /**
   * @param {!Array} a
   * @return {undefined}
   */
  function k(a) {
    createElement(a, function(ldata, howMany) {
      func(ldata);
      delete a[howMany];
    });
  }
  /**
   * @param {!Object} type
   * @param {string} name
   * @param {!Function} listener
   * @return {?}
   */
  function addListener(type, name, listener) {
    return on(type, name, function(event) {
      try {
        if (true === listener(event)) {
          event.preventDefault();
        }
        if (!ie) {
          execute();
        }
      } catch (e) {
        debug(e);
      }
    });
  }
  /**
   * @param {!Object} element
   * @param {number} key
   * @param {!Function} a
   * @return {undefined}
   */
  function bindKeyboardHandler(element, key, a) {
    var lastKey = {};
    bind(element, "keydown", function(event) {
      if (!(event.defaultPrevented || event.keyCode !== key || void 0 !== lastKey.pe && event.ctrlKey !== lastKey.pe || void 0 !== lastKey.shift && event.shiftKey !== lastKey.shift || void 0 !== lastKey.metaKey && event.metaKey !== lastKey.metaKey)) {
        event.preventDefault();
        a(event);
      }
    });
  }
  /**
   * @param {!Function} context
   * @param {!Function} el
   * @return {undefined}
   */
  function main(context, el) {
    select(function() {
      /**
       * @param {!Object} event
       * @return {undefined}
       */
      function render(event) {
        if (0 < event.deltaY) {
          el();
        } else {
          context();
        }
      }
      window.addEventListener("wheel", render);
      return function() {
        window.removeEventListener("wheel", render);
      };
    }, [el, context]);
  }
  /**
   * @param {!Object} options
   * @param {number} value
   * @param {!Object} type
   * @param {!Object} values
   * @return {undefined}
   */
  function exports(options, value, type, values) {
    var attrs = parse({
      x : 0,
      y : 0
    });
    var tree = parse(type);
    var b = floor(type);
    var browser = parse({
      width : 0,
      height : 0
    });
    var min = floor(values);
    var point = config(options, value);
    select(function() {
      /**
       * @param {!Event} event
       * @return {undefined}
       */
      function start(event) {
        var element_box = options.current.getBoundingClientRect();
        browser.current = {
          width : element_box.width,
          height : element_box.height
        };
        tree.current = b.current;
        attrs.current = {
          x : event.clientX,
          y : event.clientY
        };
        event.preventDefault();
        id = addListener(window, "mouseup", callback);
        escape = addListener(window, "mousemove", onClick);
      }
      /**
       * @param {!Event} event
       * @return {undefined}
       */
      function onClick(event) {
        var imagePointToPatientPoint = min.current;
        var next = b.current;
        var target = {
          x : event.clientX,
          y : event.clientY
        };
        if (1 === event.buttons) {
          event.preventDefault();
          next = navigate(target, attrs.current, tree.current, browser.current, value);
        } else {
          callback();
        }
        imagePointToPatientPoint({
          x : point.current.$a ? next.x : b.current.x,
          y : point.current.gb ? next.y : b.current.y
        });
      }
      /**
       * @return {undefined}
       */
      function callback() {
        k([escape, id]);
      }
      /** @type {null} */
      var down = null;
      /** @type {null} */
      var escape = null;
      /** @type {null} */
      var id = null;
      if (point.current.$a || point.current.gb) {
        down = addListener(options.current, "mousedown", start);
      }
      return function() {
        k([down, escape, id]);
      };
    }, [value, b, options, min, point]);
  }
  /**
   * @param {!Object} options
   * @param {undefined} mode
   * @param {!Object} name
   * @param {string} value
   * @param {!Function} params
   * @param {!Function} data
   * @param {!Function} x
   * @return {undefined}
   */
  function print(options, mode, name, value, params, data, x) {
    main(params, data);
    var element = parse(window);
    /** @type {boolean} */
    var dayMode = /Firefox/.test(navigator.userAgent);
    /** @type {number} */
    var i = dayMode ? 61 : 187;
    bindKeyboardHandler(element, dayMode ? 173 : 189, data);
    bindKeyboardHandler(element, i, params);
    bindKeyboardHandler(element, 109, data);
    bindKeyboardHandler(element, 107, params);
    bindKeyboardHandler(element, 27, x);
    exports(options, mode, name, value);
  }
  /**
   * @param {!Object} t
   * @return {?}
   */
  function down(t) {
    if (2 === t.length) {
      var e = forEach([{
        x : t[0].pageX,
        y : t[0].pageY
      }, {
        x : t[1].pageX,
        y : t[1].pageY
      }]);
      t = e.next().value;
      e = e.next().value;
      return Math.sqrt(Math.pow(e.x - t.x, 2) + Math.pow(e.y - t.y, 2));
    }
    return 0;
  }
  /**
   * @param {!Object} c
   * @param {?} a
   * @param {!Function} callback
   * @param {!Object} x
   * @param {string} i
   * @return {undefined}
   */
  function link(c, a, callback, x, i) {
    var editor = parse({
      vd : 0,
      jf : 1,
      ud : {
        x : 0,
        y : 0
      },
      yb : "none"
    });
    var opts = config(c, a);
    var o = parse(x);
    var data = floor(x);
    var browser = parse();
    var v = floor(i);
    var y = attr(function(event) {
      /** @type {string} */
      editor.current.yb = "pinch";
      editor.current.jf = a;
      editor.current.vd = down(event.touches);
    }, [a]);
    var element = attr(function(b) {
      /** @type {number} */
      b = down(b.touches) / editor.current.vd;
      callback(a * b);
    }, [a, callback]);
    var zIndex = attr(function(touch) {
      var element_box = c.current.getBoundingClientRect();
      touch = touch.touches[0];
      browser.current = {
        width : element_box.width,
        height : element_box.height
      };
      editor.current.ud = {
        x : touch.clientX,
        y : touch.clientY
      };
      o.current = data.current;
      /** @type {string} */
      editor.current.yb = "drag";
    }, [data, c]);
    var val = attr(function(s) {
      var rotatePoint = v.current;
      var state = data.current;
      var start = s.touches[0];
      start = {
        x : start.clientX,
        y : start.clientY
      };
      if (s.touches && 1 === s.touches.length) {
        state = navigate(start, editor.current.ud, o.current, browser.current, a);
      }
      rotatePoint({
        x : opts.current.$a ? state.x : data.current.x,
        y : opts.current.gb ? state.y : data.current.y
      });
    }, [data, a, v, opts]);
    var type = attr(function(p) {
      if (p.cancelable) {
        p.preventDefault();
      }
      if (p.touches) {
        if (2 === p.touches.length && "pinch" === editor.current.yb) {
          element(p);
        } else {
          if (1 === p.touches.length && "drag" === editor.current.yb && (opts.current.$a || opts.current.gb)) {
            val(p);
          }
        }
      }
    }, [val, element, opts]);
    var name = attr(function() {
      /** @type {string} */
      editor.current.yb = "none";
      window.removeEventListener("touchmove", type);
      window.removeEventListener("touchend", name);
    }, [type]);
    var target = attr(function(e) {
      if (e.touches) {
        if (2 === e.touches.length) {
          y(e);
        } else {
          if (1 === e.touches.length) {
            zIndex(e);
          }
        }
        window.addEventListener("touchmove", type, {
          passive : false
        });
        window.addEventListener("touchend", name);
      }
    }, [zIndex, y, name, type]);
    select(function() {
      window.addEventListener("touchstart", target);
      return function() {
        window.removeEventListener("touchstart", target);
      };
    }, [target]);
  }
  /**
   * @return {?}
   */
  function table() {
    var api = forEach(join(1));
    var b = api.next().value;
    var format = api.next().value;
    return {
      Sa : b,
      td : function(i) {
        if (6 <= i) {
          format(6);
        } else {
          if (1 >= i) {
            format(1);
          } else {
            format(i);
          }
        }
      },
      sc : function() {
        var name = b + .5;
        if (6 >= name) {
          format(name);
        } else {
          format(6);
        }
      },
      nc : function() {
        /** @type {number} */
        var diff = b - .5;
        if (1 <= diff) {
          format(diff);
        } else {
          format(1);
        }
      },
      Bc : function() {
        format(1);
      }
    };
  }
  /**
   * @param {!Object} c
   * @param {!Object} t
   * @return {?}
   */
  function resize(c, t) {
    var b = forEach(join(true));
    var a = b.next().value;
    b = b.next().value;
    select(function() {
      if (a) {
        c.current.style.bottom = $(getComputedStyle(document.documentElement)).getPropertyValue("--tool-bar-bottom");
        t.current.style.top = $(getComputedStyle(document.documentElement)).getPropertyValue("--close-button-top");
      } else {
        /** @type {string} */
        c.current.style.bottom = "-" + c.current.offsetHeight + "px";
        /** @type {string} */
        t.current.style.top = "-" + t.current.offsetHeight + "px";
      }
    }, [a, b, c, t]);
    return {
      Re : a,
      gf : b
    };
  }
  /**
   * @param {string} a
   * @param {?} b
   * @return {?}
   */
  function div(a, b) {
    var cb = b.fe;
    return combine(a, {
      pa : resolve(_init, {
        style : "secondary",
        size : "small",
        F : function() {
          return cb();
        },
        ya : "icon",
        icon : resolve(level),
        state : "default"
      }, "x2385b__close-cross"),
      Oa : log("Common.Close"),
      I : "bottom",
      align : "start"
    });
  }
  /**
   * @param {?} callback
   * @param {!Object} options
   * @return {?}
   */
  function setState(callback, options) {
    return callback(isFirstTimeShow, callback(_init, {
      style : "ghost",
      size : "small",
      F : options.F,
      ya : "icon",
      icon : options.icon,
      state : options.disabled ? "disabled" : "default"
    }));
  }
  /**
   * @param {string} type
   * @param {!Object} settings
   * @return {?}
   */
  function Widget(type, settings) {
    var new_share_blocks = settings.Oa;
    return combine(type, {
      pa : resolve(setState, {
        icon : settings.icon,
        Oa : new_share_blocks,
        F : settings.F,
        disabled : settings.disabled
      }),
      Oa : new_share_blocks,
      show : !getText()
    });
  }
  /**
   * @param {?} $
   * @param {!Object} options
   * @return {?}
   */
  function core($, options) {
    var column = options.sc;
    var nc = options.nc;
    var NUMBER = options.Bc;
    var scaleOptions = options.scale;
    options = options.La;
    bind(options, "click", function(event) {
      return event.preventDefault();
    });
    return $(templ, "x2385b__zoom-tool-bar", options, $(Widget, {
      icon : resolve(info, {}, "x2385b__icon"),
      Oa : log("Common.ImageZoom.Tooltip.Decrease"),
      F : nc,
      disabled : 1 == scaleOptions
    }), $(Widget, {
      icon : resolve(iter, {}, "x2385b__icon"),
      Oa : log("Common.ImageZoom.Tooltip.ResetScale"),
      F : NUMBER,
      disabled : 1 == scaleOptions
    }, "x2385b__center-button"), $(Widget, {
      icon : resolve(app, {}, "x2385b__icon"),
      Oa : log("Common.ImageZoom.Tooltip.Increase"),
      F : column,
      disabled : 6 == scaleOptions
    }));
  }
  /**
   * @param {?} callback
   * @param {!Node} options
   * @return {?}
   */
  function img(callback, options) {
    var url = options.src;
    return callback(currentLoaderObject, extend("x2385b__image", {
      transition : options.Be,
      touchscreen : options.Te
    }), url);
  }
  /**
   * @param {?} element
   * @param {!Object} data
   * @return {?}
   */
  function update(element, data) {
    /**
     * @param {number} scale
     * @return {undefined}
     */
    function show(scale) {
      var obj = moment(a, scale);
      var window = openWindow(e.x, e.y, {
        width : a.current.offsetWidth * scale,
        height : a.current.offsetHeight * scale
      }, scale);
      scale = window.hd;
      window = window.jd;
      match({
        x : obj.$a ? scale : 0,
        y : obj.gb ? window : 0
      });
    }
    /**
     * @return {undefined}
     */
    function index() {
      render(true);
      s.sc();
    }
    /**
     * @return {undefined}
     */
    function cb() {
      render(true);
      s.nc();
      show(s.Sa - .5);
    }
    /**
     * @return {undefined}
     */
    function f() {
      render(true);
      s.Bc();
      match({
        x : 0,
        y : 0
      });
    }
    /**
     * @param {(Object|string)} doc
     * @return {undefined}
     */
    function end(doc) {
      render(false);
      match(doc);
    }
    var testFilenames = data.Je;
    var avatarUrl = data.Ie;
    var val = data.he;
    data = forEach(join(function() {
      return {
        x : 0,
        y : 0
      };
    }));
    var e = data.next().value;
    var match = data.next().value;
    data = forEach(join(false));
    var vulnData = data.next().value;
    var render = data.next().value;
    var s = table();
    var item = parse();
    data = parse();
    var value = parse();
    var a = parse();
    var nodeText = getText();
    var options = resize(value, data);
    var inverse = options.Re;
    var assert = options.gf;
    bind(value, "click", function(event) {
      return event.preventDefault();
    });
    bind(item, "click", function(event) {
      if (!event.defaultPrevented) {
        event.preventDefault();
        val();
      }
    });
    bindEvents(a, function() {
      if (nodeText) {
        if (inverse) {
          assert(false);
        }
        if (3 <= s.Sa) {
          f();
        } else {
          render(true);
          s.td(3);
        }
      }
    }, function() {
      if (nodeText) {
        assert(!inverse);
      }
    });
    link(a, s.Sa, function(n) {
      render(false);
      s.td(n);
      show(n);
    }, e, end);
    print(a, s.Sa, e, end, index, cb, val);
    select(function() {
      var m = moment(a, s.Sa);
      /** @type {number} */
      var thWidth = a.current.offsetWidth / 2;
      /** @type {number} */
      var startPosY = a.current.offsetHeight / 2;
      /** @type {string} */
      a.current.style.cursor = m.$a || m.gb ? "move" : "default";
      /** @type {string} */
      a.current.style.transformOrigin = thWidth - e.x + "px " + (startPosY - e.y) + "px";
      /** @type {string} */
      a.current.style.transform = "translate(" + e.x + "px, " + e.y + "px) scale(" + s.Sa + ")";
    }, [a, s.Sa, e, match]);
    call(function() {
      /** @type {string} */
      item.current.style.height = window.innerHeight + "px";
    });
    options = {
      La : a
    };
    return element(datum, "x2385b__preview", item, extend("x2385b__image-container", {
      touchscreen : nodeText
    }), element.K(img, function() {
      return !avatarUrl && {
        src : testFilenames,
        Be : vulnData,
        Te : nodeText
      };
    }, options), element.K(length, function() {
      return avatarUrl && {
        image : avatarUrl,
        tf : true,
        ke : item
      };
    }, options), element(core, {
      sc : index,
      nc : cb,
      Bc : f,
      scale : s.Sa,
      La : value
    }), element(div, {
      fe : val
    }, {
      La : data
    }));
  }
  /**
   * @param {string} url
   * @return {?}
   */
  function qs(url) {
    var options = getOptions({
      ea : url,
      I : "center",
      pa : update,
      ge : true
    });
    return attr(function(canCreateDiscussions, opts) {
      return options.open({
        Je : canCreateDiscussions,
        Ie : opts && opts.U ? Object.assign({}, opts, {
          U : opts.U
        }) : void 0,
        he : options.close
      });
    }, [options]);
  }
  /**
   * @param {!Comment} node
   * @return {undefined}
   */
  function Button(node) {
    var b = node.block;
    transform.call(this, {
      block : b,
      ib : node.ib
    });
    f(this, "hide-caption", !b.B.length);
  }
  /**
   * @param {!Object} instance
   * @param {!Object} obj
   * @return {?}
   */
  function sync(instance, obj) {
    obj = get({
      key : obj.key,
      B : obj.B
    });
    return render(new init({
      Ja : obj,
      Ya : set(instance, "caption"),
      Pb : null,
      md : "caption"
    }), function() {
    }, true);
  }
  /**
   * @param {string} input
   * @param {string} name
   * @return {?}
   */
  function authorize(input, name) {
    var src = name.He;
    name = name.className;
    var cb = qs(input);
    return test(input, resolve(length, {
      image : src,
      Gc : true,
      className : name,
      F : function() {
        return cb(getNodeValue(src.src), src);
      }
    }));
  }
  /**
   * @param {!Object} obj
   * @param {string} cssClass
   * @return {?}
   */
  function addCircleText(obj, cssClass) {
    obj = $(obj && "i" === obj.type && obj.Xb ? obj : null);
    return require(delegate, ":collaged-image-" + obj.key)(req, isArray(authorize)({
      He : obj,
      className : cssClass + " image-block"
    }));
  }
  /**
   * @param {!Comment} c
   * @return {undefined}
   */
  function m(c) {
    var b = c.block;
    Button.call(this, {
      block : b,
      ib : c.ib
    });
    f(this, "hide-caption", !b.B.length);
    /** @type {null} */
    this.l = null;
    /** @type {boolean} */
    this.h = false;
  }
  /**
   * @param {!Array} time
   * @param {!Array} value
   * @return {undefined}
   */
  function tick(time, value) {
    var index = time.map(function(cell) {
      var o = fn(cell);
      return o.U ? o.width / o.height : cell.width / cell.height;
    });
    /** @type {number} */
    var length = Math.min.apply(Math, each(index));
    value.forEach(function(out, k) {
      /** @type {number} */
      out.l = index[k] / length;
    });
  }
  /**
   * @param {string} b
   * @return {undefined}
   */
  function Matrix(b) {
    b = b.block;
    context.call(this, {
      X : "collage-block"
    });
    /** @type {string} */
    this.b = b;
    f(this, "align", this.b.w);
  }
  /**
   * @param {!Object} options
   * @param {!Array} data
   * @return {?}
   */
  function sorter(options, data) {
    var fps = data.map(function(result) {
      if (results[result.key]) {
        var res = results[result.key];
        res.f(result);
      } else {
        res = new m({
          block : result,
          ib : addCircleText
        });
        results[result.key] = res;
      }
      result = res;
      /** @type {boolean} */
      result.h = 1 === data.length && "float-left" !== options.b.w;
      f(result, "prevent-collaging", result.h);
      return result;
    });
    tick(data, fps);
    return fps.map(function(value) {
      return value.W();
    });
  }
  /**
   * @param {string} b
   * @param {string} c
   * @return {?}
   */
  function warn(b, c) {
    var s;
    if (LIST[b.key]) {
      var t = s = LIST[b.key];
      if (t.b !== b) {
        /** @type {string} */
        t.b = b;
        f(t, "align", t.b.w);
        push(t);
      }
    } else {
      s = new Matrix({
        block : b
      });
      LIST[b.key] = s;
    }
    f(s, "after", c);
    return s;
  }
  /**
   * @param {!Object} data
   * @param {string} path
   * @param {?} instantiator
   * @return {?}
   */
  function destroy(data, path, instantiator) {
    /** @type {string} */
    path = path + " embed-block";
    data = $("e" !== data.type ? null : data);
    return require(undefined, ":embed-" + data.key)(devices, function(a) {
      a = a.target;
      try {
        var aFrame = $(a.contentWindow);
        var observer = new ResizeObserver(function() {
          observer.unobserve(aFrame.document.body);
          instantiator(aFrame.document.body.clientHeight / aFrame.document.body.clientWidth);
        });
        observer.observe(aFrame.document.body);
      } catch (g) {
      }
    }, path, data.src);
  }
  /**
   * @param {(Object|string)} name
   * @param {string} a
   * @return {?}
   */
  function e(name, a) {
    if (properties[name.key]) {
      var item = properties[name.key];
      item.f(name);
    } else {
      item = new Button({
        block : name,
        ib : destroy
      });
      properties[name.key] = item;
    }
    f(item, "after", a);
    return item;
  }
  /**
   * @param {?} params
   * @return {undefined}
   */
  function runTest(params) {
    params.pb("gotoNextQuestion", function(value, data) {
      value = data.ca;
      if (value == data.la.length - 1) {
        debug("Goto next operation isn't  available for last question");
      } else {
        data.ca = value + 1;
      }
    });
  }
  /**
   * @param {?} params
   * @return {undefined}
   */
  function Connection(params) {
    params.pb("gotoPreviousQuestion", function(ca, options) {
      ca = options.ca;
      if (0 == ca) {
        debug("Goto previous operation isn't  available for first question");
      } else {
        /** @type {number} */
        options.ca = ca - 1;
      }
    });
  }
  /**
   * @param {?} state
   * @return {undefined}
   */
  function instance(state) {
    state.pb("restartExercise", function(b, data) {
      var val = data.Sb.R.Ma;
      data.la = data.la.map(function(f) {
        return translate(f.ba, val);
      });
      /** @type {number} */
      data.ca = 0;
    });
  }
  /**
   * @param {?} state
   * @return {undefined}
   */
  function _initialize(state) {
    state.pb("setTypeInAnswer", function(ua, data) {
      data = $(is(data));
      var day = parseInt(data);
      if (day) {
        /** @type {string} */
        day.j.ua = ua;
      } else {
        debug("For question with type: " + data.ba.type + " not available 'Set type in answer' action");
      }
    });
  }
  /**
   * @param {?} elem
   * @return {undefined}
   */
  function setFocus(elem) {
    elem.pb("submit", function(b, label) {
      /** @type {boolean} */
      $(is(label)).o = true;
    });
  }
  /**
   * @param {?} state
   * @return {undefined}
   */
  function validate(state) {
    state.pb("setChoiceCorrect", function(id, data) {
      data = $(is(data));
      var object_t = data.ba.type;
      if (data = trim(data)) {
        data.j.da.forEach(function(tab) {
          if ("mc" == object_t) {
            /** @type {boolean} */
            tab.selected = tab.ja.key == id;
          } else {
            if (tab.ja.key == id) {
              /** @type {boolean} */
              tab.selected = !tab.selected;
            }
          }
        });
      } else {
        debug("For question with type: " + object_t + " not available 'Set choice correct' action");
      }
    });
  }
  /**
   * @param {?} url
   * @return {undefined}
   */
  function getItem(url) {
    [Connection, runTest, setFocus, _initialize, validate, instance].forEach(function(original) {
      return original(url);
    });
  }
  /**
   * @param {?} fn
   * @return {?}
   */
  function Router(fn) {
    /** @type {!Map} */
    var b = new Map;
    getItem({
      pb : function(n, d) {
        b.set(n, d);
      }
    });
    return {
      dispatch : function(value, name) {
        name = void 0 === name ? "" : name;
        var listener = b.get(value);
        if (listener) {
          fn(function(removedIndex) {
            return listener(name, removedIndex);
          });
        } else {
          fail("Unknown action: " + value);
        }
      }
    };
  }
  /**
   * @param {?} cb
   * @param {!Object} opts
   * @return {?}
   */
  function env(cb, opts) {
    return cb(iconCtx, h("button", {
      hidden : !opts.visible
    }, opts.className), opts.F);
  }
  /**
   * @param {?} done
   * @param {!Object} options
   * @return {?}
   */
  function CheckBox(done, options) {
    return done(tk, options.text);
  }
  /**
   * @param {!Object} value
   * @return {?}
   */
  function _build(value) {
    var api = forEach(join(false));
    var instance = api.next().value;
    var d = api.next().value;
    select(function() {
      /** @type {null} */
      var up = null;
      var mousedown = addListener(value.current, "mousedown", function() {
        up = addListener(window, "mouseup", function() {
          d(false);
          func(up);
        });
        d(true);
      });
      return function() {
        return k([up, mousedown]);
      };
    }, [value]);
    return instance;
  }
  /**
   * @param {!Object} el
   * @return {?}
   */
  function install(el) {
    var api = forEach(join(false));
    var target = api.next().value;
    var d = api.next().value;
    select(function() {
      var view = el.current;
      /** @type {!Array} */
      var val = [addListener(view, "mouseover", function() {
        return d(true);
      }), addListener(view, "mouseout", function() {
        return d(false);
      })];
      return function() {
        return k(val);
      };
    }, [el]);
    return target;
  }
  /**
   * @param {?} callback
   * @param {!Element} obj
   * @return {?}
   */
  function Component(callback, obj) {
    var value = parse();
    var item = install(value);
    var err = _build(value);
    var m = obj.v;
    /** @type {!Object} */
    item = Object.assign({}, obj.Ug, {
      over : item,
      down : err
    });
    /** @type {(number|undefined)} */
    err = false === obj.Dg ? -1 : void 0;
    return callback(drw, value, obj.disabled, "" + m("button", item, obj.className), obj.onmouseover, obj.onmouseout, err, function(event) {
      if (obj.onclick) {
        event.preventDefault();
        obj.onclick();
      }
    }, obj.onkeydown, obj.onmousedown, wrap(obj.Ob), obj.text || "");
  }
  /**
   * @param {?} result
   * @return {?}
   */
  function compile(result) {
    result = match(result);
    if (!result) {
      throw Error("OrderedMap is empty");
    }
    result = toString(result);
    if (!result) {
      throw Error("First block is not paragraph");
    }
    return result;
  }
  /**
   * @param {!Function} result
   * @return {?}
   */
  function count(result) {
    var val = parseInt(result);
    if (val) {
      a: {
        val = val.j;
        var c = val.Bd;
        result = c.Da;
        val = val.ua;
        val = (result ? val : val.toLowerCase()).trim();
        c = forEach(c.ia);
        var s = c.next();
        for (; !s.done; s = c.next()) {
          if (s = s.value.text, s = (result ? s : s.toLowerCase()).trim(), val == s) {
            /** @type {boolean} */
            result = true;
            break a;
          }
        }
        /** @type {boolean} */
        result = false;
      }
      return result;
    }
    if (val = trim(result)) {
      a: {
        result = forEach(val.j.da);
        val = result.next();
        for (; !val.done; val = result.next()) {
          if (val = val.value, val.selected != val.ja.L) {
            /** @type {boolean} */
            result = false;
            break a;
          }
        }
        /** @type {boolean} */
        result = true;
      }
      return result;
    }
    debug("Unknown question type: " + result.ba.type);
    return false;
  }
  /**
   * @param {!Function} data
   * @return {?}
   */
  function restore(data) {
    var $ = trim(data);
    if ($) {
      return !!$.j.da.find(function(cbCollection) {
        return cbCollection.selected;
      });
    }
    if ($ = parseInt(data)) {
      return !!$.j.ua;
    }
    debug("Unknown question type: " + data.ba.type);
    return false;
  }
  /**
   * @param {?} callback
   * @param {!Object} name
   * @return {?}
   */
  function action(callback, name) {
    var b = union(function() {
      return fn(name);
    }, [name]);
    /** @type {number} */
    var d = b.height / b.width;
    return callback(identifierPositions, parseHTML("image"), "max-width: " + b.width + "px", "padding-bottom: " + 100 * d + "%", callback(length, {
      image : name
    }));
  }
  /**
   * @param {?} callback
   * @param {!Node} error
   * @return {?}
   */
  function parseResponse(callback, error) {
    return callback(fpurl, parseHTML("embed-attachment"), error.src);
  }
  /**
   * @param {?} callback
   * @param {?} options
   * @return {?}
   */
  function configure(callback, options) {
    var data = options.M;
    return callback(pac, parseHTML("attachment"), callback.Na(function() {
      a: {
        switch(data.type) {
          case "i":
            var url = resolve(action, data);
            break a;
          case "e":
            url = resolve(parseResponse, data);
            break a;
          default:
            throw isNaN(data.type), Error("Unknown attachment type");
        }
      }
      return url;
    }));
  }
  /**
   * @param {?} callback
   * @param {!Object} self
   * @return {?}
   */
  function pfx(callback, self) {
    return callback(isFirstTimeShown, self.style, self.text);
  }
  /**
   * @param {?} h
   * @param {string} params
   * @return {?}
   */
  function layout(h, params) {
    var y = params.style;
    var text = params.text;
    var href = params.url;
    params = params.Fc;
    var value = parse();
    template({
      ea : h,
      Za : value,
      text : {
        html : "<a href=" + href + ' class=x1059a__tooltip-link target="_blank">' + href + "</a>"
      },
      size : "small",
      color : "dark",
      show : params,
      I : "bottom",
      $b : true
    });
    return h(input, href, value, y, text);
  }
  /**
   * @param {?} item
   * @param {!Array} array
   * @param {!Object} done
   * @return {?}
   */
  function clean(item, array, done) {
    if (!array.length) {
      array.push({
        text : i,
        ga : replace(),
        Wb : true
      });
    }
    var d = new RegExp;
    return item.Ac(array, function(m) {
      var b = m.ga;
      var props = {};
      /** @type {!Array} */
      var val = [];
      if (color(b.style, "bold")) {
        /** @type {string} */
        props["font-weight"] = "bold";
      }
      if (color(b.style, "italic")) {
        /** @type {string} */
        props["font-style"] = "italic";
      }
      if (color(b.style, "underline")) {
        val.push("underline");
      }
      if (color(b.style, "strikethrough")) {
        val.push("line-through");
      }
      if (val.length) {
        /** @type {string} */
        props["text-decoration"] = val.join(" ");
      }
      b = m.ga.url;
      val = m.text;
      if (m.Wb && val.endsWith("\n")) {
        /** @type {string} */
        val = val + i;
      }
      m = pad(d, "chunk_" + m.text + (b ? "_url" : "_text"));
      return b ? assign(m, layout, {
        style : props,
        text : val,
        url : b,
        Fc : done
      }) : assign(m, pfx, {
        style : props,
        text : val
      });
    });
  }
  /**
   * @param {?} fn
   * @param {!Object} opts
   * @return {?}
   */
  function invoke(fn, opts) {
    var params = opts.Ja;
    var value = void 0 === opts.placeholder ? "" : opts.placeholder;
    var key = opts.className;
    var path = opts.Ob;
    opts = opts.Fc;
    var options = $A(params);
    value = options.length ? "" : value;
    key = key && value ? key.split(" ").map(function(formset_prefix) {
      return formset_prefix + " " + formset_prefix + "_empty";
    }).join(" ") : key;
    return fn(lms, key, params.key, value, wrap(path), clean(fn, options, opts));
  }
  /**
   * @param {?} options
   * @param {!Object} node
   * @return {?}
   */
  function option(options, node) {
    return invoke(options, Object.assign({}, node, {
      className : element("x91b27__exercise-paragraph", node.className),
      Fc : false
    }));
  }
  /**
   * @param {!Object} props
   * @param {string} key
   * @param {string} val
   * @return {?}
   */
  function checked(props, key, val) {
    var value = props.ja;
    return key && val ? value.L ? "correct" : "incorrect" : props.selected ? "selected" : "none";
  }
  /**
   * @param {?} contains
   * @param {!Object} message
   * @return {?}
   */
  function onShow(contains, message) {
    return contains(selectedText, message.className);
  }
  /**
   * @param {?} expect
   * @param {!Object} msg
   * @return {?}
   */
  function expectError(expect, msg) {
    return expect(expanded_list, msg.className);
  }
  /**
   * @param {?} c
   * @param {!Object} m
   * @return {?}
   */
  function d(c, m) {
    return c(z, m.className);
  }
  /**
   * @param {?} callback
   * @param {!Object} params
   * @return {?}
   */
  function events(callback, params) {
    return callback(roomPeers, params.className);
  }
  /**
   * @param {?} type
   * @return {?}
   */
  function isEqual(type) {
    switch(type) {
      case "selected":
        return resolve(onShow, {
          className : assert("radio")
        });
      case "correct":
        return resolve(d, {
          className : assert("correct")
        });
      case "incorrect":
        return resolve(events, {
          className : assert("incorrect")
        });
      default:
        return null;
    }
  }
  /**
   * @param {?} s
   * @param {!Object} data
   * @return {?}
   */
  function p(s, data) {
    return s(a4, assert("control", {}, data.v("control")), s.Na(function() {
      return isEqual(data.status);
    }));
  }
  /**
   * @param {?} h
   * @param {!Object} query
   * @return {?}
   */
  function route(h, query) {
    /**
     * @param {!Event} event
     * @return {undefined}
     */
    function handler(event) {
      var el = event.target;
      var settingElement = $(el.parentElement);
      if ("A" != el.tagName && "A" != settingElement.tagName) {
        event.preventDefault();
        update("setChoiceCorrect", query.state.ja.key);
      }
    }
    var update = split(id);
    var node = query.o;
    var data = query.state.selected;
    var path = checked(query.state, node, query.H);
    data = {
      selected : data,
      status : path
    };
    var rootElem = compile(query.state.ja.content.T);
    return h(page_code, assert("choice", data, query.v("choice")), !node && handler, node ? true : null, h(p, {
      status : path,
      v : query.v
    }), h(option, {
      className : assert("text"),
      Ja : rootElem
    }));
  }
  /**
   * @param {?} done
   * @param {!Object} self
   * @return {?}
   */
  function evaluate(done, self) {
    return done(flash_env, assert("choices", {
      answered : self.o
    }), done.Ac(self.state.da, function(state) {
      return assign(state.ja.key, route, {
        v : self.v,
        state : state,
        o : self.o,
        H : self.H
      });
    }));
  }
  /**
   * @param {?} status
   * @return {?}
   */
  function handleClick(status) {
    switch(status) {
      case "selected":
        return resolve(expectError, {
          className : getData("checkbox")
        });
      case "correct":
        return resolve(d, {
          className : getData("correct")
        });
      case "incorrect":
        return resolve(events, {
          className : getData("incorrect")
        });
      default:
        return null;
    }
  }
  /**
   * @param {?} err
   * @param {!Object} data
   * @return {?}
   */
  function parseData(err, data) {
    return err(ex, getData("control", {}, data.v("control")), err.Na(function() {
      return handleClick(data.status);
    }));
  }
  /**
   * @param {?} createElement
   * @param {!Object} self
   * @return {?}
   */
  function setter(createElement, self) {
    /**
     * @param {!Event} e
     * @return {undefined}
     */
    function set(e) {
      var el = e.target;
      var settingElement = $(el.parentElement);
      if ("A" != el.tagName && "A" != settingElement.tagName) {
        e.preventDefault();
        isActive("setChoiceCorrect", self.state.ja.key);
      }
    }
    var isActive = split(id);
    var index = self.o;
    var data = self.state.selected;
    var part = checked(self.state, index, self.H);
    data = {
      selected : data,
      status : part
    };
    var rootElem = compile(self.state.ja.content.T);
    return createElement(mark, getData("choice", data, self.v("choice")), !index && set, index ? true : null, createElement(parseData, {
      v : self.v,
      status : part
    }), createElement(option, {
      className : getData("text"),
      Ja : rootElem
    }));
  }
  /**
   * @param {?} callback
   * @param {!Object} state
   * @return {?}
   */
  function submit(callback, state) {
    return callback(cached_wifi_network_list, getData("choices", {
      answered : state.o
    }), callback.Ac(state.state.da, function(task) {
      return assign(task.ja.key, setter, {
        v : state.v,
        state : task,
        o : state.o,
        H : state.H
      });
    }));
  }
  /**
   * @param {?} callback
   * @param {!Array} response
   * @return {?}
   */
  function onChange(callback, response) {
    response = response.ia.map(function(c) {
      return c.text;
    });
    return callback(liveLeakUrl, parseFloat("responses"), log("Roll.Exercise.Player.Content.TypeIn.AcceptableResponses"), response.join(", "));
  }
  /**
   * @param {?} _
   * @param {!Object} c
   * @return {?}
   */
  function edit(_, c) {
    var callback = split(id);
    var a = c.o;
    var data = c.state;
    var f = data.Bd;
    var was_open = f.Da;
    var name = data.ua;
    /** @type {boolean} */
    var b = !!f.ia.find(function(tok) {
      return was_open ? tok.text == name : tok.text.toUpperCase() == name.toUpperCase();
    });
    return _(teamStageData, parseFloat("type-in", {
      correct : a && b,
      incorrect : a && !b
    }, c.v("type-in")), _.K(d, function() {
      return a && b && {
        className : parseFloat("correct")
      };
    }), _.K(events, function() {
      return a && !b && {
        className : parseFloat("incorrect")
      };
    }), parseFloat("answer"), data.ua, log("Roll.Exercise.Player.Content.TypeIn.Placeholder"), function(l) {
      callback("setTypeInAnswer", l.target.value);
    }, a ? "" : void 0, _.K(onChange, function() {
      return a && c.H && {
        ia : f.ia
      };
    }));
  }
  /**
   * @param {?} cb
   * @param {!Object} self
   * @return {?}
   */
  function clear(cb, self) {
    var name = self.me;
    var db = sub(self.V, name);
    /** @type {boolean} */
    var e = !handleTaskNonConflict(db);
    var type = {
      "with-text" : e
    };
    var score = {
      correct : name,
      incorrect : !name
    };
    var va = name ? log("Roll.Exercise.Player.Status.Correct") : log("Roll.Exercise.Player.Status.Incorrect");
    return cb(lyr, removeItem("feedback-container", type, self.v("feedback-container")), removeItem("status-icon", score, self.v("status-icon")), cb.Na(function() {
      return name ? resolve(d, {
        className : ""
      }) : resolve(events, {
        className : ""
      });
    }), removeItem("status-label", {}, self.v("status-label")), va, cb.K(option, function() {
      return e && {
        className : element(removeItem("text"), self.v("feedback-text")),
        Ja : db
      };
    }));
  }
  /**
   * @param {?} h
   * @return {?}
   */
  function handleError(h) {
    var f = h.zc;
    h = h.H;
    var s = f.ba;
    if (s = "mc" == s.type && s ? f : null) {
      return resolve(evaluate, {
        v : reject,
        state : s.j,
        o : f.o,
        H : h
      });
    }
    s = f.ba;
    if (s = "mr" == s.type && s ? f : null) {
      return resolve(submit, {
        v : reject,
        state : s.j,
        o : f.o,
        H : h
      });
    }
    if (s = parseInt(f)) {
      return resolve(edit, {
        v : reject,
        state : s.j,
        o : f.o,
        H : h
      });
    }
    debug("Unknown question type: " + f.ba.type);
    return null;
  }
  /**
   * @param {?} createElement
   * @param {!Object} item
   * @return {?}
   */
  function refresh(createElement, item) {
    var ctn = item.bf;
    var value = item.H;
    var ds = item.Qd;
    var data = $(item.zc);
    var obj = data.ba;
    var V = obj.V;
    var rootElem = compile(obj.description.T);
    var expect = split(id);
    var resolve = split(address);
    var parseInt = split(glue);
    var o = data.o;
    var result = count(data);
    var request = {
      zc : data,
      H : value
    };
    var props = void 0;
    if (o && ds) {
      props = {
        "with-animations" : true
      };
      if (value) {
        /** @type {boolean} */
        props["show-correct-answers"] = true;
      }
      value = sub(V, result);
      if (!handleTaskNonConflict(value)) {
        /** @type {boolean} */
        props["with-feedback"] = true;
      }
    }
    return createElement(param, reject("question", props), createElement.K(CheckBox, function() {
      return !!ctn && {
        text : ctn
      };
    }, reject("question-number")), createElement(option, {
      className : reject("description"),
      Ja : rootElem
    }), createElement.K(configure, function() {
      return obj.M && {
        M : obj.M
      };
    }), createElement.Na(function() {
      return handleError(request);
    }), createElement.K(_init, function() {
      return !o && {
        style : "primary",
        ya : "text",
        text : log("Roll.Exercise.Player.Button.Submit"),
        state : restore(data) ? "default" : "disabled",
        F : function() {
          resolve(true);
          parseInt("bottom");
          expect("submit");
        },
        size : "xLarge"
      };
    }, reject("submit-button")), createElement.K(clear, function() {
      return o && {
        v : reject,
        me : result,
        V : V
      };
    }), createElement.K(_init, function() {
      return o && item.Ve && {
        style : "primary",
        ya : "text",
        text : log("Roll.Exercise.Player.Button.Next"),
        F : function() {
          resolve(false);
          parseInt("top");
          expect("gotoNextQuestion");
        },
        size : "xLarge"
      };
    }, reject("next-button")), createElement.K(Component, function() {
      return item.df && {
        v : reject,
        className : reject("restart-button"),
        text : log("Roll.Exercise.Player.Button.RestartExercise"),
        onclick : function() {
          resolve(false);
          parseInt("top");
          expect("restartExercise");
        }
      };
    }));
  }
  /**
   * @param {?} _
   * @param {!Object} self
   * @return {?}
   */
  function process(_, self) {
    var item = self.state;
    var resolve = self.dispatch;
    var $ = self.Fb;
    var f = forEach(join(false));
    var vulnData = f.next().value;
    var init = f.next().value;
    f = forEach(join("none"));
    var a = f.next().value;
    var test = f.next().value;
    var value = item.ca;
    f = item.la.length;
    /** @type {boolean} */
    var q = value + 1 != f;
    /** @type {boolean} */
    var tag = !!item.la.find(function(newPaths) {
      return !newPaths.o;
    });
    if (1 < f) {
      /** @type {string} */
      value = String((void 0 == value ? -1 : value) + 1).padStart(2, "0");
      /** @type {string} */
      f = String(f).padStart(2, "0");
      /** @type {string} */
      var r = value + "/" + f;
    }
    var mime = is(item);
    f = {};
    self = (f[self.mode] = true, f);
    var browser = parse();
    select(function() {
      if ("none" != a) {
        $(browser.current, a);
        test("none");
      }
    }, [a, $]);
    return _.Dc(id, resolve).Dc(address, init).Dc(glue, test)(right_pal, victorySpin("exercise", self), browser, _(env, {
      className : victorySpin("prev-button"),
      visible : 0 < item.ca,
      F : function() {
        init(false);
        test("top");
        resolve("gotoPreviousQuestion");
      }
    }), _.K(refresh, function() {
      return !!mime && {
        bf : r,
        zc : mime,
        H : item.Sb.R.H,
        Ve : q,
        df : !tag && !q,
        Qd : vulnData
      };
    }), _(env, {
      className : victorySpin("next-button"),
      visible : item.ca < item.la.length - 1,
      F : function() {
        init(false);
        test("top");
        resolve("gotoNextQuestion");
      }
    }));
  }
  /**
   * @param {number} value
   * @param {?} type
   * @param {number} lhs
   * @return {undefined}
   */
  function Node(value, type, lhs) {
    context.call(this, {
      X : "exercise-block"
    });
    this.h = {};
    /** @type {number} */
    this.b = value;
    this.g = Router(type).dispatch;
    /** @type {number} */
    this.l = lhs;
    /** @type {null} */
    this.f = null;
  }
  /**
   * @param {!Object} a
   * @return {?}
   */
  function toJSON(a) {
    var b = a.state;
    var c = a.Ib;
    var A = a.Pb;
    var x = a.Fb;
    a = b.Sb.key;
    if (colors[a]) {
      a = c = colors[a];
      if (a.b != b) {
        /** @type {null} */
        a.f = null;
        a.b = b;
        push(a);
      }
    } else {
      c = new Node(b, c, x);
      colors[a] = c;
    }
    f(c, "after", A);
    return c;
  }
  /**
   * @param {!Object} a
   * @return {?}
   */
  function x(a) {
    var f = a.getState;
    var boo = a.Ib;
    var aImagery = a.Fb;
    a = equal(f()).j;
    a = new A(a);
    /** @type {function(string, string): ?} */
    a.f.c = warn;
    /** @type {function((Object|string), string): ?} */
    a.f.e = e;
    u(a, function(cursor, canCreateDiscussions) {
      return toJSON({
        state : $(iterator(f(), cursor.key)),
        Ib : function(defined) {
          return boo(function(type) {
            type = $(iterator(type, cursor.key));
            defined(type);
          });
        },
        Pb : canCreateDiscussions,
        Fb : aImagery
      });
    });
    /** @type {function(string, string): ?} */
    a.f.a = open;
    return a;
  }
  /**
   * @param {!Object} value
   * @param {?} key
   * @return {?}
   */
  function iterator(value, key) {
    return (value = equal(value).j.oa.find(function(keys) {
      return key === keys.key;
    })) && "E" === value.type ? value : null;
  }
  /**
   * @param {!Object} dx
   * @param {string} dy
   * @return {undefined}
   */
  function b(dx, dy) {
    val.call(this, {
      block : "tooltip-with-tail"
    });
    var data = this;
    if (dy) {
      random(this, dy);
    }
    /** @type {!Object} */
    this.l = dx;
    max(this, unshift(this), function() {
      return finalize(data);
    });
    this.xa();
  }
  /**
   * @param {!Object} data
   * @return {undefined}
   */
  function finalize(data) {
    var e = $(map(data, "tail"));
    var r = data.D();
    data = data.l.getBoundingClientRect();
    r = r.getBoundingClientRect();
    var anitarget = {};
    write(e, (anitarget.left = Math.round(data.left - r.left + .5 * (data.width - e.clientWidth)) + "px", anitarget));
  }
  /**
   * @return {undefined}
   */
  function bar() {
    context.call(this, {
      X : "top-layer"
    });
    var that = this;
    /** @type {null} */
    this.f = this.b = this.g = null;
    max(this, unshift(this), function() {
      if (that.b) {
        that.b.tc();
      }
    });
  }
  /**
   * @param {!Object} item
   * @param {!Object} data
   * @return {undefined}
   */
  function renderPalette(item, data) {
    var err = data.ie;
    item.g = data.pa;
    item.b = err;
    getRgba(item);
    item.f = max(item, item.b.cc, function() {
      getRgba(item);
      /** @type {null} */
      item.g = null;
      /** @type {null} */
      item.b = null;
      push(item);
    });
    item.b.Xc();
    push(item);
  }
  /**
   * @param {!Object} a
   * @return {undefined}
   */
  function getRgba(a) {
    if (a.f) {
      assertEquals(a, a.f);
    }
    /** @type {null} */
    a.f = null;
  }
  /**
   * @param {number} prop
   * @return {undefined}
   */
  function pre(prop) {
    container.call(this);
    /** @type {number} */
    this.b = prop;
    this.cc = append(this);
  }
  /**
   * @param {!Object} a
   * @return {undefined}
   */
  function _(a) {
    var f = a.Ua;
    var g = void 0 === a.offsetX ? 20 : a.offsetX;
    var val = void 0 === a.offsetY ? 10 : a.offsetY;
    pre.call(this, a.pa);
    this.f = f;
    this.g = g;
    this.m = val;
  }
  /**
   * @param {!Event} d
   * @return {undefined}
   */
  function api(d) {
    var C = d.xa;
    _.call(this, {
      pa : d.Wa,
      Ua : d.Ua,
      offsetX : void 0 === d.offsetX ? 5 : d.offsetX,
      offsetY : void 0 === d.offsetY ? 10 : d.offsetY
    });
    this.C = C;
    /** @type {null} */
    this.h = this.l = this.P = null;
  }
  /**
   * @param {!Object} t
   * @param {!Object} e
   * @return {undefined}
   */
  function isMouseEventWithinElement(t, e) {
    e = e.relatedTarget;
    if (e instanceof Node && !t.f.contains(e) && !t.b.D().contains(e)) {
      t.Fa();
    }
  }
  /**
   * @param {!Object} self
   * @return {?}
   */
  function onerror(self) {
    var e = self.b.D();
    e = g(e);
    return constructor({
      fb : e,
      Hb : object,
      duration : 150,
      Uc : function() {
        self.b.Fa();
      }
    });
  }
  /**
   * @param {!Event} from
   * @return {undefined}
   */
  function pkg(from) {
    var l = from.xa;
    _.call(this, {
      pa : from.Wa,
      Ua : from.Ua,
      offsetX : void 0 === from.offsetX ? 5 : from.offsetX,
      offsetY : void 0 === from.offsetY ? 10 : from.offsetY
    });
    this.l = l;
    /** @type {null} */
    this.h = this.P = null;
  }
  /**
   * @return {undefined}
   */
  function circle() {
    bar.apply(this, arguments);
  }
  /**
   * @param {!Element} label
   * @return {?}
   */
  function pushPage(label) {
    return {
      animate : function(prop) {
        /** @type {string} */
        label.style.transform = "undefined scaleX(" + prop + ")";
      },
      complete : function() {
        /** @type {string} */
        label.style.transform = "";
      },
      wb : ["transform"]
    };
  }
  /**
   * @param {?} type
   * @param {?} text
   * @return {undefined}
   */
  function Config(type, text) {
    b.call(this, text);
    this.C = type;
  }
  /**
   * @param {?} callback
   * @param {!Array} a
   * @return {?}
   */
  function limit(callback, a) {
    var b = a.Td;
    a = a.Ud;
    var target = parse();
    var newValue = forEach(listen(target, void 0));
    newValue.next();
    newValue = newValue.next().value;
    template({
      ea : callback,
      Za : target,
      text : b,
      show : newValue
    });
    return callback(file, a, target, b);
  }
  /**
   * @param {string} prefix
   * @return {?}
   */
  function recurse(prefix) {
    return prefix ? prefix.replace(/^\s|\s$/, key) : i;
  }
  /**
   * @param {?} real
   * @param {!Object} oParam2
   * @return {?}
   */
  function lazyloadiframe_replace(real, oParam2) {
    oParam2 = 0 == oParam2.progress ? css("not-started") : css("complete");
    return real(interval, oParam2);
  }
  /**
   * @param {?} request
   * @param {!Object} params
   * @return {?}
   */
  function fileUpload(request, params) {
    /** @type {number} */
    params = (100 - 100 * params.progress) / 100 * 18 * Math.PI;
    return request(opt, css("progress-bar"), 9, css("bar"), 9, params);
  }
  /**
   * @param {?} dispatch
   * @param {!Object} options
   * @return {?}
   */
  function authenticate(dispatch, options) {
    var defaults = {
      selected : options.selected,
      intro : options.Pe
    };
    var progressCallback = options.progress;
    var touch = getText();
    var type = options.disabled;
    var mousemove = type ? options.Xe : null;
    var MOVE_EVENT = touch ? null : mousemove;
    touch = touch ? mousemove : null;
    mousemove = type ? null : options.F;
    return dispatch(UPDATE_NEWS, css("navigation-item", defaults), type ? true : null, mousemove, MOVE_EVENT, {
      ontouchstart : touch
    }, dispatch.Na(function() {
      return 0 == progressCallback || 1 == progressCallback ? resolve(lazyloadiframe_replace, {
        progress : progressCallback
      }) : resolve(fileUpload, {
        progress : progressCallback
      });
    }), css("label"), recurse(options.label));
  }
  /**
   * @return {undefined}
   */
  function C() {
    context.call(this, {
      X : "total-progress"
    });
    /** @type {number} */
    this.b = 0;
  }
  /**
   * @param {string} b
   * @param {!Array} s
   * @param {number} h
   * @return {undefined}
   */
  function copy(b, s, h) {
    context.call(this, {
      X : "navigation-panel"
    });
    /** @type {string} */
    this.b = b;
    /** @type {!Array} */
    this.S = s;
    /** @type {number} */
    this.h = h;
    this.g = new C;
    random(this.g, set(this, "progress"));
    this.C = diff(this, b);
    this.l = append(this);
    /** @type {boolean} */
    this.f = false;
    this.ha = this.b.intro.u.title || log("Roll.Common.IntroDefaultTitle");
  }
  /**
   * @param {!Object} item
   * @return {undefined}
   */
  function disconnect(item) {
    f(item, "showed", item.b.Ta);
    f(item, "in-overlay", spin() || 739 >= window.innerWidth);
    var m = item.g;
    var b = item.b;
    b = deepEqual(b.intro, b.chapters);
    m.b = b;
    push(m);
    item.C = diff(item, item.b);
    if (item.f) {
      /** @type {boolean} */
      item.f = false;
      f(item, "ignore-transition", true);
    } else {
      if (Object.keys(item.tb).includes("ignore-transition")) {
        f(item, "ignore-transition", false);
      }
    }
    push(item);
  }
  /**
   * @param {!Object} e
   * @param {!Object} data
   * @return {?}
   */
  function diff(e, data) {
    /** @type {!Array} */
    var result = [];
    /** @type {number} */
    var label = -1;
    /** @type {boolean} */
    var key = "sequence" == e.S.cb;
    var error = log("Roll.Common.ChapterDefaultTitle");
    [data.intro].concat(each(data.chapters)).forEach(function(object, str) {
      var rightBtnLabel = object == data.intro ? log("Roll.Common.NavigationPanel.IntroTitle") : object.u.title || error;
      var max = object.progress;
      /** @type {boolean} */
      var currentPanelType = object == equal(data);
      if (currentPanelType) {
        /** @type {number} */
        label = 1 > max ? str : str + 1;
      }
      /** @type {boolean} */
      str = key && -1 != label && str > label && 0 == max;
      var test = isArray(authenticate)({
        label : rightBtnLabel,
        progress : object.progress,
        selected : currentPanelType,
        Pe : object == data.intro,
        disabled : str,
        F : function() {
          return e.l.dispatch(object);
        },
        Xe : function() {
          var config = new Config(log("Roll.Player.LockedNavigationItemTooltipText"), test);
          var hash = e.h;
          var data = {
            Wa : config,
            Ua : test,
            xa : false,
            ed : true
          };
          config = data.Wa;
          var currentAnime = data.Ua;
          var x = data.offsetX;
          var y = data.offsetY;
          /** @type {boolean} */
          var xa = void 0 === data.xa ? true : data.xa;
          data = (void 0 === data.ed ? 0 : data.ed) ? new api({
            Wa : config,
            Ua : currentAnime,
            offsetX : x,
            offsetY : y,
            xa : xa
          }) : new pkg({
            Wa : config,
            Ua : currentAnime,
            offsetX : x,
            offsetY : y,
            xa : xa
          });
          renderPalette(hash, {
            pa : config,
            ie : data
          });
        }
      }, keyDown(e, object.key));
      result.push(test);
    });
    return result;
  }
  /**
   * @param {!Object} event
   * @return {?}
   */
  function mouseDown(event) {
    var newClass = removeClass([set(event, "article-title")]);
    return isArray(limit)({
      Td : event.ha,
      Ud : newClass
    }, keyDown(event, "article-title"));
  }
  /**
   * @param {?} index
   * @param {undefined} key
   * @param {!Object} data
   * @return {undefined}
   */
  function close(index, key, data) {
    if (!timeCounters.has(key)) {
      timeCounters.set(key, {});
      parsePath(key, function() {
        timeCounters.delete(key);
      });
    }
    var players = $(timeCounters.get(key));
    $(!players[index]);
    /** @type {!Object} */
    players[index] = data;
  }
  /**
   * @param {!Object} name
   * @param {(Object|string)} callback
   * @return {?}
   */
  function getStyle(name, callback) {
    return function() {
      var method = "function" == typeof callback ? callback() : callback;
      return log(name, method);
    };
  }
  /**
   * @param {!Object} o
   * @return {?}
   */
  function watch(o) {
    var v = o.id;
    var i = o.cf;
    var constr = o.Ph;
    var box = (o = o.parameters) ? getStyle(v, o) : function() {
      return log(v);
    };
    /** @type {null} */
    o = null;
    if (constr) {
      var m = constr.bind(i);
      /**
       * @return {?}
       */
      o = function() {
        return m(box());
      };
      o();
    } else {
      /**
       * @return {undefined}
       */
      o = function() {
        push(i);
      };
    }
    close(v.toString(), i, o);
    return {
      ye : box,
      Fg : o
    };
  }
  /**
   * @param {!Object} state
   * @return {undefined}
   */
  function Image(state) {
    var scalar = state.u;
    var base = state.de;
    var cc = state.ee;
    var lens = state.Ia;
    var callback = state.xg;
    var tmp = state.marginTop;
    state = state.paddingTop;
    context.call(this, {
      X : "chapter-header",
      relatedData : scalar
    });
    this.b = scalar;
    this.f = callback;
    this.h = tmp;
    /** @type {!Object} */
    this.C = state;
    this.g = watch({
      id : {
        value : "Roll.Common.CurrentChapterFromTotal"
      },
      cf : this,
      parameters : {
        current : base + 1,
        total : cc
      }
    }).ye;
    this.l = lens;
  }
  /**
   * @param {number} b
   * @return {undefined}
   */
  function Request(b) {
    context.call(this, {
      X : "intro-header",
      relatedData : b
    });
    /** @type {number} */
    this.b = b;
    f(this, "with-cover", !!this.b.jb);
    f(this, "without-kicker", "" == this.b.aa);
  }
  /**
   * @param {string} value
   * @return {?}
   */
  function updateSize(value) {
    var rect = value.b.jb;
    if (!rect) {
      return null;
    }
    var b = {
      "background-image" : "url(" + rect.src + ")"
    };
    var result = round(rect.color);
    rect = {
      "background-color" : "rgba(" + result[0] + ", " + result[1] + ", " + result[2] + ", " + rect.opacity + ")"
    };
    result = removeClass(["intro-cover", set(value, "cover")]);
    var newClass = removeClass(["intro-cover-overlay", set(value, "cover-overlay")]);
    return merge(value, "cover")(a, b, result, rect, newClass);
  }
  /**
   * @param {?} p
   * @return {undefined}
   */
  function def(p) {
    var level = p.Ob;
    val.call(this, {
      Ya : p.Ya,
      X : p.X
    });
    /** @type {boolean} */
    this.h = false;
    this.l = level;
  }
  /**
   * @param {!Function} h
   * @return {undefined}
   */
  function message(h) {
    var hData = h.Eb;
    h = h.Gb;
    val.call(this, {
      X : "top-panel"
    });
    var _cmy = this;
    this.hb = hData;
    this.ha = append(this);
    this.S = new def({
      Ya : set(this, "show-navigation")
    });
    /** @type {number} */
    this.l = 0;
    /** @type {boolean} */
    this.C = false;
    max(this, unshift(this), function() {
      /** @type {boolean} */
      _cmy.C = false;
    });
    /** @type {!Function} */
    this.h = h;
    this.Ba = set(this, "watermark");
    this.Ca = set(this, "watermark-logo");
  }
  /**
   * @param {!Object} options
   * @return {?}
   */
  function load(options) {
    if (options.hb) {
      return null;
    }
    var node = isArray(level)({});
    var obj = set(options, "close").className();
    if (isInputEventSupported()) {
      var d = node.getAttribute("class") || "";
      d = d.split(" ").includes(obj) ? d : node.className + " " + obj;
      node.setAttribute("class", d);
    } else {
      node.classList.add(obj);
    }
    on(node, "click", function() {
      return options.ha.dispatch();
    });
    return node;
  }
  /**
   * @param {!Object} a
   * @return {undefined}
   */
  function start(a) {
    var value = a.state;
    var tmp = a.R;
    var aImagery = a.Eb;
    var aRawData = a.Ib;
    var aSortValue = a.Gb;
    a = a.Zb;
    context.call(this, {
      X : "roll-player"
    });
    var self = this;
    this.Pa = new circle;
    /** @type {!Window} */
    this.h = window;
    try {
      if (window.top.location.href) {
        /** @type {!Window} */
        this.h = window.top;
      }
    } catch (h) {
    }
    this.b = value;
    this.S = tmp;
    this.f = new message({
      Eb : aImagery,
      Gb : aSortValue,
      Zb : a
    });
    this.g = new copy(value, tmp, this.Pa);
    random(this.g, set(this, "navigation"));
    /** @type {string} */
    this.Kc = "";
    /** @type {null} */
    this.C = this.Aa = null;
    this.Ca = x({
      getState : function() {
        return self.b;
      },
      Ib : aRawData,
      Fb : function(val, type) {
        var el = {
          top : 71,
          bottom : self.h.innerHeight - 15
        };
        reset(val, jQuery(self), type, el);
      }
    });
    set(this, "watermark").className();
    this.Jc = set(this, "content").className();
    this.Ed = set(this, "next").className();
    this.Md = set(this, "main").className();
    this.b = value;
    remove(this);
    this.Pc = append(this, this.f.S.F());
    this.Hc = append(this);
    max(this, this.g.l, function(key) {
      var ok = equal(self.b);
      if (key == ok) {
        self.Mb(false);
      } else {
        /** @type {boolean} */
        self.g.f = true;
        self.Mb(true);
      }
      self.Hc.dispatch(key);
    });
    this.Nc = append(this);
    this.Oc = append(this);
    this.hb = append(this, this.f.ha);
    this.Mc = this.b.fa;
    /** @type {boolean} */
    this.Ba = true;
    /** @type {number} */
    this.l = 0;
    this.Ic = new Model;
    this.ha = new Model;
    new Model;
    if (now()) {
      end(this, this.h, "resize", function() {
        createDivInRect(self);
      });
      end(this, this.h, "scroll", function() {
        createDivInRect(self);
      });
      end(this, this.h, "orientationchange", function() {
        setTimeout(function() {
          return self.h.scrollTo(0, 0);
        }, 500);
      });
    }
    end(this, this.h, "orientationchange", function() {
      self.Pa.Fa();
    });
    this.ce = new ResizeObserver(function() {
      f(self.f, "without-transition", true);
      self.f.move(null, 0);
      /** @type {number} */
      self.l = 0;
      remove(self);
    });
  }
  /**
   * @param {!Object} el
   * @param {string} b
   * @return {undefined}
   */
  function onProgress(el, b) {
    /** @type {string} */
    el.b = b;
    b = el.b.fa;
    /** @type {boolean} */
    var result = el.Mc != b;
    remove(el);
    if (result) {
      /** @type {string} */
      el.Mc = b;
      /** @type {number} */
      jQuery(el).scrollTop = 0;
    }
    dispatch(el);
    if (result) {
      L(el);
      setValue(el);
    }
  }
  /**
   * @param {!Object} e
   * @return {undefined}
   */
  function L(e) {
    var p = jQuery(e);
    f(e, "hide-content-scroll", p.scrollHeight == p.clientHeight);
    /** @type {!Array} */
    p = [];
    if (e.Aa) {
      var rec = e.Aa.D();
      p.push(g(rec));
    }
    if (e.C) {
      rec = e.C.D();
      p.push(g(rec));
    }
    rec = e.Ca.D();
    p.push(g(rec));
    p = exec(p);
    e.Ic.dispatch();
    tail(constructor({
      fb : p,
      Hb : wrapper,
      duration : 600,
      Xa : e.Ic
    }), function() {
      f(e, "hide-content-scroll", false);
    });
  }
  /**
   * @param {!Object} options
   * @return {undefined}
   */
  function setValue(options) {
    if (options.ha) {
      options.ha.dispatch();
    }
    var video = $(map(options.f, "progress"));
    var sources = pushPage(video);
    options.ha.dispatch();
    tail(constructor({
      fb : sources,
      Hb : _domain_list,
      duration : 850,
      Xa : options.ha,
      Yd : function() {
        /** @type {string} */
        video.style.transformOrigin = "0 50%";
      },
      Uc : function() {
        /** @type {string} */
        video.style.transformOrigin = "";
      }
    }), function() {
    });
  }
  /**
   * @param {!Object} item
   * @return {undefined}
   */
  function remove(item) {
    var data = item.f;
    var file = item.b;
    var entry = data.S;
    entry.h = file.Ta;
    f(entry, "pressed", entry.h);
    file = equal(file);
    data.l = file.progress;
    /** @type {boolean} */
    data.C = file.progress < data.l;
    f(data, "floating", spin() || 739 >= window.innerWidth);
    push(data);
    data = item.g;
    data.b = item.b;
    disconnect(data);
    f(item, "with-floating-top-panel", spin() || 739 >= window.innerWidth);
    data = item.b;
    data = -1 == data.fa ? data.intro.u : null;
    item.Aa = data ? new Request(data) : null;
    data = -1 == item.b.fa ? null : equal(item.b).u;
    item.Wd(item.C);
    /** @type {null} */
    item.C = null;
    if (data) {
      item.C = new Image({
        u : data,
        de : item.b.fa,
        ee : item.b.chapters.length,
        Ia : item.S.Ia
      });
    }
    data = equal(item.b).j;
    file = item.Ca;
    /** @type {boolean} */
    entry = -1 < item.b.fa;
    if (entry !== file.l) {
      /** @type {boolean} */
      file.l = entry;
      push(file);
    }
    file = item.Ca;
    if (file.g != data) {
      file.g = data;
      push(file);
    }
    data = item.S.Ha ? stringify(item.b) : null;
    item.Kc = data ? log("Roll.Player.GotoNextChapterLink", {
      title : data.u.title || log("Roll.Common.ChapterDefaultTitle")
    }) : "";
    push(item);
  }
  /**
   * @param {!Object} c
   * @return {?}
   */
  function jQuery(c) {
    return $(c.D().querySelector("." + c.Jc));
  }
  /**
   * @param {!Object} a
   * @return {undefined}
   */
  function dispatch(a) {
    var c = jQuery(a);
    var m = map(a, "next");
    c = clip(Math.ceil(c.scrollTop + c.clientHeight) / (c.scrollHeight - (m ? m.clientHeight : 0)), 0, 1);
    a.Oc.dispatch(c);
  }
  /**
   * @param {!Object} self
   * @return {undefined}
   */
  function createDivInRect(self) {
    var h = self.h.innerHeight;
    /** @type {string} */
    self.D().style.height = h + "px";
    /** @type {string} */
    self.g.D().style.height = h + "px";
  }
  /**
   * @param {!Object} options
   * @return {?}
   */
  function pass(options) {
    var response = options.S.Ha && stringify(options.b) ? isArray(_init)({
      style : "primary",
      ya : "textAndIcon",
      text : log("Roll.Player.Continue"),
      icon : resolve(width),
      F : function() {
        return options.Nc.dispatch();
      },
      size : "xLarge"
    }) : isArray(_init)({
      style : "primary",
      ya : "text",
      text : log("Roll.Player.Complete"),
      F : function() {
        return options.hb.dispatch();
      },
      size : "xLarge"
    });
    response.classList.add(options.Ed);
    return response;
  }
  /**
   * @param {string} data
   * @return {undefined}
   */
  function list(data) {
    var m = data.Ye;
    var config = void 0 === data.Eb ? false : data.Eb;
    var b = data.ef;
    var currentAnime = data.Gb;
    data = data.Zb;
    container.call(this);
    var node = this;
    this.b = onMessage(m, b);
    m = new Client(m.R);
    this.onSelectedSectionChange = append(this);
    this.onSectionProgressChange = append(this);
    this.f = memDir(this, new start({
      state : this.b,
      Ib : function(key) {
        return read(node, key);
      },
      Eb : config,
      R : m,
      Gb : currentAnime,
      Zb : data
    }));
    this.onClosePlayerRequest = append(this, this.f.hb);
    max(this, this.f.Pc, function() {
      return fromNode(node);
    });
    max(this, this.f.Hc, function(maxItem) {
      return range(node, maxItem);
    });
    max(this, this.f.Nc, function() {
      return restart(node);
    });
    max(this, this.f.Oc, function(json) {
      poll(node, json);
      node.onSectionProgressChange.dispatch();
    });
    config = handler(function() {
      return resolvePath(node);
    });
    /** @type {number} */
    this.m = 0;
    end(this, window, "resize", config.call);
    resolvePath(this);
  }
  /**
   * @param {!Object} node
   * @return {undefined}
   */
  function resolvePath(node) {
    /** @type {(boolean|undefined)} */
    var p1 = void 0 === p1 ? true : p1;
    /** @type {number} */
    var inner = window.innerWidth;
    if (!(1180 < node.m && 1180 < window.innerWidth)) {
      /** @type {number} */
      node.m = inner;
      /** @type {boolean} */
      var colId = 1180 < inner;
      read(node, function(t) {
        if (colId == t.Ta) {
          if (colId) {
            t = node.f;
            push(t);
            disconnect(t.g);
          }
        } else {
          if (!p1) {
            /** @type {boolean} */
            node.f.g.f = true;
          }
          if (t.Ta != colId) {
            /** @type {boolean} */
            t.Ta = colId;
          }
        }
      });
    }
  }
  /**
   * @param {!Object} e
   * @param {!Function} fn
   * @return {undefined}
   */
  function read(e, fn) {
    var p = normalizeProvider(e.b, function(responce) {
      fn(responce);
    });
    if (e.b != p) {
      e.b = p;
      onProgress(e.f, p);
      e.onSelectedSectionChange.dispatch();
    }
  }
  /**
   * @param {!Object} a
   * @param {!Object} b
   * @return {undefined}
   */
  function range(a, b) {
    read(a, function(data) {
      data.fa = b.key == data.intro.key ? -1 : data.chapters.findIndex(function(a) {
        return a.key == b.key;
      });
    });
  }
  /**
   * @param {!Object} n
   * @return {undefined}
   */
  function restart(n) {
    read(n, function(data) {
      var b = stringify(data);
      if (b) {
        data.fa = b.key == data.intro.key ? -1 : data.chapters.findIndex(function(a) {
          return a.key == b.key;
        });
      }
    });
  }
  /**
   * @param {!Object} id
   * @param {number} val
   * @return {undefined}
   */
  function poll(id, val) {
    read(id, function(d) {
      d = equal(d);
      if (!(isNaN(val) || d.progress >= val)) {
        /** @type {number} */
        d.progress = val;
      }
    });
  }
  /**
   * @param {!Object} node
   * @return {undefined}
   */
  function fromNode(node) {
    read(node, function(b) {
      /** @type {boolean} */
      b.Ta = !b.Ta;
    });
  }
  /**
   * @param {string} name
   * @return {?}
   */
  function showPopup(name) {
    /** @type {!Element} */
    var node = document.createElement("div");
    node.setAttribute("data-root", "");
    if ("popup" == name) {
      node.classList.add("xced93__popup-layer");
    }
    return ops[name] = node;
  }
  /**
   * @param {!Object} data
   * @param {string} url
   * @return {?}
   */
  function sendMessage(data, url) {
    return new Promise(function(callback, errback) {
      if ("undefined" === typeof data.arrayBuffer) {
        /** @type {!FileReader} */
        var reader = new FileReader;
        on(reader, "loadend", function() {
          if (!reader.result) {
            errback();
          }
          callback(loadData(reader.result, url));
        });
        on(reader, "error", errback);
        reader.readAsArrayBuffer(data);
      } else {
        data.arrayBuffer().then(function(data) {
          return callback(loadData(data, url));
        }).catch(errback);
      }
    });
  }
  /**
   * @param {!Array} data
   * @param {!Object} type
   * @return {?}
   */
  function loadData(data, type) {
    /** @type {number} */
    var def = (new Uint32Array(data.slice(0, Uint32Array.BYTES_PER_ELEMENT)))[0];
    /** @type {!Uint8Array} */
    data = new Uint8Array(data.slice(Uint32Array.BYTES_PER_ELEMENT));
    type = type(def).Type;
    data = type.decode(data);
    type = type.toObject(data);
    return {
      version : def,
      data : type
    };
  }
  /**
   * @param {string} options
   * @return {undefined}
   */
  function Variable(options) {
    /** @type {string} */
    this.keys = options;
  }
  /**
   * @param {!Object} a
   * @return {?}
   */
  function getIndex(a) {
    return new Variable(a);
  }
  /**
   * @param {!Array} node
   * @param {(Object|string)} value
   * @param {!Array} deps
   * @return {?}
   */
  function filter(node, value, deps) {
    if (Array.isArray(node)) {
      return node.map(function(f) {
        return filter(f, value, deps);
      });
    }
    var actual = value instanceof Variable ? value.keys : null;
    if (actual) {
      return exit(node, function(f) {
        return filter(f, $(actual), deps);
      });
    }
    if (isEmpty(node)) {
      var messages = {};
      createElement(node, function(key, c) {
        a: {
          var name = forEach(Object.keys(value));
          var p = name.next();
          for (; !p.done; p = name.next()) {
            p = p.value;
            /** @type {(Object|string)} */
            var x = value;
            var v = x[p];
            if (v) {
              /** @type {({ob: ?, wc: ??}|{ob: ??, wc: ??})} */
              x = Number(v) ? {
                wc : v,
                ob : x
              } : {
                wc : ((v instanceof Variable ? v.keys : null) || v)._,
                ob : v
              };
            } else {
              debug("key not found: " + p + ", path: " + deps.join("."));
              x = {
                wc : p,
                ob : x
              };
            }
            v = x.ob;
            if (x.wc === c && "_" !== p) {
              c = {
                key : p,
                ob : v
              };
              break a;
            }
          }
          debug("cant find value: " + c + ", path: " + deps.join("."));
          c = {
            key : c,
            ob : value
          };
        }
        name = c.key;
        messages[name] = filter(key, c.ob, each(deps).concat([name]));
      });
      return messages;
    }
    return node;
  }
  /**
   * @param {!Object} items
   * @return {?}
   */
  function recursive(items) {
    if ("object" === typeof items && items && items.hasOwnProperty("#")) {
      var value = items["#"];
      /** @type {!Array} */
      items = [];
      var input = !value.length || "#" === value[0] || "number" != typeof value[0] || isNaN(value[0]) ? void 0 : value[0];
      var requestOrUrl = value.length && "#" !== value[0] && Number(value[0]) ? value[0] : 1 < value.length && "#" !== value[1] && Number(value[1]) ? value[1] : void 0;
      var val = value[2];
      var processValue = value[3];
      var next = value[4];
      value = value[5];
      if (!("#" === input || "number" !== typeof input || isNaN(input))) {
        if (input & 1) {
          items.push({
            name : "bold"
          });
        }
        if (input & 2) {
          items.push({
            name : "italic"
          });
        }
        if (input & 4) {
          items.push({
            name : "underline"
          });
        }
        if (input & 8) {
          items.push({
            name : "strikethrough"
          });
        }
      }
      if (!("#" === val || "number" !== typeof val || isNaN(val))) {
        items.push({
          name : "fontSize",
          size : val
        });
      }
      if ("#" !== processValue && Number(processValue)) {
        items.push({
          name : "fontColor",
          color : processValue
        });
      }
      if ("#" !== next && Number(next)) {
        items.push({
          name : "textHighlightColor",
          color : next
        });
      }
      if (!("#" === value || "number" !== typeof value || isNaN(value))) {
        items.push({
          name : "textHighlightOpacity",
          opacity : value
        });
      }
      return replace({
        style : items,
        url : requestOrUrl
      });
    }
  }
  /**
   * @param {string} code
   * @return {?}
   */
  function finish(code) {
    switch(code) {
      case 4:
        return ContinueSentinel;
      case 5:
        return ContinueSentinel;
      case 6:
        return ContinueSentinel;
      case 7:
        return ContinueSentinel;
      default:
        throw Error("cannot decode json document with version " + code);
    }
  }
  /**
   * @param {string} fn
   * @return {?}
   */
  function nextTick(fn) {
    switch(fn) {
      case 4:
        return .num_const;
      case 5:
        return modules;
      case 6:
        return chapters;
      case 7:
        return tickObj;
      default:
        throw Error("cannot decode json document with version " + fn);
    }
  }
  /**
   * @return {undefined}
   */
  function monkeys() {
    var req = {
      Le : 1,
      sf : [{
        Va : 2,
        rb : returnType
      }, {
        Va : 3,
        rb : addContent
      }, {
        Va : 4,
        rb : getStyles
      }, {
        Va : 5,
        rb : compose
      }, {
        Va : 6,
        rb : parseCustomUrl
      }, {
        Va : 7,
        rb : bv_trim
      }]
    };
    var alias = this;
    /** @type {number} */
    var s = req.Le;
    /** @type {!Array} */
    var activityList = req.sf;
    req = void 0 === req.we ? [] : req.we;
    /** @type {number} */
    this.s = s;
    /** @type {!Map} */
    this.b = new Map;
    activityList.forEach(function(self, to) {
      if (s != self.Va - to - 1) {
        throw Error();
      }
      alias.b.set(self.Va, self.rb);
    });
    /** @type {!Map} */
    this.f = new Map;
    req.forEach(function(settingItem) {
      return alias.f.set(settingItem.Va, settingItem.Ag);
    });
  }
  /**
   * @param {!Object} element
   * @param {!Object} value
   * @param {!Object} options
   * @return {?}
   */
  function listener(element, value, options) {
    element = element.f.get(options);
    options = {
      document : value,
      fixed : false
    };
    if (element) {
      options = element(value);
    }
    return {
      document : options.document,
      fixed : options.fixed
    };
  }
  /**
   * @param {?} actual
   * @return {?}
   */
  function returnType(actual) {
    /** @type {boolean} */
    actual.yc.Ra = true;
    return actual;
  }
  /**
   * @param {!Object} args
   * @return {undefined}
   */
  function plugin(args) {
    if (args.J) {
      args.J.forEach(function(b) {
        b.style = {
          name : b.style
        };
      });
    }
    if (args.urls) {
      args.urls.forEach(function(params) {
        params = {
          start : params.start,
          end : params.end,
          style : {
            name : "underline"
          }
        };
        if (args.J) {
          args.J.push(params);
        } else {
          /** @type {!Array} */
          args.J = [params];
        }
      });
    }
  }
  /**
   * @param {!Object} opts
   * @return {undefined}
   */
  function _drawBarcodeText(opts) {
    plugin(opts);
    /** @type {string} */
    opts.textAlign = "pullquote" === opts.N ? "center" : "left";
  }
  /**
   * @param {!Array} substitutions
   * @return {undefined}
   */
  function format(substitutions) {
    /** @type {null} */
    var dir = null;
    substitutions.forEach(function(self) {
      if ("p" === self.type) {
        _drawBarcodeText(self);
        if (!("ol" !== self.N || dir && "ol" === dir)) {
          /** @type {boolean} */
          self.bb = true;
        }
        dir = self.N;
      } else {
        if ("i" === self.type) {
          plugin(self);
        } else {
          if ("e" === self.type) {
            plugin(self);
          } else {
            if ("E" === self.type && self.Z) {
              self.Z.forEach(function(item) {
                plugin(item.description);
                if (("mc" === item.type || "mr" === item.type) && item.content.ka) {
                  item.content.ka.forEach(function(config) {
                    return plugin(config.text);
                  });
                }
                plugin(item.V.o);
                plugin(item.V.L);
                plugin(item.V.Ga);
              });
            }
          }
        }
        /** @type {null} */
        dir = null;
      }
    });
  }
  /**
   * @param {!Object} config
   * @return {?}
   */
  function addContent(config) {
    format(config.content.intro.A);
    if (config.content.chapters) {
      config.content.chapters.forEach(function(r) {
        format(r.A);
      });
    }
    return config;
  }
  /**
   * @param {!Object} state
   * @return {?}
   */
  function success(state) {
    var path = state.text;
    var deviceState = state.J;
    state = state.urls;
    var action = replace();
    var results = each(path).concat().map(function(textToFormat) {
      return {
        text : textToFormat,
        sa : action
      };
    });
    if (deviceState) {
      deviceState.forEach(function(e) {
        var i = e.start;
        for (; i < e.end; ++i) {
          if (i >= results.length) {
            fail("incorrect style ranges");
            break;
          }
          var v = results[i];
          var template = results[i].sa;
          template = replace(Object.assign({}, template, {
            style : trigger(template.style, e.style)
          }));
          v.sa = template;
        }
      });
    }
    if (state) {
      state.forEach(function(f) {
        var i = f.start;
        for (; i < f.end; ++i) {
          if (i >= results.length) {
            fail("incorrect url ranges");
            break;
          }
          results[i].sa = replace(Object.assign({}, results[i].sa, {
            url : f.url
          }));
        }
      });
    }
    return results;
  }
  /**
   * @param {?} replacements
   * @param {!Function} key
   * @return {?}
   */
  function convert(replacements, key) {
    return replacements.map(function(item) {
      var codePoint = item.type;
      var options = item.M;
      var result = key ? uuid() : item.key;
      var name = cb(item.description);
      if (options) {
        /** @type {!Object} */
        var params = Object.assign({}, {
          type : options.type,
          width : options.width,
          height : options.height,
          src : options.src
        }, {
          U : options.U || void 0,
          title : options.title || void 0,
          Yb : options.Yb || void 0
        });
        options = params.U;
        var content = params.title;
        var actual_count = params.Yb;
        params = {
          type : params.type,
          width : params.width,
          height : params.height,
          src : params.src
        };
        if (options) {
          /** @type {!Object} */
          params = Object.assign({}, params, {
            U : options
          });
        }
        if (content) {
          /** @type {!Object} */
          params = Object.assign({}, params, {
            title : content
          });
        }
        if (actual_count) {
          /** @type {!Object} */
          params = Object.assign({}, params, {
            Yb : actual_count
          });
        }
        /** @type {!Object} */
        options = params;
      } else {
        options = void 0;
      }
      a: {
        switch(content = item.content, codePoint) {
          case "mc":
          case "mr":
            content = insert(content, key);
            break a;
          case "ti":
            content = put(content, key);
            break a;
          default:
            throw isNaN(codePoint), Error("Unknown question type: " + codePoint);
        }
      }
      item = item.V;
      item = getTypes({
        type : item.type,
        L : cb(item.L),
        Ga : cb(item.Ga),
        o : cb(item.o)
      });
      return generate({
        key : result,
        type : codePoint,
        description : name,
        M : options,
        content : content,
        V : item
      });
    });
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function cb(data) {
    return success({
      text : data.text,
      J : data.J,
      urls : data.urls
    });
  }
  /**
   * @param {string} node
   * @param {!Function} key
   * @return {?}
   */
  function insert(node, key) {
    node = (node = node.ka) ? node.map(function(child) {
      return createButton({
        key : key ? uuid() : child.key,
        text : cb(child.text),
        L : child.L
      });
    }) : [];
    return insertBefore({
      ka : node
    });
  }
  /**
   * @param {?} c
   * @param {!Function} key
   * @return {?}
   */
  function put(c, key) {
    var parsers = c.ia;
    parsers = parsers ? parsers.map(function(child) {
      return reply({
        key : key ? uuid() : child.key,
        text : child.text
      });
    }) : [];
    return store({
      Da : c.Da,
      ia : parsers
    });
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function initialize(data) {
    switch(data.type) {
      case "p":
        return get({
          key : data.key,
          N : data.N,
          textAlign : data.textAlign,
          B : success({
            text : data.text,
            J : data.J,
            urls : data.urls
          }),
          bb : data.bb
        });
      case "i":
        var options = {
          key : data.key,
          B : success({
            text : data.text || "",
            J : data.J,
            urls : data.urls
          }),
          src : data.src,
          width : data.width,
          height : data.height,
          w : data.w
        };
        /** @type {({B: ?, height: ?, key: ?, src: ?, w: ?, width: ?}|{})} */
        options = void 0 === options ? {} : options;
        return Object.assign({}, getDefaults(options), {
          type : "i",
          src : options.src || "",
          va : options.va || false,
          U : options.U || void 0
        });
      case "e":
        return options = {
          key : data.key,
          B : success({
            text : data.text || "",
            J : data.J || [],
            urls : data.urls
          }),
          src : data.src,
          width : data.width,
          height : data.height,
          w : data.w
        }, options = void 0 === options ? {} : options, Object.assign({}, getDefaults(options), {
          type : "e",
          width : options.width || 0,
          height : options.height || 0,
          src : options.src || "",
          w : options.w || "inner"
        });
      case "E":
        /** @type {(boolean|undefined)} */
        var key = void 0 === key ? false : key;
        options = (options = data.Z) ? convert(options, key) : [];
        key = key ? uuid() : data.key;
        data = data.R;
        data = arrToConnection({
          ab : data.ab,
          H : data.H,
          Ma : data.Ma
        });
        options = {
          key : key,
          R : data,
          Z : options
        };
        /** @type {({R: ?, Z: ?, key: ?}|{})} */
        key = options = void 0 === options ? {} : options;
        options = void 0 === key.key ? uuid() : key.key;
        data = void 0 === key.R ? arrToConnection() : key.R;
        key = void 0 === key.Z ? [generate({
          type : "mc"
        })] : key.Z;
        return {
          key : options,
          type : "E",
          R : data,
          Z : key
        };
      case "a":
        options = {
          key : data.key,
          fileName : data.fileName,
          fileSize : data.fileSize,
          lb : data.lb
        };
        data = options.fileName;
        key = options.lb;
        var fileSize = options.fileSize;
        var fileList = options.src;
        var readOnlyFn = options.uc;
        return {
          key : options.key || uuid(),
          type : "a",
          fileName : data || "",
          lb : key || "pdf",
          fileSize : fileSize || 0,
          src : fileList,
          uc : readOnlyFn || false
        };
    }
    debug("unknown block type " + data.type);
    return null;
  }
  /**
   * @param {!Object} name
   * @return {?}
   */
  function loadHook(name) {
    /** @type {!Array} */
    var transactionIDList = [];
    name.forEach(function(data) {
      if (data = initialize(data)) {
        transactionIDList.push(data);
      }
    });
    return transactionIDList;
  }
  /**
   * @param {(Object|string)} value
   * @return {?}
   */
  function find(value) {
    var b = loadHook(value);
    value = {
      order : [],
      A : {}
    };
    b = forEach(b);
    var child = b.next();
    for (; !child.done; child = b.next()) {
      child = child.value;
      value.order.push(child.key);
      value.A[child.key] = child;
    }
    return {
      T : value
    };
  }
  /**
   * @param {!Object} o
   * @return {?}
   */
  function alert(o) {
    var meridiem = o.aa;
    var actionTitle = o.title;
    /** @type {null} */
    var data = null;
    if (o.kc && o.hc && void 0 != o.lc && void 0 != o.ic && void 0 != o.jc) {
      data = {
        src : o.kc,
        width : o.lc,
        height : o.ic,
        color : o.hc,
        opacity : o.jc,
        va : false
      };
    }
    return {
      u : {
        aa : meridiem,
        title : actionTitle,
        qa : data
      },
      j : find(o.A)
    };
  }
  /**
   * @param {!Object} data
   * @return {?}
   */
  function _render(data) {
    var intro = alert(data.intro);
    data = (data.chapters || []).map(function(record) {
      return {
        key : record.key,
        u : {
          title : record.title
        },
        j : find(record.A)
      };
    });
    return {
      intro : intro,
      chapters : _update(data, function(shortcutKeyObject) {
        return shortcutKeyObject.key;
      })
    };
  }
  /**
   * @param {!Object} o
   * @return {?}
   */
  function getStyles(o) {
    var string = _render(o.content);
    var options = o.yc;
    return {
      content : string,
      R : {
        Cb : {
          cb : options.cb,
          Ia : options.Ia,
          Ha : options.Ha,
          Ra : options.Ra
        },
        kb : {
          Tc : o.$c.ACCENT_COLOR,
          qc : options.fonts.playerMain,
          ec : options.fonts.content
        }
      }
    };
  }
  /**
   * @param {!Array} options
   * @return {undefined}
   */
  function verify(options) {
    options = options.A;
    var api = forEach(Object.keys(options));
    var i = api.next();
    for (; !i.done; i = api.next()) {
      i = i.value;
      var item = options[i];
      if ("i" === item.type) {
        item = options[i];
        item = {
          w : item.w,
          key : item.key,
          type : "c",
          images : [{
            key : uuid(),
            type : item.type,
            Xb : item.key,
            width : item.width,
            height : item.height,
            src : item.src,
            B : item.B,
            va : item.va,
            w : "inner"
          }]
        };
        options[i] = item;
      } else {
        if ("e" === item.type) {
          item = options[i];
          options[i] = {
            key : item.key,
            type : item.type,
            width : item.width,
            height : item.height,
            src : item.src,
            w : item.w,
            B : item.B
          };
        } else {
          options[i] = item;
        }
      }
    }
  }
  /**
   * @param {?} linkProvider
   * @return {?}
   */
  function make(linkProvider) {
    return normalizeProvider(linkProvider, function(node) {
      verify(node.intro.j.T);
      node = forEach(Object.values(node.chapters.A));
      var obj = node.next();
      for (; !obj.done; obj = node.next()) {
        if (obj = obj.value) {
          verify(obj.j.T);
        }
      }
    });
  }
  /**
   * @param {!Object} node
   * @return {?}
   */
  function compose(node) {
    var R = node.R;
    return {
      content : make(node.content),
      R : R
    };
  }
  /**
   * @param {?} url
   * @return {?}
   */
  function parseCustomUrl(url) {
    return url;
  }
  /**
   * @param {?} s
   * @return {?}
   */
  function bv_trim(s) {
    return s;
  }
  /**
   * @param {!Object} target
   * @param {number} item
   * @return {?}
   */
  function emit(target, item) {
    var data = new monkeys;
    if (7 == item) {
      data = listener(data, target, 7);
      data = {
        Ec : data.document,
        Me : data.fixed
      };
    } else {
      if (item < data.s || 7 > data.s + data.b.size) {
        throw Error();
      }
      var result = listener(data, target, item);
      target = result.document;
      var fixed = result.fixed;
      item = item + 1;
      for (; 7 >= item; ++item) {
        target = $(data.b.get(item))(target);
        result = listener(data, target, item);
        target = result.document;
        fixed = fixed || result.fixed;
      }
      data = {
        Ec : target,
        Me : fixed
      };
    }
    return data;
  }
  /**
   * @param {!Object} result
   * @return {?}
   */
  function upload(result) {
    var s;
    var o;
    var a;
    var d;
    return promise(function(f) {
      if (1 == f.b) {
        return ok(f, sendMessage(result, function(canCreateDiscussions) {
          switch(canCreateDiscussions) {
            case 1:
              return rightNode;
            case 2:
              return rightNode;
            case 3:
              return tempUser1;
            default:
              throw Error("invalid blob document format");
          }
        }), 2);
      }
      o = s = f.s;
      a = o.version;
      d = o.data;
      return f.return(emit(d, a).Ec);
    });
  }
  /**
   * @param {!Object} options
   * @return {?}
   */
  function deleteUser(options) {
    var value;
    return promise(function(result) {
      if (options instanceof Blob) {
        value = upload(options);
      } else {
        if ("content" in options && "settings" in options && "version" in options) {
          var d = options.version;
          var s = options.settings;
          var result = options.content;
          var r = nextTick(d);
          result = filter(result, r, []);
          result = all(result, recursive);
          r = finish(d);
          s = filter(s, r, []);
          d = emit({
            content : result,
            R : s
          }, d).Ec;
        } else {
          /** @type {null} */
          d = null;
        }
        value = d;
      }
      return result.return(value);
    });
  }
  /**
   * @return {?}
   */
  function xhr() {
    var result;
    return promise(function(response) {
      return 1 == response.b ? ok(response, login("data-1.json"), 2) : 3 != response.b ? (result = response.s) ? response.return(result) : ok(response, fetchBuffer(), 3) : (result = response.s) ? response.return(result) : response.return(null);
    });
  }
  /**
   * @param {string} callback
   * @param {!Function} fn
   * @param {?} name
   * @param {?} func
   * @param {string} req
   * @return {undefined}
   */
  function getUser(callback, fn, name, func, req) {
    Promise.all([login(callback), xhr()]).then(function(results) {
      results = forEach(results);
      var args = results.next().value;
      results = results.next().value;
      if (!results) {
        throw Error();
      }
      deleteUser(results).then(function(c) {
        if (!c) {
          throw Error();
        }
        var data = c.R;
        var to = {
          ta : data.kb.Tc
        };
        write(document.documentElement, {
          "--roll-player-accent-color" : to.ta,
          "--player-main-font" : getFont(data.kb.qc),
          "--content-font" : getFont(data.kb.ec)
        });
        to = (data = {
          Xd : to
        }, data.b);
        to = void 0 === to ? replacements : to;
        to = void 0 === to ? replacements : to;
        write(document.documentElement, {
          "--header-font-family" : to.Ce,
          "--content-font-family" : to.le
        });
        /** @type {!Object} */
        to = data = Object.assign({}, found, data && data.Xd);
        /** @type {!Object} */
        to = void 0 === to ? found : to;
        to = {
          "--color-accent-default" : to.ta,
          "--color-accent-8" : setPoint(to.ta, .08),
          "--color-accent-16" : setPoint(to.ta, .16),
          "--color-accent-30" : setPoint(to.ta, .3),
          "--color-accent-60" : setPoint(to.ta, .6),
          "--color-accent-80" : setPoint(to.ta, .8),
          "--color-accent-dark" : equals(to.ta),
          "--color-link-default" : to.link,
          "--color-link-dark" : equals(to.link),
          "--color-top-panel-background-default" : to.xd,
          "--color-top-panel-text-default" : to.yd
        };
        write(document.documentElement, to);
        data = {
          Y : data.ta,
          qe : data.xd,
          Rb : data.yd
        };
        /** @type {!Object} */
        state = Object.assign({}, data, {
          vf : "#EB5B47",
          wf : "#C4C7CA",
          xf : "#E2422C",
          yf : "#CB3420",
          Nd : data.Y,
          zf : "#C4C7CA",
          Od : traverse(data.Y, -4),
          Pd : traverse(data.Y, -8),
          Af : "#32353729",
          Bf : "#C4C7CA",
          Cf : "#D6D7D7",
          Df : "#EFEFEF",
          Ef : "#BDBEBF",
          Ff : "#3235370F",
          Gf : "#F6F6F7",
          Hf : "#3235370A",
          $d : data.Y,
          Kf : "#C4C7CA",
          ae : traverse(data.Y, -4),
          be : traverse(data.Y, -8),
          Lf : "#EB5B47",
          Mf : "#D6D7D7",
          Nf : "#E6E6E7",
          Of : "#BDBEBF",
          re : text(data.Rb, .12),
          se : text(data.Rb, .08),
          te : text(data.Rb, .72),
          Qf : "#323537",
          Rf : "#517CE9",
          Sf : "#CB3420",
          Tf : "#983484",
          Uf : "#BDBEBF",
          Vf : "#5DC9B5",
          Wf : "#2984E0",
          Xf : "#EE5091",
          Yf : "#745CDE",
          Zf : "#EFEFEF",
          $f : "#DEDEDFB8",
          ag : "#E2422C",
          bg : "#E2422C3D",
          cg : "#2985E0",
          dg : "#2985E03D",
          eg : "#3CB46E",
          fg : "#3CB46E3D",
          gg : "#FFC252",
          hg : "#FFC2527A",
          ig : "#EEA811",
          jg : "#33AFBD",
          kg : "#80B43C",
          lg : "#3CB46E",
          mg : "#FB8B28",
          ng : "#503AB0",
          og : "#80B43C",
          pg : "#983484",
          qg : "#EEA811",
          rg : "#1F4198",
          sg : "#FB8B28",
          tg : "#B8195B",
          ug : "#5C247A",
          vg : "#EE5091",
          wg : "#745CDE",
          yg : "#3235371F",
          zg : "#E6E6E7",
          Bg : "#FFD6D1",
          xe : text(data.Y, .4),
          Cg : "#FFFFFF",
          Ee : data.Y,
          Fe : traverse(data.Y, -4),
          Ge : traverse(data.Y, -8),
          Gg : "#E2422C",
          Hg : "#323537",
          Ig : "#BDBEBF",
          Jg : "#2078CF",
          Kg : "#654BD8",
          Lg : "#FFFFFF",
          Mg : "#FFFFFF66",
          Ng : "#848687",
          Og : "#6C737B",
          Pg : "#389F62",
          Qg : "#FB8B28",
          Wg : "#32353766",
          Xg : "#CB3420",
          Yg : "#323537",
          Zg : "#FFFFFF66",
          $g : "#654BD8",
          ah : "#FFFFFF",
          bh : "#FFFFFF33",
          lf : text(data.Y, .12),
          mf : text(data.Y, .2),
          gh : "#FEE4DF",
          hh : "#FFD6D1",
          ih : "#FFF5F5",
          jh : "#FFFFFF",
          kh : "#3235370A",
          lh : "#3235371F",
          mh : "#D8ECFF",
          nh : "#C7E3FF",
          oh : "#ECF5FF",
          ph : "#EFEFEF",
          qh : "#E6E6E7",
          rh : "#F7F7F7",
          sh : "#E7E2FE",
          th : "#DED6FC",
          uh : "#F3F0FF",
          vh : "#32353729",
          wh : "#32353714",
          xh : "#DFF4E8",
          yh : "#C5ECD5",
          zh : "#EFF9F3",
          Ah : "#FFEED4",
          Bh : "#FFE6C1",
          Ch : "#FFF6E9",
          nf : data.Y,
          pf : traverse(data.Y, -4),
          qf : traverse(data.Y, -8),
          Dh : "#E2422C",
          Eh : "#323537",
          Fh : "#BDBEBF",
          Gh : "#295DE0",
          Hh : "#214FC4",
          Ih : "#FFFFFF",
          Jh : "#FFFFFF66",
          Kh : "#FFFFFFA3",
          Lh : "#ADAEAF",
          Mh : "#6C737B",
          Nh : "#389F63",
          Oh : "#FB8B28"
        });
        write(document.documentElement, {
          "--color-action-primary-default" : state.Nd,
          "--color-action-primary-hovered" : state.Od,
          "--color-action-primary-pressed" : state.Pd,
          "--color-border-accent-default" : state.$d,
          "--color-border-accent-hovered" : state.ae,
          "--color-border-accent-pressed" : state.be,
          "--color-custom-accent" : state.Y,
          "--color-custom-panel-default" : state.qe,
          "--color-custom-panel-hovered" : state.re,
          "--color-custom-panel-subdued" : state.se,
          "--color-custom-text-default" : state.Rb,
          "--color-custom-text-subdued" : state.te,
          "--color-focus-default" : state.xe,
          "--color-icon-accent-default" : state.Ee,
          "--color-icon-accent-hovered" : state.Fe,
          "--color-icon-accent-pressed" : state.Ge,
          "--color-surface-accent-default" : state.lf,
          "--color-surface-accent-hovered" : state.mf,
          "--color-text-accent-default" : state.nf,
          "--color-text-accent-hovered" : state.pf,
          "--color-text-accent-pressed" : state.qf
        });
        if (arrData) {
          data = arrData;
          /** @type {!Object} */
          data.b = Object.assign({}, data.b, args);
        } else {
          data = new expression(args);
          if (arrData) {
            fail("i18n has already been declared");
          } else {
            arrData = data;
          }
        }
        c = new list({
          Ye : c,
          ef : name,
          Gb : func,
          Zb : req
        });
        fn(c);
        document.body.appendChild(c.D());
        c = showPopup("popup");
        document.body.appendChild(c);
        c = showPopup("tooltip");
        document.body.appendChild(c);
      });
    });
  }
  /**
   * @param {string} id
   * @param {?} to
   * @param {?} callback
   * @param {?} base
   * @param {string} type
   * @return {?}
   */
  function prev(id, to, callback, base, type) {
    return new Promise(function(users) {
      getUser(id, users, callback, base, type);
    });
  }
  var item;
  /** @type {!Function} */
  var spawn = "function" == typeof Object.create ? Object.create : function(a) {
    /**
     * @return {undefined}
     */
    function GUI() {
    }
    /** @type {!Object} */
    GUI.prototype = a;
    return new GUI;
  };
  var parent;
  if ("function" == typeof Object.setPrototypeOf) {
    /** @type {function(!Object, ?): !Object} */
    parent = Object.setPrototypeOf;
  } else {
    var value;
    a: {
      var _docProto = {
        Ld : true
      };
      var doc = {};
      try {
        doc.__proto__ = _docProto;
        value = doc.Ld;
        break a;
      } catch (a) {
      }
      /** @type {boolean} */
      value = false;
    }
    /** @type {(function(!Object, !Function): ?|null)} */
    parent = value ? function(object, prototype) {
      /** @type {!Function} */
      object.__proto__ = prototype;
      if (object.__proto__ !== prototype) {
        throw new TypeError(object + " is not extensible");
      }
      return object;
    } : null;
  }
  /** @type {(function(!Object, !Function): ?|null)} */
  var inner = parent;
  var global = addMethods(this);
  /** @type {!Function} */
  var defineProperty = "function" == typeof Object.defineProperties ? Object.defineProperty : function(object, name, descriptor) {
    if (object != Array.prototype && object != Object.prototype) {
      object[name] = descriptor.value;
    }
  };
  getCollection("Promise", function(a) {
    /**
     * @param {?} executor
     * @return {undefined}
     */
    function Promise(executor) {
      /** @type {number} */
      this.f = 0;
      this.G = void 0;
      /** @type {!Array} */
      this.b = [];
      var future = this.s();
      try {
        executor(future.resolve, future.reject);
      } catch (reasons) {
        future.reject(reasons);
      }
    }
    /**
     * @return {undefined}
     */
    function m() {
      /** @type {null} */
      this.b = null;
    }
    /**
     * @param {!Object} onSuccess
     * @return {?}
     */
    function resolve(onSuccess) {
      return onSuccess instanceof Promise ? onSuccess : new Promise(function(exec) {
        exec(onSuccess);
      });
    }
    if (a) {
      return a;
    }
    /**
     * @param {?} value
     * @return {undefined}
     */
    m.prototype.f = function(value) {
      if (null == this.b) {
        /** @type {!Array} */
        this.b = [];
        var bgCol2 = this;
        this.s(function() {
          bgCol2.G();
        });
      }
      this.b.push(value);
    };
    var parseFloat = global.setTimeout;
    /**
     * @param {!Function} substitution
     * @return {undefined}
     */
    m.prototype.s = function(substitution) {
      parseFloat(substitution, 0);
    };
    /**
     * @return {undefined}
     */
    m.prototype.G = function() {
      for (; this.b && this.b.length;) {
        var stats = this.b;
        /** @type {!Array} */
        this.b = [];
        /** @type {number} */
        var i = 0;
        for (; i < stats.length; ++i) {
          var b = stats[i];
          /** @type {null} */
          stats[i] = null;
          try {
            b();
          } catch (px) {
            this.m(px);
          }
        }
      }
      /** @type {null} */
      this.b = null;
    };
    /**
     * @param {?} value
     * @return {undefined}
     */
    m.prototype.m = function(value) {
      this.s(function() {
        throw value;
      });
    };
    /**
     * @return {?}
     */
    Promise.prototype.s = function() {
      /**
       * @param {!Function} connection
       * @return {?}
       */
      function app(connection) {
        return function(methodArgs) {
          if (!k) {
            /** @type {boolean} */
            k = true;
            connection.call(pkg, methodArgs);
          }
        };
      }
      var pkg = this;
      /** @type {boolean} */
      var k = false;
      return {
        resolve : app(this.l),
        reject : app(this.m)
      };
    };
    /**
     * @param {?} value
     * @return {undefined}
     */
    Promise.prototype.l = function(value) {
      if (value === this) {
        this.m(new TypeError("A Promise cannot resolve to itself"));
      } else {
        if (value instanceof Promise) {
          this.C(value);
        } else {
          a: {
            switch(typeof value) {
              case "object":
                /** @type {boolean} */
                var changed = null != value;
                break a;
              case "function":
                /** @type {boolean} */
                changed = true;
                break a;
              default:
                /** @type {boolean} */
                changed = false;
            }
          }
          if (changed) {
            this.h(value);
          } else {
            this.O(value);
          }
        }
      }
    };
    /**
     * @param {?} value
     * @return {undefined}
     */
    Promise.prototype.h = function(value) {
      var then = void 0;
      try {
        then = value.then;
      } catch (px) {
        this.m(px);
        return;
      }
      if ("function" == typeof then) {
        this.ma(then, value);
      } else {
        this.O(value);
      }
    };
    /**
     * @param {?} value
     * @return {undefined}
     */
    Promise.prototype.m = function(value) {
      this.g(2, value);
    };
    /**
     * @param {!Array} b
     * @return {undefined}
     */
    Promise.prototype.O = function(b) {
      this.g(1, b);
    };
    /**
     * @param {!Function} val
     * @param {!Object} name
     * @return {undefined}
     */
    Promise.prototype.g = function(val, name) {
      if (0 != this.f) {
        throw Error("Cannot settle(" + val + ", " + name + "): Promise already settled in state" + this.f);
      }
      /** @type {!Function} */
      this.f = val;
      /** @type {!Object} */
      this.G = name;
      this.P();
    };
    /**
     * @return {undefined}
     */
    Promise.prototype.P = function() {
      if (null != this.b) {
        /** @type {number} */
        var i = 0;
        for (; i < this.b.length; ++i) {
          res.f(this.b[i]);
        }
        /** @type {null} */
        this.b = null;
      }
    };
    var res = new m;
    /**
     * @param {?} value
     * @return {undefined}
     */
    Promise.prototype.C = function(value) {
      var d = this.s();
      value.Qb(d.resolve, d.reject);
    };
    /**
     * @param {!Function} then
     * @param {!Array} value
     * @return {undefined}
     */
    Promise.prototype.ma = function(then, value) {
      var deferred = this.s();
      try {
        then.call(value, deferred.resolve, deferred.reject);
      } catch (exception) {
        deferred.reject(exception);
      }
    };
    /**
     * @param {!Function} type
     * @param {!Function} state
     * @return {?}
     */
    Promise.prototype.then = function(type, state) {
      /**
       * @param {!Function} callback
       * @param {(!Function|boolean)} event
       * @return {?}
       */
      function callback(callback, event) {
        return "function" == typeof callback ? function(reason) {
          try {
            cast(callback(reason));
          } catch (allNewFiles) {
            update(allNewFiles);
          }
        } : event;
      }
      var cast;
      var update;
      var thenPromise = new Promise(function(boolean, res) {
        cast = boolean;
        update = res;
      });
      this.Qb(callback(type, cast), callback(state, update));
      return thenPromise;
    };
    /**
     * @param {!Function} rejected
     * @return {?}
     */
    Promise.prototype.catch = function(rejected) {
      return this.then(void 0, rejected);
    };
    /**
     * @param {?} y
     * @param {?} h
     * @return {undefined}
     */
    Promise.prototype.Qb = function(y, h) {
      /**
       * @return {undefined}
       */
      function n() {
        switch(data.f) {
          case 1:
            y(data.G);
            break;
          case 2:
            h(data.G);
            break;
          default:
            throw Error("Unexpected state: " + data.f);
        }
      }
      var data = this;
      if (null == this.b) {
        res.f(n);
      } else {
        this.b.push(n);
      }
    };
    /** @type {function(!Object): ?} */
    Promise.resolve = resolve;
    /**
     * @param {?} reason
     * @return {?}
     */
    Promise.reject = function(reason) {
      return new Promise(function(canCreateDiscussions, reject$2) {
        reject$2(reason);
      });
    };
    /**
     * @param {!Object} array
     * @return {?}
     */
    Promise.race = function(array) {
      return new Promise(function(howManyToRound, h0_sample) {
        var dataSource = forEach(array);
        var result = dataSource.next();
        for (; !result.done; result = dataSource.next()) {
          resolve(result.value).Qb(howManyToRound, h0_sample);
        }
      });
    };
    /**
     * @param {!Array} opts
     * @return {?}
     */
    Promise.all = function(opts) {
      var api = forEach(opts);
      var e = api.next();
      return e.done ? resolve([]) : new Promise(function(has, h0_sample) {
        /**
         * @param {number} name
         * @return {?}
         */
        function track(name) {
          return function(data) {
            props[name] = data;
            w--;
            if (0 == w) {
              has(props);
            }
          };
        }
        /** @type {!Array} */
        var props = [];
        /** @type {number} */
        var w = 0;
        do {
          props.push(void 0);
          w++;
          resolve(e.value).Qb(track(props.length - 1), h0_sample);
          e = api.next();
        } while (!e.done);
      });
    };
    return Promise;
  });
  /**
   * @return {?}
   */
  Cell.prototype.toString = function() {
    return this.b;
  };
  var fakeSymbol = function() {
    /**
     * @param {!Object} dir
     * @return {?}
     */
    function toDest(dir) {
      if (this instanceof toDest) {
        throw new TypeError("Symbol is not a constructor");
      }
      return new Cell("jscomp_symbol_" + (dir || "") + "_" + widgetUniqueIDIndex++, dir);
    }
    /** @type {number} */
    var widgetUniqueIDIndex = 0;
    return toDest;
  }();
  /**
   * @param {!Object} s
   * @return {undefined}
   */
  RunHandlerTask.prototype.O = function(s) {
    /** @type {!Object} */
    this.s = s;
  };
  /**
   * @param {string} value
   * @return {undefined}
   */
  RunHandlerTask.prototype.return = function(value) {
    this.m = {
      return : value
    };
    this.b = this.g;
  };
  getCollection("globalThis", function(asm) {
    return asm || global;
  });
  getCollection("Array.prototype.keys", function(position) {
    return position ? position : function() {
      return client_api(this, function(b) {
        return b;
      });
    };
  });
  getCollection("Object.is", function(position) {
    return position ? position : function(a, b) {
      return a === b ? 0 !== a || 1 / a === 1 / b : a !== a && b !== b;
    };
  });
  getCollection("Array.prototype.includes", function(position) {
    return position ? position : function(b, index) {
      var result = this;
      if (result instanceof String) {
        /** @type {string} */
        result = String(result);
      }
      var length = result.length;
      index = index || 0;
      if (0 > index) {
        /** @type {number} */
        index = Math.max(index + length, 0);
      }
      for (; index < length; index++) {
        var a = result[index];
        if (a === b || Object.is(a, b)) {
          return true;
        }
      }
      return false;
    };
  });
  getCollection("String.prototype.includes", function(position) {
    return position ? position : function(key, startIndex) {
      return -1 !== filterObject(this, key, "includes").indexOf(key, startIndex || 0);
    };
  });
  getCollection("WeakMap", function(TransitionController) {
    /**
     * @param {!Object} props
     * @return {undefined}
     */
    function b(props) {
      this.b = (buf = buf + (Math.random() + 1)).toString();
      if (props) {
        props = forEach(props);
        var result;
        for (; !(result = props.next()).done;) {
          result = result.value;
          this.set(result[0], result[1]);
        }
      }
    }
    /**
     * @return {undefined}
     */
    function Date() {
    }
    /**
     * @param {string} o
     * @return {?}
     */
    function isObject(o) {
      /** @type {string} */
      var type = typeof o;
      return "object" === type && null !== o || "function" === type;
    }
    /**
     * @param {!Object} name
     * @return {undefined}
     */
    function e(name) {
      if (!isFunction(name, prop)) {
        var initialDateTime = new Date;
        defineProperty(name, prop, {
          value : initialDateTime
        });
      }
    }
    /**
     * @param {string} key
     * @return {undefined}
     */
    function wrapObjectMethod(key) {
      var hOP = Object[key];
      if (hOP) {
        /**
         * @param {!Object} ref
         * @return {?}
         */
        Object[key] = function(ref) {
          if (ref instanceof Date) {
            return ref;
          }
          e(ref);
          return hOP(ref);
        };
      }
    }
    if (function() {
      if (!TransitionController || !Object.seal) {
        return false;
      }
      try {
        var k = Object.seal({});
        var l = Object.seal({});
        var c = new TransitionController([[k, 2], [l, 3]]);
        if (2 != c.get(k) || 3 != c.get(l)) {
          return false;
        }
        c.delete(k);
        c.set(l, 4);
        return !c.has(k) && 4 == c.get(l);
      } catch (q) {
        return false;
      }
    }()) {
      return TransitionController;
    }
    /** @type {string} */
    var prop = "$jscomp_hidden_" + Math.random();
    wrapObjectMethod("freeze");
    wrapObjectMethod("preventExtensions");
    wrapObjectMethod("seal");
    /** @type {number} */
    var buf = 0;
    /**
     * @param {string} n
     * @param {string} data
     * @return {?}
     */
    b.prototype.set = function(n, data) {
      if (!isObject(n)) {
        throw Error("Invalid WeakMap key");
      }
      e(n);
      if (!isFunction(n, prop)) {
        throw Error("WeakMap key fail: " + n);
      }
      /** @type {string} */
      n[prop][this.b] = data;
      return this;
    };
    /**
     * @param {string} obj
     * @return {?}
     */
    b.prototype.get = function(obj) {
      return isObject(obj) && isFunction(obj, prop) ? obj[prop][this.b] : void 0;
    };
    /**
     * @param {string} obj
     * @return {?}
     */
    b.prototype.has = function(obj) {
      return isObject(obj) && isFunction(obj, prop) && isFunction(obj[prop], this.b);
    };
    /**
     * @param {string} obj
     * @return {?}
     */
    b.prototype.delete = function(obj) {
      return isObject(obj) && isFunction(obj, prop) && isFunction(obj[prop], this.b) ? delete obj[prop][this.b] : false;
    };
    return b;
  });
  getCollection("Map", function(Map) {
    /**
     * @return {?}
     */
    function clear() {
      var head = {};
      return head.Ka = head.next = head.head = head;
    }
    /**
     * @param {!Object} map
     * @param {!Function} template
     * @return {?}
     */
    function load(map, template) {
      var entry = map.b;
      return genIterator(function() {
        if (entry) {
          for (; entry.head != map.b;) {
            entry = entry.Ka;
          }
          for (; entry.next != entry.head;) {
            return entry = entry.next, {
              done : false,
              value : template(entry)
            };
          }
          /** @type {null} */
          entry = null;
        }
        return {
          done : true,
          value : void 0
        };
      });
    }
    /**
     * @param {string} x
     * @param {string} key
     * @return {?}
     */
    function get(x, key) {
      var type = key && typeof key;
      if ("object" == type || "function" == type) {
        if (priv.has(key)) {
          type = priv.get(key);
        } else {
          /** @type {string} */
          type = "" + ++nextGuid;
          priv.set(key, type);
        }
      } else {
        /** @type {string} */
        type = "p_" + key;
      }
      var result = x.f[type];
      if (result && isFunction(x.f, type)) {
        /** @type {number} */
        x = 0;
        for (; x < result.length; x++) {
          var entry = result[x];
          if (key !== key && entry.key !== entry.key || key === entry.key) {
            return {
              id : type,
              list : result,
              index : x,
              $ : entry
            };
          }
        }
      }
      return {
        id : type,
        list : result,
        index : -1,
        $ : void 0
      };
    }
    /**
     * @param {!Object} args
     * @return {undefined}
     */
    function Map(args) {
      this.f = {};
      this.b = clear();
      /** @type {number} */
      this.size = 0;
      if (args) {
        args = forEach(args);
        var result;
        for (; !(result = args.next()).done;) {
          result = result.value;
          this.set(result[0], result[1]);
        }
      }
    }
    if (function() {
      if (!Map || "function" != typeof Map || !Map.prototype.entries || "function" != typeof Object.seal) {
        return false;
      }
      try {
        var key = Object.seal({
          x : 4
        });
        var map = new Map(forEach([[key, "s"]]));
        if ("s" != map.get(key) || 1 != map.size || map.get({
          x : 4
        }) || map.set({
          x : 4
        }, "t") != map || 2 != map.size) {
          return false;
        }
        var deletedChar = map.entries();
        var item = deletedChar.next();
        if (item.done || item.value[0] != key || "s" != item.value[1]) {
          return false;
        }
        item = deletedChar.next();
        return item.done || 4 != item.value[0].x || "t" != item.value[1] || !deletedChar.next().done ? false : true;
      } catch (q) {
        return false;
      }
    }()) {
      return Map;
    }
    register();
    /** @type {!WeakMap} */
    var priv = new WeakMap;
    /**
     * @param {string} type
     * @param {string} data
     * @return {?}
     */
    Map.prototype.set = function(type, data) {
      type = 0 === type ? 0 : type;
      var res = get(this, type);
      if (!res.list) {
        /** @type {!Array} */
        res.list = this.f[res.id] = [];
      }
      if (res.$) {
        /** @type {string} */
        res.$.value = data;
      } else {
        res.$ = {
          next : this.b,
          Ka : this.b.Ka,
          head : this.b,
          key : type,
          value : data
        };
        res.list.push(res.$);
        this.b.Ka.next = res.$;
        this.b.Ka = res.$;
        this.size++;
      }
      return this;
    };
    /**
     * @param {!Object} data
     * @return {?}
     */
    Map.prototype.delete = function(data) {
      data = get(this, data);
      return data.$ && data.list ? (data.list.splice(data.index, 1), data.list.length || delete this.f[data.id], data.$.Ka.next = data.$.next, data.$.next.Ka = data.$.Ka, data.$.head = null, this.size--, true) : false;
    };
    /**
     * @return {undefined}
     */
    Map.prototype.clear = function() {
      this.f = {};
      this.b = this.b.Ka = clear();
      /** @type {number} */
      this.size = 0;
    };
    /**
     * @param {string} data
     * @return {?}
     */
    Map.prototype.has = function(data) {
      return !!get(this, data).$;
    };
    /**
     * @param {string} node
     * @return {?}
     */
    Map.prototype.get = function(node) {
      return (node = get(this, node).$) && node.value;
    };
    /**
     * @return {?}
     */
    Map.prototype.entries = function() {
      return load(this, function(dataItemObj) {
        return [dataItemObj.key, dataItemObj.value];
      });
    };
    /**
     * @return {?}
     */
    Map.prototype.keys = function() {
      return load(this, function(shortcutKeyObject) {
        return shortcutKeyObject.key;
      });
    };
    /**
     * @return {?}
     */
    Map.prototype.values = function() {
      return load(this, function(select_ele) {
        return select_ele.value;
      });
    };
    /**
     * @param {!Function} path
     * @param {!Object} post
     * @return {undefined}
     */
    Map.prototype.forEach = function(path, post) {
      var entries = this.entries();
      var list;
      for (; !(list = entries.next()).done;) {
        list = list.value;
        path.call(post, list[1], list[0], this);
      }
    };
    /** @type {function(): ?} */
    Map.prototype[Symbol.iterator] = Map.prototype.entries;
    /** @type {number} */
    var nextGuid = 0;
    return Map;
  });
  getCollection("Set", function(fn) {
    /**
     * @param {!Object} b
     * @return {undefined}
     */
    function Set(b) {
      /** @type {!Map} */
      this.b = new Map;
      if (b) {
        b = forEach(b);
        var item;
        for (; !(item = b.next()).done;) {
          this.add(item.value);
        }
      }
      /** @type {number} */
      this.size = this.b.size;
    }
    if (function() {
      if (!fn || "function" != typeof fn || !fn.prototype.entries || "function" != typeof Object.seal) {
        return false;
      }
      try {
        var value = Object.seal({
          x : 4
        });
        var object = new fn(forEach([value]));
        if (!object.has(value) || 1 != object.size || object.add(value) != object || 1 != object.size || object.add({
          x : 4
        }) != object || 2 != object.size) {
          return false;
        }
        var deletedChar = object.entries();
        var item = deletedChar.next();
        if (item.done || item.value[0] != value || item.value[1] != value) {
          return false;
        }
        item = deletedChar.next();
        return item.done || item.value[0] == value || 4 != item.value[0].x || item.value[1] != item.value[0] ? false : deletedChar.next().done;
      } catch (g) {
        return false;
      }
    }()) {
      return fn;
    }
    register();
    /**
     * @param {string} b
     * @return {?}
     */
    Set.prototype.add = function(b) {
      b = 0 === b ? 0 : b;
      this.b.set(b, b);
      this.size = this.b.size;
      return this;
    };
    /**
     * @param {string} obj
     * @return {?}
     */
    Set.prototype.delete = function(obj) {
      obj = this.b.delete(obj);
      this.size = this.b.size;
      return obj;
    };
    /**
     * @return {undefined}
     */
    Set.prototype.clear = function() {
      this.b.clear();
      /** @type {number} */
      this.size = 0;
    };
    /**
     * @param {string} o
     * @return {?}
     */
    Set.prototype.has = function(o) {
      return this.b.has(o);
    };
    /**
     * @return {?}
     */
    Set.prototype.entries = function() {
      return this.b.entries();
    };
    /**
     * @return {?}
     */
    Set.prototype.values = function() {
      return this.b.values();
    };
    /** @type {function(): ?} */
    Set.prototype.keys = Set.prototype.values;
    /** @type {function(): ?} */
    Set.prototype[Symbol.iterator] = Set.prototype.values;
    /**
     * @param {!Function} slice
     * @param {!Object} array
     * @return {undefined}
     */
    Set.prototype.forEach = function(slice, array) {
      var force_saveable_type = this;
      this.b.forEach(function(i) {
        return slice.call(array, i, i, force_saveable_type);
      });
    };
    return Set;
  });
  getCollection("String.prototype.repeat", function(position) {
    return position ? position : function(flags) {
      var filename = filterObject(this, null, "repeat");
      if (0 > flags || 1342177279 < flags) {
        throw new RangeError("Invalid count value");
      }
      /** @type {number} */
      flags = flags | 0;
      /** @type {string} */
      var src = "";
      for (; flags;) {
        if (flags & 1 && (src = src + filename), flags = flags >>> 1) {
          filename = filename + filename;
        }
      }
      return src;
    };
  });
  getCollection("String.prototype.startsWith", function(position) {
    return position ? position : function(keys, index) {
      var params = filterObject(this, keys, "startsWith");
      var count = params.length;
      var len = keys.length;
      /** @type {number} */
      index = Math.max(0, Math.min(index | 0, params.length));
      /** @type {number} */
      var i = 0;
      for (; i < len && index < count;) {
        if (params[index++] != keys[i++]) {
          return false;
        }
      }
      return i >= len;
    };
  });
  getCollection("Array.prototype.findIndex", function(position) {
    return position ? position : function(fnName, items) {
      return indexOf(this, fnName, items).ld;
    };
  });
  getCollection("Array.prototype.find", function(position) {
    return position ? position : function(fnName, items) {
      return indexOf(this, fnName, items).Cd;
    };
  });
  getCollection("Object.entries", function(position) {
    return position ? position : function(computed) {
      /** @type {!Array} */
      var withscore = [];
      var key;
      for (key in computed) {
        if (isFunction(computed, key)) {
          withscore.push([key, computed[key]]);
        }
      }
      return withscore;
    };
  });
  getCollection("Object.values", function(position) {
    return position ? position : function(a) {
      /** @type {!Array} */
      var result = [];
      var key;
      for (key in a) {
        if (isFunction(a, key)) {
          result.push(a[key]);
        }
      }
      return result;
    };
  });
  getCollection("String.prototype.endsWith", function(position) {
    return position ? position : function(val, v) {
      var value = filterObject(this, val, "endsWith");
      if (void 0 === v) {
        v = value.length;
      }
      /** @type {number} */
      v = Math.max(0, Math.min(v | 0, value.length));
      var paswordLength = val.length;
      for (; 0 < paswordLength && 0 < v;) {
        if (value[--v] != val[--paswordLength]) {
          return false;
        }
      }
      return 0 >= paswordLength;
    };
  });
  getCollection("String.prototype.padStart", function(position) {
    return position ? position : function(width, b) {
      var value = filterObject(this, null, "padStart");
      /** @type {number} */
      width = width - value.length;
      /** @type {string} */
      b = void 0 !== b ? String(b) : " ";
      return (0 < width && b ? b.repeat(Math.ceil(width / b.length)).substring(0, width) : "") + value;
    };
  });
  /** @type {!Function} */
  var formatted = "function" == typeof Object.assign ? Object.assign : function(data, s) {
    /** @type {number} */
    var i = 1;
    for (; i < arguments.length; i++) {
      var arg = arguments[i];
      if (arg) {
        var key;
        for (key in arg) {
          if (isFunction(arg, key)) {
            data[key] = arg[key];
          }
        }
      }
    }
    return data;
  };
  getCollection("Object.assign", function(text) {
    return text || formatted;
  });
  /** @type {function(string, !Array, string): ?} */
  window.__getCssClass = extend;
  /** @type {number} */
  var nextKey = 0;
  expect(Event, report);
  /**
   * @param {string} data
   * @param {!Function} object
   * @param {string} type
   * @param {undefined} path
   * @param {!Object} options
   * @return {?}
   */
  Module.prototype.add = function(data, object, type, path, options) {
    var i = data.toString();
    data = this.b[i];
    if (!data) {
      /** @type {!Array} */
      data = this.b[i] = [];
      this.f++;
    }
    var o = send(data, object, path, options);
    if (-1 < o) {
      object = data[o];
      if (!type) {
        /** @type {boolean} */
        object.dc = false;
      }
    } else {
      object = new Event(object, null, this.src, i, !!path, options);
      /** @type {string} */
      object.dc = type;
      data.push(object);
    }
    return object;
  };
  /**
   * @param {string} name
   * @param {number} c
   * @param {number} a
   * @param {!Object} i
   * @return {?}
   */
  Module.prototype.remove = function(name, c, a, i) {
    name = name.toString();
    if (!(name in this.b)) {
      return false;
    }
    var b = this.b[name];
    c = send(b, c, a, i);
    return -1 < c ? (isNumber(b[c]), b.splice(c, 1), 0 == b.length && (delete this.b[name], this.f--), true) : false;
  };
  /** @type {string} */
  var j = "ispring_lm_" + (1E6 * Math.random() | 0);
  var title;
  /** @type {number} */
  var timerId = 0;
  expect(method, nano);
  /**
   * @return {undefined}
   */
  method.prototype.cd = function() {
    this.bc();
    if (this.sb) {
      var request = forEach(this.sb);
      var out = request.next();
      for (; !out.done; out = request.next()) {
        out = out.value;
        out();
      }
    }
    if (this.G) {
      request = forEach(this.G);
      out = request.next();
      for (; !out.done; out = request.next()) {
        getNode(out.value);
      }
    }
    if (this.O) {
      out = forEach(this.O);
      request = out.next();
      for (; !request.done; request = out.next()) {
        func(request.value);
      }
      /** @type {null} */
      this.O = null;
    }
    if (this.s) {
      out = forEach(Object.keys(this.s));
      request = out.next();
      for (; !request.done; request = out.next()) {
        assertEquals(this, request.value);
      }
    }
  };
  /**
   * @param {?} a
   * @return {undefined}
   */
  method.prototype.Wd = function(a) {
    /** @type {!Array} */
    var values = [];
    /** @type {number} */
    var n = 0;
    for (; n < arguments.length; ++n) {
      values[n] = arguments[n];
    }
    if (this.G) {
      values = forEach(values);
      n = values.next();
      for (; !n.done; n = values.next()) {
        if (n = n.value) {
          visit(this, n);
          var r2 = this.G.indexOf(n);
          if (0 <= r2) {
            this.G.splice(r2, 1);
            getNode(n);
          }
        }
      }
    }
  };
  /**
   * @return {undefined}
   */
  method.prototype.bc = function() {
  };
  /**
   * @param {string} value
   * @param {string} name
   * @return {undefined}
   */
  SelectorSet.prototype.push = function(value, name) {
    if (0 == name) {
      this.f = this.f || [];
    } else {
      if (this.s = this.s || [0], this.b = this.b || {}, !(name in this.b)) {
        /** @type {!Array} */
        this.b[name] = [];
        var s = this.s;
        var f = void 0;
        /** @type {(function(string, string): ?|undefined)} */
        f = void 0 === f ? linkMarkers : f;
        /** @type {number} */
        var e = 0;
        var t = s.length;
        for (; e < t;) {
          /** @type {number} */
          var i = e + t >> 1;
          var structure = f(name, s[i]);
          if (0 < structure) {
            /** @type {number} */
            e = i + 1;
          } else {
            /** @type {number} */
            t = i;
            /** @type {boolean} */
            var right = !structure;
          }
        }
        /** @type {number} */
        right = right ? e : ~e;
        if (0 > right) {
          s.splice(-(right + 1), 0, name);
        }
      }
    }
    if (name = sprintf(this, name)) {
      name.push(value);
    } else {
      fail("Priority array values are zero");
    }
  };
  /**
   * @param {number} object
   * @param {!Array} s
   * @return {undefined}
   */
  SelectorSet.prototype.remove = function(object, s) {
    if ((s = sprintf(this, s)) && s) {
      object = s.indexOf(object);
      if (0 <= object) {
        s.splice(object, 1);
      }
    }
  };
  expect(Model, method);
  /**
   * @param {string} x
   * @param {!Object} o
   * @param {number} b
   * @return {undefined}
   */
  Model.prototype.add = function(x, o, b) {
    b = void 0 === b ? 0 : b;
    this.b = this.b || new SelectorSet;
    this.b.push({
      xb : x,
      context : o
    }, b);
  };
  /**
   * @param {!Function} id
   * @param {!Object} path
   * @param {?} value
   * @return {undefined}
   */
  Model.prototype.remove = function(id, path, value) {
    value = void 0 === value ? 0 : value;
    if (this.b) {
      var tokens = tokenize(this.b, value);
      var numTokens = tokens.length;
      /** @type {number} */
      var i = 0;
      for (; i < numTokens; ++i) {
        var p = tokens[i];
        if (p.xb == id && p.context == path) {
          /** @type {number} */
          id = i;
          if (value = sprintf(this.b, value)) {
            value.splice(id, 1);
          }
          break;
        }
      }
    } else {
      fail("Signal has no handlers!");
    }
  };
  /**
   * @param {string} key
   * @param {?} ctx
   * @param {number} value
   * @return {?}
   */
  Model.prototype.has = function(key, ctx, value) {
    if (!this.b) {
      return false;
    }
    value = tokenize(this.b, void 0 === value ? 0 : value);
    var valueLength = value.length;
    /** @type {number} */
    var j = 0;
    for (; j < valueLength; ++j) {
      var c = value[j];
      if (c.xb == key && c.context == ctx) {
        return true;
      }
    }
    return false;
  };
  /**
   * @param {?} value
   * @return {undefined}
   */
  Model.prototype.dispatch = function(value) {
    if (this.b) {
      var val = keys(this.b);
      var valLength = val.length;
      /** @type {number} */
      var j = 0;
      for (; j < valLength; ++j) {
        var p = val[j];
        if (-1 != keys(this.b).indexOf(p)) {
          try {
            p.xb.apply(p.context, [value]);
          } catch (n) {
            tag(n);
          }
        }
      }
    }
  };
  var w = Function("return this")();
  Object.getPrototypeOf({});
  /**
   * @param {?} event
   * @return {?}
   */
  w.onerror = function(event) {
    /** @type {!Array} */
    var data = [];
    /** @type {number} */
    var n = 0;
    for (; n < arguments.length; ++n) {
      data[n] = arguments[n];
    }
    n = forEach(data);
    data = n.next().value;
    n.next();
    n.next();
    n.next();
    if (n = n.next().value) {
      tag(n);
    } else {
      debug(data);
    }
    return true;
  };
  var defaults = {
    include : 'link[href*="bundle"],link[href*="legacy_components_customization"]',
    silent : true
  };
  var variables = {};
  expect(container, method);
  /** @type {!Array<string>} */
  var es6methods = "Learn.Reports.SendReportOnSchedulePopup.Schedule.Month.Day Learn.EditContent.CompletionTab.ReenrollmentAfterCourseCompletion.Part1 Learn.EditContent.CompletionTab.ReenrollmentBeforeCertExpires.Part1 Learn.EditContent.CompletionTab.ReenrollmentAfterCourseCompletion.Part2 Studio.SideBar.StorageBar.Progress.Part1 Studio.SideBar.StorageBar.Progress.Part2".split(" ");
  expect(expression, container);
  /**
   * @param {?} str
   * @return {?}
   */
  expression.prototype.f = function(str) {
    return "{" + str.toLowerCase() + "}";
  };
  var arrData;
  var found = {
    ta : "#2972E0",
    link : "#2972E0",
    xd : "#FFFFFF",
    yd : "#42484E"
  };
  var replacements = {
    Ce : "Roboto",
    le : "Roboto"
  };
  /** @type {null} */
  var state = null;
  immer.enableAllPlugins();
  var normalizeProvider = immer.produce;
  /** @type {!Array} */
  var nodes = Array(64);
  /** @type {!Array} */
  var decodeChars = Array(123);
  /** @type {number} */
  var offset = 0;
  for (; 64 > offset;) {
    /** @type {number} */
    decodeChars[nodes[offset] = 26 > offset ? offset + 65 : 52 > offset ? offset + 71 : 62 > offset ? offset - 4 : offset - 59 | 43] = offset++;
  }
  var pro6pp_cache = {};
  /** @type {number} */
  var caveWidth = Math.pow(2, 24);
  /** @type {!WeakMap} */
  var wm = new WeakMap;
  /** @type {!Array} */
  var attributes = ["ul", "ol"];
  each(["header", "subheader"]).concat(each(["blockquote", "pullquote"]), each(attributes), ["text"]);
  var Color = protobuf.Type;
  var Date = protobuf.Field;
  /** @type {number} */
  var fd = 0;
  var edgePointB = new protobuf.Root;
  var newValue = callback({
    key : t(1),
    selected : foo(2)
  });
  var customization = callback({
    da : commit(1, newValue)
  });
  var compiled = callback({
    da : commit(1, newValue)
  });
  var world = callback({
    ua : t(1)
  });
  var cbStat = callback({
    key : t(1),
    o : foo(2),
    j : next(3, customization)
  });
  var mr = callback({
    key : t(1),
    o : foo(2),
    j : next(3, compiled)
  });
  var ti = callback({
    key : t(1),
    o : foo(2),
    j : next(3, world)
  });
  var payload = clone({
    mc : cbStat,
    mr : mr,
    ti : ti
  });
  var forwardPosition = callback({
    key : t(1),
    ca : skip(2),
    la : commit(3, payload)
  });
  var valResult = {};
  var loginInfo = clone((valResult.E = forwardPosition, valResult));
  var body = callback({
    intro : next(1, {
      progress : pick(1, step),
      oa : commit(2, loginInfo)
    }),
    chapters : commit(2, {
      progress : pick(1, step),
      oa : commit(2, loginInfo)
    }),
    rd : pick(3, skip)
  });
  var user = new User(body);
  var apply = w.requestAnimationFrame || w.mozRequestAnimationFrame || w.webkitRequestAnimationFrame || w.msRequestAnimationFrame || setTimeout;
  var removeFromList = w.cancelAnimationFrame || w.mozCancelAnimationFrame || w.webkitCancelAnimationFrame || w.msCancelAnimationFrame || clearTimeout;
  /** @type {!Map} */
  var storage = new Map;
  /** @type {null} */
  var mObs = null;
  /** @type {!Map} */
  var items = new Map;
  /** @type {!Array} */
  var array = [];
  /** @type {!Array} */
  var rest = [];
  /** @type {null} */
  var operatorIsActive = null;
  /** @type {!Map} */
  var moduleFactories = new Map;
  /** @type {boolean} */
  var pattern = false;
  /** @type {boolean} */
  var ie = false;
  /** @type {!Map} */
  var availableMachines = new Map;
  /** @type {!Array} */
  var categories = [];
  hyperHTML.define("save-ref", function(instance, b) {
    b = void 0 === b ? {} : b;
    /** @type {string} */
    b.current = instance;
  });
  /** @type {!WeakMap} */
  var modes = new WeakMap;
  hyperHTML.define("passive-events", function(e, a) {
    var o = modes.get(e) || modes.set(e, {}).get(e);
    var api = forEach(Object.keys(a));
    var i = api.next();
    for (; !i.done; i = api.next()) {
      if (i = i.value, "on" === i.slice(0, 2) && o[i] !== a[i]) {
        var type = i.slice(2);
        if (o[i]) {
          e.removeEventListener(type, o[i]);
        }
        o[i] = a[i];
        if (o[i]) {
          e.addEventListener(type, o[i], {
            passive : true
          });
        }
      }
    }
    return "";
  });
  var require = hyperHTML.wire;
  /** @type {!Map} */
  var recFrames = new Map;
  /** @type {!WeakMap} */
  var path = new WeakMap;
  /** @type {!Array} */
  var outChance = [];
  /** @type {string} */
  var optionUsed3 = "";
  new Set;
  /** @type {!Array} */
  var returnFalse = [];
  /** @type {!Array} */
  var topic = ["<div class=", ">", "</div>"];
  /** @type {!Array<?>} */
  topic.raw = topic.slice();
  /** @type {!Array} */
  var fixture = ["\n<div save-ref=", " class=", ">\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  fixture.raw = fixture.slice();
  var ops = {};
  /** @type {!Array} */
  var left = ["\n<div\n\tclass=", "\n\tsave-ref=", "\n\tdata-at=", "\n>\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  left.raw = left.slice();
  /** @type {!Array} */
  var fileData = ["\n<svg\n\tclass=", '\n\tviewBox="0 0 32 32"\n\txmlns="http://www.w3.org/2000/svg"\n>\n\t<path\n\t\td="M30 16C30 23.732 23.732 30 16 30C8.26801 30 2 23.732 2 16C2 8.26801 8.26801 2 16 2"\n\t\tstroke="currentColor"\n\t\tstroke-width="2.5"\n\t\tstroke-linecap="round"\n\t\tfill="none"\n\t/>\n</svg>\n'];
  /** @type {!Array<?>} */
  fileData.raw = fileData.slice();
  /** @type {!Array} */
  var folderPath = ["\n<div class=", ">\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  folderPath.raw = folderPath.slice();
  /** @type {!Array} */
  var imgTag = ["\n<div class=", ">\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  imgTag.raw = imgTag.slice();
  /** @type {!Array} */
  var commaParts = ["\n<div class=", ">\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  commaParts.raw = commaParts.slice();
  /** @type {!Array<string>} */
  var type = "\n<button\n\tclass= \n\tdisabled= \n\tonclick= \n\tsave-ref= \n\tdata-at= \n>\n\t \n\t \n\t \n\t \n</button>\n".split(" ");
  /** @type {!Array<string>} */
  type.raw = type.slice();
  var defaultOptions = {
    button : {
      za : function(a) {
        return extend("xeba1a__button", a);
      }
    },
    icon : "xeba1a__icon",
    text : "xeba1a__text",
    wa : "xeba1a__preloader"
  };
  var builder = {
    button : {
      za : function(a) {
        return extend("x3e989__button", a);
      }
    },
    icon : "x3e989__icon",
    text : "x3e989__text",
    wa : "x3e989__preloader"
  };
  var args = {
    button : {
      za : function(a) {
        return extend("x1ebbb__button", a);
      }
    },
    text : "x1ebbb__text",
    icon : "x1ebbb__icon",
    wa : "x1ebbb__preloader"
  };
  var menu = {
    button : {
      za : function(a) {
        return extend("xb6cd3__button", a);
      }
    },
    icon : "xb6cd3__icon",
    text : "xb6cd3__text",
    wa : "xb6cd3__preloader"
  };
  var errorLayer = {
    button : {
      za : function(a) {
        return extend("x133d5__button", a);
      }
    },
    text : "x133d5__text",
    icon : "x133d5__icon",
    wa : "x133d5__preloader"
  };
  var components = {
    button : {
      za : function(a) {
        return extend("x959fd__button", a);
      }
    },
    text : "x959fd__text",
    icon : "x959fd__icon",
    wa : "x959fd__preloader"
  };
  var linkObj = {
    button : {
      za : function(a) {
        return extend("x9131e__button", a);
      }
    },
    text : "x9131e__text",
    icon : "x9131e__icon",
    wa : "x9131e__preloader"
  };
  var model = {
    button : {
      za : function(a) {
        return extend("x08261__button", a);
      }
    },
    text : "x08261__text",
    icon : "x08261__icon",
    wa : "x08261__preloader"
  };
  var root = {
    button : {
      za : function(a) {
        return extend("xc515f__button", a);
      }
    },
    text : "xc515f__text",
    icon : "xc515f__icon",
    wa : "xc515f__preloader"
  };
  var types = {
    button : {
      za : function(a) {
        return extend("x2278a__button", a);
      }
    },
    text : "x2278a__text",
    icon : "x2278a__icon",
    wa : "x2278a__preloader"
  };
  /** @type {!Array} */
  var tokens = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="m8 15 5-5-5-5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>\n'];
  /** @type {!Array<?>} */
  tokens.raw = tokens.slice();
  /**
   * @return {?}
   */
  Promise.prototype.className = function() {
    return this.f ? this.b + "__" + this.f : this.b;
  };
  /** @type {!Array} */
  var actions = ["click"];
  /** @type {!Map} */
  var _cachedContacts = new Map;
  /** @type {!Map} */
  var idToBufferFileMap = new Map;
  apply(_done);
  expect(context, container);
  item = context.prototype;
  /**
   * @return {?}
   */
  item.D = function() {
    if (_cachedContacts.has(this)) {
      stats(this);
    }
    if (this.ma) {
      return this.ma;
    }
    stats(this);
    return this.D();
  };
  /**
   * @param {(Object|string)} e
   * @return {undefined}
   */
  item.handleEvent = function(e) {
    if (!e.defaultPrevented || !actions.includes(e.type)) {
      var Q = this.m && this.m[e.type] || null;
      if (Q) {
        Q.dispatch(e);
      }
    }
  };
  /**
   * @param {!Function} data
   * @return {undefined}
   */
  item.sd = function(data) {
    /** @type {!Function} */
    this.ac = data;
  };
  /**
   * @param {!Array} a
   * @param {string} n
   * @return {?}
   */
  item.na = function(a, n) {
    /** @type {!Array} */
    var args = [];
    /** @type {number} */
    var i = 1;
    for (; i < arguments.length; ++i) {
      args[i - 1] = arguments[i];
    }
    return this.Rc.apply(this, ["", a].concat(each(args)));
  };
  /**
   * @param {string} name
   * @param {!HTMLDocument} options
   * @param {?} suggestedVariableValueCallback
   * @return {?}
   */
  item.Rc = function(name, options, suggestedVariableValueCallback) {
    /**
     * @param {!Object} target
     * @return {?}
     */
    function render(target) {
      return target.map(function(obj, i) {
        if (obj instanceof Promise) {
          obj = obj.className();
        } else {
          if (obj instanceof Model) {
            obj = obj.dispatch.bind(obj);
          } else {
            if ("function" == typeof obj) {
              obj = obj.bind(undefined);
            } else {
              if (obj == undefined) {
                return obj.handleEvent.bind(undefined);
              }
              if (obj && obj != undefined && "function" == typeof obj.D) {
                return "function" == typeof obj.sd && obj.sd(function() {
                  return stats(undefined);
                }), memDir(undefined, obj), obj.D();
              }
              if (Array.isArray(obj)) {
                return render(obj);
              }
            }
          }
        }
        if (options[i] && options[i].endsWith("passive-events=")) {
          i = forEach(Object.keys(obj));
          var event = i.next();
          for (; !event.done; event = i.next()) {
            event = event.value;
            if ("function" == typeof obj[event]) {
              obj[event] = obj[event].bind(undefined);
            }
          }
        }
        return obj;
      });
    }
    /** @type {!Array} */
    var params = [];
    /** @type {number} */
    var i = 2;
    for (; i < arguments.length; ++i) {
      params[i - 2] = arguments[i];
    }
    var undefined = this;
    params = render(params);
    params = require(this.Lc, name && ":" + name).apply(null, [options].concat(each(params)));
    if (name) {
      this.Lb = this.Lb || {};
      this.Lb[name] = params;
    }
    return params;
  };
  /**
   * @return {undefined}
   */
  item.bc = function() {
    _cachedContacts.delete(this);
    container.prototype.bc.call(this);
  };
  /** @type {string} */
  var key = String.fromCharCode(160);
  /** @type {string} */
  var i = String.fromCharCode(8203);
  String.fromCharCode(8805);
  String.fromCharCode(60);
  String.fromCharCode(37);
  /**
   * @return {?}
   */
  init.prototype.Ja = function() {
    return this.b;
  };
  /**
   * @return {?}
   */
  init.prototype.type = function() {
    return this.b.key === this.m ? "subtitle" : this.b.N;
  };
  /**
   * @return {?}
   */
  init.prototype.textAlign = function() {
    return this.b.textAlign;
  };
  /**
   * @return {?}
   */
  init.prototype.className = function() {
    return this.f;
  };
  /** @type {!Array<string>} */
  var featureTable = "\n\t\t<p\n\t\t\tclass= \n\t\t\tdata-key= \n\t\t\tdata-placeholder= \n\t\t\tdata-at= \n\t\t\tcontenteditable= \n\t\t> </p>".split(" ");
  /** @type {!Array<string>} */
  featureTable.raw = featureTable.slice();
  /** @type {!Array<string>} */
  var _this7 = "<li class=; data-key=; data-at=; contenteditable=;>;</li>".split(";");
  /** @type {!Array<string>} */
  _this7.raw = _this7.slice();
  /** @type {!Array<string>} */
  var newPost = "<blockquote class=; data-key=; data-at=; contenteditable=;>;</blockquote>".split(";");
  /** @type {!Array<string>} */
  newPost.raw = newPost.slice();
  /** @type {!Array} */
  var elem = ["<h4 class=", " data-key=", " contenteditable=", ">", "</h4>"];
  /** @type {!Array<?>} */
  elem.raw = elem.slice();
  /** @type {!Array} */
  var project_name = ["<h3 class=", " data-key=", " contenteditable=", ">", "</h3>"];
  /** @type {!Array<?>} */
  project_name.raw = project_name.slice();
  /** @type {!Array} */
  var settings = ["<span>", "</span>"];
  /** @type {!Array<?>} */
  settings.raw = settings.slice();
  /** @type {!Array} */
  var source = ["\n\t\t<a href=", " target=_blank onmouseover=", "><span style=", ">", "</span></a>"];
  /** @type {!Array<?>} */
  source.raw = source.slice();
  /** @type {!Array} */
  var doc = ["\n\t\t<span style=", ">", "</span>"];
  /** @type {!Array<?>} */
  doc.raw = doc.slice();
  var arrayKey = {};
  /** @type {!Array} */
  var bufTable = ["\n\t\t\t<div class=", "></div>"];
  /** @type {!Array<?>} */
  bufTable.raw = bufTable.slice();
  /** @type {!Array} */
  var pageContent = ["<ol class='list-block' style=", ">", "</ol>"];
  /** @type {!Array<?>} */
  pageContent.raw = pageContent.slice();
  /** @type {!Array} */
  var src = ["<ul class='list-block' data-at=", ">", "</ul>"];
  /** @type {!Array<?>} */
  src.raw = src.slice();
  expect(calc, context);
  /**
   * @return {?}
   */
  calc.prototype.C = function() {
    return false;
  };
  /** @type {!Array} */
  var vForward = ["\n\t\t\t<div class=", ">\n\t\t\t\t", "\n\t\t\t</div>"];
  /** @type {!Array<?>} */
  vForward.raw = vForward.slice();
  expect(A, calc);
  /**
   * @return {?}
   */
  A.prototype.C = function() {
    return true;
  };
  /**
   * @return {?}
   */
  A.prototype.W = function() {
    return this.na(vForward, showPlaceholderIfEmpty(this), stream(this));
  };
  /** @type {!Array} */
  var date = ["\n<div class=", ">\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  date.raw = date.slice();
  /** @type {!Array} */
  var mutationsMap = ['\n<svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M39 45.375H9A2.375 2.375 0 0 1 6.625 43V5A2.375 2.375 0 0 1 9 2.625h21.28c.647 0 1.267.264 1.715.731l8.72 9.093c.423.442.66 1.031.66 1.644V43A2.375 2.375 0 0 1 39 45.375z" fill="#fff" stroke="#D0D0D1" stroke-width="1.25"/><path d="M30.841 31.182V19.068h1.803c.61 0 1.106.495 1.106 1.106v9.902c0 .61-.495 1.106-1.106 1.106H30.84zM14.25 17.68v15.166L28.773 34.5V15.75L14.25 17.68z" fill="#4169B4"/><path d="m23.61 27.043 1.118-6.117h1.705l-1.842 8.597H22.87l-1.352-5.751-1.351 5.75h-1.723l-1.842-8.596h1.705l1.124 6.105 1.369-6.105h1.448l1.363 6.117z" fill="#fff"/></svg>\n'];
  /** @type {!Array<?>} */
  mutationsMap.raw = mutationsMap.slice();
  /** @type {!Array} */
  var n = ['\n<svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M39 45.375H9A2.375 2.375 0 0 1 6.625 43V5A2.375 2.375 0 0 1 9 2.625h21.28c.647 0 1.267.264 1.715.731l8.72 9.093c.423.442.66 1.031.66 1.644V43A2.375 2.375 0 0 1 39 45.375z" fill="#fff" stroke="#D0D0D1" stroke-width="1.25"/><path d="M14.658 26.355v2.895h-2.056v-8.531h3.404c.652 0 1.228.12 1.728.363.504.238.893.58 1.166 1.025.278.442.416.944.416 1.506 0 .832-.299 1.498-.896 1.998-.594.496-1.41.744-2.45.744h-1.312zm0-1.587h1.348c.398 0 .701-.1.908-.3.21-.198.316-.48.316-.843 0-.398-.107-.717-.322-.955-.215-.238-.508-.36-.879-.363h-1.37v2.46zM21.018 29.25v-8.531h2.748c.754 0 1.432.172 2.033.515a3.56 3.56 0 0 1 1.406 1.448c.34.62.512 1.318.516 2.091v.393c0 .781-.166 1.482-.498 2.104a3.619 3.619 0 0 1-1.395 1.453 3.956 3.956 0 0 1-2.003.527h-2.807zm2.056-6.943v5.361h.715c.59 0 1.043-.209 1.36-.627.316-.422.474-1.047.474-1.875v-.37c0-.823-.158-1.444-.474-1.862-.317-.418-.778-.627-1.383-.627h-.691zM34.749 25.852h-3.317v3.398h-2.056v-8.531h5.707v1.588h-3.65v1.963h3.316v1.582z" fill="#E05D36"/></svg>\n'];
  /** @type {!Array<?>} */
  n.raw = n.slice();
  /** @type {!Array} */
  var hex = ['\n<svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M39 45.375H9A2.375 2.375 0 0 1 6.625 43V5A2.375 2.375 0 0 1 9 2.625h21.28c.647 0 1.267.264 1.715.731l8.72 9.093c.423.442.66 1.031.66 1.644V43A2.375 2.375 0 0 1 39 45.375z" fill="#fff" stroke="#D0D0D1" stroke-width="1.25"/><path d="M30.5 31.25V19h1.75a1.5 1.5 0 0 1 1.5 1.5v9.25a1.5 1.5 0 0 1-1.5 1.5H30.5zM14.25 17.68v15.166L28.875 34.5V15.75L14.25 17.68z" fill="#D1684D"/><path d="M20.49 26.523v3.102h-1.746V20.82h3.305c.636 0 1.194.12 1.675.363a2.6 2.6 0 0 1 1.118 1.034c.26.443.39.95.39 1.518 0 .862-.286 1.544-.856 2.044-.566.496-1.352.744-2.357.744h-1.53zm0-1.47h1.559c.461 0 .812-.113 1.053-.339.244-.226.366-.548.366-.967 0-.432-.122-.78-.366-1.046-.245-.267-.582-.404-1.013-.412h-1.6v2.764z" fill="#fff"/></svg>\n'];
  /** @type {!Array<?>} */
  hex.raw = hex.slice();
  /** @type {!Array} */
  var even_more_comments = ['\n<svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M39 45.375H9A2.375 2.375 0 0 1 6.625 43V5A2.375 2.375 0 0 1 9 2.625h21.28c.647 0 1.267.264 1.715.731l8.72 9.093c.423.442.66 1.031.66 1.644V43A2.375 2.375 0 0 1 39 45.375z" fill="#fff" stroke="#D0D0D1" stroke-width="1.25"/></svg>\n'];
  /** @type {!Array<?>} */
  even_more_comments.raw = even_more_comments.slice();
  /** @type {!Array} */
  var s = ['\n<svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M39 45.375H9A2.375 2.375 0 0 1 6.625 43V5A2.375 2.375 0 0 1 9 2.625h21.28c.647 0 1.267.264 1.715.731l8.72 9.093c.423.442.66 1.031.66 1.644V43A2.375 2.375 0 0 1 39 45.375z" fill="#fff" stroke="#D0D0D1" stroke-width="1.25"/><path d="M30.5 31.25V19h1.75a1.5 1.5 0 0 1 1.5 1.5v9.25a1.5 1.5 0 0 1-1.5 1.5H30.5zM14.25 17.68v15.166L28.875 34.5V15.75L14.25 17.68z" fill="#468B5B"/><path d="m21.563 23.855 1.588-3.035h2.007l-2.467 4.366 2.531 4.439h-2.03l-1.63-3.084-1.629 3.084h-2.03l2.53-4.439-2.466-4.366h2.007l1.588 3.035z" fill="#fff"/></svg>\n'];
  /** @type {!Array<?>} */
  s.raw = s.slice();
  /** @type {!Array} */
  var returnObj = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="m6.293 10.707 3 3a1 1 0 0 0 1.414 0l3-3a1 1 0 0 0-1.414-1.414L11 10.586V3a1 1 0 1 0-2 0v7.586L7.707 9.293a1 1 0 0 0-1.414 1.414zM5 15a1 1 0 1 0-2 0v1a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-1a1 1 0 1 0-2 0v1H5v-1z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  returnObj.raw = returnObj.slice();
  /** @type {!Array} */
  var token = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M6.654 5.02a6 6 0 0 1 8.6 2.08l-.432-.173a1 1 0 0 0-.744 1.857l2.625 1.052a1 1 0 0 0 1.3-.556l1.052-2.626A1 1 0 0 0 17.2 5.91l-.134.335a8 8 0 0 0-14.533.889 1 1 0 1 0 1.867.716 6 6 0 0 1 2.255-2.83zm6.693 10.07a6 6 0 0 1-8.583-2.05l.335.162a1 1 0 0 0 .873-1.8l-2.545-1.235a1 1 0 0 0-1.336.463L.856 13.175a1 1 0 1 0 1.8.873l.181-.375a8 8 0 0 0 14.632-.696 1 1 0 1 0-1.868-.717 6 6 0 0 1-2.255 2.83z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  token.raw = token.slice();
  /** @type {!Array<string>} */
  var expected = "\n<div\n\tclass=;\n\tsave-ref=;\n\tdraggable=true\n>\n\t;\n\t<div class=;>\n\t\t<div class=;>\n\t\t\t<div class=;>\n\t\t\t\t;\n\t\t\t</div>\n\t\t\t<div class=;>;</div>\n\t\t</div>\n\t\t;\n\t\t;\n\t</div>\n</div>".split(";");
  /** @type {!Array<string>} */
  expected.raw = expected.slice();
  /** @type {!Array} */
  var SiteRow = ["\n<img class=", ' aria-hidden=true src="data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7" />'];
  /** @type {!Array<?>} */
  SiteRow.raw = SiteRow.slice();
  /** @type {!Array} */
  var output = ["\n<div class=", ">\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  output.raw = output.slice();
  /** @type {!Array} */
  var ch = ["\n<div class=", ">\n\t", "\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  ch.raw = ch.slice();
  /** @type {!Array} */
  var ref = ["\n<figure class=", ">\n\t", "\n</figure>"];
  /** @type {!Array<?>} */
  ref.raw = ref.slice();
  var maps = {};
  var evt = {};
  expect(Class, context);
  /**
   * @return {?}
   */
  Class.prototype.W = function() {
    var e = this.b;
    var script = e.src;
    var fileName = e.fileName;
    e = stop(evt, ":attachment-block-" + e.key);
    return e(ref, element(showPlaceholderIfEmpty(this), extend("x99dbb__attachment-figure", {
      playerView : true
    })), e(display, Object.assign({}, this.b, {
      Qe : true,
      xc : function() {
        /** @type {!Element} */
        var downloadNode = document.createElement("a");
        downloadNode.setAttribute("href", getNodeValue($(script)));
        downloadNode.setAttribute("download", fileName);
        downloadNode.click();
      }
    })));
  };
  var style = {
    top : 0,
    right : 0,
    bottom : 0,
    left : 0
  };
  /** @type {!Array} */
  var valueProgess = ["\n<div\n\tclass=", "\n\tstyle=", "\n\tondisconnected=", "\n>\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  valueProgess.raw = valueProgess.slice();
  expect(val, context);
  item = val.prototype;
  /**
   * @return {undefined}
   */
  item.show = function() {
    if (this.b) {
      /** @type {boolean} */
      this.b = false;
      push(this);
    }
  };
  /**
   * @return {undefined}
   */
  item.Fa = function() {
    if (!this.b) {
      /** @type {boolean} */
      this.b = true;
      push(this);
    }
  };
  /**
   * @return {?}
   */
  item.visible = function() {
    return !this.b;
  };
  /**
   * @return {?}
   */
  item.x = function() {
    return this.f;
  };
  /**
   * @return {?}
   */
  item.y = function() {
    return this.g;
  };
  /**
   * @param {!Function} a
   * @param {number} b
   * @return {undefined}
   */
  item.move = function(a, b) {
    if (a != this.f || b != this.g) {
      /** @type {!Function} */
      this.f = a;
      /** @type {number} */
      this.g = b;
      push(this);
    }
  };
  /**
   * @return {?}
   */
  item.W = function() {
    return this.na(valueProgess, showPlaceholderIfEmpty(this), makeupBalloon(this), this, "");
  };
  /** @type {!Array<string>} */
  var settingsContent = '\n\t\t\t<input\n\t\t\t\tclass=;\n\t\t\t\tstyle=;\n\t\t\t\ttype="file" accept=;\n\t\t\t\tonchange=;\n\t\t\t\tdata-at=;\n\t\t\t>'.split(";");
  /** @type {!Array<string>} */
  settingsContent.raw = settingsContent.slice();
  /** @type {!Array} */
  var parsedSidCookie = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M2 6a1 1 0 0 1 1-1h14a1 1 0 1 1 0 2H3a1 1 0 0 1-1-1zm0 4a1 1 0 0 1 1-1h14a1 1 0 1 1 0 2H3a1 1 0 0 1-1-1zm1 3a1 1 0 1 0 0 2h14a1 1 0 1 0 0-2H3z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  parsedSidCookie.raw = parsedSidCookie.slice();
  /** @type {!Array} */
  var moznode = ["\n<div class=", ' draggable="true">\n\t', "\n</div>"];
  /** @type {!Array<?>} */
  moznode.raw = moznode.slice();
  /** @type {!Array} */
  var MAT2 = ["\n<div class=", ">\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  MAT2.raw = MAT2.slice();
  /** @type {!Array} */
  var expectedL10n = ["\n<div class=", ">\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  expectedL10n.raw = expectedL10n.slice();
  /** @type {!Array} */
  var charListNotLatin = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="m9 4-6 6m0 0 6 6m-6-6h14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>\n'];
  /** @type {!Array<?>} */
  charListNotLatin.raw = charListNotLatin.slice();
  /** @type {!Array} */
  var copy_of_children = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="m11 4 6 6m0 0-6 6m6-6H3" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>\n'];
  /** @type {!Array<?>} */
  copy_of_children.raw = copy_of_children.slice();
  /** @type {!Array} */
  var dcdInfo = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M6 2a1 1 0 0 0-2 0v2H2a1 1 0 0 0 0 2h2v8a2 2 0 0 0 2 2h8v2a1 1 0 1 0 2 0v-2h2a1 1 0 1 0 0-2H6V2zm3 2a1 1 0 0 0 0 2h5v5a1 1 0 1 0 2 0V6a2 2 0 0 0-2-2H9z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  dcdInfo.raw = dcdInfo.slice();
  /** @type {!Array} */
  var scopedListeners = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M13.697 1.875 13.212 1l-.875.485-2.474 1.372a1 1 0 1 0 .97 1.749l.315-.175a4.497 4.497 0 0 0 3.215 5.497 4.5 4.5 0 0 0 3.102-8.402 1 1 0 0 0-.873 1.8 2.5 2.5 0 1 1-3.501 1.581l.228.411a1 1 0 1 0 1.749-.97l-1.371-2.473zM0 6a2 2 0 0 1 2-2h5a1 1 0 0 1 0 2H2v11h13v-3a1 1 0 1 1 2 0v3a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V6zm3.5 4.517a1.25 1.25 0 1 0 2.5 0 1.25 1.25 0 0 0-2.5 0zm8.945.864a1 1 0 0 0-1.307-.093L8.5 13.268l-1.178-.588a1 1 0 0 0-.96.038L3.5 14.434v1.333h10v-3.331l-1.055-1.055z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  scopedListeners.raw = scopedListeners.slice();
  /** @type {!Array} */
  var mergeBuffer = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M8 4a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1H8zM6 4H3a1 1 0 0 0 0 2v10a3 3 0 0 0 3 3h8a3 3 0 0 0 3-3V6a1 1 0 1 0 0-2h-3a3 3 0 0 0-3-3H9a3 3 0 0 0-3 3zm9 12V6H5v10a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1zm-7-1a1 1 0 0 1-1-1V9a1 1 0 0 1 2 0v5a1 1 0 0 1-1 1zm3-1a1 1 0 1 0 2 0V9a1 1 0 1 0-2 0v5z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  mergeBuffer.raw = mergeBuffer.slice();
  /** @type {!Array<string>} */
  var slotBind = "\n<div class=;>\n\t<div class=;></div>\n\t<div class=;></div>\n\t;\n\t;\n</div>".split(";");
  /** @type {!Array<string>} */
  slotBind.raw = slotBind.slice();
  /** @type {!Array} */
  var toTable = ["\n<div class=", ">\n\t", "\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  toTable.raw = toTable.slice();
  expect(transform, context);
  /**
   * @param {string} value
   * @return {undefined}
   */
  transform.prototype.f = function(value) {
    /** @type {string} */
    this.b = value;
    f(this, "align", this.b.w);
  };
  /**
   * @param {string} value
   * @return {undefined}
   */
  transform.prototype.Ca = function(value) {
    /** @type {string} */
    this.g = value;
    push(this);
  };
  /**
   * @return {?}
   */
  transform.prototype.C = function() {
    return 740 >= document.body.clientWidth || "float-left" !== this.b.w || "e" === this.b.type ? null : {
      width : this.b.width + "px"
    };
  };
  /**
   * @return {?}
   */
  transform.prototype.S = function() {
    return this.b.width ? {
      "padding-bottom" : 100 * (this.g ? this.g : this.b.height / this.b.width) + "%"
    } : {
      width : "100%",
      height : this.b.height + "px"
    };
  };
  /** @type {!Array<string>} */
  var plugman = '\n<figure\n\tclass=;\n\tstyle=;\n\tdata-key=;\n\tcontenteditable="false"\n\ttabindex="-1"\n\tonkeydown=;\n\tonmousedown=;\n\tonblur=;\n>\n\t;\n\t<div class=; style=; draggable=true>\n\t\t;\n\t\t<div class=; style=;></div>\n\t</div>\n\t<figcaption\n\t\tsave-ref=;\n\t\tclass="caption-wrapper"\n\t\tstyle=;\n\t\tcontenteditable=;\n\t>\n\t\t;\n\t</figcaption>\n</figure>'.split(";");
  /** @type {!Array<string>} */
  plugman.raw = plugman.slice();
  /** @type {!Array<string>} */
  var requirePathLiteral = '\n<figure\n\tclass=;\n\tstyle=;\n\tdata-collaged-key=;\n\tdata-parent-collage-key=;\n\tcontenteditable="false"\n\tonkeydown=;\n\tonmousedown=;\n\tonblur=;\n>\n\t<div class=; style=; draggable=true>\n\t\t;\n\t\t<div class=; style=;></div>\n\t\t;\n\t</div>\n\t<figcaption\n\t\tsave-ref=;\n\t\tclass="caption-wrapper"\n\t\tstyle=;\n\t\tcontenteditable=;\n\t>\n\t\t;\n\t</figcaption>\n</figure>'.split(";");
  /** @type {!Array<string>} */
  requirePathLiteral.raw = requirePathLiteral.slice();
  /** @type {!Array} */
  var triggerPublishSeed = ["\n<div\n\tclass=", "\n\tsave-ref=", "\n>\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  triggerPublishSeed.raw = triggerPublishSeed.slice();
  /** @type {!Array} */
  var parserErr = ["\n<div\n\tclass=", "\n\tstyle=", "\n\tonclick=", "\n>\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  parserErr.raw = parserErr.slice();
  /** @type {!Array} */
  var jpath = ["\n<img\n\tclass=", '\n\tsrc="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII="\n\ttabIndex="-1"\n\taria-hidden=true\n\tdraggable=false\n/>\n'];
  /** @type {!Array<?>} */
  jpath.raw = jpath.slice();
  /** @type {!Array} */
  var child = ["\n<div\n\tclass=", "\n\tonclick=", "\n>\n</div>"];
  /** @type {!Array<?>} */
  child.raw = child.slice();
  var sequence = animate({
    opacity : hideAll(0, 1),
    duration : 200,
    after : function(block) {
      /** @type {string} */
      block.style.opacity = "1";
    }
  });
  var animdTransforms = animate({
    opacity : hideAll(1, 0),
    duration : 200,
    after : function(block) {
      /** @type {string} */
      block.style.opacity = "0";
    }
  });
  /** @type {!Map} */
  var dateFormatterCache = new Map;
  /** @type {!Array} */
  var urConfigJson = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M16.293 2.293a1 1 0 1 1 1.414 1.414L14.414 7H16a1 1 0 1 1 0 2h-4a1 1 0 0 1-1-1V4a1 1 0 1 1 2 0v1.586l3.293-3.293zm-14 14a1 1 0 1 0 1.414 1.414L7 14.414V16a1 1 0 1 0 2 0v-4a1 1 0 0 0-1-1H4a1 1 0 1 0 0 2h1.586l-3.293 3.293zM18 17v-4a1 1 0 1 0-2 0v3h-3a1 1 0 1 0 0 2h4a1 1 0 0 0 1-1zM3 2a1 1 0 0 0-1 1v4a1 1 0 0 0 2 0V4h3a1 1 0 0 0 0-2H3z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  urConfigJson.raw = urConfigJson.slice();
  /** @type {!Array} */
  var expr = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M15.707 5.707a1 1 0 0 0-1.414-1.414L10 8.586 5.707 4.293a1 1 0 0 0-1.414 1.414L8.586 10l-4.293 4.293a1 1 0 1 0 1.414 1.414L10 11.414l4.293 4.293a1 1 0 0 0 1.414-1.414L11.414 10l4.293-4.293z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  expr.raw = expr.slice();
  /** @type {!Array} */
  var ast = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M3 10h14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>\n'];
  /** @type {!Array<?>} */
  ast.raw = ast.slice();
  /** @type {!Array} */
  var json = ['\n<svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M11 3a1 1 0 1 0-2 0v6H3a1 1 0 0 0 0 2h6v6a1 1 0 1 0 2 0v-6h6a1 1 0 1 0 0-2h-6V3z" fill="currentColor"/></svg>\n'];
  /** @type {!Array<?>} */
  json.raw = json.slice();
  /** @type {!Array<string>} */
  var datum = "\n<div\n\tclass=;\n\tsave-ref=;\n>\n\t<div class=;\n\t>\n\t\t;\n\t\t;\n\t</div>\n\t;\n\t;\n</div>".split(";");
  /** @type {!Array<string>} */
  datum.raw = datum.slice();
  /** @type {!Array} */
  var currentLoaderObject = ["\n\t\t<img\n\t\t\tclass=", "\n\t\t\tsrc=", "\n\t\t\talt=''\n\t\t/>"];
  /** @type {!Array<?>} */
  currentLoaderObject.raw = currentLoaderObject.slice();
  /** @type {!Array<string>} */
  var templ = "\n<div class=; save-ref=;>\n\t;\n\t;\n\t;\n\t</div>\n".split(";");
  /** @type {!Array<string>} */
  templ.raw = templ.slice();
  /** @type {!Array} */
  var isFirstTimeShow = ["\n<div>\n\t", "\n</div>\n"];
  /** @type {!Array<?>} */
  isFirstTimeShow.raw = isFirstTimeShow.slice();
  /** @type {!Array<string>} */
  var module = '\n<figure\n\tclass=;\n\tstyle=;\n>\n\t<div class=; style=;>\n\t\t;\n\t\t<div class=; style=;></div>\n\t</div>\n\t<figcaption class="caption-wrapper">\n\t\t;\n\t</figcaption>\n</figure>'.split(";");
  /** @type {!Array<string>} */
  module.raw = module.slice();
  var properties = {};
  var processors = {};
  expect(Button, transform);
  /**
   * @return {?}
   */
  Button.prototype.ha = function() {
    var b = this.b;
    return "i" === b.type && "inner" === b.w ? {
      "max-width" : b.width + "px",
      "max-height" : b.height + "px"
    } : null;
  };
  /**
   * @return {?}
   */
  Button.prototype.W = function() {
    var e = this.b;
    return require(processors, ":media-block-" + e.key)(module, showPlaceholderIfEmpty(this), this.C(), this.hb, this.ha(), this.Ba(e, this.Aa, this.Ca.bind(this)), this.Pa, this.S(), sync(this, e));
  };
  /** @type {!Array} */
  var req = ["\n\t\t\t", "\n\t\t"];
  /** @type {!Array<?>} */
  req.raw = req.slice();
  var results = {};
  var delegate = {};
  expect(m, Button);
  /**
   * @param {?} value
   * @return {undefined}
   */
  m.prototype.f = function(value) {
    Button.prototype.f.call(this, value);
    if (value = value && "i" === value.type && value.Xb ? value : null) {
      f(this, "align", false);
      f(this, "blob", value.va);
      f(this, "collaged", !!value.Xb);
      f(this, "prevent-collaging", this.h);
    }
  };
  /**
   * @return {?}
   */
  m.prototype.C = function() {
    return this.l ? {
      flex : this.l + " 0 0"
    } : null;
  };
  /**
   * @return {?}
   */
  m.prototype.S = function() {
    var w = this.b;
    if (!w.width) {
      return {
        width : "100%",
        height : w.height + "px"
      };
    }
    var s = fn(w);
    w = s.width;
    s = s.height;
    return {
      "padding-bottom" : 100 * (this.g ? this.g : s / w) + "%"
    };
  };
  /**
   * @return {?}
   */
  m.prototype.ha = function() {
    var e = this.b;
    return this.h ? {
      "max-width" : fn(e).width + "px"
    } : null;
  };
  /** @type {!Array} */
  var webdriver = ["\n<figure\n\tclass=", "\n\tstyle=", "\n>\n\t", "\n</figure>"];
  /** @type {!Array<?>} */
  webdriver.raw = webdriver.slice();
  var LIST = {};
  var rtdSeleniumServer = {};
  expect(Matrix, context);
  /**
   * @return {?}
   */
  Matrix.prototype.W = function() {
    var b = this.b;
    return require(rtdSeleniumServer, ":collage-block-" + b.key)(webdriver, showPlaceholderIfEmpty(this), 1 === this.b.images.length && (740 >= document.body.clientWidth || "float-left" === this.b.w) ? {
      display : "block",
      width : fn(this.b.images[0]).width + "px"
    } : null, sorter(this, b.images));
  };
  /** @type {!Array} */
  var devices = ["\n<iframe onload=", " class=", " src=", ' allowfullscreen loading="lazy">\n</iframe>'];
  /** @type {!Array<?>} */
  devices.raw = devices.slice();
  var undefined = {};
  /** @type {!Array} */
  var iconCtx = ["\n<div\n\tclass=", "\n\tonclick=", "\n></div>"];
  /** @type {!Array<?>} */
  iconCtx.raw = iconCtx.slice();
  /** @type {!Array} */
  var arg = ["/src/roll/components/richtext/renderer/exercise/NavigationButton.css"];
  /** @type {!Array<?>} */
  arg.raw = arg.slice();
  var h = c(arg).v;
  /** @type {!Array} */
  var tk = ["<div>", "</div>"];
  /** @type {!Array<?>} */
  tk.raw = tk.slice();
  /** @type {!Array<string>} */
  var drw = "\n<button\n\tsave-ref= \n\tdisabled= \n\tclass= \n\tonmouseover= \n\tonmouseout= \n\ttabindex= \n\tonclick= \n\tonkeydown= \n\tonmousedown= \n\tdata-at= \n>\n\t \n</button>".split(" ");
  /** @type {!Array<string>} */
  drw.raw = drw.slice();
  /** @type {!Array} */
  var pac = ["\n<div\n\tclass=", "\n>\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  pac.raw = pac.slice();
  /** @type {!Array} */
  var fpurl = ["\n<iframe\n\tclass=", "\n\tsrc=", "\n></iframe>"];
  /** @type {!Array<?>} */
  fpurl.raw = fpurl.slice();
  /** @type {!Array} */
  var identifierPositions = ["\n<div class=", " style=", ">\n\t<div style=", ">\n\t\t", "\n\t</div>\n</div>\n"];
  /** @type {!Array<?>} */
  identifierPositions.raw = identifierPositions.slice();
  /** @type {!Array} */
  var node = ["/src/roll/components/richtext/renderer/exercise/question/AttachmentRenderer.css"];
  /** @type {!Array<?>} */
  node.raw = node.slice();
  var parseHTML = c(node).v;
  /** @type {!Array} */
  var elmObjsSorted = ["<h4\n\tclass=", "\n\tdata-key=", "\n\tdata-at=", "\n>", "</h4>"];
  /** @type {!Array<?>} */
  elmObjsSorted.raw = elmObjsSorted.slice();
  /** @type {!Array} */
  var farmNode = ["<h3\n\tclass=", "\n\tdata-key=", "\n\tdata-at=", "\n>", "</h3>"];
  /** @type {!Array<?>} */
  farmNode.raw = farmNode.slice();
  /** @type {!Array} */
  var packedXmlData = ["<li\n\tclass=", "\n\tdata-key=", "\n\tdata-at=", "\n>", "</li>"];
  /** @type {!Array<?>} */
  packedXmlData.raw = packedXmlData.slice();
  /** @type {!Array<string>} */
  var lms = "<p\n\tclass= \n\tdata-key= \n\tdata-placeholder= \n\tdata-at= \n> </p>".split(" ");
  /** @type {!Array<string>} */
  lms.raw = lms.slice();
  /** @type {!Array} */
  var input = ["<a href='", "' save-ref=", " target=_blank><span style=", ">", "</span></a>"];
  /** @type {!Array<?>} */
  input.raw = input.slice();
  /** @type {!Array} */
  var isFirstTimeShown = ["<span style=", ">", "</span>"];
  /** @type {!Array<?>} */
  isFirstTimeShown.raw = isFirstTimeShown.slice();
  /** @type {!Array} */
  var roomPeers = ["\n<svg\n\tclass=", ' \n\tviewBox="0 0 10 10" \n\txmlns="http://www.w3.org/2000/svg"\n>\n\t<path d="M5 3.571L7.857.714a1.01 1.01 0 0 1 1.429 1.429L6.429 5l2.857 2.857a1.01 1.01 0 0 1-1.429 1.428L5 6.428 2.143 9.285A1.01 1.01 0 0 1 .714 7.857L3.571 5 .714 2.142A1.01 1.01 0 0 1 2.143.714L5 3.57z" fill="#42484E"/>\n</svg>\n'];
  /** @type {!Array<?>} */
  roomPeers.raw = roomPeers.slice();
  /** @type {!Array} */
  var z = ["\n<svg\n\tclass=", '  \n\tviewBox="0 0 12 10" \n\txmlns="http://www.w3.org/2000/svg"\n>\n\t<path d="M10.135.32a1.09 1.09 0 0 1 1.548 1.538L4.405 9.182.317 5.072a1.09 1.09 0 0 1 1.547-1.538l2.54 2.553L10.135.32z" fill="none"/>\n</svg>'];
  /** @type {!Array<?>} */
  z.raw = z.slice();
  /** @type {!Array} */
  var expanded_list = ["\n<svg\n\tclass=", ' \n\tviewBox="0 0 12 10" \n\txmlns="http://www.w3.org/2000/svg"\n>\n\t<path d="M10.135.32a1.09 1.09 0 0 1 1.548 1.538L4.405 9.182.317 5.072a1.09 1.09 0 0 1 1.547-1.538l2.54 2.553L10.135.32z" fill="none"/>\n</svg>'];
  /** @type {!Array<?>} */
  expanded_list.raw = expanded_list.slice();
  /** @type {!Array} */
  var selectedText = ["\n<svg\n\tclass=", '\n\tviewBox="0 0 6 6" \n\txmlns="http://www.w3.org/2000/svg"\n>\n\t<circle cx="828" cy="556" r="3" transform="translate(-825 -553)" fill="none"/>\n</svg>'];
  /** @type {!Array<?>} */
  selectedText.raw = selectedText.slice();
  /** @type {!Array} */
  var flash_env = ["\n<div\n\tclass=", "\n>\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  flash_env.raw = flash_env.slice();
  /** @type {!Array<string>} */
  var page_code = "\n<label\n\tclass= \n\tonclick= \n\tdisabled= \n>\n\t \n\t \n</label>".split(" ");
  /** @type {!Array<string>} */
  page_code.raw = page_code.slice();
  /** @type {!Array} */
  var a4 = ['\n<input type="radio" name="radio"/>\n<span class=', ">\n\t", "\n</span>"];
  /** @type {!Array<?>} */
  a4.raw = a4.slice();
  /** @type {!Array} */
  var command = ["/src/roll/components/richtext/renderer/exercise/question/content/multiplechoice/MultipleChoicesRenderer.css"];
  /** @type {!Array<?>} */
  command.raw = command.slice();
  var assert = c(command).v;
  /** @type {!Array} */
  var cached_wifi_network_list = ["\n<div\n\tclass=", "\n>\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  cached_wifi_network_list.raw = cached_wifi_network_list.slice();
  /** @type {!Array<string>} */
  var mark = "\n<label\n\tclass= \n\tonclick= \n\tdisabled= \n>\n\t \n\t \n</label>".split(" ");
  /** @type {!Array<string>} */
  mark.raw = mark.slice();
  /** @type {!Array} */
  var ex = ['\n<input type="checkbox" />\n<span class=', ">\n\t", "\n</span>"];
  /** @type {!Array<?>} */
  ex.raw = ex.slice();
  /** @type {!Array} */
  var code = ["/src/roll/components/richtext/renderer/exercise/question/content/multipleresponse/MultipleResponsesRenderer.css"];
  /** @type {!Array<?>} */
  code.raw = code.slice();
  var getData = c(code).v;
  /** @type {!Array<string>} */
  var teamStageData = "\n<div\n\tclass= \n>\n\t \n\t \n\t<input\n\t\tclass= \n\t\tvalue= \n\t\tplaceholder= \n\t\toninput= \n\t\treadonly= \n\t/>\n\t \n</div>".split(" ");
  /** @type {!Array<string>} */
  teamStageData.raw = teamStageData.slice();
  /** @type {!Array} */
  var liveLeakUrl = ["\n<p class=", ">\n\t", ": ", "\n</p>"];
  /** @type {!Array<?>} */
  liveLeakUrl.raw = liveLeakUrl.slice();
  /** @type {!Array} */
  var row = ["/src/roll/components/richtext/renderer/exercise/question/content/typein/TypeInRenderer.css"];
  /** @type {!Array<?>} */
  row.raw = row.slice();
  var parseFloat = c(row).v;
  /** @type {!Array<string>} */
  var lyr = "\n<div class=;>\n\t<div class=;>\n\t\t;\n\t</div>\n\t<div class=;>\n\t\t;\n\t</div>\n\t;\n</div>\n".split(";");
  /** @type {!Array<string>} */
  lyr.raw = lyr.slice();
  /** @type {!Array} */
  var url = ["/src/roll/components/richtext/renderer/exercise/question/FeedbackRenderer.css"];
  /** @type {!Array<?>} */
  url.raw = url.slice();
  var removeItem = c(url).v;
  /** @type {!Array<string>} */
  var param = "\n<div class=;>\n\t;\n\t;\n\t;\n\t;\n\t;\n\t;\n\t;\n\t;\n</div>".split(";");
  /** @type {!Array<string>} */
  param.raw = param.slice();
  /** @type {!Array} */
  var v = ["/src/roll/components/richtext/renderer/exercise/question/QuestionRenderer.css"];
  /** @type {!Array<?>} */
  v.raw = v.slice();
  var reject = c(v).v;
  /** @type {!Array<string>} */
  var right_pal = '\n<div\n\tclass= \n\tcontenteditable="false"\n\tsave-ref= \n>\n\t \n\t \n\t \n</div>'.split(" ");
  /** @type {!Array<string>} */
  right_pal.raw = right_pal.slice();
  /** @type {!Array} */
  var allData = ["/src/roll/components/richtext/renderer/exercise/ExerciseBlockRenderer.css"];
  /** @type {!Array<?>} */
  allData.raw = allData.slice();
  var victorySpin = c(allData).v;
  var id = getRequirements(function() {
  });
  var address = getRequirements(function() {
  });
  var glue = getRequirements(function() {
  });
  /** @type {!Array} */
  var signal = ["\n<figure class=", ">\n\t<div class=", ">\n\t\t", "\n\t</div>\n</figure>"];
  /** @type {!Array<?>} */
  signal.raw = signal.slice();
  var colors = {};
  var event = {};
  expect(Node, context);
  /**
   * @return {?}
   */
  Node.prototype.W = function() {
    var query = this;
    if (!this.f) {
      handle(function(from) {
        /** @type {!Function} */
        query.f = from;
      }, this.h)({
        state : this.b,
        dispatch : this.g,
        mode : "player",
        Fb : this.l
      });
    }
    var newClass = removeClass([set(this, "wrapper")]);
    return stop(event, ":exercise-block-" + this.b.Sb.key)(signal, showPlaceholderIfEmpty(this), newClass, this.f);
  };
  /** @type {!Array<string>} */
  var messageRegExp = "\n<div class=; style=; onconnected=;>\n\t;\n\t;\n</div>".split(";");
  /** @type {!Array<string>} */
  messageRegExp.raw = messageRegExp.slice();
  /** @type {!Array} */
  var first = ["<div class=", "></div>"];
  /** @type {!Array<?>} */
  first.raw = first.slice();
  /** @type {!Array} */
  var common = ["\n<div></div>"];
  /** @type {!Array<?>} */
  common.raw = common.slice();
  expect(b, val);
  /**
   * @return {undefined}
   */
  b.prototype.xa = function() {
    f(this, "show", "below");
  };
  /**
   * @return {?}
   */
  b.prototype.h = function() {
    return merge(this, "content")(common);
  };
  /**
   * @return {?}
   */
  b.prototype.W = function() {
    var data = this;
    var prod = removeClass([set(this, "tail")]);
    return this.na(messageRegExp, showPlaceholderIfEmpty(this), makeupBalloon(this), function() {
      return finalize(data);
    }, merge(this, "tail")(first, prod), this.h());
  };
  /** @type {!Array} */
  var stopperLight = ["\n<div class=", ">\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  stopperLight.raw = stopperLight.slice();
  expect(bar, context);
  /**
   * @return {undefined}
   */
  bar.prototype.Fa = function() {
    if (this.b) {
      this.b.Fa();
    }
  };
  /**
   * @return {?}
   */
  bar.prototype.W = function() {
    return this.na(stopperLight, showPlaceholderIfEmpty(this), this.g);
  };
  expect(pre, container);
  /**
   * @return {undefined}
   */
  pre.prototype.Fa = function() {
    var t = this;
    var selector = this.ma();
    if (selector) {
      tail(selector, function() {
        return t.cc.dispatch();
      });
    } else {
      this.cc.dispatch();
    }
  };
  expect(_, pre);
  /**
   * @return {undefined}
   */
  _.prototype.tc = function() {
    var options = this.f.getBoundingClientRect();
    if (!(options.width && options.height && this.f.parentNode)) {
      fail("incorrect bounding box (" + (null == this.f.parentNode) + ")");
    }
    this.b.move(options.left - this.g, options.bottom + this.m);
    var left = this.b.x() || 0;
    if (0 > left) {
      left = this.g;
    }
    if (left + this.b.D().clientWidth > window.innerWidth) {
      /** @type {number} */
      left = window.innerWidth - this.g - this.b.D().clientWidth;
    }
    var top = this.b.y() || 0;
    if (0 > top) {
      top = this.m;
    }
    if (top + this.b.D().clientHeight > window.innerHeight) {
      /** @type {number} */
      top = options.top - this.m - this.b.D().clientHeight;
    }
    this.b.move(left, top);
  };
  expect(api, _);
  /**
   * @return {undefined}
   */
  api.prototype.Xc = function() {
    var nameArgs = this;
    this.P = end(this, this.f, "mouseout", function(el) {
      return isMouseEventWithinElement(nameArgs, el);
    });
    this.l = end(this, this.b.D(), "mouseout", function(el) {
      return isMouseEventWithinElement(nameArgs, el);
    });
    this.h = end(this, document.body, "touchstart", function(el) {
      return isMouseEventWithinElement(nameArgs, el);
    });
    var e = this.b.D();
    e = g(e);
    constructor({
      fb : e,
      Hb : wrapMethod(object),
      duration : 150
    });
  };
  /**
   * @return {?}
   */
  api.prototype.ma = function() {
    if (this.P) {
      error(this, this.P);
    }
    if (this.l) {
      error(this, this.l);
    }
    if (this.h) {
      error(this, this.h);
    }
    return onerror(this);
  };
  /**
   * @return {undefined}
   */
  api.prototype.tc = function() {
    var C = this.C;
    var box = this.b;
    var options = this.f.getBoundingClientRect();
    if (!(options.width && options.height && this.f.parentNode)) {
      fail("incorrect bounding box (" + (null == this.f.parentNode) + ")");
    }
    var delta = box.D().getBoundingClientRect();
    box.move(options.left + .5 * options.width - .5 * delta.width, this.C ? options.bottom + this.m : options.top - this.m - delta.height);
    var left = box.x() || 0;
    if (0 > left) {
      left = this.g;
    }
    if (left + delta.width > window.innerWidth) {
      /** @type {number} */
      left = window.innerWidth - this.g - delta.width;
    }
    delta = box.y() || 0;
    if (0 > delta) {
      delta = options.bottom + this.m;
      /** @type {boolean} */
      C = true;
    }
    this.b.move(left, delta);
    if (C) {
      box.xa();
    } else {
      f(box, "show", "above");
    }
  };
  expect(pkg, _);
  /**
   * @return {undefined}
   */
  pkg.prototype.Xc = function() {
    var self = this;
    this.P = end(this, this.f, "mouseout", function() {
      return self.Fa();
    });
    this.h = end(this, document.body, "touchstart", function(value) {
      value = value.target;
      if (value instanceof Node && !self.f.contains(value) && !self.b.D().contains(value)) {
        self.Fa();
      }
    });
  };
  /**
   * @return {?}
   */
  pkg.prototype.ma = function() {
    if (this.P) {
      error(this, this.P);
    }
    if (this.h) {
      error(this, this.h);
    }
    return null;
  };
  /**
   * @return {undefined}
   */
  pkg.prototype.tc = function() {
    var l = this.l;
    var box = this.b;
    var options = this.f.getBoundingClientRect();
    if (!(options.width && options.height && this.f.parentNode)) {
      fail("incorrect bounding box (" + (null == this.f.parentNode) + ")");
    }
    var delta = box.D().getBoundingClientRect();
    box.move(options.left + .5 * options.width - .5 * delta.width, this.l ? options.bottom + this.m : options.top - this.m - delta.height);
    var left = box.x() || 0;
    if (0 > left) {
      left = this.g;
    }
    if (left + delta.width > window.innerWidth) {
      /** @type {number} */
      left = window.innerWidth - this.g - delta.width;
    }
    delta = box.y() || 0;
    if (0 > delta) {
      delta = options.bottom + this.m;
      /** @type {boolean} */
      l = true;
    }
    this.b.move(left, delta);
    if (l) {
      box.xa();
    } else {
      f(box, "show", "above");
    }
  };
  expect(circle, bar);
  /** @type {!Array} */
  var feedsObject = ["\n<div class=", ">\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  feedsObject.raw = feedsObject.slice();
  expect(Config, b);
  /**
   * @return {?}
   */
  Config.prototype.h = function() {
    var prod = removeClass([set(this, "text")]);
    return merge(this, "content")(feedsObject, prod, this.C);
  };
  /** @type {!Array} */
  var file = ["\n<div class=", " save-ref=", ">", "</div>"];
  /** @type {!Array<?>} */
  file.raw = file.slice();
  /** @type {!Array<string>} */
  var UPDATE_NEWS = "\n<div\n\tclass=;\n\tdisabled=;\n\tonclick=;\n\tonmouseover=;\n\tpassive-events=;\n>\n\t;\n\t<div class=;>;</div>\n</div>".split(";");
  /** @type {!Array<string>} */
  UPDATE_NEWS.raw = UPDATE_NEWS.slice();
  /** @type {!Array<string>} */
  var opt = '\n<div class=;>\n\t<svg width="22" height="22" viewPort="0 0 11 11" version="1.1" xmlns="http://www.w3.org/2000/svg">\n\t\t<circle r=";" cx="11" cy="11" fill="transparent" stroke-dasharray="56.5" stroke-dashoffset="0"></circle>\n\t\t<circle class=; r=";" cx="11" cy="11" fill="transparent" stroke-dasharray="56.5" stroke-dashoffset=";"></circle>\n\t</svg>\n</div>'.split(";");
  /** @type {!Array<string>} */
  opt.raw = opt.slice();
  /** @type {!Array} */
  var interval = ["\n<div class=", "></div>"];
  /** @type {!Array<?>} */
  interval.raw = interval.slice();
  /** @type {!Array} */
  var day = ["/src/roll/player/renderer/navigation/NavigationItemRenderer.css"];
  /** @type {!Array<?>} */
  day.raw = day.slice();
  var css = c(day).v;
  /** @type {!Array<string>} */
  var glassLight = "\n<div class=;>\n\t<div class=;>;</div>\n\t<div class=;>\n\t\t<div class=; style=;></div>\n\t</div>\n</div>".split(";");
  /** @type {!Array<string>} */
  glassLight.raw = glassLight.slice();
  expect(C, context);
  /**
   * @return {?}
   */
  C.prototype.W = function() {
    /** @type {string} */
    var artistTrack = Math.floor(100 * this.b) + "%";
    return this.na(glassLight, showPlaceholderIfEmpty(this), set(this, "numeric").className(), artistTrack, set(this, "indicator-container").className(), set(this, "indicator-progress").className(), "width: " + artistTrack);
  };
  /** @type {!Array} */
  var buf0 = ["\n<div\n\tclass=", "\n\tontransitionend=", "\n\tonscroll=", "\n>\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  buf0.raw = buf0.slice();
  /** @type {!Array} */
  var keyReads = ["\n<div class='main-panel'>\n\t", "\n\t", "\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  keyReads.raw = keyReads.slice();
  expect(copy, context);
  /**
   * @return {?}
   */
  copy.prototype.W = function() {
    var footerBox = this;
    return this.na(buf0, showPlaceholderIfEmpty(this), this, function() {
      return footerBox.h.Fa();
    }, merge(this, "main-panel")(keyReads, mouseDown(this), this.g, this.C));
  };
  /** @type {!Map} */
  var timeCounters = new Map;
  /** @type {!Array} */
  var cperm = ["\n<div\n\tclass=", "\n\tstyle=", "\n>\n\t", "\n\t", "\n</div>"];
  /** @type {!Array<?>} */
  cperm.raw = cperm.slice();
  /** @type {!Array} */
  var left_pal = ["\n\t\t<p class=", ">", "</p>\n\t"];
  /** @type {!Array<?>} */
  left_pal.raw = left_pal.slice();
  /** @type {!Array} */
  var double_expanded_list = ["<p class=", ">", "</p>"];
  /** @type {!Array<?>} */
  double_expanded_list.raw = double_expanded_list.slice();
  expect(Image, context);
  /**
   * @return {?}
   */
  Image.prototype.title = function() {
    return $(map(this, "chapter-title"));
  };
  /**
   * @return {?}
   */
  Image.prototype.W = function() {
    var newClass = removeClass(["text-chapter-number", set(this, "chapter-number")]);
    var prod = removeClass(["text-title", set(this, "title")]);
    var artistTrack = this.b.title || this.f || log("Roll.Common.ChapterDefaultTitle");
    return this.na(cperm, showPlaceholderIfEmpty(this), {
      "margin-top" : this.h,
      "padding-top" : this.C
    }, this.l ? merge(this, "numbering")(double_expanded_list, newClass, this.g()) : null, merge(this, "chapter-title")(left_pal, prod, artistTrack));
  };
  /** @type {!Array<string>} */
  var suTypes = "\n<div class=;>\n\t;\n\t<div class=;>\n\t\t;\n\t\t;\n\t\t;\n\t</div>\n</div>".split(";");
  /** @type {!Array<string>} */
  suTypes.raw = suTypes.slice();
  /** @type {!Array} */
  var Tile = ["\n\t\t\t<p class=", ">", "</p>\n\t\t"];
  /** @type {!Array<?>} */
  Tile.raw = Tile.slice();
  /** @type {!Array} */
  var adjacentMobs = ["<p class=", ">", "</p>"];
  /** @type {!Array<?>} */
  adjacentMobs.raw = adjacentMobs.slice();
  /** @type {!Array} */
  var tableContents = ["\n\t\t\t\t<div class=", "></div>"];
  /** @type {!Array<?>} */
  tableContents.raw = tableContents.slice();
  /** @type {!Array} */
  var a = ["\n<div style=", " class=", ">\n\t<div style=", ' class="', '">\n</div>'];
  /** @type {!Array<?>} */
  a.raw = a.slice();
  expect(Request, context);
  /**
   * @return {?}
   */
  Request.prototype.W = function() {
    var artistTrack = removeClass(["intro-container"]);
    var newClass = removeClass(["text-kicker", set(this, "kicker")]);
    var b = removeClass(["text-title", set(this, "title")]);
    var prod = removeClass([set(this, "strip")]);
    prod = this.b.jb ? null : merge(this, "strip")(tableContents, prod);
    return this.na(suTypes, showPlaceholderIfEmpty(this), updateSize(this), artistTrack, this.b.aa ? merge(this, "kicker")(adjacentMobs, newClass, this.b.aa.trim() || i) : null, merge(this, "intro-title")(Tile, b, this.b.title ? this.b.title.trim() || i : log("Roll.Common.IntroDefaultTitle")), prod);
  };
  /** @type {!Array<string>} */
  var old_decoding = "\n\t\t\t<button class=;\n\t\t\t\t\tdisabled=;\n\t\t\t\t\tstyle=;\n\t\t\t\t\tonclick=;\n\t\t\t\t\tdata-at=;\n\t\t\t\t\tonmousedown=;\n\t\t\t\t\tonmouseover=;\n\t\t\t\t\tonmouseout=;>\n\t\t\t\t;\n\t\t\t</button>".split(";");
  /** @type {!Array<string>} */
  old_decoding.raw = old_decoding.slice();
  expect(def, val);
  /**
   * @return {?}
   */
  def.prototype.F = function() {
    return addEventListener(this, "click");
  };
  /**
   * @return {?}
   */
  def.prototype.W = function() {
    return this.na(old_decoding, showPlaceholderIfEmpty(this), false, makeupBalloon(this), removeEvent(this, "click") ? this : null, wrap(this.l), removeEvent(this, "mousedown") ? this : null, removeEvent(this, "mouseover") ? this : null, removeEvent(this, "mouseout") ? this : null, "");
  };
  /** @type {!Array<string>} */
  var last = "\n<div class=; style=;>\n\t;\n\t;\n\t;\n\t;\n</div>".split(";");
  /** @type {!Array<string>} */
  last.raw = last.slice();
  /** @type {!Array} */
  var best_params = ["\n\t\t<a href=", ' target="_blank" class=', '>\n\t\t\t<div class="', '"></div>\n\t\t\t<span>', "</span>\n\t\t</a>"];
  /** @type {!Array<?>} */
  best_params.raw = best_params.slice();
  /** @type {!Array} */
  var outputSourceMapPath = ["<div class=", " style=", "></div>"];
  /** @type {!Array<?>} */
  outputSourceMapPath.raw = outputSourceMapPath.slice();
  expect(message, val);
  /**
   * @return {?}
   */
  message.prototype.W = function() {
    var prod = removeClass(["top-panel-progress"]);
    var defaults = {
      width : "calc(100% * " + this.l + ")",
      transition : this.C ? "none" : ""
    };
    this.Ca.className();
    if (this.h) {
      createItem(this.Ca, "product", this.h);
    }
    this.Ba.className();
    if (!("isstudio" !== this.h && "isspace" !== this.h)) {
      encode(this.Ba, "removable");
    }
    return this.na(last, showPlaceholderIfEmpty(this), makeupBalloon(this), this.S, merge(this, "progress")(outputSourceMapPath, prod, defaults), load(this), null);
  };
  /** @type {!Array<string>} */
  var result0 = '\n<div data-root=";" class=; onconnected=;>\n\t;\n\t<div class=;>\n\t\t;\n\t\t<div class=; onscroll=;>\n\t\t\t;\n\t\t\t;\n\t\t\t;\n\t\t\t;\n\t\t</div>\n\t</div>\n\t;\n\t;\n</div>'.split(";");
  /** @type {!Array<string>} */
  result0.raw = result0.slice();
  /** @type {!Array} */
  var breakedLines = ["<div class=", " passive-events=", " onmousedown=", "></div>"];
  /** @type {!Array<?>} */
  breakedLines.raw = breakedLines.slice();
  /** @type {!Array} */
  var request = ["\n\t\t\t\t\t<div class=", ">\n\t\t\t\t\t\t<div class=", ">\n\t\t\t\t\t\t\t", "\n\t\t\t\t\t\t</div>\n\t\t\t\t\t\t", "\n\t\t\t\t\t</div>"];
  /** @type {!Array<?>} */
  request.raw = request.slice();
  var wrapper = wrapMethod(mapResponse(.14));
  var _domain_list = mapResponse(.64);
  expect(start, context);
  item = start.prototype;
  /**
   * @return {?}
   */
  item.state = function() {
    return this.b;
  };
  /**
   * @param {number} e
   * @return {undefined}
   */
  item.Jd = function(e) {
    dispatch(this);
    if (spin() || 739 >= window.innerWidth) {
      if (e.preventDefault(), f(this.f, "without-transition", false), e = Math.max(e.target.scrollTop, 0), e >= this.l) {
        if (53 < e) {
          this.f.move(null, -Math.min(e, 53));
          /** @type {number} */
          this.l = e;
        }
      } else {
        if (e < this.l) {
          if (53 >= e) {
            f(this.f, "without-transition", true);
            /** @type {number} */
            var dtStep = Math.abs(parseFloat($(this.f.D().style.top))) || 0;
            this.f.move(null, -Math.min(e, dtStep));
            /** @type {number} */
            this.l = e;
          }
          if (56 < this.l - e) {
            this.f.move(null, 0);
            /** @type {number} */
            this.l = e;
          }
        }
      }
    }
  };
  /**
   * @return {undefined}
   */
  item.Id = function() {
    var vForward = this;
    this.ce.observe(this.D());
    if (now()) {
      createDivInRect(this);
    }
    dispatch(this);
    if (/Firefox/.test(navigator.userAgent)) {
      /**
       * @return {undefined}
       */
      var join = function() {
        apply(function() {
          dispatch(vForward);
          join();
        });
      };
      join();
    }
  };
  /**
   * @param {boolean} a
   * @return {undefined}
   */
  item.Mb = function(a) {
    var existing = this;
    if (!(1180 < window.innerWidth || !this.Ba)) {
      this.Ba = void 0 === a ? false : a;
      max(this, addEventListener(this.g, "transitionend"), function() {
        /** @type {boolean} */
        existing.Ba = true;
        push(existing);
      });
      this.Pc.dispatch();
    }
  };
  /**
   * @return {?}
   */
  item.W = function() {
    var b = removeClass(["overlay"]);
    var prod = removeClass(["next-section-content-container"]);
    var cb = removeClass(["next-section-label"]);
    var newClass = removeClass(["copying-disabled"]);
    newClass = this.S.Ra ? showPlaceholderIfEmpty(this) : showPlaceholderIfEmpty(this) + " " + newClass;
    return this.na(result0, "data-root", newClass, this.Id, this.g, this.Md, this.f, this.Jc, this.Jd, this.Aa, this.C, this.Ca, this.S.Ha ? merge(this, "next")(request, prod, cb, this.Kc, pass(this)) : null, (spin() || 739 >= window.innerWidth) && this.b.Ta || !this.Ba ? merge(this, "overlay")(breakedLines, b, {
      ontouchstart : this.Mb
    }, this.Mb) : null, this.Pa);
  };
  expect(list, container);
  item = list.prototype;
  /**
   * @return {?}
   */
  item.getTitle = function() {
    return this.b.intro.u.title || log("Roll.Common.IntroDefaultTitle");
  };
  /**
   * @return {?}
   */
  item.getChaptersCount = function() {
    return this.b.chapters.length;
  };
  /**
   * @return {?}
   */
  item.getCurrentSectionIndex = function() {
    return this.b.fa;
  };
  /**
   * @return {?}
   */
  item.getCurrentSectionProgress = function() {
    return equal(this.b).progress;
  };
  /**
   * @return {?}
   */
  item.getCurrentSectionsStateProgress = function() {
    return this.b.chapters.map(function(_input) {
      return 1 === _input.progress;
    });
  };
  /**
   * @return {?}
   */
  item.getSuspendData = function() {
    var data = Animation(this.f.state());
    var b = user.Type;
    var result = b.verify(data);
    if (result) {
      throw Error(result);
    }
    data = b.fromObject(data);
    b = b.encode(data).finish();
    /** @type {!Uint8Array} */
    data = new Uint8Array((new Uint32Array([1])).buffer);
    /** @type {!Uint8Array} */
    b = new Uint8Array(each(data).concat(each(b)));
    /** @type {(number|undefined)} */
    var type = void 0 === type ? 0 : type;
    /** @type {(number|undefined)} */
    var node = void 0 === node ? b.length : node;
    /** @type {null} */
    data = null;
    /** @type {!Array} */
    result = [];
    /** @type {number} */
    var i = 0;
    /** @type {number} */
    var g = 0;
    var current;
    for (; type < node;) {
      /** @type {number} */
      var c = b[type++];
      switch(g) {
        case 0:
          result[i++] = nodes[c >> 2];
          /** @type {number} */
          current = (c & 3) << 4;
          /** @type {number} */
          g = 1;
          break;
        case 1:
          result[i++] = nodes[current | c >> 4];
          /** @type {number} */
          current = (c & 15) << 2;
          /** @type {number} */
          g = 2;
          break;
        case 2:
          result[i++] = nodes[current | c >> 6];
          result[i++] = nodes[c & 63];
          /** @type {number} */
          g = 0;
      }
      if (8191 < i) {
        (data || (data = [])).push(String.fromCharCode.apply(String, each(result)));
        /** @type {number} */
        i = 0;
      }
    }
    if (g) {
      result[i++] = nodes[current];
      /** @type {number} */
      result[i++] = 61;
      if (1 === g) {
        /** @type {number} */
        result[i++] = 61;
      }
    }
    if (data) {
      if (i) {
        data.push(String.fromCharCode.apply(String, each(result.slice(0, i))));
      }
      /** @type {string} */
      type = data.join("");
    } else {
      /** @type {string} */
      type = String.fromCharCode.apply(String, each(result.slice(0, i)));
    }
    return type;
  };
  /**
   * @return {?}
   */
  item.getArticle = function() {
    var tutorial = this.b.chapters.map(function(b) {
      return {
        key : b.key,
        title : b.u.title
      };
    });
    return {
      intro : {
        key : this.b.intro.key,
        title : this.b.intro.u.title
      },
      chapters : tutorial
    };
  };
  /**
   * @return {?}
   */
  item.D = function() {
    return this.f.D();
  };
  var sectionsDropdownObject = {};
  var eventOption = (sectionsDropdownObject["#"] = "#", sectionsDropdownObject);
  var props = {
    text : "t",
    sa : Object.assign({}, {
      _ : "m"
    }, eventOption),
    length : "l"
  };
  var values = {
    key : "k",
    type : "t",
    N : "v",
    textAlign : "tA",
    B : Object.assign({}, {
      _ : "c"
    }, props),
    bb : "iO"
  };
  var opts = {
    type : "t",
    src : "s",
    key : "k",
    width : "w",
    height : "h",
    w : "a",
    B : Object.assign({}, {
      _ : "c"
    }, props),
    va : "iB"
  };
  /** @type {!Object} */
  var presetOptions = Object.assign({}, opts, {
    Xb : "cK"
  });
  var data = {
    type : "t",
    key : "k",
    w : "a",
    images : Object.assign({}, {
      _ : "is"
    }, presetOptions, {
      length : "l"
    })
  };
  var options = {
    type : "t",
    src : "s",
    key : "k",
    width : "w",
    height : "h",
    w : "a",
    B : Object.assign({}, {
      _ : "c"
    }, props)
  };
  var sort = {
    type : "t",
    src : "s",
    key : "k",
    fileName : "fn",
    lb : "fe",
    fileSize : "fs",
    uc : "iL"
  };
  var customizedConfig = {
    T : {
      _ : "b",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, values))
    }
  };
  var site = {
    ka : {
      _ : "ch",
      key : "k",
      content : Object.assign({}, {
        _ : "c"
      }, customizedConfig),
      L : "cr",
      length : "l"
    }
  };
  var dojoWebpackLoaderOptions = {
    type : "t",
    L : Object.assign({}, {
      _ : "c"
    }, customizedConfig),
    Ga : Object.assign({}, {
      _ : "i"
    }, customizedConfig),
    o : Object.assign({}, {
      _ : "a"
    }, customizedConfig)
  };
  var params = {
    key : "k",
    type : "t",
    description : Object.assign({}, {
      _ : "d"
    }, customizedConfig),
    M : Object.assign({}, {
      _ : "a"
    }, {
      type : "t",
      width : "w",
      height : "h",
      src : "s"
    }),
    content : Object.assign({}, {
      _ : "c"
    }, site, {
      Da : "cs",
      ia : {
        _ : "a",
        key : "k",
        text : "t",
        length : "l"
      }
    }),
    V : Object.assign({}, {
      _ : "f"
    }, dojoWebpackLoaderOptions)
  };
  var result = {
    type : "t",
    key : "k",
    R : {
      _ : "ss",
      ab : "iip",
      Ma : "sa",
      H : "sca"
    },
    Z : Object.assign({}, {
      _ : "q"
    }, params)
  };
  var githubSections = {
    T : {
      _ : "b",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, values, opts, options, sort, result))
    }
  };
  var convertedModifier = {
    u : {
      _ : "h",
      aa : "k",
      title : "t",
      qa : {
        _ : "c",
        src : "s",
        width : "w",
        height : "h",
        color : "c",
        opacity : "o",
        va : "i"
      }
    },
    j : Object.assign({}, {
      _ : "cs"
    }, githubSections)
  };
  var document = {
    key : "k",
    u : {
      _ : "h",
      title : "t"
    },
    j : Object.assign({}, {
      _ : "cs"
    }, githubSections)
  };
  var .num_const = {
    intro : Object.assign({}, {
      _ : "i"
    }, convertedModifier),
    chapters : {
      _ : "c",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, document))
    }
  };
  var ContinueSentinel = {
    Cb : {
      _ : "m",
      cb : "nT",
      Ia : "nE",
      Ha : "nbE",
      Ra : "cE"
    },
    kb : {
      _ : "d",
      Tc : "aC",
      qc : "hF",
      ec : "cF"
    }
  };
  var proArgs = {
    T : {
      _ : "b",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, values, data, options, sort, result))
    }
  };
  var numericOptions = {
    u : {
      _ : "h",
      aa : "k",
      title : "t",
      qa : {
        _ : "c",
        src : "s",
        width : "w",
        height : "h",
        color : "c",
        opacity : "o",
        va : "i"
      }
    },
    j : Object.assign({}, {
      _ : "cs"
    }, proArgs)
  };
  var schema = {
    key : "k",
    u : {
      _ : "h",
      title : "t"
    },
    j : Object.assign({}, {
      _ : "cs"
    }, proArgs)
  };
  var modules = {
    intro : Object.assign({}, {
      _ : "i"
    }, numericOptions),
    chapters : {
      _ : "c",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, schema))
    }
  };
  var convertedMetadata = {
    U : {
      _ : "cp",
      top : "tp",
      left : "lf",
      right : "rt",
      bottom : "bm"
    }
  };
  /** @type {!Object} */
  var dateOperators = Object.assign({}, data, {
    images : Object.assign({}, data.images, convertedMetadata)
  });
  /** @type {!Object} */
  var self = Object.assign({}, result, {
    Z : Object.assign({}, result.Z, {
      M : Object.assign({}, result.Z.M, convertedMetadata)
    })
  });
  var mockOverrides = {
    T : {
      _ : "b",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, values, options, sort, self, dateOperators))
    }
  };
  var operationProps = {
    u : {
      _ : "h",
      aa : "k",
      title : "t",
      qa : {
        _ : "c",
        src : "s",
        width : "w",
        height : "h",
        color : "c",
        opacity : "o",
        va : "i"
      }
    },
    j : Object.assign({}, {
      _ : "cs"
    }, mockOverrides)
  };
  var flags = {
    key : "k",
    u : {
      _ : "h",
      title : "t"
    },
    j : Object.assign({}, {
      _ : "cs"
    }, mockOverrides)
  };
  var chapters = {
    intro : Object.assign({}, {
      _ : "i"
    }, operationProps),
    chapters : {
      _ : "c",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, flags))
    }
  };
  /** @type {!Object} */
  var functionVal = Object.assign({}, self, {
    Z : Object.assign({}, self.Z, {
      M : Object.assign({}, self.Z.M, {
        title : "tl",
        Yb : "pws"
      })
    })
  });
  var parameters = {
    T : {
      _ : "b",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, values, options, sort, dateOperators, functionVal))
    }
  };
  var fullContext = {
    u : {
      _ : "h",
      aa : "k",
      title : "t",
      qa : {
        _ : "c",
        src : "s",
        width : "w",
        height : "h",
        color : "c",
        opacity : "o",
        va : "i"
      }
    },
    j : Object.assign({}, {
      _ : "cs"
    }, parameters)
  };
  var obj = {
    key : "k",
    u : {
      _ : "h",
      title : "t"
    },
    j : Object.assign({}, {
      _ : "cs"
    }, parameters)
  };
  var tickObj = {
    intro : Object.assign({}, {
      _ : "i"
    }, fullContext),
    chapters : {
      _ : "c",
      order : {
        _ : "o",
        length : "l"
      },
      A : getIndex(Object.assign({}, {
        _ : "B"
      }, obj))
    }
  };
  var composition = callback({
    start : skip(1),
    end : skip(2),
    style : t(3)
  });
  var html = callback({
    start : skip(1),
    end : skip(2),
    url : t(3)
  });
  var lang = callback({
    text : t(1),
    J : commit(2, composition),
    urls : commit(3, html)
  });
  var apptEndTime = callback({
    src : t(1),
    width : skip(2),
    height : skip(3)
  });
  var tileI = callback({
    src : t(1),
    width : skip(2),
    height : skip(3)
  });
  var _data = clone({
    i : tileI,
    e : apptEndTime
  });
  var progress = callback({
    key : t(1),
    text : next(2, lang),
    L : foo(3)
  });
  var images = callback({
    ka : commit(1, progress)
  });
  var dataset = callback({
    ka : commit(1, progress)
  });
  var res = callback({
    key : t(1),
    text : t(2)
  });
  var directory = callback({
    Da : foo(1),
    ia : commit(2, res)
  });
  var memberOf = callback({
    type : t(1),
    L : next(2, lang),
    Ga : next(3, lang),
    o : next(4, lang)
  });
  var fields = callback({
    ab : foo(1),
    Ma : foo(2),
    H : foo(3)
  });
  var mc = callback({
    key : t(1),
    description : next(2, lang),
    M : pick(3, _data),
    content : next(4, images),
    V : next(5, memberOf)
  });
  var accu = callback({
    key : t(1),
    description : next(2, lang),
    M : pick(3, _data),
    content : next(4, dataset),
    V : next(5, memberOf)
  });
  var intercept = callback({
    key : t(1),
    description : next(2, lang),
    M : pick(3, _data),
    content : next(4, directory),
    V : next(5, memberOf)
  });
  var auth = clone({
    mc : mc,
    mr : accu,
    ti : intercept
  });
  var _e = callback({
    key : t(1),
    R : next(2, fields),
    Z : commit(3, auth)
  });
  var entity = callback({
    start : skip(1),
    end : skip(2),
    url : t(3)
  });
  var proceed = callback({
    start : skip(1),
    end : skip(2),
    style : t(3)
  });
  var pwd = callback({
    key : t(1),
    text : t(2),
    N : t(3),
    J : commit(4, proceed),
    urls : commit(5, entity)
  });
  var addr = callback({
    key : t(1),
    src : t(2),
    width : skip(3),
    height : skip(4),
    w : t(5),
    text : t(6),
    J : commit(7, proceed),
    urls : commit(8, entity)
  });
  var reason = callback({
    key : t(1),
    src : t(2),
    width : skip(3),
    height : skip(4),
    w : t(5),
    text : t(6),
    J : commit(7, proceed),
    urls : commit(8, entity)
  });
  var ret = {};
  var newState = clone((ret.p = pwd, ret.i = addr, ret.e = reason, ret.E = _e, ret));
  var description = callback({
    intro : next(1, {
      aa : t(1),
      title : t(2),
      kc : pick(3, t),
      lc : pick(4, skip),
      ic : pick(5, skip),
      A : commit(6, newState),
      hc : pick(7, t),
      jc : pick(8, step)
    }),
    chapters : commit(2, {
      key : t(1),
      title : t(2),
      A : commit(3, newState)
    })
  });
  var operation = {};
  var jade = {};
  var mo = callback({
    content : next(1, description),
    yc : next(2, {
      cb : t(1),
      qd : foo(2),
      Ia : foo(3),
      Ha : foo(4),
      Ra : pick(6, foo),
      fonts : next(5, (operation.tooltip = t(1), operation.playerMain = t(2), operation.content = t(3), operation))
    }),
    $c : next(3, (jade.ACCENT_COLOR = t(1), jade.CONTENT_FONT = t(2), jade.PLAYER_MAIN_FONT = t(3), jade))
  });
  var rightNode = new User(mo);
  var err = callback({
    start : skip(1),
    end : skip(2),
    style : next(3, {
      name : t(1),
      size : pick(2, skip),
      color : pick(3, t),
      opacity : pick(4, step)
    })
  });
  var record = callback({
    start : skip(1),
    end : skip(2),
    url : t(3)
  });
  var name = callback({
    text : t(1),
    J : commit(2, err),
    urls : commit(3, record)
  });
  var apptOrigEndTime = callback({
    src : t(1),
    width : skip(2),
    height : skip(3)
  });
  var el = callback({
    src : t(1),
    width : skip(2),
    height : skip(3)
  });
  var configOptions = clone({
    i : el,
    e : apptOrigEndTime
  });
  var num = callback({
    key : t(1),
    text : next(2, name),
    L : foo(3)
  });
  var msg = callback({
    ka : commit(1, num)
  });
  var files = callback({
    ka : commit(1, num)
  });
  var sectionText = callback({
    key : t(1),
    text : t(2)
  });
  var resource = callback({
    Da : foo(1),
    ia : commit(2, sectionText)
  });
  var entry = callback({
    type : t(1),
    L : next(2, name),
    Ga : next(3, name),
    o : next(4, name)
  });
  var test10 = callback({
    ab : foo(1),
    Ma : foo(2),
    H : foo(3)
  });
  var footHtml = callback({
    key : t(1),
    description : next(2, name),
    M : pick(3, configOptions),
    content : next(4, msg),
    V : next(5, entry)
  });
  var callbackValue = callback({
    key : t(1),
    description : next(2, name),
    M : pick(3, configOptions),
    content : next(4, files),
    V : next(5, entry)
  });
  var testFn = callback({
    key : t(1),
    description : next(2, name),
    M : pick(3, configOptions),
    content : next(4, resource),
    V : next(5, entry)
  });
  var tree = clone({
    mc : footHtml,
    mr : callbackValue,
    ti : testFn
  });
  var Ty = callback({
    key : t(1),
    R : next(2, test10),
    Z : commit(3, tree)
  });
  var time = callback({
    start : skip(1),
    end : skip(2),
    style : next(3, {
      name : t(1),
      size : pick(2, skip),
      color : pick(3, t),
      opacity : pick(4, step)
    })
  });
  var y = callback({
    key : t(1),
    text : t(2),
    N : t(3),
    J : commit(4, time),
    urls : commit(5, entity),
    textAlign : pick(6, t),
    bb : pick(7, foo)
  });
  var Opcja = callback({
    key : t(1),
    src : t(2),
    width : skip(3),
    height : skip(4),
    w : t(5),
    text : t(6),
    J : commit(7, time),
    urls : commit(8, entity)
  });
  var str = callback({
    key : t(1),
    src : t(2),
    width : skip(3),
    height : skip(4),
    w : t(5),
    text : t(6),
    J : commit(7, time),
    urls : commit(8, entity)
  });
  var angl = callback({
    key : t(1),
    fileName : t(2),
    lb : t(3),
    fileSize : skip(4)
  });
  var out = {};
  var changes = clone((out.p = y, out.i = Opcja, out.e = str, out.E = Ty, out.a = angl, out));
  var r = callback({
    intro : next(1, {
      aa : t(1),
      title : t(2),
      kc : pick(3, t),
      lc : pick(4, skip),
      ic : pick(5, skip),
      A : commit(6, changes),
      hc : pick(7, t),
      jc : pick(8, step)
    }),
    chapters : commit(2, {
      key : t(1),
      title : t(2),
      A : commit(3, changes)
    })
  });
  var response = {};
  var lastTextToken = {};
  var otherNestedUser = callback({
    content : next(1, r),
    yc : next(2, {
      cb : t(1),
      qd : foo(2),
      Ia : foo(3),
      Ha : foo(4),
      Ra : pick(6, foo),
      fonts : next(5, (response.tooltip = t(1), response.playerMain = t(2), response.content = t(3), response))
    }),
    $c : next(3, (lastTextToken.ACCENT_COLOR = t(1), lastTextToken.CONTENT_FONT = t(2), lastTextToken.PLAYER_MAIN_FONT = t(3), lastTextToken))
  });
  var tempUser1 = new User(otherNestedUser);
  /** @type {!Array} */
  var parts = ["start"];
  var cur = w;
  if (!(parts[0] in cur || "undefined" == typeof cur.execScript)) {
    cur.execScript("var " + parts[0]);
  }
  var prop = void 0;
  for (; parts.length && (prop = parts.shift());) {
    if (!parts.length && isString(prev)) {
      /** @type {function(string, ?, ?, ?, string): ?} */
      cur[prop] = prev;
    } else {
      if (cur[prop] && cur[prop] !== Object.prototype[prop]) {
        cur = cur[prop];
      } else {
        cur = cur[prop] = {};
      }
    }
  }
}).call(this);
